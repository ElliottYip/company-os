{
  "schemaVersion": 1,
  "sourceCommit": "213dabab4f8e1f3bb1803a2924c0fea1289fcd4c",
  "purpose": "Track exact, non-overlapping read ranges for oversized audit units. Entries in this ledger remain outside COMPLETE coverage until every source line and required history/test evidence has been reviewed.",
  "units": [
    {
      "id": "application:server/services/company-portability",
      "path": "server/src/services/company-portability.ts",
      "lineCount": 6179,
      "sha256": "5edef5e6a98e2ffbeac5f384a347257c7b38b15b946b075f7c53a905d93dce2b",
      "linkedEvidence": { "history": ["5da382fd59222819cff110f85ca738a74fb335d1 feat(skills): require explicit merge modes (#10978)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 6179 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 show a 6,179-line portability service directly coupled to database tables, shared DTOs, adapter-utils, an OpenCode-local adapter, server adapter registry, GitHub fetch, storage, secrets, org-chart rendering and most domain services. Portability is not a narrow application boundary; vendor/runtime/presentation concerns all flow inward.",
        "The bundle writes schema version 6 and treats unstamped bundles as version 5. Inline completeness relies on an optional client-declared expectedFileCount and rejects only received<expected; it does not authenticate the set, reject extra files or replace a manifest-level canonical file inventory/digest.",
        "Agent-safe mode is implemented as a blacklist of selected project workspace commands/settings and non-schedule triggers. This is vulnerable to policy drift as new executable fields/capabilities are added; Company OS needs capability/data classifications with default-deny admission.",
        "Org-tree export hardcodes English role labels and active status in the service, silently treats missing-parent Agents as roots, omits pure reporting cycles, and leaves an unused rootSlugs set. Presentation and validation are mixed into migration logic.",
        "Lines 251-500 devote substantial portability logic to Paperclip skill identity/provenance and export directory naming across catalog, GitHub, skills.sh, URL, bundled and local-path sources. This is product/vendor-specific taxonomy rather than a neutral migration kernel.",
        "Skill paths are ASCII-normalized, lowercased by default and collision-resolved deterministically with source suffixes then truncated eight-hex hashes. The behavior is useful for stable exports but may collapse international names/case-sensitive identities; original canonical key/source provenance must remain authoritative and collision mapping must be explicit in a manifest.",
        "Lines 501-750 redact secret_ref values and heuristic-sensitive plain env defaults, omit PATH, and label absolute-command defaults system-dependent. The key-name heuristic is neither a data classification contract nor complete secret detection (for example generic key/DSN/cert material can evade it, while unrelated names containing auth can be over-redacted).",
        "Portable env inputs are always marked optional and generated descriptions are English. Original binding policy, secret owner/audience, requiredness, data-export permission, residency and rebind workflow are not preserved here.",
        "Internal portability types mirror broad database/service DTOs and introduce explicit board_full versus agent_safe modes, while runtime/adapter defaults begin to be encoded in the same module—further evidence that serialization, policy, product UX and vendor runtimes lack separation.",
        "Lines 751-1000 hardcode default runtime and adapter settings for Codex, Gemini, OpenCode, Cursor, Claude and OpenClaw, including Paperclip-specific fixed session/admin scope defaults. Provider configuration migration is therefore coupled to this central service and will drift whenever adapters evolve.",
        "Permission grants accept known keys but silently discard malformed/unknown entries and preserve any plain-record scope without a versioned scope schema or warning. This can quietly reduce or distort authority during migration instead of producing an explicit review plan.",
        "Comment/document/work-product parsers are tolerant: malformed entries are skipped, invalid dates become null, labels and blockers are deduplicated, and arbitrary work-product metadata survives. Warnings are English strings; authorUserId, URLs and metadata lack explicit privacy/egress classification, while documents normalize referenced paths but do not yet prove existence or content binding.",
        "Lines 1001-1250 normalize embedded assets/blobs/attachments but only require nonempty sha256 strings, not canonical 64-hex digests; byte sizes are merely nonnegative/unbounded, content types and filenames are untrusted strings, and attachment comment indices are not checked against the comment collection here.",
        "Import silently injects --skip-git-repo-check into Codex-local adapter arguments. This changes runtime security posture during data migration and is provider-specific behavior that must instead be an explicit, reviewed Connector compatibility decision.",
        "Routine variables/triggers are permissively normalized and imported timer heartbeat is disabled by default—a useful safety instinct. However absolute/system-path detection is heuristic recursive string scanning and executable settings remain interleaved with serialization rather than passed through a versioned capability/policy admission layer.",
        "Lines 1251-1500 translate workspace IDs to portable keys and may execute local git subprocesses against database-supplied cwd values to infer remote/ref/default branch. execFile avoids shell injection, but there is no timeout/path ownership boundary and exported remote URLs are not scrubbed for embedded credentials or restricted schemes.",
        "Workspace deduplication signature uses only name/repoUrl/repoRef/defaultRef, ignoring visibility, source type, setup/cleanup commands and metadata; distinct security/execution configurations can collapse into one exported workspace.",
        "Arbitrary setup/cleanup commands remain portable unless they contain an absolute-path fragment, and arbitrary repository schemes/metadata are retained. The heuristic removes some machine paths but does not constitute executable-code admission or data-egress governance.",
        "Lines 1501-1750 migrate legacy recurrence into enabled cron triggers with validation and explicit errors for unsupported shapes. End bounds (until/count) are deliberately discarded with only a warning, so a previously finite task can become indefinitely recurring.",
        "The cron approximations are not semantically equivalent for interval schedules: hourly start-23/step resets daily and daily */N is day-of-month based rather than elapsed-day cadence. Migrated automations are labeled but enabled in the manifest, making approval/pause policy load-bearing later in import.",
        "Routine trigger kinds/policies and cron syntax are checked here, which is stronger than the earlier permissive normalization, but the migration contract still lacks a machine-readable semantic-loss classification requiring human acceptance before activation.",
        "Lines 1751-2000 provide deterministic collision naming and file/selection helpers, but binary decoding and streamToBuffer have no local decoded-size/aggregate-memory limit; Node base64 decoding is permissive and unsupported encodings alone fail closed.",
        "normalizeFileMap normalizes and root-strips every path into one object but silently overwrites earlier entries when distinct raw paths normalize to the same key. There is no collision error/provenance record at this boundary, enabling ambiguous or attacker-controlled last-write-wins content.",
        "Agent tree sorting treats missing parents as roots but omits pure cycles and uses repeated queue.shift, which is quadratic for large flat organizations. Sidebar selection/YAML filtering is presentation-aware and depends on path-derived slugs rather than a signed entity/file inventory.",
        "Lines 2001-2250 prune labels/assets/blobs/sidebar metadata for selected exports, but filterExportFiles accepts an arbitrary selected path set and does not itself close transitive dependencies. It can retain manifest references whose actual blob/document file was not selected, producing an internally incomplete package unless callers perfectly expand dependencies.",
        "Extension discovery prefers root names but otherwise takes the first nested suffix match. ensureMarkdownPath checks only .md after slash replacement and does not normalize/reject traversal at this point.",
        "Portable adapter config is produced with a field blacklist (cwd, instruction/prompt paths, env, skill sync). Unknown future filesystem, credential or executable fields are exported by default; this repeats the unsafe policy-drift pattern. Default pruning uses JSON serialization equality and may drop explicit false values or map undefined array entries into later nulls, changing configuration intent.",
        "Lines 2251-2500 recursively strip null/empty values, which improves readability but can erase meaningful explicit empty/null reset semantics. The iterative YAML renderer avoids call-stack/argument overflow for large structures, a good robustness detail.",
        "YAML scalar values are JSON quoted, but object keys are emitted raw. Arbitrary metadata/config keys containing YAML delimiters, control characters or newlines are not escaped here, creating parser divergence or document injection risk in the hand-rolled format.",
        "Selected-file import/export rebuilds COMPANY.md includes and always injects the company document even if not selected. It normalizes selected paths but still relies on the normalized map's last-write behavior and rebuilds entity truth from a presentation package rather than verifying a signed file/entity inventory.",
        "Lines 2501-2750 build skill references from the server process cwd git HEAD and mutable master URLs/trackingRef. The commit may be null or unrelated when cwd is not the source checkout, and withSkillSourceMetadata appends another source entry on each pass without deduplication, so exports are not necessarily stable/idempotent.",
        "The custom YAML parser is intentionally a small subset but is highly tolerant: invalid lines/indentation are skipped, duplicate keys overwrite, malformed quoted/JSON values become raw strings, and unquoted colons are structural. No depth/line/key/value/resource caps appear in this parser segment.",
        "Keys are assigned into ordinary objects without a forbidden-key check or null prototype. __proto__/constructor/prototype inputs can alter local object prototypes or interact dangerously with later Object.assign/spread operations; this requires dedicated adversarial testing and a hardened parser/data container.",
        "Lines 2751-3000 fetch remote text/binary/JSON fully into memory and propagate the full URL/status into errors; no local response-size/content-type limit is visible. Env inputs dedupe case-insensitively with first-wins semantics and build ordinary key-indexed objects, repeating reserved-key/prototype hazards.",
        "Import value resolution falls back from scoped agent/project keys to a global key of the same name. A single supplied secret can therefore bind across multiple principals/scopes unless the caller provides every scoped value, with no data-owner/audience confirmation.",
        "readCompanyApprovalDefault unconditionally returns false, so package reconstruction discards any company-level new-Agent approval default instead of preserving or requiring explicit review. This is a direct governance-semantic loss.",
        "Remote/environment parsing remains tolerant and string-driven; source URLs, secret labels/keys and env descriptions are not yet bound to structured provenance, classification or stable error codes.",
        "Lines 3001-3250 reject only schema versions newer than 6 and otherwise parse every positive legacy number without an explicit per-version migration pipeline. Referenced paths are unioned with every discovered AGENTS/PROJECT/TASK/SKILL file, so unreferenced hidden package content is automatically admitted rather than requiring an allowlisted manifest.",
        "Company reconstruction carries feedbackDataSharingEnabled plus prior consent timestamp, user ID and terms version into the manifest. Consent from another deployment/account context is not inherently transferable; Company OS must require re-consent and preserve old consent only as historical evidence.",
        "Agent fields accept explicit unnormalized/duplicate slugs, arbitrary roles/config/permissions/metadata and default missing adapter type to process. Malformed known permission grants are silently dropped. Safety depends on later validation rather than a typed manifest boundary.",
        "Skill parsing scans all file keys for every skill (O(skills×files)) and trusts the first metadata source. Arbitrary source hostname/URL/trackingRef and mutable refs are reconstructed, while commit may be absent; source provenance and later network resolution are not yet constrained to a verified digest.",
        "Lines 3251-3500 admit project/task slugs and most project/workspace/issue execution, adapter override, metadata, billing, monitor and relationship fields as loosely typed records/strings. Status/priority and referential integrity are deferred, while older-schema semantic losses remain only a warning.",
        "GitHub URL parsing enforces HTTPS but accepts any hostname, credentials and query-provided ref/path/companyPath after only slash trimming; dot segments are not rejected here. Tree URLs also treat only one path segment as ref, so branch names containing slashes are ambiguous.",
        "The service composes nearly every domain service plus mutable environment-controlled secret mode. Imported Agent permission application first creates membership then loops individual grants without one transaction; partial authority can persist, and grantedByUserId may be null for Agent-initiated import.",
        "Provider-specific OpenCode validation and adapter registry admission live inside portability. This confirms adapter compatibility must be isolated behind Connector-owned import planners rather than copied into Company OS core.",
        "Lines 3501-3750 materialize supplied secrets one by one before binding them into the manifest, tracking created IDs for later cleanup but without an evident transaction in this segment. Secrets use the configured default provider and latest version, actorUserId may be null, and scoped inputs can still receive a global fallback value.",
        "Remote GitHub resolution falls main→master, suppresses recursive-tree errors into an empty tree, then sequentially fetches every Markdown and every path under skills plus explicit includes. There is no visible file/count/aggregate-byte/concurrency cap; a large or hostile repository can cause many network calls and memory growth.",
        "Include paths are joined to basePath without rejecting dot segments here, and arbitrary HTTPS hostnames are supported. This creates an egress/SSRF and repository-boundary question delegated to ghFetch/URL helpers; Company OS requires an explicit destination allowlist and normalized path containment proof.",
        "The export path begins by loading managed-resource markers, all Agents including terminated, full skills and other domain data into memory. Built-in/terminated resources are filtered with warnings, showing product-specific lifecycle policy embedded in serialization.",
        "Critical lines 3751-4000 implement explicit-selector fallback unsafely: if include.agents/projects/issues is true but every requested selector is missing or skipped, the empty selected map is interpreted as select all. A typo or forbidden built-in selector can therefore export the entire category rather than nothing/error, violating data minimization and user intent.",
        "Issue selector dispatch uses contains('-') to choose identifier lookup; UUID IDs also contain hyphens, so direct UUID selection is misrouted and can trigger the export-all fallback above.",
        "Issue/project selector resolution performs sequential service calls, project-issue expansion can load full lists repeatedly, and routine details use unbounded Promise.all. The whole export is memory-resident with no pagination/snapshot boundary, so data can be inconsistent under concurrent writes and expensive for large companies.",
        "Permission grants and built-in/archived/terminated filtering are copied into export-specific maps with silent drops and warnings rather than governed export policy/evidence. No per-data-domain authorization is visible inside the service.",
        "Critical lines 4001-4250 repeat the selector bug for skills and appear to export selectedSkillRows without an include.skills guard. Because skills are loaded whenever Agents are included (the default), an Agent-only/default export can place every non-managed company skill file into the bundle even when include.skills is false.",
        "Skill file reads are sequential and failures are silently ignored, yielding incomplete packages without a warning. Agent instruction files, arbitrary metadata/config/permissions and project workspace commands are assembled into one in-memory file map; only a single adapter command field gets an absolute-path check.",
        "Company logo storage streams are fully buffered, and raw storage/asset errors are copied into warnings. Exported binary size/content is not bounded here.",
        "Task export begins per-issue comment and relationship service calls inside a loop, creating clear N+1 behavior. Labels are loaded once and missing references warned, which is the stronger pattern.",
        "Lines 4251-4500 add per-task document, work-product and attachment queries/storage reads, compounding N+1 and fully buffering every attachment. Blob bytes are correctly rehashed and deduplicated rather than trusting stale database hashes—a strong integrity pattern—but no total/file size cap or snapshot consistency is visible.",
        "Raw board user IDs on comments are deliberately removed, improving privacy but collapsing author identity to kind=user; arbitrary presentation/metadata still travels. Company OS needs pseudonymous/source principal references and provenance, not either raw cross-tenant IDs or erased accountability.",
        "Invalid comment timestamps can throw during new Date(...).toISOString(), while missing external blocker/workspace/run references are dropped with English warnings. Migration loss is not structured or acceptance-gated.",
        "Embedded asset export correctly verifies asset.companyId before reading bytes, blocking crafted cross-company asset URLs. It still performs sequential asset/storage reads into memory and derives ownership from regex matches in rendered text/JSON rather than an authoritative relation table.",
        "Lines 4501-4750 serialize prior feedback-sharing consent into the bundle, rebuild the parsed manifest twice, and generate an org-chart PNG through a route-layer renderer. Renderer errors are silently swallowed; transport preview invokes the full export including storage, local instructions, Git/GitHub and image rendering instead of a cheap side-effect-free plan.",
        "README and org-chart presentation artifacts are inserted after selection/reparse rules with special cases, while the manifest has no authoritative complete file inventory/digest. Package truth is repeatedly re-derived from files, increasing parser differential and selection inconsistency risk.",
        "Import preview correctly treats missing explicitly selected Agent slugs as errors (unlike export selector fallback), validates referenced Markdown existence and checks ambiguous skill references. However ensureMarkdownPath still only enforces suffix, and warning/error contracts remain localized English strings.",
        "The previewExport issues expression contains Boolean(...) ?? false, whose fallback is unreachable; minor dead logic illustrates how this monolith mixes planning and execution semantics.",
        "Lines 4751-5000 validate routine/workspace references and plan collisions against current target Agents/projects/skills, but the preview is not bound to a target revision/lease; actual state can change before apply, invalidating names and overwrite decisions.",
        "Existing Agent matching is based on normalized display name slug, projects on urlKey, and skills on key OR slug. Replace can therefore update a coincidental target entity rather than a provenance-bound identity; source reportsToExistingAgentId is also trusted if an opaque ID happens to exist in the target.",
        "nameOverrides are applied to issuePlans before issuePlans are populated, so task title overrides are silently ignored. Agent/project overrides are not collision-revalidated after replacement and can create duplicates or alter the preview/apply mapping.",
        "Agent-safe project env validation catches errors into raw message strings. Required human/data-owner confirmation, stable codes and an immutable reviewed plan digest are absent from the preview contract.",
        "Lines 5001-5250 verify every blobs/<suffix> file hash before writes, a strong fail-before-mutation integrity step. The suffix is not proven canonical in earlier parsing, and non-blob files such as company logo/Markdown are not sealed by a complete manifest digest.",
        "Import creates or updates the company—including feedback data-sharing consent—before secret materialization and all other entities. There is no enclosing transaction, so later failure leaves a partial company or already-mutated governance/data-sharing state.",
        "Agent-safe new-company import requires merely one active source user membership then copies all active memberships to the new company. It does not require each human to accept the new organization/responsibility contract, and role/authority transfer is automatic.",
        "Board-full creation falls back to synthetic user board when actorUserId is null. Logo upload→asset row→company link is multi-step and failures become warnings; stored objects/assets can orphan and the old/new logo lifecycle is not atomic with the import.",
        "Lines 5251-5500 upload embedded assets before skills/Agents and continue on per-asset errors, leaving partial assets by design. Later import failure has no shown asset/object compensation, only secret IDs are tracked.",
        "Skills are imported whenever include.agents OR include.skills, confirming that an Agent-only import mutates the target skill catalog even when skills were not requested. Package text files are passed broadly to the skill importer and conflict behavior is only rename/skip in safe mode.",
        "Agent replace/create is a long nontransactional chain: mutate Agent, materialize filesystem instruction bundle, add membership/default and imported grants, then sync secret bindings. Materialization failures are downgraded to warnings, so an idle/updated Agent can exist with empty or stale instructions; later failures leave partial authority/configuration.",
        "Replace updates broad Agent configuration but imported permission grants are additive and existing grants are not cleared here, so replace does not reproduce source authority. New Agents receive tasks:assign by default in addition to imported grants, again changing the source permission model.",
        "Lines 5501-5750 assign reporting links after Agent creation and downgrade assignment failures to warnings; no explicit cycle/responsible-human validation is visible here. Opaque existing-manager IDs/slugs can bind source hierarchy to target principals by coincidence.",
        "Project replace updates the project but then creates every imported workspace without reconciling/removing existing workspaces, so replace is additive and may duplicate executable workspace configurations. Secret sync, workspace creation and policy hydration are separate side effects with no rollback.",
        "Board-full import stores repo URLs plus setup/cleanup commands directly in target workspaces. Safe mode's earlier blacklist helps, but full import has no risk approval, code provenance or activation gate for these executable instructions.",
        "Issue import improves performance by prebuffering typed rows for batched writers and resolves labels once. Legacy raw label IDs survive when coincidentally present in the target, another cross-deployment opaque-ID collision that can silently attach unintended labels.",
        "Lines 5751-6000 create each routine and then its triggers in separate calls. Trigger failure leaves a partial routine; imported schedules/webhooks/API triggers may be enabled immediately unless pauseAutomations or missing-assignee behavior prevents execution, with no per-action human approval.",
        "Comment authorship is rewritten: source user comments become authored by the importing user, missing users/Agents become system. This destroys source responsibility provenance; metadata and createdAt travel without a source principal/evidence envelope.",
        "Issue rows and child rows are buffered with pre-generated IDs, a sound performance/atomic-batch preparation pattern. Documents/work products deliberately clear run/workspace/sourceTrust references and monitors land unarmed, good safety choices but still unstructured semantic loss.",
        "Attachment hashes are rechecked and board size limit enforced, but only after the base64/blob has already been decoded and held in memory. This prevents oversized persistence, not memory-exhaustion during parse/apply.",
        "Lines 6001-6179 flush issues, inbox archives, comments, documents, work products, attachments and blocker relations through separate service/database calls. A later failure leaves earlier categories committed; storage objects were uploaded even earlier. There is no whole-import transaction, durable saga or replayable checkpoint.",
        "Only newly created secret IDs receive best-effort compensation on failure, and cleanup errors are swallowed. Companies, memberships, consent, logos/assets, embedded assets, skills, Agents, permissions, instruction bundles, projects/workspaces, routines/triggers, labels, issues and stored objects are not rolled back or reconciled.",
        "Blocking-cycle filtering is a useful fail-soft integrity step and relation insertion is batched/on-conflict-ignore, but repeated queue.shift can become quadratic. Monitors remain unarmed and imported inbox items are suppressed for a board user, both thoughtful operational safeguards.",
        "The result reports company/Agents/skills/projects/routines/env inputs but not imported issue/document/evidence identities or a canonical request/result digest. Route activity is therefore unable to provide a complete immutable responsibility chain for the actual migrated data."
      ],
      "completionRequirements": ["Read lines 1-6179 exactly once and trace export/import formats, schemas, parsing/path safety, every entity lifecycle, transactions, collision/idempotency, secrets/data provenance, responsibility, performance and dependency direction against completed route/batching evidence."]
    },
    {
      "id": "application:server/__tests__/company-portability-routes",
      "path": "server/src/__tests__/company-portability-routes.test.ts",
      "lineCount": 1236,
      "sha256": "8ebe51a11acdb3698d57d37c75f7fd55d9294784561b445b82f42d2060135f92",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 1236 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-300 build mocked Express/Supertest apps with dynamic module imports to isolate each route-local in-memory job map, plus a shared app for per-user job isolation. Service side effects are mock-observable, but no real persistence, transaction or schema behavior is exercised.",
        "Primary board/cloud fixtures are owner plus instance admin, so positive cases cannot distinguish membership, ownership, platform authority or accountable-human approval. Polling uses twenty 10ms retries, creating timing sensitivity while proving only same-process completion.",
        "The suite prepares JSON and hand-built STORE ZIP inputs and explicit cloud stack/company headers, useful for transport-path coverage but not yet adversarial compression/resource/provenance testing.",
        "Lines 301-600 deliberately permit a same-company free-form ceo Agent to preview and export full bundles through both legacy and safe routes, and permit a board actor whose fixture has only companyIds (no explicit membership role) to export. They correctly deny non-CEO/cross-company calls before validation/service side effects.",
        "Agent-safe import blocks replace and mismatched route/target companies, while the global preview remains board-only. These are valuable negative boundaries, but no human approval, data classification/export authority, purpose contract, evidence digest or step-up authentication is required for the positive high-risk paths.",
        "Lines 601-900 require instance admin only for new-company global imports, then codify Cloud async behavior: return 202 before full payload validation, store raw service error messages, keep terminal jobs one hour and drop them to 404, while synchronous execution remains the default absent an opt-in header.",
        "Successful job state and company.imported activity are checked, but the activity follows import completion and failure activity is absent. The test intentionally accepts invalid requests as running jobs and exposes internal exception text to the tenant, confirming unstable error semantics and ambiguous responsibility rather than guarding against them.",
        "Lines 901-1236 cover board full-result jobs, cross-user 404 isolation, same-user same-request adoption, different-request rejection, retry after terminal failure, board-only status, pauseAutomations, JSON/multipart ZIP sync and async signals, and truncated-ZIP rejection.",
        "Duplicate control is per user, so two board users may concurrently import the same target; tests explicitly allow this and do not cover target-level serialization. Board jobs retain and return the entire import result in memory. Only a truncated ZIP is adversarially tested—no traversal, duplicate names, symlink, encryption, compression ratio, entry count, Unicode collision or oversized meta cases.",
        "CEO Agent apply is expected to call importBundle with a null boardUserId. This proves the high-risk mutation has no accountable human in the service call; Agent ID/run context appears only in a later best-effort activity log and is not bound to an approval, request digest or durable job."
      ],
      "completionRequirements": ["Read lines 1-1236 exactly once and assess every portability route actor/profile, preview/export/import contract, async job lifecycle, idempotency, errors, side effects and test quality."]
    },
    {
      "id": "application:server/__tests__/company-portability-import-batching",
      "path": "server/src/__tests__/company-portability-import-batching.test.ts",
      "lineCount": 485,
      "sha256": "f964b5a772adf2fb368c4f1acd0d6343f6fb4328fc484f0521f14f5dae4e2150",
      "linkedEvidence": { "history": ["075951f6bde525de0ff5b0daad216f60c3083ded Fix import completion UX: inbox flood, false-failure message, stale company list (#10538)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 485 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 use embedded PostgreSQL and monkey-patch Drizzle db/transaction insert methods to count statements. This can detect regressions to one-insert-per-row but couples the test to implementation mechanics and does not measure query size, parameters, memory, locks or commit duration.",
        "Synthetic schema-v6 bundles exercise issues, comments, documents and inbox behavior. Test data is rendered with a hand-rolled block-YAML algorithm specifically because the importer is not a general YAML reader; round-trip compatibility is covered, but adversarial YAML/parser divergence and schema evolution are not yet visible.",
        "Lines 251-485 prove 50 issues/comments use at most two insert statements per table, imported row counts and identifier uniqueness, per-user inbox suppression, and transaction rollback when the second of two 500-row work-product chunks violates a foreign key. The optional 500-issue benchmark asserts at least 50x statement reduction but is skipped unless explicitly enabled.",
        "Atomicity coverage is limited to createManyForImport for work products, not the full multi-entity company import. No memory/ZIP/parser limits, cancellation, progress, concurrent import, retry/idempotency, lock duration, deadlock or crash-recovery cases appear.",
        "Imported comments marked authorType=user are deliberately re-attributed to the importing board user. This makes inbox behavior convenient but changes authorship provenance; Company OS must preserve original author/source and separately record importer/normalization actor rather than rewriting responsibility history."
      ],
      "completionRequirements": ["Read lines 1-485 exactly once and assess batching semantics, transaction/rollback, progress/event volume, large import safety, ordering and test quality."]
    },
    {
      "id": "application:server/routes/company-import-paths",
      "path": "server/src/routes/company-import-paths.ts",
      "lineCount": 2,
      "sha256": "91f11b27a570d9c826e7d145c186847ee96fbe759a0e044f7090e3047eda658e",
      "linkedEvidence": { "history": ["c91a06232625d1939858fcdfbdc0c9b3a64a8296 [codex] Runtime control-plane fixes (#6380)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 2 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "This module only centralizes the relative /import route and assembled /api/companies/import path. It prevents string drift across server/UI consumers but is an unversioned transport constant, not an API, capability or portability contract.",
        "Company OS can apply the single-owner route-name pattern inside its Web/API adapter, while core/application and Connector SDK must depend on typed versioned commands/events rather than HTTP path constants."
      ],
      "completionRequirements": ["Read lines 1-2 exactly once and determine whether this is a stable API boundary, compatibility re-export or ownership leak."]
    },
    {
      "id": "application:server/__tests__/companies-service",
      "path": "server/src/__tests__/companies-service.test.ts",
      "lineCount": 867,
      "sha256": "c67df6127649209de5ca4b042dabf65ba0b2cd76388cde6f996116b9b9575934",
      "linkedEvidence": { "history": ["1f1f54523876481ab0011c3fdd0b842753cd511a feat: add built-in summarizer and summary slots (#9713)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 867 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-220 use real embedded Postgres and thorough table cleanup. They cover generated issue-prefix collision retry, automatic creation of a paused Reflection Coach Agent/skill/routine bundle and the beginning of company archive cascade setup.",
        "Fresh company creation silently provisions first-party Agent resources. The test proves idempotent startup reconciliation but not whether installation is explicitly consented, profile-controlled, transactionally coupled to company creation or attributable to a human decision.",
        "Lines 221-440 prove archive atomically-looking behavior at the observable level: runnable Agent states become paused with company_archived provenance, active heartbeat run and wakeup request are cancelled, and one activity records actor plus counts. Reactivation restores only archive-paused Agents and preserves manual/pending states.",
        "The assertions cover state discrimination well but use a free-form test user and English cancellation text; they do not yet bind approval/action digest, exact responsible human, connector cancellation acknowledgement, evidence or failure rollback.",
        "Lines 441-660 verify update(status=archived) shares the archive cascade, that archive provenance survives an intermediate company paused state, and that archived-to-active emits a reactivation event even when zero Agents restore while ordinary paused-to-active should not. This is a thoughtful lifecycle distinction Company OS should preserve.",
        "Lines 661-867 add orphan queued-wakeup cancellation, repeat-archive idempotency and paused-to-archived coverage. The suite is strong on database end state and duplicate lifecycle activity, but omits concurrent archive/reactivate races, injected mid-transaction failures, external Connector cancellation/attestation, human authorization/approval, event outbox durability, hard-delete behavior and defaultResponsibleUserId integrity."
      ],
      "completionRequirements": ["Read lines 1-867 exactly once and assess company lifecycle, archive/reactivation cascades, responsible-human semantics, side-effect ordering, failure recovery and test quality."]
    },
    {
      "id": "application:server/services/companies",
      "path": "server/src/services/companies.ts",
      "lineCount": 508,
      "sha256": "923b1c5ece751346722724651660f1771ff7d0cfffa3d8084f8dadc8cd9d2f55",
      "linkedEvidence": { "history": ["ded813ad6fae2c0b2c9009e04f59b117ecae0151 feat(interactions): add governed agent addressees (#10252)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 508 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-180 reveal a very broad service dependency surface across organization, identity grants, Agents/runtime, work, approvals, finance, secrets, skills, documents, routines, assets and activity.",
        "Archive first pauses Agents and cancels orphan wakeups inside a database transaction, but active run cancellation through heartbeatService and the company.archived activity write occur afterward in finalizeArchive outside that transaction. A failure can leave company/Agent state committed while runs continue or audit evidence is absent; the activity count reflects intended run cancellations rather than acknowledged cancellations.",
        "Lifecycle cancellation reasons are persisted English display strings, not stable reason codes with structured parameters.",
        "Lines 181-360 compute monthly spend in a second grouped query, avoiding per-company N+1. Issue prefixes retry unique-constraint collisions up to 9,999 times with progressively repeated A suffixes; it handles wrapped database errors but has no bounded naming policy or idempotency key.",
        "Company creation inserts the company, then ensures a local environment and auto-provisions bundled Agents in separate service calls without one transaction/compensation. Partial creation is possible. Update does use a transaction for company fields, logo ownership, Agent reactivation and initial archive cascade.",
        "Lines 361-508 confirm reactivation activity and archive run cancellation/activity happen after commit. A failure returns an error after durable state changed; no outbox/retry reconciler closes the gap. Sequential idempotency exists, but archive reads status without an explicit row lock or compare-and-set, so concurrent callers can both observe non-archived and emit duplicate cascades/evidence.",
        "Hard delete manually erases activity, approvals/comments, cost/finance events, secrets, work, runs and organization data inside one transaction before deleting the company. This is internally atomic but destroys the responsibility/evidence chain and is fragile as new child tables are added; Company OS requires retention/legal hold, tombstones and separately governed cryptographic erasure instead.",
        "The service accepts a broad partial database insert shape rather than explicit lifecycle commands. It has no authorization or responsible-human invariant internally and depends completely on callers for these product-critical rules."
      ],
      "completionRequirements": ["Read lines 1-508 exactly once and trace CRUD, lifecycle cascades, responsible principals, events/activity, database transactions, idempotency and dependency direction."]
    },
    {
      "id": "application:server/services/company-member-roles",
      "path": "server/src/services/company-member-roles.ts",
      "lineCount": 65,
      "sha256": "45cd72eb296156bc1d24212d586b47b33f6642a33c92a9fcce9da06a743537ef",
      "linkedEvidence": { "history": ["8b6a06ee254b06cbadbe334e891285e22084caab [codex] Add built-in agents and Reflection Coach bundle (#9206)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 65 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The module defines four fixed human roles and deterministic default grants. Owner/admin/operator/viewer are understandable, and legacy member maps to operator, but unknown or malformed input also defaults to operator rather than denying or requiring repair.",
        "All grants use null/global scope. There is no department/project/data-domain scoping, deny, expiry, delegation, separation of duties, risk class, approval authority, role version or policy provenance. Owner differs from admin only by users:manage_permissions.",
        "Critical company lifecycle, portability and data-sharing operations are absent from permission keys and the audited company route checks only active membership, so this role model does not actually govern archive, hard delete, export or import. Company OS may reference the simple role-to-grant projection but must own a versioned policy/authority model tied to accountable humans."
      ],
      "completionRequirements": ["Read lines 1-65 exactly once and assess role derivation, privilege stability, extension behavior and coupling to company/Agent responsibility semantics."]
    },
    {
      "id": "application:server/__tests__/companies-route-cross-company-authz",
      "path": "server/src/__tests__/companies-route-cross-company-authz.test.ts",
      "lineCount": 357,
      "sha256": "05bfe9c57ce625915a6e5a20a31433d602cafa828fd4fbf756fed27b217d4449",
      "linkedEvidence": { "history": ["916c13501f80c7d1d659b89e4a06dc47366aa80f Replace host-to-host Cloud Sync with full-fidelity company Import/Export (#10507)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 357 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The table-driven suite denies ten cross-company CEO Agent operations before service/activity side effects and covers board non-member/viewer/member/local-implicit/instance-admin access.",
        "It deliberately allows a same-company CEO Agent to update branding, export full company data and apply imports; only archive/delete are board-only. It also deliberately allows any active board member—not only owner/admin—to archive, delete, export and apply imports.",
        "Instance admin without target membership is correctly denied, while local_implicit instance admin bypasses membership. Tests omit accountable-human/approval/data-contract requirements, concurrency/idempotency, transaction rollback and audit completeness."
      ],
      "completionRequirements": ["Read lines 1-357 exactly once and assess cross-company list/get/update/delete, actor/member/admin authorization, responsibility ownership and test quality."]
    },
    {
      "id": "application:server/routes/companies",
      "path": "server/src/routes/companies.ts",
      "lineCount": 1027,
      "sha256": "d7b733d199ac37f61b5c3072b4c86a63ab2abe881cfee1cf49c37a1d6b74870b",
      "linkedEvidence": { "history": ["5ca752dc81cfb01f74282e1b562694079e1d0cf1 fix(server): raise company import zip upload limit to 1 GB and make it operator-configurable (#11184)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 1027 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-520 centralize company reads, timeline, portability export/fidelity and import orchestration. Board access is membership-based; same-company Agents are admitted only by the free-form role string ceo, with no accountable-human, approval or data-export contract.",
        "Import accepts JSON or in-memory multipart/bare ZIP. The configurable upload ceiling can reach 1 GiB while decompression permits at least four times that ceiling; the request is fully buffered and parsed before async handoff, creating material memory/decompression denial-of-service exposure.",
        "Async jobs are process-memory records, disappear on restart and may return 202 before target authorization/body validation. The client is told to treat a later 404 as soft success after observing running, so completion is ambiguous. Cloud actors lack the local duplicate-import guard; import mutation and activity logging are not shown as one transaction.",
        "Lines 521-700 expose same-company safe export/import to the CEO Agent and every active board member. Replace/cross-company targeting is blocked, but there is no human approval, export classification, evidence digest or owner/admin role requirement; import mutation precedes separately fallible activity logging.",
        "Company creation is cloud-disabled and instance-admin/local-implicit only, but creation, owner membership, default grants, activity and budget policy are sequential operations without an enclosing transaction. The default accountable principal falls back to the synthetic local-board value, conflating convenience identity with a verifiable human.",
        "Lines 701-850 show company update/branding mutations followed by separate activity writes. Archive and hard delete require only an active board membership, not owner/admin, step-up authentication, approval, retention/legal-hold policy or deletion evidence.",
        "Import job records retain full board results in process memory and key board jobs by user ID (or local-board); cloud job identity also trusts selected middleware-populated headers. Error state stores only a message and the lifecycle has no durable claim, idempotency key, cancellation, timeout, progress, result digest or restart recovery.",
        "Lines 851-1027 confirm deduplication is only an in-process per-actor scan and a SHA-256 of JSON serialization, not a durable idempotency contract. Terminal responses duplicate raw English error messages across error/message/reason, and successful board jobs retain the entire import result until timer cleanup without explicit data minimization or stable error codes."
      ],
      "completionRequirements": ["Read lines 1-1027 exactly once and trace company CRUD, membership/responsible human, budgets, deletion, portability/import/export, data limits, approvals/audit and transaction boundaries."]
    },
    {
      "id": "application:server/routes/cloud",
      "path": "server/src/routes/cloud.ts",
      "lineCount": 118,
      "sha256": "0e193c6e4eb1e0e61ef55cac0a8b039e763a57e626e3287488e3e22bb39f4faf",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 118 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The route correctly derives identity from the trusted cloud_tenant actor, never forwards client authorization/query identity, requires full server-side context and uses a 10-second abort with stable upstream/not-configured codes.",
        "cloudOrigin accepts any parseable absolute URL without HTTPS/host allowlist/private-network validation, then receives the tenant bearer token. Configuration compromise or mistake can exfiltrate the server token or create SSRF; validation should fail at immutable startup, not per request.",
        "The upstream payload is unknown/unbounded and cached without schema/content-type/size validation. The per-router Map has no entry cap; every request scans all entries, enabling memory and O(n) cleanup growth across many valid actor IDs.",
        "No circuit breaker, retry/backoff, token rotation cache invalidation or response provenance exists. The Paperclip Cloud API, headers and portfolio DTO are tightly vendor coupled and unsuitable for Company OS core/application."
      ],
      "completionRequirements": ["Read lines 1-118 exactly once and trace cloud upstream URL/token/actor derivation, caching, response/error handling, SSRF/secret boundaries and vendor coupling."]
    },
    {
      "id": "application:server/services/cloud-instance",
      "path": "server/src/services/cloud-instance.ts",
      "lineCount": 52,
      "sha256": "9551e3ef6a74825a38af67e8d6463f62a05cd08647ded265ba8dc49b901c9129",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 52 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Cloud-managed mode is inferred from a nonblank tenant token or mere presence of PAPERCLIP_MANAGED_CONFIG; the union intentionally preserves restrictive legacy floors.",
        "The predicate and context read a mutable ambient environment by default. An empty managed-config value switches product semantics, while stack IDs/slug/host/origin are only trimmed, not structurally validated or frozen at startup.",
        "Paperclip-specific deployment and provisioner concepts live in a server adapter helper, but consumers receive a broad boolean/context rather than a validated immutable DeploymentProfile/capability contract."
      ],
      "completionRequirements": ["Read lines 1-52 exactly once and assess managed-profile inference, metadata validation, runtime mutability and adapter/core ownership using existing cloud-instance test evidence without rereading it."]
    },
    {
      "id": "application:server/__tests__/cloud-routes",
      "path": "server/src/__tests__/cloud-routes.test.ts",
      "lineCount": 149,
      "sha256": "96054e835d6363311ea03e4303e1397b4aec2ba459b426f1a3962bede130a35b",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 149 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Five tests prove server-derived actor identity replaces client query/header/bearer inputs, successful portfolios cache 30 seconds per actor, self-hosted returns 404 and non-cloud-tenant actors are denied.",
        "Upstream timeout/error/non-JSON/oversize/schema, cloud origin validation/SSRF, token leakage/rotation, cache bounds/invalidation, stack/account-group isolation and response sanitization are absent.",
        "Approved unsandboxed focused Vitest with PAPERCLIP_HOME isolated under /private/tmp passed all 5 route tests."
      ],
      "completionRequirements": ["Read lines 1-149 exactly once and assess cloud route exposure, metadata redaction, platform floors and test quality."]
    },
    {
      "id": "application:server/__tests__/company-cloud-floor",
      "path": "server/src/__tests__/company-cloud-floor.test.ts",
      "lineCount": 124,
      "sha256": "4949866937b2d42e14e49215c4ff02d0fb2531575b3a726af3118a51c7b28b1d",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 124 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Three tests prove cloud-managed company creation is denied before body validation/service mutation and self-hosted local implicit creation remains available with owner membership.",
        "Only tenant-token signal is exercised despite setup clearing managed-config too. Session/Agent actors, existing-company count/races, malformed ambient profile, authorization/audit, idempotency and deletion/import/restore lifecycle floors are absent.",
        "Approved unsandboxed focused Vitest with PAPERCLIP_HOME isolated under /private/tmp passed all 3 company-floor tests."
      ],
      "completionRequirements": ["Read lines 1-124 exactly once and assess restrictive platform floors for tenant/company lifecycle, actor distinctions and test quality."]
    },
    {
      "id": "application:server/__tests__/dev-server-status",
      "path": "server/src/__tests__/dev-server-status.test.ts",
      "lineCount": 103,
      "sha256": "73955b7b1403a81ef65558f79c53638ad29d12024dab1738529b0db2f3a33696",
      "linkedEvidence": { "history": ["f25753053749d42fa808a9d92132467d2d75dcd0 [codex] UI and dev ops quality-of-life (#6384)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 103 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four helper tests cover normal status normalization, waiting-for-idle projection, 64KiB file cap and restart request placement/content.",
        "Malformed types/ranges/dates, path traversal, file ownership/mode/links, atomic partial writes, concurrent requests, stale status/request and cleanup/acknowledgement are absent.",
        "Focused Vitest passed all 4 helper tests in the combined 7-test run."
      ],
      "completionRequirements": ["Read lines 1-103 exactly once and assess persisted status/restart request parsing, file trust, projection semantics and test quality."]
    },
    {
      "id": "application:server/__tests__/health-dev-server-token",
      "path": "server/src/__tests__/health-dev-server-token.test.ts",
      "lineCount": 213,
      "sha256": "c0649844c8fca7124b4fbb73bff2009b651f33797fc458b8b22caecad0eb2cc4",
      "linkedEvidence": { "history": ["51bb41c7e334440566ecf63cfc96fc36da05d7d2 Expose the running build commit on the unauthenticated health response (#10563)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 213 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Three route tests cover correct diagnostic token disclosure, unauthenticated local_trusted restart request and unauthenticated authenticated-mode denial.",
        "Wrong/empty/different-length tokens, token rotation/replay, Agent and ordinary board callers, CSRF, production mode, restart idempotency/concurrency, file trust and audit evidence are absent.",
        "Approved unsandboxed focused Vitest with PAPERCLIP_HOME isolated under /private/tmp passed all 3 route tests in the combined 7-test run."
      ],
      "completionRequirements": ["Read lines 1-213 exactly once and assess diagnostic-token disclosure, board/anonymous restart authorization, error codes and test quality."]
    },
    {
      "id": "application:server/dev-server-status.ts",
      "path": "server/src/dev-server-status.ts",
      "lineCount": 134,
      "sha256": "a614c80e82f1c4cdad3471d76556d8281507b02ef86ee0f4c267b124bef42209",
      "linkedEvidence": { "history": ["f25753053749d42fa808a9d92132467d2d75dcd0 [codex] UI and dev ops quality-of-life (#6384)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 134 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "A file-based, unversioned protocol reads supervisor change/migration state and writes a fixed sibling restart request; a pure projection derives restart reason and waiting-for-idle from active run count.",
        "Status size is checked before synchronous read, but stat/read follows symlinks and has a TOCTOU growth window. File ownership/mode/type are not validated; timestamps are arbitrary strings and changedPathCount/activeRunCount have weak or no bounds.",
        "Restart request writing creates directories and truncates/follows an existing file with default permissions, no atomic rename/fsync/link defense or ownership check. It contains no request ID, actor, expected status/config digest, expiry, acknowledgement or result.",
        "The protocol is useful for local development but is not a safe production control contract and cannot prove active work drain/adoption or responsibility continuity."
      ],
      "completionRequirements": ["Read lines 1-134 exactly once and trace status/restart file paths, schema validation, atomicity, permission/link safety and control-plane coupling."]
    },
    {
      "id": "application:server/__tests__/health",
      "path": "server/src/__tests__/health.test.ts",
      "lineCount": 547,
      "sha256": "5ee55dbd437c5081bccc2c49e9c8d9e1c7d2af321c66306a1f942761b1ec2cd0",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 547 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Fifteen health route tests cover basic/version/git metadata, database failure 503, cloud public metadata, backup stale/failure warnings, anonymous redaction, authenticated detail and bootstrap status.",
        "Anonymous authenticated-mode tests explicitly require redacted backup warning messages and omit paths/serverInfo, while local_trusted returns full source metadata and raw backup paths/messages.",
        "Backup fixtures are not removed. No tests cover backup corruption/zero size/future mtime/symlink, DB probe timeout, health abuse/rate limiting, Agent actor detail, malformed runtime environment or stable schema compatibility.",
        "Approved unsandboxed focused Vitest with PAPERCLIP_HOME isolated under /private/tmp passed 15/15."
      ],
      "completionRequirements": ["Read lines 1-547 exactly once and assess public/internal health redaction, backup warnings, deployment/auth state, liveness/readiness semantics and test quality."]
    },
    {
      "id": "application:server/routes/health",
      "path": "server/src/routes/health.ts",
      "lineCount": 284,
      "sha256": "9bfc08b73aa75532788eb947bdf9472ee7b6689baab6ffb1f2dd2d768db8506f",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 284 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The router exposes liveness/readiness-like GET health plus a mutating dev-server restart endpoint. It probes DB, bootstrap roles/invites, optional dev-run state and synchronous backup health, with public cloud/commit metadata and redacted anonymous authenticated responses.",
        "Authenticated Agent actors receive the same full details as board users, including serverInfo, absolute backup paths and raw failure marker text. This is broader than least privilege and is not covered by the reviewed health tests.",
        "Every public health request can cause DB role/invite/settings/run-count queries and synchronous unbounded filesystem scanning; no timeout, cache or rate control exists. Secondary query/inspection failures are not normalized, and status remains ok for backup warning, bootstrap_pending or authReady false, so liveness/readiness/degraded semantics are conflated.",
        "POST /dev-server/restart allows any board actor in authenticated mode and any caller in local_trusted when a persisted supervisor file exists; it lacks admin/permission, CSRF, approval, environment-mode assertion, idempotency and audit evidence despite mutating process state.",
        "Anonymous backup path/message redaction and timing-safe dev status token comparison are good, but public commit/cloud metadata and full-detail policy must be explicit deployment contracts."
      ],
      "completionRequirements": ["Read lines 1-284 exactly once and trace public/internal health authorization, data redaction, readiness dependencies, backup health projection and stable contracts."]
    },
    {
      "id": "application:cli/commands/db-backup",
      "path": "cli/src/commands/db-backup.ts",
      "lineCount": 102,
      "sha256": "b15ab7ac36e72845e9680be38e7ab968380ff1417211608be75968db985cf2af",
      "linkedEvidence": { "history": ["73eb23734f25c570d88665e672f80d740797c632 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 102 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The CLI resolves external or embedded Postgres without printing the credential, delegates the actual backup/retention mechanics to the already-audited @paperclipai/db package, and supports config/dir/retention/prefix/JSON options.",
        "JSON mode still emits banner, prompts/spinner and outro, so stdout is not a clean machine contract. Results expose absolute backup paths and lack backup ID, checksum, schema/app version, encryption state, consistency point and stable error codes.",
        "No CLI restore command exists in the reviewed CLI tree even though update rollback instructs users to restore a pre-update backup. This command creates a gzip SQL backup but exposes no encryption/envelope, offsite verification or accountable operator/evidence binding."
      ],
      "completionRequirements": ["Read lines 1-102 exactly once and assess backup target/config, consistency, encryption, retention, output and failure semantics against already-audited package:db evidence."]
    },
    {
      "id": "application:server/__tests__/instance-database-backups-routes",
      "path": "server/src/__tests__/instance-database-backups-routes.test.ts",
      "lineCount": 195,
      "sha256": "a2928440169df27ddd9a67cfbfab8f8ef4ed9cd8053eabfe8a6a3a7f00ead3a4",
      "linkedEvidence": { "history": ["c274f10abc0e3c60a47e6ac3f786443b0e197543 feat(server): computed owner instance-admin elevation for cloud-managed instances, behind platform floors (#10343)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 195 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Seven route tests cover instance-admin/local-implicit authorization, ordinary user and Agent denial, concurrent-backup conflict, and a cloud-managed platform floor that denies every tenant admin.",
        "The response exposes absolute backupFile/backupDir paths. Tests do not cover unauthenticated/malformed actors, CSRF, audit records, rate limits, backup result integrity/encryption, stable conflict codes outside the cloud floor or cross-tenant/server-token edge cases.",
        "The first sandboxed run could not import because logger initialized ~/.paperclip; an isolated PAPERCLIP_HOME rerun entered tests but all 7 were blocked by sandbox TCP policy. The approved unsandboxed run with the isolated home passed 7/7."
      ],
      "completionRequirements": ["Read lines 1-195 exactly once and assess identity/permission boundaries, trigger/status behavior, tenant isolation and test quality."]
    },
    {
      "id": "application:server/routes/instance-database-backups",
      "path": "server/src/routes/instance-database-backups.ts",
      "lineCount": 41,
      "sha256": "99152213c4e7a3632d7def6c57fc3bafe7487beff694c89068b37393b7c276e9",
      "linkedEvidence": { "history": ["5858ccb981717b6589b66e7f0cc69abe3e5fc439 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 41 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "A single POST route requires instance-admin authority, disables tenant-triggered backups on cloud-managed instances and returns the full server backup result.",
        "There is no idempotency key, request/approval/evidence record, step-up authentication, data-export policy or restore API. Local implicit board authority can trigger full-instance export, while the response reveals filesystem paths in non-cloud profiles.",
        "Concurrency is delegated to the service and represented by generic error handling rather than a versioned backup operation/status resource."
      ],
      "completionRequirements": ["Read lines 1-41 exactly once and assess backup API authorization, status/trigger contract, concurrency and evidence semantics."]
    },
    {
      "id": "application:server/services/database-backup-health",
      "path": "server/src/services/database-backup-health.ts",
      "lineCount": 153,
      "sha256": "34d8e2adb7b38e76369492c964fddb7a96aa8f656a19918a99c3930fb5839b25",
      "linkedEvidence": { "history": ["be821a4f7ecd9a61dc7e29982ee65ff06ae6559c Fix DB backup health alerts (#9147)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 153 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Health inspection reports newest .sql.gz mtime/size, stale/missing state and the newest failure-marker first line with stable warning codes.",
        "Freshness is only filesystem metadata: a zero-byte, corrupt, wrong-schema or malicious symlinked file can pass. Future mtimes produce negative age and pass; backup/alert file ownership, mode, link, checksum, decompression and restore verification are absent.",
        "An old failure marker warns forever even after newer success unless external code removes it. Synchronous unbounded directory enumeration/sort and full alert-file reads can block health requests or consume memory; one bad candidate collapses the entire check into a generic failure.",
        "The function still emits missing/stale warnings when enabled is false if called directly, and returns absolute server paths/messages that require redaction at every external projection."
      ],
      "completionRequirements": ["Read lines 1-153 exactly once and assess backup freshness/integrity/alert health semantics, filesystem trust, monitoring privacy and operational usefulness."]
    },
    {
      "id": "application:cli/__tests__/update-command",
      "path": "cli/src/__tests__/update-command.test.ts",
      "lineCount": 240,
      "sha256": "44cd6b71a658823904636d5058b7f77a2ae2ce555949377f9bece719af83547d",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 240 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Fourteen tests cover SemVer ordering, install-mode/channel resolution, moving/pinned Git refs, downgrade consent, hostile npm registry isolation, backup policy, side-by-side activation, instant rollback and restart-failure rollback.",
        "The tests strongly prove current/manifest rollback after service validation failure, including surfacing a second failure when the rolled-back service cannot restart.",
        "Artifact signatures/digests, malicious package scripts/archive contents, database migration compatibility/restoration, concurrent updates, crash points between current and manifest writes and accountable operator evidence are absent.",
        "Focused Vitest passed all 14 update-command tests."
      ],
      "completionRequirements": ["Read lines 1-240 exactly once and assess update/rollback behavior, source provenance, service restart, failure recovery and test quality."]
    },
    {
      "id": "application:cli/__tests__/update-notice",
      "path": "cli/src/__tests__/update-notice.test.ts",
      "lineCount": 20,
      "sha256": "52079e0424641d641068d96e1f5c00fa6ea6c02d4cec54f335feb688e7623604",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 20 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Two compact tests cover environment/config kill switches and 24-hour registry-result caching.",
        "Malformed/hostile cache, filesystem links/permissions, request failure/timeout, response validation, version comparison and privacy disclosure are not tested.",
        "Focused Vitest passed both update-notice tests."
      ],
      "completionRequirements": ["Read lines 1-20 exactly once and assess update-notice isolation, version semantics and test quality."]
    },
    {
      "id": "application:cli/commands/install",
      "path": "cli/src/commands/install.ts",
      "lineCount": 420,
      "sha256": "b62b32871e115ad3b57b12455100fd29db243fc6d21046d7cb8e0487867a254a",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 420 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The command pins npm resolution to the public registry or resolves a Git ref to a full GitHub SHA, stages side-by-side payloads, smoke-checks the CLI version, flips current, writes provenance manifest, writes a managed shim and prunes retained payloads under a store lock.",
        "Git source builds warn and require explicit noninteractive consent, use frozen lockfiles and package the workspace. They still execute arbitrary archive, dependency and repository build scripts; npm release installs also execute lifecycle scripts while explicitly disabling audit, and neither path records/verifies an independent signature, content digest, SBOM or vulnerability policy.",
        "Ambient GH_TOKEN/GITHUB_TOKEN is interpolated into a curl config header without rejecting quotes/newlines, and custom authorization headers plus --location need redirect-host leakage analysis. Reused payloads are trusted after only a version-report smoke check.",
        "Current/manifest rollback covers manifest-write failure, but shim/prune failure occurs after activation. More importantly, running install over an existing managed install bypasses update backup, service restart, migration validation and health rollback, so active process, current link and manifest can represent different versions.",
        "Downloaded/build stderr may be surfaced verbatim, no stable JSON result exists, and install approval is not bound to artifact/dependency digests or an accountable operator evidence record."
      ],
      "completionRequirements": ["Read lines 1-420 exactly once and trace source resolution, download/build trust, install-store mutation, service interaction, consent, uninstall and user-data safety."]
    },
    {
      "id": "application:cli/commands/update",
      "path": "cli/src/commands/update.ts",
      "lineCount": 268,
      "sha256": "8d604f3ea2011ab9a7e364d4ca851543b1f99dd976ae1e0a1c0ac513c1891e22",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 268 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Managed updates back up onboarded data, install side-by-side, flip atomically enough to restore current on manifest failure, restart/validate an active service and automatically flip back when validation fails. Git moving refs require consent and resolve to full SHA; check mode uses exit code 10.",
        "Automatic binary rollback does not restore database migrations before restarting the older payload. A failed new-version start may already have migrated schema, making automatic old-binary restart unsafe; the backup is not cryptographically linked to the update or automatically restored.",
        "The store lock covers only payload/current/manifest mutation. Version/source resolution, backup and service restart occur outside it, while the manifest captured before locking is reused inside; concurrent updates can lose history and validate/restart a different state.",
        "Inactive services receive no server/migration health validation. Explicit rollback lacks a compensating flip if the old service fails; global npm updates have no backup, side-by-side rollback or health validation and can corrupt JSON output with a human Running line.",
        "Version comparison is a partial home-grown SemVer implementation with permissive input and Number precision risk. Outcomes and errors are not stable coded responsibility evidence."
      ],
      "completionRequirements": ["Read lines 1-268 exactly once and trace version selection, payload staging/switch, active service restart, health, rollback and status evidence."]
    },
    {
      "id": "application:cli/update-notice.ts",
      "path": "cli/src/update-notice.ts",
      "lineCount": 19,
      "sha256": "34e5f17495885027fd0fa8127971bc9cd3ee4a1aa1ee7000d11b81149cf9c353",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 19 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "A default-enabled registry check selects latest/canary from managed-install channel, caches results for 24 hours and silently degrades on errors; environment/config can disable it.",
        "Invalid configuration fails open to network access. Cache reading/writing lacks schema/future-time bounds, symlink/ownership/mode checks and atomic replacement; a future checkedAt can suppress checks indefinitely.",
        "Response size and version syntax are not bounded/validated before comparison, and the default outbound request/privacy behavior is not surfaced as explicit consent."
      ],
      "completionRequirements": ["Read lines 1-19 exactly once and assess notice eligibility, install detection and coupling."]
    },
    {
      "id": "application:cli/__tests__/service-manager",
      "path": "cli/src/__tests__/service-manager.test.ts",
      "lineCount": 216,
      "sha256": "56770f471a7a54c83004154bbfefd646543555ebcc1aef79f36d13637055edce",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 216 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Tests cover systemd/launchd rendering, newline and expansion injection, drift replacement, unsupported detection, launchd login state, foreground single-writer behavior and symlinked definition refusal.",
        "The systemd uninstall failure test explicitly expects the definition to remain after stop fails, favoring recoverability over partial removal.",
        "Definition ownership/mode/hardlink, shim provenance, malicious instance identifiers beyond newlines/regex characters, concurrent lifecycle operations, command timeouts and rollback are not covered.",
        "Focused Vitest passed all 19 service-manager tests."
      ],
      "completionRequirements": ["Read lines 1-216 exactly once and assess service definitions, instance isolation, permissions, lifecycle failures and test quality."]
    },
    {
      "id": "application:cli/__tests__/service-health-check",
      "path": "cli/src/__tests__/service-health-check.test.ts",
      "lineCount": 142,
      "sha256": "1c79005736f0279597e717cb0c5b35d3e23c8dfc6fb59d8ee0647388b6df2829",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 142 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Seven tests cover managed-process doctor skip, explicit restart-version semantics, in-process restart serialization/stale-PID recovery, IPv6 health URL formatting, healthy service and foreground-process conflict.",
        "Temporary restart and doctor directories are not removed. Cross-process lock races, PID reuse, lock-file links/ownership/modes, service definition drift/failure and health probe timeout/auth/spoofing are absent.",
        "Focused Vitest passed all 7 service-health tests."
      ],
      "completionRequirements": ["Read lines 1-142 exactly once and assess diagnostic accuracy, stale service detection, remediation and test quality."]
    },
    {
      "id": "application:cli/services/service-manager",
      "path": "cli/src/services/service-manager.ts",
      "lineCount": 311,
      "sha256": "e5123f216e12f16e07ea4b87c65127903dbe40ad503fb0012e058d206f31e2cd",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 311 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "A small ServiceManager abstraction owns systemd-user and launchd-agent definitions/lifecycle. Definitions escape systemd/XML values, contain no vendor credentials and scope names/logs by instance.",
        "writeIfChanged rejects unsafe existing definition types, hard links and wrong ownership and uses wx plus rename, but validates only the immediate directory, does not repair permissive mode when current content already matches and has no directory/file fsync.",
        "PAPERCLIP_SHIM_PATH and home/instance inputs become persistent executable/environment configuration; this module does not verify the shim against a signed managed-install manifest or independently validate instance identifiers.",
        "Systemd status collapses every show error into not-installed; disable/reset errors are swallowed. A disable failure followed by definition removal can leave stale enablement. Launchd bootstrap failure falls back to kickstart, status may report loaded without a definition, and stop unloads without changing saved enablement.",
        "Neither service definition applies OS sandbox/hardening boundaries. Lifecycle commands have no timeout, transaction, cross-process serialization, postcondition verification or structured partial-state result."
      ],
      "completionRequirements": ["Read lines 1-311 exactly once and trace launchd/systemd definitions, command execution, file trust, instance scoping, install/start/stop/uninstall and log behavior."]
    },
    {
      "id": "application:cli/commands/service",
      "path": "cli/src/commands/service.ts",
      "lineCount": 223,
      "sha256": "8bfcfc447e654f0a954d143c9bba2d7c862024ea3d6b800c1c8c23f61d192bd8",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 223 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The CLI exposes install/uninstall/start/stop/restart/status/logs and a hot-restart protocol with an instance lock, restart intent, supervisor restart, health/version wait and optional drain report.",
        "Unsupported service management prints a value then returns without a failing exit status. JSON install can still prompt on a TTY, and errors are prose rather than stable codes.",
        "The restart lock is PID-liveness based and vulnerable to PID reuse; it lacks lstat/owner/mode/link checks and robust atomic stale-owner reclamation. Intent/report files are not atomically or safely written against links and contain no accountable operator/action/config/artifact digest.",
        "Health success is based on an unauthenticated local endpoint response, not a cryptographic/process binding to the supervisor PID. Restart has no rollback on health/version failure, and a missing drain/adoption report is returned as null rather than making the preservation claim fail.",
        "healthUrl mutates process-global instance state. Service mutation postconditions are checked only after uninstall; start/stop/install merely print status and do not require the requested state."
      ],
      "completionRequirements": ["Read lines 1-223 exactly once and assess CLI lifecycle policy, explicit operator intent, output contracts, failure status and managed-install coupling."]
    },
    {
      "id": "application:cli/checks/service-health-check",
      "path": "cli/src/checks/service-health-check.ts",
      "lineCount": 136,
      "sha256": "ca908a7a83d86a15cf1856b2d3134600c373cfdc9f691feb08d3c5d8c1d267d9",
      "linkedEvidence": { "history": ["fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 136 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Doctor treats unsupported or uninstalled background service as an acceptable optional state, then checks exact definition text, supervisor activity, local HTTP health/version and systemd linger.",
        "Exact text detects ordinary drift but fs.readFile follows symlinks and no ownership/mode/hardlink/shim-manifest provenance is checked. Manifest read errors are silently ignored, disabling version comparison.",
        "The local health response is not bound to the reported supervisor PID, so another process can satisfy health/version. Results are human names/prose rather than stable diagnostic codes, limiting automation and localization.",
        "Skipping all checks inside a managed process avoids recursive activation problems but also removes self-diagnostics; an explicit self-safe subset would be stronger."
      ],
      "completionRequirements": ["Read lines 1-136 exactly once and assess health invariants, executable/config provenance, service activity and remediation semantics."]
    },
    {
      "id": "application:cli/__tests__/onboard",
      "path": "cli/src/__tests__/onboard.test.ts",
      "lineCount": 226,
      "sha256": "7af797d0d0d97707968317f450cba58e2b4dd6269e6a476236f6bce8e091cc71",
      "linkedEvidence": {
        "history": [
          "35132af1610c7a0435830e2d7cd834536514ffd4 fix(config): preserve extensions and guard invalid repairs (#11005)"
        ]
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 226
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Eight tests cover existing valid/invalid configuration preservation, fail-closed noninteractive defaults, per-instance data paths and tailnet fallback.",
        "The suite does not clean up its temporary directories and does not test config-write failure, environment-file/master-key permissions, partial creation rollback, authenticated identity bootstrap or malicious filesystem links.",
        "Sandboxed run passed 4 and failed 4 because four tests resolve the default local encryption key outside their temporary config roots under ~/.paperclip. An approved unsandboxed rerun passed all 8 onboarding tests, but also created/reused that real user-home test key path; test isolation is defective."
      ],
      "completionRequirements": [
        "Read lines 1-226 exactly once and assess onboarding behavior, security defaults, configuration persistence, failure recovery and test quality."
      ]
    },
    {
      "id": "application:cli/__tests__/onboard-service",
      "path": "cli/src/__tests__/onboard-service.test.ts",
      "lineCount": 84,
      "sha256": "73845b1ee5cb1e5f2478d8e0e988cc75fde24bace2f6332a1cf52fbb72c951ca",
      "linkedEvidence": {
        "history": [
          "fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"
        ]
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 84
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four tests prove unattended service installation requires explicit opt-in, interactive mode asks, and an explicit negative flag suppresses detection and hints.",
        "Unsupported managers, declined prompts, already-installed services, install/start failure, cleanup and service-definition privilege assertions are absent.",
        "Focused Vitest passed all 4 service-policy tests in both sandboxed and approved unsandboxed runs."
      ],
      "completionRequirements": [
        "Read lines 1-84 exactly once and assess service-install gating, existing-service handling, error propagation and test quality."
      ]
    },
    {
      "id": "application:cli/commands/onboard",
      "path": "cli/src/commands/onboard.ts",
      "lineCount": 756,
      "sha256": "6f153176b8d234b9383e60ea4637f6ecf87df4edd102557e1b5a784b772461fa",
      "linkedEvidence": {
        "history": [
          "35132af1610c7a0435830e2d7cd834536514ffd4 fix(config): preserve extensions and guard invalid repairs (#11005)"
        ]
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 756
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The command separates quickstart/advanced paths, ignores deployment environment overrides for unattended trusted-local setup, forces loopback/private for local_trusted, preserves invalid bytes before interactive repair and derives per-instance data paths.",
        "Existing valid configuration is not rewritten, but rerunning onboarding still creates missing Agent JWT/local secret material and may install a service or start the runtime; preservation is therefore not a side-effect-free operation.",
        "Fresh setup creates the JWT environment file and local encryption key before writing config. Config-write failure leaves orphan credentials; bootstrap invite/service/start failures occur after durable config and have no transaction, compensating action or resumable phase ledger.",
        "Advanced setup directly probes a database and vendor model APIs and continues on most failures. The LLM object, including prompt-returned apiKey when present, is inserted into config, while local-encrypted secrets default to strictMode false and telemetry defaults enabled without an explicit consent step in this command.",
        "Authenticated external-database setup creates a bootstrap CEO invite, encoding a privileged role bootstrap that Company OS must replace with accountable-human identity proof, scoped initial authority and auditable recovery.",
        "Unattended quickstart safety is strong, but environment-derived URL/host/port/provider inputs, filesystem trust, partial state, real credential validation and service lifecycle need stronger validation and stable machine-readable outcomes."
      ],
      "completionRequirements": [
        "Read lines 1-756 exactly once and trace onboarding state, identity/auth/network/secret defaults, config writes, service setup and rollback behavior."
      ]
    },
    {
      "id": "application:cli/onboard-service.ts",
      "path": "cli/src/onboard-service.ts",
      "lineCount": 77,
      "sha256": "07e2264645f477930372f626f35224c1428ccdcbdce8f9d740ec33c8922e715a",
      "linkedEvidence": {
        "history": [
          "fc5a30805ed3c95ac6598c8660261a018b5b5a7f feat(cli): add managed install, update, and service lifecycle (#10045)"
        ]
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 77
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Unattended service install is opt-in and an explicit negative flag returns before platform detection; interactive install defaults yes and linger defaults no.",
        "Unsupported detection after an explicit request only warns and returns false, so the parent onboarding can still report success even though the requested service was not installed.",
        "Install runs startNow/startOnLogin before optional linger. Install or linger failure propagates without status reconciliation or compensating uninstall; a linger failure can make the caller see failure after the service was already installed and started.",
        "The policy delegates definition permissions, executable/config path integrity, existing-service idempotency and privilege boundary to ServiceManager."
      ],
      "completionRequirements": [
        "Read lines 1-77 exactly once and assess service-install policy, privilege boundary, lifecycle and failure handling."
      ]
    },
    {
      "id": "application:server/services/tool-access",
      "path": "server/src/services/tool-access.ts",
      "sha256": "4b3f135c0c5cf363e3402ca3ed2151980b1b519dbeb27eba5140ceef8f98204b",
      "lineCount": 7771,
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 1000,
          "readAt": "2026-08-18",
          "notes": [
            "Imports show a very broad orchestration boundary spanning applications, connections, OAuth, secrets, catalogs, profiles, policy decisions, runtime slots, invocations, action requests, events, metrics, and runtime supervision.",
            "OAuth registration uses an in-process single-flight map; later ranges must verify whether database leases and multi-instance recovery fully cover this lifecycle.",
            "The generic service embeds Google Sheets-specific schemas, environment normalization, and catalog fallback behavior, indicating provider logic may not be isolated behind a connector boundary.",
            "OAuth actor binding checks actor type, actor id, and optionally session id; missing persisted actor identity fails closed.",
            "Class-3 static lease credentials require an explicit allowlist key and config path and return structured error details, while several other boundary failures in this range expose only English message text.",
            "The range is dominated by schemas, conversion projections, and lifecycle helpers; no final correctness, security, reuse, or ownership decision is valid until the remaining ranges and tests are reviewed."
          ]
        },
        {
          "start": 1001,
          "end": 2000,
          "readAt": "2026-08-18",
          "notes": [
            "Tool-profile summaries and new-tool review projections are implemented in-memory over full profile, catalog, binding, and agent arrays; later callers must be checked for pagination and tenant-size bounds.",
            "Risk classification combines annotations, explicit Notion tool lists, and verb heuristics, but an unrecognized or renamed mutation defaults to read, creating a fail-open classification risk unless catalog quarantine or review always intercepts it.",
            "Remote MCP and OAuth helpers validate every manual redirect against the public-endpoint guard in authenticated public deployments, and local stdio enablement requires a trusted runtime host in that deployment profile.",
            "The connection-token broker rate limit counts successful issuances and then proceeds without an atomic reservation, so concurrent mints can exceed the configured hourly limit.",
            "Broker authorization requires an exact, condition-free connection_token.mint include in the narrowest active profiles; exclusions are evaluated only within each profile, so cross-profile include/exclude semantics require test confirmation.",
            "The broker derives responsibleUserId from the run context snapshot; this range does not revalidate that accountable human against current organization membership or responsibility contracts.",
            "Generic and Pages token exchange send a vault-resolved parent token to a configured exchange URL using direct fetch rather than the guarded remote fetch helper; configuration authority and later validation must be traced because this can become a credential-egress boundary.",
            "The RFC 8693 branch labels a base64url-encoded JSON actor_token as a JWT without signing it, and the response path shown here does not re-check returned scope or cap an explicit upstream expiresAt to the requested TTL.",
            "These are preliminary findings from this range only; final severity and reuse judgment remain invalid until invocation paths, mutations, tests, migrations, and the rest of the file are reviewed."
          ]
        },
        {
          "start": 2001,
          "end": 3000,
          "readAt": "2026-08-18",
          "notes": [
            "Runtime health loads every company slot and connection plus an hour of audit and call events, then aggregates in memory on each request; no query limit, pagination, pre-aggregation, or retention bound is visible in this range.",
            "Operator stop/restart completes before the activity log write, so a logging failure can return an error after the runtime mutation already succeeded and make retries ambiguous.",
            "Google Sheets ownership enforcement scans every non-archived connection across all companies and returns conflicting spreadsheet ids, coupling a provider-specific global uniqueness rule and cross-tenant scan to the generic service.",
            "Default app-profile creation, default workspace-grant creation, and runtime-slot creation use select-then-insert flows without a transaction or conflict recovery in this range; concurrent callers depend on unseen database constraints and may surface errors.",
            "Profile detail and new-tool review projections load full company catalogs and related entity sets, confirming the earlier scalability concern for large tenants.",
            "New-tool review lists pending state, inserts allow entries, marks catalog rows reviewed, and advances the profile watermark through separate writes without a transaction or compare-and-set; partial failure and tools first seen during the review window can produce inconsistent or skipped review state.",
            "Profile entry replacement deletes all entries before re-insertion, and secret-binding synchronization deletes all bindings before re-insertion, without a transaction in the shown code; validation reduces input errors but not database or process-failure gaps.",
            "Remote MCP discovery validates only the initial endpoint and then uses fetch with default redirect following instead of the manual redirect-validation helper; redirect targets, request timeout, response-size limits, and parsing-depth limits are not enforced in this range.",
            "Health status is persisted before its audit event, so audit failure can leave a successful state mutation paired with a failed request and incomplete traceability.",
            "These remain range-local findings; database uniqueness constraints, route authorization, tests, and later transactional wrappers must be checked before assigning final severity or reuse status."
          ]
        },
        {
          "start": 3001,
          "end": 4000,
          "readAt": "2026-08-18",
          "notes": [
            "Catalog refresh performs one update or insert per discovered descriptor and then separate connection, runtime-slot, and audit writes without a surrounding transaction; large catalogs incur sequential N+1 writes and partial refresh states are possible.",
            "New or changed tools are quarantined only when connection configuration enables it, and safeDefault exempts entries classified as read; combined with the earlier fail-open heuristic, an unknown mutation can become active without review.",
            "No removal or disablement of catalog entries missing from the latest discovery response is visible in this range, so stale upstream capabilities may remain represented unless later code handles them.",
            "The apps-needing-attention projection loads every company invocation solely to map pending action requests back to connections, plus complete profiles, entries, and active catalog sets, creating an avoidable tenant-growth cost.",
            "The health sweep loads every active enabled connection across all companies before filtering staleness and slicing the requested limit in memory; it lacks a database limit, lease, or visible multi-worker claim in this range.",
            "Example installation mutates application, connection, credential bindings, runtime slot, profile, entries, and company binding through separate upserts and delete-then-insert operations without an encompassing transaction.",
            "The example smoke actor selects the first real company agent when one exists and writes policy-decision audit events under that agent identity, which conflicts with Company OS responsibility integrity because a fixture check can be attributed to a human-managed production principal that did not act.",
            "Credential-scope type and id may be taken directly from connection OAuth configuration without validation in this range; later secret-access enforcement must be checked to determine whether this is merely metadata or an authority boundary.",
            "OAuth metadata retrieval validates each followed URL through the guarded fetch helper, including advertised authorization-server metadata, which is a reusable defense pattern; actual authorization and token POST paths still require separate verification.",
            "All observations remain provisional until the rest of the OAuth lifecycle, route authorization, schema constraints, and tests are fully reviewed."
          ]
        },
        {
          "start": 4001,
          "end": 5000,
          "readAt": "2026-08-18",
          "notes": [
            "OAuth metadata and token POSTs use the guarded remote-fetch helper, which rejects redirects for non-GET/HEAD requests; this is a reusable credential-egress defense, although response timeout and size limits remain absent in the shown path.",
            "Dynamic client registration validates PKCE S256 and public-client compatibility plus strict client-id, secret-length, redirect-uri, grant-type, response-type, auth-method, and timestamp response fields.",
            "DCR serialization is only an in-process single-flight with a database re-read; multiple service instances can still register separate clients concurrently before either persists its result.",
            "DCR secret creation or rotation, connection update, and secret-binding synchronization are separate operations, so persistence failure can leave rotated or orphaned secrets and mismatched bindings.",
            "The refresh lease uses a database JSON compare-and-set and refuses to take over an expired lease, favoring uncertainty safety over availability; it mixes injected now() with Date.now(), which complicates deterministic testing and clock control.",
            "Critical preliminary concurrency risk: refresh rotates the existing refresh-token and access-token secrets before the guarded connection-row update. Because references use the latest version, a refresh that later loses the lease CAS can still change the effective credential observed by the winning connection state.",
            "Refresh-token rotation and access-token rotation are also separate writes, so a crash between them can leave only part of the new OAuth credential set effective despite the later connection update never occurring.",
            "The refresh response accepts provider scope and token type as metadata without visible enforcement against configured requested scopes in this range.",
            "Gallery connection setup creates secrets and then mutates application, connection, bindings, and runtime state through a manual compensation path that continues beyond this range and must be reviewed before judging atomicity.",
            "Final findings require the remaining setup rollback, callback lifecycle, secret service semantics, schema constraints, and concurrency tests."
          ]
        },
        {
          "start": 5001,
          "end": 6000,
          "readAt": "2026-08-18",
          "notes": [
            "Gallery-connect failure uses best-effort manual compensation for connection, application, and newly created secrets; runtime slots and bindings depend on cascade or separate behavior not shown, and rollback failures are swallowed.",
            "Finishing a gallery connection correctly groups profile replacement, bindings, catalog review state, ask-first policies, connection activation, and application activation in one database transaction, a materially stronger reusable pattern than earlier flows.",
            "Reconnect rotates existing secrets or creates new ones before connection update and health verification, with no rollback in this function; an invalid replacement can immediately supersede the last working latest-version credential and new secrets can survive later failure.",
            "OAuth start accepts requested scopes without checking they are a subset of advertised or configured scopes in this range.",
            "The generic OAuth start function accepts agent-requested subject and issue identifiers without validating their responsibility relationship in this range; the exported agent-specific wrapper and route-level authority must be traced before judging the boundary.",
            "The OAuth interaction stores actor sessionId as sourceRunId, conflating session identity and run identity unless the caller guarantees they are identical.",
            "Callback state is deleted before token exchange, secret persistence, grant/application updates, health check, and catalog refresh; transient downstream failure consumes the one-time state and forces a complete restart.",
            "User-specific callback completion rotates or creates secrets, upserts the user grant, and resolves the issue interaction through separate writes without a transaction, allowing partial success and responsibility-record disagreement.",
            "No dedicated OAuth start or completion audit event is visible in this range, despite credentials, user consent, and delegated authority changing materially.",
            "Admin-defined local stdio templates accept arbitrary command, args, env keys, and tool descriptors; final safety depends on route authority, deployment restrictions, supervisor sandboxing, and schema constraints outside this range."
          ]
        },
        {
          "start": 6001,
          "end": 7000,
          "readAt": "2026-08-18",
          "notes": [
            "The exported agent-specific authorization wrapper validates an active run and exact responsibleUserId snapshot before delegating to generic OAuth start, narrowing the prior concern; it still does not revalidate current organization membership or a versioned responsibility contract here.",
            "Smoke execution confirms that a fixture policy check can select the first real company agent and persist allow/deny audit records under that principal, creating false responsibility evidence.",
            "Application ownerUserId is persisted without a same-company membership check in this service, while ownerAgentId is checked; plugin ownership is checked only for existence and requires global/plugin tenancy review.",
            "Connection creation can create an application and then fail during secret validation, connection insert, default grant, binding sync, or runtime-slot creation without a transaction or compensation, leaving partial control-plane state.",
            "Adding a default workspace grant clears existing defaults and inserts the replacement in separate writes, so failure or concurrency can leave zero or multiple defaults unless database constraints close the gap.",
            "putConnectionInstalls removes installation rows but only adds profile bindings for requested installs; it does not delete access-extension bindings for removed installs, so uninstalling a connection can leave effective tool access behind.",
            "Access-extension activity logs are written after the install transaction; logging failure can report an error after authority has already changed.",
            "Connection update persists credential references before non-transactional binding synchronization and runtime-slot setup, while archive does not visibly revoke grants, remove profile bindings, quarantine catalog entries, or stop runtime slots in this range.",
            "Pending action-request and profile listing endpoints are unpaginated and load complete tenant sets; invalid signed requests are cancelled as a side effect of listing without a dedicated audit event shown here.",
            "Profile create, update with delete-then-insert entries, duplicate, and delete-with-reassignment span multiple writes without transactions, allowing partial profiles, lost entries, or duplicated assignments after failure."
          ]
        },
        {
          "start": 7001,
          "end": 7771,
          "readAt": "2026-08-18",
          "notes": [
            "Profile assignment reassignment is performed before profile deletion without a transaction; delete failure can leave both source and target assignments active.",
            "Effective access unions allows across active profiles and applies excludes only inside each profile, confirming that one profile cannot centrally deny a capability granted by another peer profile.",
            "User and workspace grant selection uses limit(1) without deterministic ordering when grantId is omitted, so multiple active grants can yield unstable credential choice.",
            "The token broker properly requires an active run, active grant and connection, healthy credential state, explicit mint profile grant, policy approval, parent-scope subset, and refuses direct static/OAuth bearer projection.",
            "Critical preliminary TTL issue: an exchange response with a far-future explicit expiresAt is returned unchanged; only the separately reported ttlSeconds value is clamped to 900, so the actual delivered token lifetime can exceed the promised ceiling.",
            "Critical preliminary side-effect issue: after the upstream token is minted, issuance success, grant last-used, and two audit sinks are separate writes. A later failure enters the catch path, records failure, and returns an error even though a live token and possibly a success record already exist, making retries mint duplicates.",
            "The returned minted scope is not revalidated as a subset of requested or parent scope before delivery and recording.",
            "Run-decision projection maps action requests by invocation id to one row, silently discarding additional requests for the same invocation if the schema permits them.",
            "MCP JSON preview keeps imported stdio commands draft-only and converts remote headers into credential-field declarations, but input/object-size limits and later secret conversion require route-level review.",
            "The implementation source is now fully read; all severity, test adequacy, database-constraint, route-authority, and reuse conclusions remain provisional pending the 6996-line service test and linked schemas/routes/history."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Source lines 1-7771 are fully covered without overlap or gaps against the pinned SHA-256.",
        "Review the corresponding first-party tests and relevant file history.",
        "Only then write the canonical unit assessment and change auditStatus to COMPLETE."
      ],
      "linkedEvidence": {
        "history": [
          "0a511ed1 feat(apps): support multiple provider connections (#11060)",
          "03cfad7c feat(apps): connect Notion through MCP OAuth (#11009)",
          "cac3c0fa feat(connections): add runtime subjects and grants (#9982)",
          "d23fbf8a feat(connections): add AppDefinition Wave 1 catalog (#9981)",
          "7e00f671 feat(connections): add v3 schema core (#9958)",
          "cfa5e070 feat(mcp) [split 3/8]: add tool access policy core (#9558)"
        ],
        "focusedTest": "2026-08-18: direct repository Vitest run passed 7 pure tests and skipped 118 database-backed tests; the full database-backed suite remains required before canonical completion."
      }
    },
    {
      "id": "application:server/__tests__/tool-access-service",
      "path": "server/src/__tests__/tool-access-service.test.ts",
      "sha256": "5ed486cead2abd930d1f12baa039f250db0e427aaede58fe73c1be45805c0a41",
      "lineCount": 6996,
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 1000,
          "readAt": "2026-08-18",
          "notes": [
            "The suite uses real embedded Postgres plus Express route integration, providing stronger persistence and HTTP-contract evidence than isolated mocks.",
            "Broker tests cover active-run enforcement, exact responsible-user subject matching, revoked grants, explicit mint permission, parent-scope denial, static credential refusal, OAuth bearer non-projection, secret-access evidence, and hash-only token persistence.",
            "The successful exchange test asserts ttlSeconds is at most 900 but does not assert the returned expiresAt is bounded by 900 seconds, so it does not catch the implementation's far-future explicit-expiry defect.",
            "The hourly rate-limit test is strictly sequential and provides no evidence for concurrent reservation safety.",
            "Scoped-credential tests correctly fail closed when the requested scope cannot select the configured parent credential and verify no secret access occurs on denial.",
            "Catalog tests cover quarantine of new and changed entries and MCP Streamable HTTP/SSE interoperability.",
            "A second catalog refresh omits a previously discovered tool, but the test does not inspect or require the missing tool to be disabled or removed, leaving stale-capability lifecycle behavior untested.",
            "No final test-quality or implementation verdict is valid until lines 1001-6996 and linked route/schema/history evidence are reviewed."
          ]
        },
        {
          "start": 1001,
          "end": 2000,
          "readAt": "2026-08-18",
          "notes": [
            "Route integration verifies tools:admin for arbitrary admin-defined stdio templates, active-template enforcement, direct private-endpoint rejection in authenticated public deployment, and same-company profile target validation.",
            "The stdio tests prove commands cannot be supplied directly on a connection, but intentionally permit arbitrary command, args, and env keys once an administrator creates a template; runtime sandbox and command provenance remain outside this evidence.",
            "Remote endpoint defense is tested only against a direct link-local metadata URL; redirect targets, DNS rebinding, timeouts, and oversized responses are not covered in this range.",
            "Board-initiated test calls preserve the real user actor and separately attribute the selected agent with source=test metadata, a useful responsibility distinction that the older smoke fixture does not consistently preserve.",
            "Ask-first tests exercise pending request creation, signed arguments, approval execution, repeated side-effecting requests, rejection without execution, block policy, quarantined tools, and unassignable agents.",
            "The rejection test explicitly accepts an invocation that remains status=awaiting_approval while approvalState=rejected, leaving a non-terminal execution status after a terminal human decision.",
            "Profile CRUD and effective-access tests cover normal outcomes and narrowest-scope precedence but do not inject database failures or concurrency into the non-transactional create/update/delete paths.",
            "List and projection scale, cross-profile deny semantics, and uninstall-to-revoke behavior remain untested in this range."
          ]
        },
        {
          "start": 2001,
          "end": 3000,
          "readAt": "2026-08-18",
          "notes": [
            "Tests confirm agent-scoped profiles completely replace broader company profiles in effective previews, profile copies may include assignments, and database cascades remove entries and bindings on successful profile deletion.",
            "Delete-with-reassignment failure atomicity and duplicate-profile partial failure are not exercised; successful cascade evidence does not close those implementation risks.",
            "Profile routes enforce board-only mutation, viewer-safe read access, cross-company read hiding, and mutation denial.",
            "The safe example installation is tested as idempotent and verifies allow, deny, and audit decisions, but no real company agent exists in the fixture, so it misses the implementation path that attributes smoke audit records to the first production agent.",
            "MCP JSON preview proves raw header values are not echoed and link connection setup stores them as secrets, while payload-size and object-count limits remain untested.",
            "Class-3 static lease tests verify exact allowlist admission and fail-closed rejection without connection or binding residue.",
            "Google Sheets tests intentionally assert that a cross-company conflict returns the exact spreadsheet id, codifying a cross-tenant resource-identifier disclosure instead of a non-enumerating error.",
            "Agent-initiated user OAuth verifies active-run responsibleUserId snapshot matching, separate user/workspace secret sets, and consent-card resolution, but does not prove current membership, a versioned responsibility contract, or requested-scope restriction.",
            "No final test-quality or reuse judgment is valid until the remaining OAuth, lifecycle, concurrency, runtime, and route cases are read."
          ]
        },
        {
          "start": 3001,
          "end": 4000,
          "readAt": "2026-08-18",
          "notes": [
            "OAuth route tests strongly bind callbacks to the initiating user and exact session, reject anonymous, other-user, other-session, and permission-downgraded callbacks, and retain state after unauthorized attempts.",
            "End-to-end OAuth tests verify PKCE, secret-backed token storage, absence of raw tokens in connection config, app activation, catalog grouping, and browser redirect behavior.",
            "DCR tests validate exact registered metadata, strict response rejection, HTTPS-or-loopback constraints, and reuse, but concurrency is exercised only through two calls on one service instance, so the process-local single-flight masks multi-instance duplicate registration.",
            "Refresh tests across two service instances prove one database-leased upstream refresh and reuse of the new access token under the ordinary success path.",
            "Refresh tests do not simulate the critical window where secret rotation succeeds and the guarded connection CAS subsequently loses, so latest-version credential mutation outside the lease remains untested.",
            "invalid_grant, superseded refresh-version, abandoned lease, client-credentials, and missing-refresh-token paths have useful fail-closed coverage.",
            "No callback test in this range injects failure after state deletion but before token, secret, grant, health, or catalog completion, leaving one-shot retry semantics unverified."
          ]
        },
        {
          "start": 4001,
          "end": 5000,
          "readAt": "2026-08-18",
          "notes": [
            "Attention-feed tests verify health, quarantine, pending approval, and profile-new-tool projections, but do not test the implementation's full-invocation and full-catalog query growth.",
            "Listing pending approvals cancels missing or invalid signatures and preserves valid ones, but the test does not require an audit event or reason record for this security-relevant state mutation.",
            "New-tool review tests cover exact all-item decisions and mixed allow/keep-blocked results, but not concurrent catalog arrival, watermark races, transaction failure, or partial writes.",
            "Connect rollback tests verify application, connection, and catalog cleanup on health failure and restoration of a revived app/connection, but omit credential secrets, bindings, grants, and runtime-slot residue under failure.",
            "OAuth metadata redirect testing confirms manual redirect handling prevents a protected-resource redirect to link-local metadata, a strong reusable SSRF regression test.",
            "Generic pasted-link OAuth verifies discovery, PKCE/token exchange, secret-backed storage, and credential scope metadata; scope escalation and response-size/time limits remain untested.",
            "Smoke Lab URLs are rejected from the production OAuth pipeline unless the connection is explicitly marked as the fixture, reducing accidental demo/production credential mixing.",
            "The remaining gallery-finish, lifecycle, audit, runtime, CRUD-race, and cleanup tests must be read before final judgment."
          ]
        },
        {
          "start": 5001,
          "end": 6000,
          "readAt": "2026-08-18",
          "notes": [
            "Gallery-finish tests cover idempotent profile/policy replacement, complete review of quarantined actions, Notion-specific mutation classification, cross-company denial, and a real injected late-write failure that proves transaction rollback restores entries, bindings, and policies.",
            "Reconnect testing proves successful in-place secret rotation without duplicate references but does not test failed health validation, database failure, or recovery of the prior working secret.",
            "Runtime slot tests cover stop/restart behavior, activity attribution, board routes, tools:manage_runtime permission, and rejection of unsupported remote runtime controls.",
            "Viewer regression tests broadly protect application, connection, health, catalog, profile, policy, trust-rule, example, and runtime mutation routes while retaining read access where intended.",
            "Application deletion tests cover friendly linked-connection conflicts, last-connection application archive, multiple-connection preservation, normalized UID uniqueness, and a database-level delete/create race guarded by NO ACTION foreign-key behavior.",
            "Archive tests do not assert revocation or suspension of grants, profile bindings, catalog entries, or local runtime slots, leaving authority and execution cleanup unverified.",
            "Run-decision tests exercise one invocation with one action request only and do not expose the implementation's single-value map behavior for multiple requests."
          ]
        },
        {
          "start": 6001,
          "end": 6996,
          "readAt": "2026-08-18",
          "notes": [
            "Connection activity tests verify issue and resolver enrichment plus lifecycle event ordering and display-name projection.",
            "Runtime health tests cover stale slots, capacity/restart signals, degraded active connections, durable audit-write counters, draft/disabled exclusions, and hosted local-stdio restrictions.",
            "Health-sweep testing uses one connection and does not cover the implementation's cross-company full-table load, database limit omission, starvation, or multi-worker claim behavior.",
            "Secret-resolution failure tests confirm fail-closed health state and value-free audit records.",
            "Install synchronization tests cover only adding one agent install and the resulting access extension; they never remove an install, so the stale profile-binding revocation defect is not caught.",
            "Risk-classification tests cover namespace and camelCase verbs plus annotations and Notion's explicit catalog, but explicitly expect non-Notion move_pages to be read, codifying fail-open handling for an unrecognized mutation verb.",
            "No test covers broker response scope escalation, actual expiresAt enforcement, concurrent default rate-limit reservations, post-mint audit failure, duplicate grant selection, profile list scale, or callback state loss after downstream failure.",
            "The complete 6996-line test source is now covered; final unit status still requires history, route authority, database schemas and migrations, and execution of the focused tests."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Test lines 1-6996 are fully covered without overlap or gaps against the pinned SHA-256.",
        "Map covered and missing cases to the fully read implementation findings.",
        "Only then contribute test-quality evidence to the canonical unit assessment."
      ],
      "linkedEvidence": {
        "history": [
          "0a511ed1 feat(apps): support multiple provider connections (#11060)",
          "03cfad7c feat(apps): connect Notion through MCP OAuth (#11009)",
          "cac3c0fa feat(connections): add runtime subjects and grants (#9982)",
          "d23fbf8a feat(connections): add AppDefinition Wave 1 catalog (#9981)",
          "7e00f671 feat(connections): add v3 schema core (#9958)",
          "79473082 fix(codex): classify refresh auth failures (#9598)",
          "cfa5e070 feat(mcp) [split 3/8]: add tool access policy core (#9558)"
        ],
        "focusedTest": "2026-08-18: direct repository Vitest run passed 7 pure tests and skipped 118 database-backed tests; this is partial execution evidence, not a full pass."
      }
    },
    {
      "id": "application:server/routes/tool-access",
      "path": "server/src/routes/tool-access.ts",
      "lineCount": 1378,
      "sha256": "97281fe747f1f2562e8c9e8a937637b030132c29768529d5bcdcde4827464e49",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 400,
          "readAt": "2026-08-18",
          "notes": [
            "Route construction centralizes board and company access checks, but emits English message text for most permission and OAuth failures; only selected agent-token failures and gateway errors expose stable codes.",
            "Local-implicit actors and instance admins bypass fine-grained tool permissions, while gallery mutations use a broader active non-viewer membership check instead of tools:admin; this creates two distinct authorization regimes for related tool mutations.",
            "Agent token minting binds the authenticated agent, company, active run id, and optional run-id header before delegating to the broker, which is a useful anti-confusion boundary.",
            "Agent-initiated OAuth accepts a requested subjectUserId at the route and relies on the service to bind it to the run's responsible human; the route itself does not verify current membership or responsibility-contract authority.",
            "OAuth callback peeks state before company authorization, then delegates exact actor/session binding to the service; activity logging occurs after callback completion, so logging failure can make a successful credential mutation appear failed.",
            "Connect and finish activity records use the real board user when present, but fall back to a synthetic board id; later ranges must verify consistent actor attribution across all mutation routes.",
            "No conclusion is assigned yet because lines 401-1378, route tests, error middleware, and linked schema constraints remain to be examined."
          ]
        },
        {
          "start": 401,
          "end": 800,
          "readAt": "2026-08-18",
          "notes": [
            "Read endpoints consistently hide cross-company connection existence with a 404 before asserting company access, a reusable tenant-enumeration defense.",
            "Mutation authorization is inconsistent: application/connection CRUD and examples require only active non-viewer membership, grant/install operations require tools:manage_connections, and smoke execution requires only company access despite producing policy and audit activity.",
            "Installation grant input is manually shape-checked rather than passed through a shared schema; credentialSecretRefs elements and providerTenant fields are not validated at the route boundary in this range.",
            "Board test calls require tool-use or connection-management permission plus tasks:assign authorization for the selected agent, and preserve the real user id separately from the selected agent, which is a strong responsibility pattern.",
            "The list of testable agents performs an access decision and gateway projection serially for every company agent, creating an unbounded N+1 route path without pagination.",
            "Connection updates persist before one or multiple activity writes; partial logging failure can leave the mutation committed, and multi-event logging itself is not atomic.",
            "Install synchronization delegates the known stale-binding revocation issue to the service and then records only the resulting install targets, so route-level logging does not compensate for missing authority revocation."
          ]
        },
        {
          "start": 801,
          "end": 1200,
          "readAt": "2026-08-18",
          "notes": [
            "Health checks, reconnects, catalog refreshes, profile CRUD, bindings, and policy mutations all use the broad non-viewer mutation gate rather than purpose-specific tools permissions, while runtime-slot controls correctly require tools:manage_runtime.",
            "The activity limit accepts any finite number, including negative or very large values, and relies on the service/database layer to normalize or reject it; the route exposes no shared schema or bounded contract.",
            "Profile and policy mutations generally commit before activity logging and several service operations are already known to be nontransactional, so the route layer adds another independent partial-failure boundary.",
            "New-tool review activity includes exact reviewed catalog entry ids, useful for evidence, but the underlying review/watermark transaction and TOCTOU defects remain unresolved.",
            "Effective-profile and run-decision projections verify company access but do not visibly verify that the requested agent or run belongs to that company at the route; correctness depends entirely on service filtering.",
            "Runtime-health and other company-wide projections remain unpaginated, matching the service-level full-table aggregation concerns.",
            "Profile-binding routes pass a company-scoped profile lookup before mutation, a sound confused-deputy guard for ids embedded in company URLs."
          ]
        },
        {
          "start": 1201,
          "end": 1378,
          "readAt": "2026-08-18",
          "notes": [
            "Trust-rule and policy mutations continue to use the broad non-viewer gate, although they change future execution authority and should have a dedicated permission comparable to runtime and stdio administration.",
            "Local stdio command templates are correctly restricted to tools:admin and record command plus aggregate argument/environment/tool counts without logging secret values.",
            "MCP JSON preview and policy-test endpoints require only board company access, so viewers can trigger preview activity and can request durable policy audit writes; this conflicts with the otherwise read-only viewer model.",
            "The policy-test endpoint trusts a validated writeAuditEvent flag and writes an audit under service-defined semantics, but the route supplies no explicit actor context in the shown call.",
            "Across the complete route, error contracts mix structured gateway reasonCode responses with many English-only error strings, which is insufficient as a stable independent Web or localization contract.",
            "Across the complete route, the strongest reusable patterns are tenant-hiding reads, run-token binding, exact agent-assignment checks for test execution, schema validation on most mutations, and dedicated high-risk runtime/stdio permissions; the dominant weakness is inconsistent permission granularity for authority-changing operations."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-1378 exactly once against the pinned SHA-256.",
        "Verify route authentication, permission checks, actor attribution, input validation, and error contracts against the service findings.",
        "Link focused route tests, database constraints, and commit history before contributing a canonical assessment."
      ],
      "linkedEvidence": {
        "history": [
          "0a511ed1 feat(apps): support multiple provider connections (#11060)",
          "cac3c0fa feat(connections): add runtime subjects and grants (#9982)",
          "d23fbf8a feat(connections): add AppDefinition Wave 1 catalog (#9981)",
          "79473082 fix(codex): classify refresh auth failures (#9598)",
          "cfa5e070 feat(mcp) [split 3/8]: add tool access policy core (#9558)"
        ],
        "focusedTest": "Route behavior is exercised inside tool-access-service.test.ts; 7 pure tests passed locally and 118 database-backed cases were skipped because no test database was provisioned."
      }
    },
    {
      "id": "evidence:package:db/schema/tool_access",
      "path": "packages/db/src/schema/tool_access.ts",
      "lineCount": 869,
      "sha256": "f4f7f3d21290b4d77e16458cfc6b5a898ad84ff250f52665913a252134f5f751",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 450,
          "readAt": "2026-08-18",
          "notes": [
            "The schema is a large shared aggregate spanning applications, connections, credentials, grants, installs, OAuth state, catalogs, profiles, gateways, invocation, approval, audit, and runtime concerns; it imports many core-company tables directly and is not a narrow connector-owned store.",
            "The deliberate NO ACTION application foreign key is a strong database-level delete/create race defense and is documented with the exact PostgreSQL locking rationale.",
            "Connection grants use a composite company/connection foreign key plus subject-kind checks and partial unique indexes, providing strong tenant and default-grant invariants.",
            "Connection installs use only a single-column connection foreign key; the separate companyId is not tied to the connection's company by a composite foreign key, so cross-tenant inconsistency is structurally possible if service checks fail.",
            "OAuth states persist the PKCE verifier in plaintext and bind actor/session/subject metadata, but issueId and interactionId have no foreign keys in this schema range.",
            "Catalog risk defaults to read and profile defaults to deny; the combination is safer at the profile layer but still preserves the fail-open classification concern for installed read defaults.",
            "Profile entries and bindings carry companyId without composite foreign keys tying the referenced profile/application/connection/catalog rows to the same company, leaving tenant consistency mostly to services.",
            "Gateway bearer tokens are stored hashed and model finite expiry/override evidence, a reusable credential design, but the default TTL is 90 days and policy correctness depends on later checks and migrations."
          ]
        },
        {
          "start": 451,
          "end": 869,
          "readAt": "2026-08-18",
          "notes": [
            "Gateway sessions bind agent, run, optional issue/gateway/token metadata and store only token hashes with expiry/revocation, which is a useful runtime-proof model; cross-company consistency of those foreign keys is again not enforced by composite constraints.",
            "Invocation rows capture redacted argument/result summaries, hashes, catalog snapshots, policy evidence, credential/header summaries, run/issue/agent context, and structured error code plus message, making them a strong audit substrate but not a complete Company OS responsibility contract.",
            "Invocation idempotency is unique only when a non-null key is supplied; the schema neither requires keys for mutations nor ties them to connection/tool/agent/run scope, so semantics depend on caller discipline.",
            "Action requests bind a canonical argument hash and optional signed arguments to an invocation and approval, but have no unique one-request-per-invocation constraint, no status check, and no database-enforced company consistency with the invocation/approval/issue.",
            "Connection token issuance records requested and issued scope, responsible user, token hash, outcome, error code and a database-bounded 1-900 second ttlSeconds; the bound applies only to the recorded integer and does not constrain the external token's explicit expiresAt.",
            "Tool and gateway rate-limit tables have useful unique window keys, but this schema alone does not provide an atomic consume invariant; the previously observed count-then-act broker path can still race.",
            "The generic tool-access audit table retains structured reason codes and actor ids but lacks first-class accountable-human, run, issue, approval, evidence, and responsibility-contract links; richer tool_call_events partly duplicate it instead of one canonical responsibility ledger.",
            "The complete schema confirms strong redaction, token-hash, idempotency-option, and audit ideas alongside widespread service-enforced tenant invariants and duplicated operational/audit models; migrations and schema tests remain required before a final database judgment."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-869 exactly once against the pinned SHA-256.",
        "Map authority, lifecycle, tenant, foreign-key, uniqueness, lease, and audit invariants to the Tool Access service and route findings.",
        "Review every linked first-party migration and focused schema/migration test before this evidence can contribute to package:db completion."
      ],
      "linkedEvidence": {
        "history": [
          "0a511ed1 feat(apps): support multiple provider connections (#11060)",
          "cac3c0fa feat(connections): add runtime subjects and grants (#9982)",
          "7e00f671 feat(connections): add v3 schema core (#9958)",
          "1de0a3bb feat(mcp) [split 2/8]: add governed access contracts (#9557)"
        ],
        "remaining": "All linked first-party SQL migrations and schema/migration tests remain unread evidence; package:db stays incomplete."
      }
    },
    {
      "id": "evidence:package:db/migrations/tool_access_chain",
      "path": "packages/db/src/migrations/{0148,0149,0150,0152,0154-0158,0162,0165,0168,0182,0183,0211}*.sql",
      "lineCount": 1033,
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 426,
          "readAt": "2026-08-18",
          "notes": [
            "Migration 0148 creates the initial tool application/connection/catalog/runtime/audit slice with IF NOT EXISTS guards and single-column tenant/resource foreign keys; it initially cascades application deletion into connections, later corrected by 0152.",
            "0148 defaults catalog risk to read and establishes unique company/name and connection/name indexes, but has no enum checks and no composite foreign keys preventing cross-company references.",
            "The first 300 lines of 0149 expand the initial slice into profiles, policies, invocations, action requests, call events, rate limits, runtime ownership, secret refs, and actor attribution through additive columns and tables.",
            "0149 backfills application keys with an id suffix and catalog tool/application/schema fields from connections before setting tool_name NOT NULL; this is forward compatible but can take table locks during backfill and ALTER on large installations.",
            "Runtime connection_id is made nullable before adding multiple ownership scopes, expanding lifecycle semantics without an explicit data-validity check for owner_scope_type/id combinations.",
            "The migration uses many IF NOT EXISTS operations, improving rerun tolerance, but this pattern does not verify that an existing object has the expected definition and can silently accept schema drift.",
            "Up to 0149 line 300, tenant consistency remains dependent on separate company and resource foreign keys rather than composite constraints."
          ]
        },
        {
          "start": 427,
          "end": 738,
          "readAt": "2026-08-18",
          "notes": [
            "The rest of 0149 adds extensive single-column foreign keys and indexes but never closes cross-company consistency between duplicated company_id values and referenced resources.",
            "Historical invocations and call events intentionally retain references through SET NULL, preserving records but weakening the ability to reconstruct deleted responsibility principals and resources unless immutable snapshots exist elsewhere.",
            "The plugin backfill cross-joins every company with every tool-bearing plugin, then creates an active connection and read-classified catalog entries per company; this can create large write amplification and silently classify all legacy plugin tools as read.",
            "Plugin backfill uses ON CONFLICT company/name rather than the generated application_key, so an unrelated name collision can suppress migration of a distinct plugin without an explicit failure or report.",
            "The migration creates plugin connections with transport='remote_http', a value absent from the current schema transport check, proving a later semantic rewrite is required and mixed-version compatibility must be inspected.",
            "Most importantly for Company OS, 0149 automatically grants tools:admin, connection/profile/audit/runtime permissions not only to active owner/admin humans but also to active CEO/CTO agents. That embeds role-name-based Agent authority directly into migration data and conflicts with accountable-human approval boundaries.",
            "The migration is forward-only and contains no downgrade or compensating rollback; its broad backfills and permission grants need explicit upgrade validation and operational rollback strategy."
          ]
        },
        {
          "start": 739,
          "end": 832,
          "readAt": "2026-08-18",
          "notes": [
            "0150 and 0158 add immutable-looking catalog and provider snapshot columns to invocations, a useful incremental audit-evidence pattern, but neither backfills existing rows nor makes snapshots mandatory.",
            "0152 explicitly replaces cascade with NO ACTION to close the application delete/connection create race; the rationale matches the current schema and is backed by the already-read service test, making this one of the strongest reusable migration changes.",
            "0154 creates OAuth state with company and connection foreign keys plus expiry indexing, but actor and session binding only arrive additively in 0155-0156, meaning rolling mixed versions can create states without the stronger binding unless application deployment order is controlled.",
            "0157 adds a single profile review watermark without transaction or per-entry decision history, matching the previously identified TOCTOU limitation.",
            "0162 adds a company-scoped metric bucket with nonnegative count and a unique bucket key, a compact correct aggregation primitive; no retention/partition policy appears in the migration.",
            "All these changes are forward-only and mostly use immediate ALTER/constraint creation rather than NOT VALID plus later validation, so lock duration and zero-downtime behavior depend on table size and deployment orchestration not visible here."
          ]
        },
        {
          "start": 833,
          "end": 1033,
          "readAt": "2026-08-18",
          "notes": [
            "0165 creates a well-indexed, redacted token-issuance evidence table with responsible user, run, requested/issued scope, bounded recorded TTL, outcome and token hash; its foreign keys remain single-column and do not enforce tenant consistency.",
            "0168 repeats the structural gap from the current schema: install company_id and connection_id are independently constrained, so a cross-company install row is database-valid; target agent/company existence is not referenced at all because target_id is opaque text.",
            "0182 performs an in-place transport rewrite from remote_http to mcp_remote, derives auth_kind from JSON/secret references, then immediately adds NOT NULL, uniqueness, and enum checks without an explicit preflight report, NOT VALID phase, or rollback path.",
            "0182 introduces the strong composite company/connection foreign key for grants and useful subject/default checks, demonstrating the maintainers understood tenant-safe constraints but did not apply the same pattern to installs, profiles, invocations, approvals, or audit rows.",
            "Critical for credential scope: 0182 backfills every existing connection into an active default workspace grant carrying the connection's credential_secret_refs. If any pre-v3 reference represented user-scoped authorization, this migration broadens it to workspace scope without per-row classification or human confirmation.",
            "0183 adds subject user, requested scopes, return URL and responsibility interaction references to OAuth state with no IF NOT EXISTS and no foreign keys for issue/interaction, so rerun tolerance and referential integrity are weaker than earlier migrations.",
            "0211 drops company/name uniqueness to permit multiple same-named connections; stable uid becomes canonical identity, which aligns with opaque-id design but requires clients to stop treating display names as unique.",
            "The full selected migration chain is now read. It has good incremental evidence additions and one excellent race fix, but no reversible down migrations, no visible online-DDL strategy, inconsistent idempotence, and at least two responsibility/security migrations that require deeper test and release-history validation."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "fileManifest": [
        {"path":"packages/db/src/migrations/0148_tool_access_mcp_connections.sql","virtualRange":"1-126","sha256":"59c99b0728610e2e7ea7f29a62364e6121e2cadac747b83c61735f355d4b38c2"},
        {"path":"packages/db/src/migrations/0149_agent_access_phase2_contracts.sql","virtualRange":"127-738","sha256":"973497276131c631680c1b265b8d9d9110c3207c9eb066b1854426e4ca08347c"},
        {"path":"packages/db/src/migrations/0150_tool_invocation_catalog_snapshots.sql","virtualRange":"739-740","sha256":"8ef76e87a40cab9ab5e7fd4fa114a91f29527e525837a043df1a74bebb133938"},
        {"path":"packages/db/src/migrations/0152_tool_connection_application_no_cascade.sql","virtualRange":"741-758","sha256":"75ab3c38858e08b8ccc3b58704c449ffba67257a4904cc6ec0caebed444a0f35"},
        {"path":"packages/db/src/migrations/0154_tool_oauth_states.sql","virtualRange":"759-794","sha256":"f42d9ed58ab8ebe52a880d6fc01013f785ab8a25c447293acca9d1ed57ad5584"},
        {"path":"packages/db/src/migrations/0155_tool_oauth_state_actor_binding.sql","virtualRange":"795-799","sha256":"63d5854c39bafaca62c7e2ad22253b6d3b0590403b1438b5ad38a594f0cf113c"},
        {"path":"packages/db/src/migrations/0156_tool_oauth_state_session_binding.sql","virtualRange":"800-800","sha256":"f47da5b40228b23f7aaab712b3c915fbf08876e013cea75ee76af938972fa429"},
        {"path":"packages/db/src/migrations/0157_tool_profile_new_tools_review.sql","virtualRange":"801-801","sha256":"adb9256f4b32c7f73c738f89be8e0a3fe43cfd84f95cb6b833e81f0134adfaf9"},
        {"path":"packages/db/src/migrations/0158_tool_invocation_connected_mcp_metadata.sql","virtualRange":"802-805","sha256":"ff7836f9082ed761cdcc9a6040ab4ea664295b959994d4db279dd286934d00dd"},
        {"path":"packages/db/src/migrations/0162_tool_runtime_metric_counters.sql","virtualRange":"806-832","sha256":"57184b1fded85bfaa8db52b7cd80defd6383b246d987ded9954100d33aaf290c"},
        {"path":"packages/db/src/migrations/0165_connection_token_issuances.sql","virtualRange":"833-896","sha256":"0a3d602fd1949a57b11bc83d72f7514ad2af41b9bf81ef38d167d00f0abdbfa6"},
        {"path":"packages/db/src/migrations/0168_tool_connection_installs.sql","virtualRange":"897-952","sha256":"9c9c18ac797dc0b041c8a613594a1a20d5e7299d8e2ea374971909a256e368f2"},
        {"path":"packages/db/src/migrations/0182_connections_v3_schema_core.sql","virtualRange":"953-1026","sha256":"16ea6b8bbbf6000e26453ecc4f614e52830a5c6d8258dbf0f1ef0b743540e88c"},
        {"path":"packages/db/src/migrations/0183_connection_user_authorization_state.sql","virtualRange":"1027-1032","sha256":"34fbfbca813c54004579aa5041042ea30a2b7e7ab82a185efe510de1720c5286"},
        {"path":"packages/db/src/migrations/0211_bright_morg.sql","virtualRange":"1033-1033","sha256":"9e0a9fb9f4a1da0321224c964ed50f758851cee9a319963922235fa4cf9484f9"}
      ],
      "completionRequirements": [
        "Read every manifest file exactly once in migration order against its pinned SHA-256.",
        "Trace forward-only transformations, data backfills, destructive steps, constraint validation, locking, upgrade, rollback, and mixed-version behavior.",
        "Review migration tests and repository migration runner semantics before using this chain as package:db evidence."
      ]
    },
    {
      "id": "evidence:package:db/tests/connections-v3-schema-core-migration",
      "path": "packages/db/src/connections-v3-schema-core-migration.test.ts",
      "lineCount": 90,
      "sha256": "21f237dcd4d0005ab97f5ce49106aebc2077a8fb762e2daeb76263484ba8d1b1",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 90,
          "readAt": "2026-08-18",
          "notes": [
            "The single focused test uses embedded PostgreSQL and validates transport/auth/uid backfill, composite tenant protection, subject/default grant checks, and retention of the connection after a manually scripted teardown.",
            "The fixture deliberately places an OAuth refresh-token secret reference on the legacy connection and asserts that migration 0182 copies it into an active default workspace grant. It therefore codifies credential-scope broadening rather than detecting or preventing it.",
            "The test title says rolls back, but no product rollback migration is exercised: the test itself manually drops the new table, indexes, constraints and columns and rewrites transport. This proves a possible bespoke teardown in the fixture, not an operational rollback mechanism.",
            "No test covers user-scoped legacy credentials, multiple connections, name/uid collision, failed backfill, partial migration, rerun, concurrent writes, lock behavior, large tables, or an old server running against the new schema.",
            "The test strengthens confidence in the narrow database constraints but increases the severity of the responsibility-scope conflict for Company OS."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-90 exactly once against the pinned SHA-256.",
        "Determine whether tests detect credential-scope broadening, tenant mismatch, upgrade failure, rerun, rollback, and mixed-version behavior.",
        "Execute the focused test when the existing local environment permits it without mutating the upstream source tree."
      ],
      "linkedEvidence": {
        "focusedTest": "2026-08-18: Vitest discovered the suite but skipped its single embedded-PostgreSQL test because embedded PostgreSQL support was unavailable in the current environment; no pass is claimed."
      }
    },
    {
      "id": "evidence:package:db/client-migration-runner",
      "path": "packages/db/src/client.ts",
      "lineCount": 864,
      "sha256": "b8b5821df004a429b3d960e7e8cb24ba6bc0110934da2514b778264a8261115e",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 450,
          "readAt": "2026-08-18",
          "notes": [
            "The client validates database tuning environment variables and safely quotes dynamic SQL identifiers/literals, supporting both direct and transaction-pooler deployments without embedding vendor-specific behavior in callers.",
            "Manual pending migrations are ordered by the Drizzle journal and each migration plus its history entry executes in one explicit transaction, so ordinary statement failure rolls back that migration atomically.",
            "There is no advisory lock or other cross-process migration lease in this range. Concurrent server startups can both observe a missing history entry and race through DDL/data backfills, especially for non-idempotent later migrations.",
            "History existence is satisfied by hash OR filename when both columns exist. A file changed in place but retaining its name can therefore be treated as already applied, weakening immutable migration integrity in name-aware journals.",
            "Journal creation and migration bookkeeping support several historical table shapes, a pragmatic upgrade feature, but created_at is synthesized from local metadata/current time and is not a cryptographic ordering guarantee.",
            "Statement splitting relies exclusively on the custom breakpoint marker and manual unsafe execution; correctness therefore depends on every migration author placing markers around multi-statement SQL accurately.",
            "The runner contains schema-object introspection helpers used for history repair; the remaining lines must establish whether that repair validates exact definitions or merely object presence."
          ]
        },
        {
          "start": 451,
          "end": 864,
          "readAt": "2026-08-18",
          "notes": [
            "History repair recognizes only CREATE TABLE, ADD COLUMN, CREATE INDEX, and ADD CONSTRAINT and checks object-name presence, not column types/defaults, index columns/predicates, foreign-key targets/actions, check expressions, or constraint validation state.",
            "If every recognized object exists, reconciliation can insert or update a journal entry for the current file hash even when the installed schema definition differs materially, converting undetected drift into an apparently up-to-date database.",
            "Unrecognized statements fail closed for history repair and processing stops at the first unreconciled migration, a useful ordering safeguard; however, ordinary manual application then executes those statements without a cross-instance lock.",
            "Applied-migration inference has multiple best-effort fallbacks from hashes to timestamps, ids, and finally row counts. This improves legacy recovery but can misclassify history when migration files changed, journal rows are partial, or timestamps differ.",
            "Non-empty databases without any migration journal are correctly rejected rather than auto-initialized, a strong fail-closed bootstrap rule.",
            "There is no down-migration API, snapshot restore hook, migration preflight for destructive/locking operations, compatibility window contract, or deployment-level expand/contract gate in this client.",
            "The database reset helper validates identifiers and terminates connections before drop/recreate, but it is a destructive administrative primitive whose caller authorization and deployment exposure must be audited elsewhere.",
            "The complete runner is pragmatic for small self-hosted installs but its best-effort drift repair, lack of advisory locking, and absence of online/rollback orchestration are not sufficient as-is for Company OS managed-cloud or enterprise upgrade guarantees."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-864 exactly once against the pinned SHA-256.",
        "Trace migration ordering, journaling, transaction boundaries, drift reconciliation, failure recovery, bootstrap, locking, and concurrent startup behavior.",
        "Review focused client and migration-safety tests before contributing to package:db completion."
      ]
    },
    {
      "id": "evidence:package:db/check-migration-safety",
      "path": "packages/db/src/check-migration-safety.ts",
      "lineCount": 1052,
      "sha256": "32526568ea596d361ae798f72c894d02a524a323eee999cdf430af84eb6c5386",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 400,
          "readAt": "2026-08-18",
          "notes": [
            "The migration safety checker currently defines only four rule families, all centered on mutation/index behavior for known-large tables; it does not yet claim to cover destructive DDL, tenant/credential semantics, foreign-key locks, NOT NULL rewrites, irreversible changes, or mixed-version compatibility.",
            "Findings use deterministic content-derived ids and a baseline/new/stale model, which is a useful review workflow for introducing safety gates into an existing migration history.",
            "The SQL splitter handles explicit Drizzle breakpoints, quotes, dollar-quoted blocks, nested block comments, and semicolons more carefully than simple regex-only tooling.",
            "Any rule or all rules can be suppressed through an inline comment carrying a nonempty reason; this provides accountable escape hatches only if code review validates the reason, because there is no issue id, approver, expiry, or scope beyond the containing statement.",
            "Index parsing intentionally ignores expression columns when deciding whether a plain-column access path exists, reducing false assurance for batched ORDER BY operations.",
            "The implementation is a targeted large-table performance guard, not a general migration safety system; remaining lines and tests must be checked before judging parser reliability and false-negative surface."
          ]
        },
        {
          "start": 401,
          "end": 750,
          "readAt": "2026-08-18",
          "notes": [
            "Predicate and index analysis normalizes quoting, aliases, sort modifiers, conjunctive partial-index predicates, and expression columns with considerably more care than a naive grep-based guard.",
            "Mutation discovery targets UPDATE and DELETE only; INSERT...SELECT backfills, ALTER TABLE rewrites, CREATE TABLE AS, TRUNCATE, DROP, type changes, constraint validation and trigger-driven mutations are outside this parser path.",
            "The checker treats the presence of LIMIT/FETCH plus any DML as a batched mutation and detects DO-loop patterns, but correctness depends on later correlation between the limiting subquery, target table, ordering columns and support indexes.",
            "Selective-WHERE recognition is syntactic and accepts a broad family of comparisons, IN, EXISTS, LIKE and null checks; it cannot estimate selectivity or distinguish a predicate matching nearly every row without table statistics.",
            "The parser deliberately tracks parentheses, comments, quotes and qualified references to reduce false positives, but remains a hand-built SQL recognizer whose supported grammar and exclusions must be treated as a bounded lint contract, not semantic proof."
          ]
        },
        {
          "start": 751,
          "end": 1052,
          "readAt": "2026-08-18",
          "notes": [
            "Support indexes are collected from the entire migration before mutations are analyzed, so an index declared after a large-table mutation can suppress a finding even though it did not support that mutation at execution time.",
            "When any explicit table-size estimates are supplied, tables absent from that estimate map are treated as not large instead of falling back to the known-large catalog, allowing partial telemetry input to create false negatives.",
            "The checker recommends CREATE INDEX CONCURRENTLY for known-large tables, but the already-read manual migration runner wraps every migration in BEGIN/COMMIT. PostgreSQL forbids CREATE INDEX CONCURRENTLY inside a transaction, creating a direct contradiction between the safety remediation and the production runner.",
            "For batched mutations without ORDER BY, any overlap between a predicate column and any same-migration index column/predicate is accepted; this is weaker than proving the index supports the actual filter and batch progression.",
            "The baseline mechanism fails only on new findings. Stale baseline ids are explicitly ignored, so removed/changed historical statements leave dead suppressions without failing CI.",
            "The command scans every SQL migration and emits stable, reviewable evidence. As a narrow large-table lint it is useful, but it is not a canonical migration safety owner and cannot substantiate claims about upgrade, rollback, data integrity, authorization, or zero-downtime readiness.",
            "The complete implementation merits ADAPT for parser/test patterns only, with the rule model expanded and the transaction/concurrent-index contradiction resolved before Company OS use."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-1052 exactly once against the pinned SHA-256.",
        "Determine which destructive, locking, data-loss, mixed-version, naming, and journal-drift hazards are actually detected and which are omitted.",
        "Read its complete 492-line focused test before assigning test quality or reuse judgment."
      ],
      "linkedEvidence": {
        "history": ["22001bbd feat(db): add migration safety lint"],
        "execution": "2026-08-18: repository migration-safety command exited 0 and reported 20 historical findings covered by baseline plus 1 stale baseline id ignored; this proves the four-rule lint state only.",
        "numberingExecution": "2026-08-18: repository migration-numbering command exited 0 with no output."
      }
    },
    {
      "id": "evidence:package:db/tests/check-migration-safety",
      "path": "packages/db/src/check-migration-safety.test.ts",
      "lineCount": 492,
      "sha256": "43cfd5b4d7ebc05db9e4fe9d66042976c1673baa116081a2104b901f21c90ff6",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 492,
          "readAt": "2026-08-18",
          "notes": [
            "The tests provide strong adversarial coverage for comments, string and dollar-quoted literals, nested CTE WHERE clauses, joined-table-only predicates, aliases, expression indexes, partial-index predicate mismatch, ORDER BY prefix mismatch, LIMIT and FETCH variants.",
            "Tests confirm the intended large-table threshold and distinguish explicit large and small estimate buckets, but never pass a partial estimate map that omits a known-large table, so the implementation's false-negative fallback defect is uncovered.",
            "The positive support-index fixture creates its concurrent index before the mutation, but no test places the index after the mutation; therefore the whole-migration precollection ordering defect is not caught.",
            "No test verifies that the recommended CREATE INDEX CONCURRENTLY migration can execute through the transaction-wrapping production migration runner, leaving the direct remediation/runtime contradiction untested.",
            "No tests cover stale baselines, malformed or overly broad suppressions, INSERT...SELECT, ALTER rewrites, DROP/TRUNCATE, constraint validation, irreversible operations, foreign-key lock hazards, or authorization/data-scope changes.",
            "Within its stated four-rule scope the test quality is good and behavior-focused, but the suite should not be presented as comprehensive migration safety evidence."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-492 exactly once against the pinned SHA-256.",
        "Map positive, negative, parser-adversarial, estimate, baseline, ordering, and transaction-compatibility coverage to the implementation findings.",
        "Execute the focused test and the repository migration-safety command without modifying upstream source."
      ],
      "linkedEvidence": {
        "history": ["22001bbd feat(db): add migration safety lint"],
        "focusedTest": "2026-08-18: direct repository Vitest run passed all 25 tests in 107ms."
      }
    },
    {
      "id": "evidence:package:db/migration-safety-baseline",
      "path": "packages/db/src/migration-safety-baseline.ts",
      "lineCount": 157,
      "sha256": "bf78f57929143139b80eecb4e25510cb566e933bcff6099cfc818438b9802ae0",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 157,
          "readAt": "2026-08-18",
          "notes": [
            "The baseline contains 21 accepted findings: overwhelmingly non-concurrent indexes on historical large-table migrations plus one full-table-mutation finding for migration 0132.",
            "Reasons are mostly generic 'predates the guard' statements; entries have no approver, approval date, issue link, expiry, deployment impact, mitigation, or verification evidence.",
            "The 0132 full-table exception references PAP-1505 and keyset batching, offering materially better provenance than the other entries, but the baseline still acts as a permanent global acceptance rather than a time-bounded waiver.",
            "The live scanner reported only 20 matched findings and ignored one stale id, so this file currently contains known dead baseline state without a failing cleanup gate.",
            "This is useful as an incremental-adoption mechanism but not sufficient evidence that historical migrations are safe to replay on a large production database."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-157 exactly once and classify every accepted historical finding and stale entry." ]
    },
    {
      "id": "evidence:package:db/table-size-estimates",
      "path": "packages/db/src/table-size-estimates.ts",
      "lineCount": 176,
      "sha256": "749b617c4819820e3e41a5b99b5c8c42170c1b4f2f1c74163a53ac538276967d",
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 176,
          "readAt": "2026-08-18",
          "notes": [
            "Size estimates come from a single default local development embedded-Postgres snapshot collected 2026-07-06, multiplied uniformly by an unexplained factor of 250; they are not measurements from managed-cloud or enterprise deployments.",
            "Only tables with at least 4,000 local rows become 'large' under the 250x factor and 1,000,000-row threshold, yielding a small fixed set dominated by wakeups, activity, references, heartbeat events and comments.",
            "Unknown tables and every table absent from the snapshot default to small. Newly added tables, customer-specific hot tables, and all Tool Access tables omitted from this snapshot therefore receive no large-table protection regardless of actual production growth.",
            "Tables with zero local rows remain permanently classified small under this static file, even if their production cardinality or write rate is high.",
            "There is no refresh command, freshness threshold, production telemetry merge, per-deployment override, row-width/index-size/lock-cost dimension, or CI stale-data failure in this module.",
            "The model is transparent and deterministic but only a heuristic seed; Company OS would need deployment-aware telemetry with conservative unknown defaults before adopting it as a migration gate."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-176 exactly once and verify provenance, freshness, coverage, scaling assumptions, and false-negative behavior." ]
    },
    {
      "id": "application:server/services/tool-gateway",
      "path": "server/src/services/tool-gateway.ts",
      "lineCount": 6316,
      "sha256": "b7e7682e6b3db4ce2039cbf10909b8b8a3b86d6eff8aca93013aec443df0a774",
      "linkedEvidence": {
        "history": [
          "f6ab82d4 feat(interactions): add interaction withdrawal and terminal-issue expiry (#10251)",
          "7e00f671 feat(connections): add v3 schema core (#9958)",
          "b07b2994 feat: stamp responsible users on activity logs (#9731)",
          "cfa5e070 feat(mcp) [split 3/8]: add tool access policy core (#9558)"
        ],
        "focusedTest": "2026-08-18: Vitest discovered both Tool Gateway suites but skipped all 66 embedded-PostgreSQL tests because embedded PostgreSQL support was unavailable; no behavioral test pass is claimed."
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 800,
          "readAt": "2026-08-18",
          "notes": [
            "The 6,316-line service is another very broad orchestration boundary spanning gateway authentication, sessions/tokens, rate limits, profiles/policies, approvals, secrets, remote HTTP, local subprocesses, plugins, Paperclip self-tools, audit and UI-facing previews.",
            "Gateway and session tokens use random 256-bit suffixes and are persisted by SHA-256 hash; session TTL and Tool timeouts are bounded, while named gateway tokens may be long-lived according to separately stored policy.",
            "Caller metadata headers are length- and CR/LF-sanitized, sensitive passthrough names are blocked and audited endpoints omit query/fragment, which are useful secret-exposure defenses; endpoint paths can still contain embedded credentials or sensitive ids.",
            "Unknown Tool names default to read risk in inferToolRisk. Catalog flags improve classification, but any missing/misclassified catalog metadata can still fail open at this layer.",
            "The stable serialization/hash helper has no explicit rejection for undefined, bigint, cycles, non-finite numbers or Unicode normalization; exact approval/idempotency binding must therefore be checked against validated inputs and the separately audited content guard.",
            "The prosumer approval preview interpolates argument keys and string values directly into Markdown without escaping. Its identifier filter recognizes separator-delimited id/key/token/hash names but misses camelCase fields such as noteId, so it can leak identifiers and permit Markdown presentation manipulation in a human decision surface.",
            "Built-in fixtures, Paperclip self-tools, local stdio templates, provider-specific Google Sheets environment keys and virtual search/run tools live in the same service, making this unsuitable as a vendor-neutral Company OS core module.",
            "Protocol rate limiting begins with a database upsert design rather than the count-then-act pattern observed in the token broker; the remaining query/result semantics and all execution paths still require review."
          ]
        },
        {
          "start": 801,
          "end": 1600,
          "readAt": "2026-08-18",
          "notes": [
            "Protocol rate-limit consumption is an atomic INSERT...ON CONFLICT count increment, a strong reusable pattern, although expired-counter pruning performs a global delete from a request path and later enforcement/audit ordering still need review.",
            "Connected Tool discovery applies explicit company, status, health, quarantine, transport and application filters. Per-connection discovery nevertheless loads every eligible company Tool and filters in memory, creating unnecessary tenant-scale work.",
            "Plugin Tools receive the same fail-open name heuristic and cannot supply a first-class risk declaration in this path.",
            "Run-context resolution strongly binds company, Agent, active run, snapshot issue/project and referenced tenant objects. It does not bind a versioned accountable human or ResponsibilityContract in this range.",
            "The 'last changed by' access hint can attribute only createdByAgentId on policies/bindings and returns null for human or profile/entry edits. It can therefore present incomplete or misleading provenance on a governance UI.",
            "writeAudit writes tool_access_audit_events and the general activity log sequentially without a transaction/outbox. Failure of the second write returns an error after the dedicated audit is committed; several distinct actions are also collapsed into generic policy_decision/call_failed classifications.",
            "Session validation reads revocation/expiry/run state and then updates lastUsedAt by id only. A concurrent revocation after the read can still yield a usable session for one call because the final update is not a guarded claim.",
            "An invalid token containing a real session UUID triggers a durable auth-failure audit for that session, creating an unauthenticated audit-amplification path unless an outer limiter always intervenes.",
            "Interaction lifecycle reflection is read-modify-write without version/CAS protection and can overwrite concurrent interaction result changes.",
            "Approval setup updates Invocation, ActionRequest, signed arguments, interaction and events through separate writes; the shown missing-context and missing-signing-secret paths already demonstrate partial-state transitions rather than one atomic approval command."
          ]
        },
        {
          "start": 1601,
          "end": 2400,
          "readAt": "2026-08-18",
          "notes": [
            "Destructive Tools create a formal approvals row while ordinary writes use only the issue interaction confirmation path; the formal-risk threshold is hard-coded to destructive instead of a company/data/action policy decision.",
            "Formal approval, issue link, interaction, ActionRequest update, ToolCallEvent and two audit streams are sequential independent writes. Any intermediate failure can leave orphan approval/interaction state or a parked invocation without its complete responsibility evidence.",
            "The technical approval Markdown embeds a JSON summary in a code fence and the prosumer preview embeds raw fields; adversarial backticks/Markdown can alter presentation even if the stored canonical hash remains intact.",
            "Tool listing and on-demand search load all connected Tools and perform one asynchronous policy decision per Tool on every request. This is an unbounded N+1-style policy projection with no pagination, cache/version key or database-side filtering.",
            "The Paperclip self get_issue_context Tool checks only Agent session presence and company ownership of the requested issue. It does not require the issue to be the run's issue, assigned to the Agent, or otherwise authorized, so a Tool-enabled Agent can request arbitrary known issue ids within its company.",
            "Remote endpoint parsing and public-network validation are applied before execution, and header precedence correctly lets managed credentials override metadata/static/caller values while dropping reserved and sensitive caller headers.",
            "Static headers are allowed from connection config without the sensitive-header prohibition applied to caller passthrough. Operators can therefore store Authorization/API-key-like values directly in ordinary JSON config and forward them, bypassing the Secret reference/audit boundary.",
            "Optional metadata headers export company, Agent, issue, project, run and session identifiers to the remote provider; this is configurable but not visibly authorized through a Company OS-style data egress contract.",
            "Credential resolution records system access and marks connection health on failure, but connection health writes are separate side effects that can race with concurrent successful calls."
          ]
        },
        {
          "start": 2401,
          "end": 3200,
          "readAt": "2026-08-18",
          "notes": [
            "Approval snapshots bind application/connection/catalog identity and status, configuration hashes, credential-reference hashes and exact resolved Secret versions. This is a strong reusable stale-approval defense, though later execution must prove it is compared immediately before dispatch.",
            "Local stdio inherits only a small OS environment allowlist, which limits ambient Secret leakage. Template env values are nevertheless read directly from ordinary connection config rather than Secret references, allowing credentials such as service-account JSON to reside in non-secret configuration.",
            "The local stdio stdout accumulator is unbounded until a newline arrives; a malicious or broken child can exhaust memory. SIGTERM is followed by an unbounded wait for process exit, so a child that ignores the signal can defeat the advertised timeout.",
            "Local JSON-RPC parsing matches numeric ids but does not visibly validate jsonrpc version, response shape or duplicate responses; returned error objects are attached to gateway error details and require downstream redaction review.",
            "Remote HTTP uses manual redirect rejection, AbortController timeout and a 1 MB declared/actual body check. The actual-size check occurs only after response.text() buffers the full body, so an absent/false Content-Length can still cause memory pressure beyond the limit.",
            "Individual call failures and successes update shared connection health through independent last-writer-wins writes; concurrent calls can make health oscillate or allow an older result to overwrite newer state.",
            "Remote elicitation converts provider-controlled messages/schema into human interaction content and stores the requested schema in event metadata. Length/count caps help presentation, but no data-classification or prompt-injection boundary is applied before the external content enters the human/Agent workflow.",
            "Elicitation reuses Invocation status awaiting_approval and policy decision defer_runtime, conflating missing Tool input with governance approval in operational state projections.",
            "The generic runWithTimeout races a timer against a promise but does not cancel the underlying operation. A timed-out builtin/plugin execution can continue and perform a side effect after the caller and audit path record timeout."
          ]
        },
        {
          "start": 3201,
          "end": 4000,
          "readAt": "2026-08-18",
          "notes": [
            "Named gateway tokens validate hash, gateway status, revocation, expiry and heartbeat-run company/Agent/active context, then apply atomic token- and gateway-level protocol limits. This is a strong layered bearer boundary.",
            "Token lastUsedAt is updated before protocol rate limiting and without a guarded revoked/expiry predicate. A concurrent revocation can still permit one request, and throttled requests appear as successful token use.",
            "Non-run named gateway sessions are attributed as system even when the gateway is Agent-scoped; there is no accountable-human subject or current membership/responsibility validation in the session identity.",
            "Null-expiry named tokens are projected with a synthetic expiry ten years in the future, underscoring that long-lived bearer credentials are a supported operating mode rather than an exceptional short-lived runtime proof.",
            "Authentication failures are durably audited only after the failure limiter is exceeded; lower-volume invalid/revoked/expired attempts leave no audit event in this function.",
            "Test Tool execution marks the invocation executing and writes an allow audit before dispatch, then performs the external side effect before success event/audit persistence. Any later validation or database/audit failure can report failure after the side effect occurred, and retry can duplicate it.",
            "The execution catch returns decision=allowed even with an error object, conflating authorization decision with execution outcome in the API contract.",
            "Critical correctness bug: runApprovedTestInvocation always reflects the interaction lifecycle as executed after awaiting runTestToolInvocation, but runTestToolInvocation catches execution failures and returns an error instead of throwing. Approved test calls that fail can therefore be shown as executed in the human interaction while the Invocation is failed/timed_out.",
            "Result prompt-injection validation happens after the external action. Blocking a malicious result protects downstream context but cannot undo the side effect; the audit must distinguish executed-with-quarantined-result from execution failure.",
            "The gateway still lacks an atomic execution claim/outbox or provider idempotency proof that closes the side-effect/persistence gap."
          ]
        },
      {
        "start": 4001,
        "end": 4800,
          "readAt": "2026-08-18",
          "notes": [
            "Approved Agent actions use an atomic approved-to-executing claim and wait/replay behavior, verify signed canonical arguments, compare live remote target/Secret-version snapshots and check issue openness twice before dispatch. These are strong single-execution and stale-target patterns.",
          "The execution helper contains a stuck-state branch: after winning the executing claim, a legacy signed payload with executionOnApprove not true throws without a terminal update. Current public callers guard executionOnApprove before invoking the helper and the linked regression test keeps legacy requests approved, so this appears defensive/unreachable in audited paths rather than a demonstrated production bug; any new caller could expose it.",
            "Issue-closed expiry updates the ActionRequest and interaction but not the Invocation approval/status, leaving split lifecycle state.",
            "Approval execution does not re-evaluate current Tool policy, Profile binding, Agent authority, accountable human or ResponsibilityContract. The signed snapshot excludes those governance versions, so permission/policy revocation after approval does not prevent dispatch.",
            "Execution events are attributed to the original Agent session, not the approving human, and the approver identity is not carried into the call event shown here. This breaks the direct human-decision-to-side-effect responsibility link.",
            "Success/failure persistence across Invocation, ActionRequest, interaction and ToolCallEvent remains nontransactional after the external side effect.",
            "Matching/replay keys use company, issue, Agent, Tool name and argument hash but omit run, connection/catalog identity, policy/contract version and explicit idempotency key. A prior approved/executed request can be reused across a later run on the same issue; only remote Tools receive the snapshot defense.",
            "Approved requests are not rejected when their expiresAt has passed; expiry handling applies only while status is pending.",
            "Named gateway creation, Profile binding and audit are separate writes. Updating a gateway to a new Profile inserts the new binding but never removes the old gateway/Profile binding, potentially retaining or combining stale authority.",
          "Named bearer token create/revoke methods shown here have no audit write and revoke accepts no actor, despite changing persistent execution authority. Null expiry is accepted at service level and raw token is returned once."
        ]
      },
      {
        "start": 4801,
        "end": 5200,
        "readAt": "2026-08-18",
        "notes": [
          "Ephemeral session creation inserts the bearer authority and writes its audit record as separate operations. An audit failure can return an error after leaving a live session token persisted, while the caller may not retain the returned token needed to revoke it.",
          "Discovery audit records the complete list of visible Tool names on every listing. This aids traceability but creates potentially large and sensitive audit payloads without an evident cap or classification step.",
          "listPluginToolsForAgent filters through policy but strips providerType and risk from the returned descriptors. Downstream consumers therefore cannot independently display or validate the exact risk classification used by the decision engine.",
          "The management test-call path generates a fresh random idempotency key for every request, so a client retry cannot identify or safely deduplicate the same intended external side effect.",
          "Test-call argument inspection runs in redact/ignore mode and uses the resulting summary for human approval, while policy and execution still receive the original parameters. This avoids automatic prompt-injection blocking but makes the quality of the redacted approval representation critical.",
          "Approval requests produced by test calls receive signed canonical arguments and a one-hour expiry, but creation still spans policy invocation/action rows, snapshot/secret resolution, ActionRequest update, event and audit writes without a transaction or compensating state machine.",
          "getTestCallStatus correctly constrains ActionRequest, Invocation, company, connection and test-origin marker before returning status, preventing it from becoming an arbitrary cross-connection request oracle.",
          "approveActionRequest accepts both pending and already-approved states before the remaining resume logic. The subsequent range must confirm expiry enforcement, atomic claim behavior, actor authorization and idempotent replay for this public approval entry point."
        ]
      },
      {
        "start": 5201,
        "end": 5600,
        "readAt": "2026-08-18",
        "notes": [
          "The direct approveActionRequest path validates the signature and optional formal-approval status, then atomically changes pending to approved. It does not check ActionRequest.expiresAt before approval, so an expired request can be approved and, for test calls or executionOnApprove Agent calls, dispatched immediately.",
          "The approval service records either an Agent id or user id supplied by its caller but performs no actor membership, role, separation-of-duties or accountable-human authorization check itself. Correctness is therefore dependent on every route/caller enforcing the right decision authority.",
          "If a request is already approved, approveActionRequest can trigger executeApprovedAgentInvocation again; the inner approved-to-executing claim limits duplicate dispatch, but approval and execution remain coupled to a read-time branch rather than an explicit durable command/outbox.",
          "declineActionRequest atomically rejects a pending request and updates Invocation.approvalState, but this range emits no ToolCallEvent/audit and does not reflect the rejection onto the linked interaction, yielding an incomplete human-decision evidence chain compared with approval.",
          "Virtual search_tools bypasses the ordinary policy service and records a hard-coded allow decision. It may only expose already-filtered discovery, but its authority and data-return policy are encoded in bespoke control flow rather than the canonical policy boundary.",
          "The main executeTool resume path strongly scopes an approved request to company, current issue, Agent, run, originating requester, linked interaction and exact Tool, and unlike the direct approval path explicitly expires both pending and approved requests before use.",
          "An accepted issue interaction can promote a pending ActionRequest to approved and copy resolver identity into the decision record. This service does not independently verify that the resolver is an authorized human for the exact risk/action/data scope.",
          "Expiry and interaction-driven approval update ActionRequest/interactions/events through separate writes and still do not consistently synchronize Invocation approval/status, leaving recoverable but real projection divergence risks."
        ]
      },
      {
        "start": 5601,
        "end": 6000,
        "readAt": "2026-08-18",
        "notes": [
          "The explicit resume route revalidates signed arguments and the live remote approval snapshot, then atomically consumes approved to executed before dispatch. The label executed therefore means execution claimed, not side effect completed, and a process crash can strand the request while preventing retry.",
          "Consuming an approval writes resolvedByAgentId to the executing session Agent even though the row already carries the human/Agent decision fields. This mixes executor and resolver semantics on the same record and complicates direct responsibility projection.",
          "A fresh call flows through the policy service, durable Invocation/idempotency recording and an explicit require-approval branch. Policy idempotent replay returns only the stored resultSummary rather than the original result, so replay response semantics differ from first execution unless callers expect a summary projection.",
          "After a request is approved, the execution path does not call the policy service again; signature and remote-target freshness are enforced, but revoked Profile/policy/Agent/data authority remains outside the resume invariant.",
          "Result content validation occurs after dispatch and a blocked result is persisted as a failed Invocation with a deny policyDecision in the event/audit. This protects consumers but semantically mislabels an executed external side effect as denied/failed, which can trigger unsafe retries.",
          "Invocation, interaction, ToolCallEvent and dual audits are updated sequentially on both success and failure. A crash after the provider action or any persistence failure leaves no atomic evidence that distinguishes not-run, ran-with-missing-record and safely retryable states.",
          "Runtime capacity and timeout failures are grouped as deferred, but the timeout wrapper cannot prove cancellation for plugin/builtin work. A deferred classification can therefore coexist with a still-running side effect.",
          "Stable structured reason codes are consistently emitted alongside English messages in these paths, an API practice Company OS can adopt without coupling functionality to UI language."
        ]
      },
      {
        "start": 6001,
        "end": 6316,
        "readAt": "2026-08-18",
        "notes": [
          "The direct plugin route validates Agent actors against company/Agent/run and resolves the run context, but no equivalent user-actor authorization is visible inside the service. Human access control must be guaranteed entirely by the calling route.",
          "Plugin execution repeats the policy/invocation/audit pipeline and supports approval creation, but unlike the main gateway path it calls pluginToolDispatcher.executeTool without runWithTimeout. A hung plugin can hold the request indefinitely despite Tool timeout conventions elsewhere.",
          "The plugin path has the same side-effect-before-validation/evidence gap and the same semantic error of recording blocked post-execution content as a denied/failed call. It also returns resultSummary on idempotent replay rather than the original typed result.",
          "Session revocation is company-scoped and can additionally enforce Agent/run scope; it records actor metadata when supplied. The actor is optional and the service does not itself authorize a human revoker, so provenance and authority depend on caller discipline.",
          "Expired sessions are hard-deleted in bulk with no cleanup audit or retained tombstone in this service. Even if Invocation events remain, deleting execution-authority records can weaken forensic reconstruction unless retention is owned elsewhere.",
          "Runtime slot list/stop/restart are exposed through the gateway service and delegate scoping to the supervisor. This further confirms that the service combines product policy, external execution, operational control and administrative lifecycle in one ownership boundary.",
          "Across the full source, the best reusable ideas are hashed bearer tokens, tenant/run binding, atomic rate/claim updates, signed canonical arguments, live Secret/target snapshot comparison, structured reason codes and content summaries. Company OS should not copy this monolith or its Paperclip-specific types and self-tools.",
          "The dominant Company OS gaps are accountable-human and ResponsibilityContract binding, data-egress authorization, transactional evidence/outbox semantics, provider idempotency/cancellation proofs, uniform expiry/revocation checks and separation of approval decision from execution state."
        ]
      }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-6316 exactly once against the pinned SHA-256.",
        "Trace authentication, tenant and run binding, profile/policy decisions, approval pause/resume, action signatures, credentials, external dispatch, idempotency, rate limits, evidence, audit and failure recovery end to end.",
        "Read both first-party Gateway test suites and relevant history, then execute focused tests before a canonical judgment."
      ]
    },
    {
      "id": "application:server/__tests__/tool-gateway-service",
      "path": "server/src/__tests__/tool-gateway-service.test.ts",
      "lineCount": 1036,
      "sha256": "651200f045f4ede19fc620f39a2194bc0ff8e57eb3ad8aac362723a01870d31e",
      "linkedEvidence": {
        "history": [
          "f6ab82d4 feat(interactions): add interaction withdrawal and terminal-issue expiry (#10251)",
          "7e00f671 feat(connections): add v3 schema core (#9958)",
          "931eec3f feat(mcp) [split 4/8]: wire gateway runtime and Smoke Lab (#9559)"
        ],
        "focusedTest": "2026-08-18: this suite was discovered but all database-backed cases were skipped; it contributed source-review evidence only in the current environment."
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 520,
          "readAt": "2026-08-18",
          "notes": [
            "This is a real embedded-Postgres service suite with per-test database cleanup, not a mocked unit-only harness; it exercises persisted Invocation, ActionRequest, interaction, event and audit state.",
            "Tests strongly cover signed reviewed arguments, tamper resistance, same-run scoping, single execution under a concurrent identical-argument race and rejection after issue closure.",
            "The approval-preview assertion checks that raw JSON and Tool/risk jargon are absent and Body is readable, but it does not assert that camelCase identifiers such as noteId are redacted or that Markdown metacharacters are escaped.",
            "The direct review-queue approval happy path proves self-execution and replay but does not test expired requests, revoked policy/Profile/Agent authority, approver role/separation of duties or persistence failure after side effect.",
            "The cross-run test is an important tenant/work-context invariant. The concurrent race validates one approved_action_executed event, but does not simulate process death between the executing claim and provider completion.",
            "The legacy-signature test expects a pre-execute-on-approve request to remain approved and its Invocation awaiting_approval. Because current public callers guard executionOnApprove before invoking the execution helper, the helper's internal stuck-executing branch appears defensive/unreachable in these tested paths rather than a demonstrated production bug."
          ]
        },
        {
          "start": 521,
          "end": 1036,
          "readAt": "2026-08-18",
          "notes": [
            "The suite verifies fail-closed cancellation when signing is unavailable or invalid, missing-task recovery guidance, rejection idempotency, fresh approval after stale pending requests and a second formal board gate for destructive Tools.",
            "The stale-request test covers matching a later executeTool call, not direct approveActionRequest on an already expired request; the source-level expiry gap in that public method remains untested.",
            "The decline test asserts ActionRequest and Invocation approval state but does not assert linked interaction lifecycle, ToolCallEvent or audit evidence, matching the missing rejection evidence writes observed in source.",
            "Formal destructive approval verifies status and argument integrity but obtains approval through direct database mutation; it does not exercise approver authorization, role, quorum, separation of duties or revocation between decision and execution.",
            "Remote MCP elicitation tests durable question projection and missing-issue failure, while explicitly expecting Invocation awaiting_approval and defer_runtime. This codifies the lifecycle conflation between provider input elicitation and governance approval.",
            "The malicious-result test proves payload redaction and downstream blocking, but expects the already-dispatched plugin call to be recorded as failed/denied and does not preserve an executed-with-quarantined-output fact. It therefore confirms rather than resolves the retry/evidence ambiguity.",
            "Sensitive argument tests deliberately pass original credentials to the plugin while verifying redacted persistence. They do not impose a Company OS data-authorization/export contract or prove the plugin process is entitled to that Secret.",
            "There are no service tests here for direct human actor authorization, policy/Profile revocation after approval, crash recovery/outbox, provider idempotency, cancellation, plugin timeout, session revocation race or hard-delete retention."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-1036 exactly once against the pinned SHA-256.",
        "Map service-level tests to the authentication, policy, approval, execution, failure and evidence invariants found in the implementation."
      ]
    },
    {
      "id": "application:server/__tests__/tool-gateway",
      "path": "server/src/__tests__/tool-gateway.test.ts",
      "lineCount": 3941,
      "sha256": "367fc7e1406566c2c43d26e459e9b0e2c640e9397aa7b7f320cc6f45f1db8167",
      "linkedEvidence": {
        "history": [
          "cca2806e test(tool-gateway): make idle-down slot test deterministic via injected clock (#10226)",
          "7e00f671 feat(connections): add v3 schema core (#9958)",
          "931eec3f feat(mcp) [split 4/8]: wire gateway runtime and Smoke Lab (#9559)"
        ],
        "focusedTest": "2026-08-18: this suite was discovered but all database-backed cases were skipped; it contributed source-review evidence only in the current environment."
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 800,
          "readAt": "2026-08-18",
          "notes": [
            "This acceptance suite uses embedded Postgres, Express/Supertest, real local HTTP servers and subprocess-capable fixtures, providing substantially stronger system evidence than isolated mocks.",
            "Fixture construction spans Profiles, bindings, catalog, remote/local transports, Secrets, principals and durable execution records, but actors are still mostly bare ids rather than versioned accountable-human responsibility subjects.",
            "Named gateway tests verify opaque public ids, hashed bearer flow, Tool discovery filtering, per-token method scope, policy denial before upstream dispatch and revocation. Tokens carry labels/notes but no required owner principal or accountable human in the asserted contract.",
            "The audit assertion for a denied Tool checks only call_completed rows and payload absence, not the dedicated denied record; its confidentiality value is narrow even though the source writes a denial audit.",
            "Authentication throttling is persisted, shared by design and avoids raw bearer leakage while keeping prefix/correlation metadata. Only the threshold-crossing failure is asserted as audited, consistent with low-volume failures remaining invisible.",
            "Archived gateway listing and expired limiter pruning receive direct coverage. No test in this range checks that Profile changes remove stale gateway bindings or that token create/revoke carries a human actor and audit record."
          ]
        },
        {
          "start": 801,
          "end": 1600,
          "readAt": "2026-08-18",
          "notes": [
            "Persisted authentication and protocol rate limits are tested across separate service instances and across initialize/list/call scopes, with structured redacted audit evidence. This is a strong distributed-control pattern worth adapting.",
            "No-Profile fail-closed behavior, company isolation, catalog-entry/connection selectors, collision-safe names and exclusion of disabled/unhealthy/quarantined sources receive explicit acceptance coverage.",
            "Remote and local transports are exercised end to end, including local runtime-slot persistence and a minimal inherited environment allowlist.",
            "The local environment test intentionally sources ALLOWED_TOKEN plaintext from ordinary connectionConfig.env and merely redacts it from the returned result. It demonstrates ambient-environment isolation but also codifies the Secret-boundary bypass identified in source.",
            "Public authenticated deployment blocks a link-local metadata endpoint before fetch, providing direct SSRF evidence for a critical deployment profile.",
            "Managed Secret resolution into an Authorization header is exercised with redacted Invocation/events/audits and complete Tool/catalog provenance. The test proves storage/log hygiene, not an explicit data-use/egress authorization contract or human responsibility binding.",
            "Run and issue linkage is consistently asserted for execution evidence, but the evidence subjects remain company/Agent/run/issue; accountable human, ResponsibilityContract and authorization-version fields are absent."
          ]
        },
        {
          "start": 1601,
          "end": 2400,
          "readAt": "2026-08-18",
          "notes": [
            "Header-policy tests robustly prove managed credentials win over caller input, reserved/auth-bearing passthrough headers are dropped and raw caller/credential values do not persist. They do not test sensitive staticHeaders, the plaintext-config bypass found in source.",
            "Metadata forwarding of Agent and issue ids is explicitly expected and audited, but there is no data-classification/export authorization assertion for sending these tenant identifiers to an external provider.",
            "Virtual search/run coverage proves hidden on-demand catalog access still applies the target Tool policy and records target metadata. search_tools itself remains a bespoke hard-coded allow path rather than a policy-controlled read capability.",
            "MCP Streamable HTTP/SSE interoperability and an SDK-backed demo server are tested end to end, valuable protocol-compatibility evidence.",
            "The broad remote-MCP scenario covers default denial, approval pause/resume, signed original arguments, redacted provider output, rejection, rate limiting, tenant isolation and rich request/response execution metadata.",
            "The approval fixture marks the issue interaction accepted by resolvedByAgentId equal to the executing Agent, and rejection calls the service with that Agent as decision actor. The tests therefore permit Agent self-approval semantics at this layer rather than enforcing an accountable human.",
            "Approved execution evidence captures dispatch, endpoint, request id and HTTP response metadata, a useful run-proof pattern, but it still does not prove provider idempotency or atomically link the approver, current authority contract and completed side effect.",
            "The test verifies rejected requests block explicit reuse but does not assert rejection event/audit/interaction lifecycle completeness."
          ]
        },
        {
          "start": 2401,
          "end": 3200,
          "readAt": "2026-08-18",
          "notes": [
            "Approval freshness is directly tested for both remote endpoint/config drift and rotation of a latest-version credential; dispatch is blocked and raw Secret versions remain absent from persisted evidence. This is one of the strongest reusable designs in the module.",
            "Controlled remote failures cover HTTP status, invalid JSON, malformed MCP result, declared oversize response and AbortController timeout with structured execution metadata. The oversize test supplies Content-Length and does not exercise an unbounded chunked/no-length body that source buffers before checking.",
            "Hashed sessions are verified across service instances, query-string tokens are rejected, and authenticated revocation is tenant/run scoped with redacted dual-audit records.",
            "Route-level authorization denies viewers from approve/decline and denies Agent actors from raw audit/runtime administration. This partially answers the service's caller-dependence, but it does not show a risk-specific approver permission, accountable-human assignment or separation of duties; any non-viewer board role appears eligible for ordinary approvals unless constrained elsewhere.",
            "Runtime control and audit read require explicit permission grants for an operator and audit pagination/filtering begins server-side, useful management-plane patterns.",
            "No concurrency test races session use against revocation, and no retention test covers cleanupExpiredSessions deleting expired authority records."
          ]
        },
        {
          "start": 3201,
          "end": 3941,
          "readAt": "2026-08-18",
          "notes": [
            "Audit APIs are tested for server-side pagination, filters and humanized Agent/app/Tool projections, while durable sessions and run-bound named tokens fail closed once the heartbeat run is inactive and preserve run/issue/project attribution.",
            "Expired, revoked and tampered ephemeral sessions produce redacted rejection audits; explicit cleanup then hard-deletes expired sessions, with no assertion for a retained authority tombstone or forensic linkage after deletion.",
            "Runtime-supervisor tests cover deterministic lazy start/reuse/idle reap, explicit stop/restart, host capacity deferral, public-host fail-closed deployment, restart-storm suppression and stuck-slot recovery. These are mature operational concerns Company OS must include even if implemented differently.",
            "The public hosted profile explicitly blocks local stdio without a trusted runtime host, aligning with managed-cloud versus customer-local execution separation.",
            "Approval discovery copy and durable issue-thread pause behavior are asserted, but responsibility evidence still stops at requestedByAgentId and the assigned issue; no accountable human or contract version is required.",
            "Plugin discovery/execution is proven to share Profile policy and run context, but there is no test for hung-plugin timeout/cancellation, user-actor authorization, explicit plugin risk declaration or data-egress rights.",
            "Cross-company caller-supplied issue context is rejected. There is no corresponding same-company least-privilege test for the Paperclip self get_issue_context Tool, leaving the source-observed arbitrary same-company issue read gap unchallenged.",
            "Across 3,941 lines, the suite is broad and integration-heavy, but its systematic omissions match Company OS's differentiators: human responsibility authority, versioned permission/data contracts, transactional/outbox recovery, exactly-once external effects and audit retention."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-3941 exactly once against the pinned SHA-256.",
        "Map integration coverage to tenant/run binding, approval pause/resume, token lifecycle, retries, concurrency and evidence consistency."
      ]
    },
    {
      "id": "application:server/routes/tool-gateway",
      "path": "server/src/routes/tool-gateway.ts",
      "lineCount": 830,
      "sha256": "36171e52e6660c6741806cf6f752a9b2e31d535108504fe88400189c3a801b04",
      "linkedEvidence": {
        "history": [
          "931eec3f feat(mcp) [split 4/8]: wire gateway runtime and Smoke Lab (#9559)"
        ],
        "focusedTest": "The already-read tool-gateway.test.ts acceptance suite covers selected route permissions, protocol operations, revocation and audit pagination; local Vitest discovered but skipped all embedded-PostgreSQL tests."
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 430,
          "readAt": "2026-08-18",
          "notes": [
            "The route combines unauthenticated public MCP endpoints, named-gateway administration, ephemeral runtime sessions, approval, audit and runtime controls; protocol and management contracts are coupled in one Express file.",
            "MCP bearer tokens are header-only and ToolGatewayHttpError exposes stable reasonCode plus details, but missing-token, invalid-method and generic HttpError paths use English strings without a uniform stable code.",
            "Protocol input is cast from req.body without schema, JSON-RPC version, id-type, batch, params-shape or local size validation. tools/call serializes arbitrary result data into one text item and can throw on non-JSON values.",
            "Named gateway create/update/token operations correctly require the explicit tools:admin permission and validate shared schemas. Token revocation does not pass the authenticated actor to the service, matching the missing revoke-audit attribution observed in source.",
            "Public gateway discovery GET endpoints reveal transport and endpoint for any supplied public id without proving the gateway exists; this may be intentional documentation behavior but is not an authenticated resource read.",
            "Critical route-authority gap: POST /tool-gateway/sessions uses only assertBoardOrAgent plus company access. A board viewer can supply an Agent and active run id and receive a live bearer session for that Agent because neither read-only membership nor tasks:assign/tools permission is checked here; the service validates run binding but trusts the caller's authority to mint it.",
            "Agent session creation pins company/Agent from its authenticated actor and prefers actor runId, a sound confused-deputy defense. Board callers can also supply issue/project overrides; service consistency checks reduce spoofing but do not replace authorization to impersonate the Agent run."
          ]
        },
        {
          "start": 431,
          "end": 830,
          "readAt": "2026-08-18",
          "notes": [
            "Critical companion authority gap: board session revocation also requires only company access, so a viewer can revoke any known company gateway session. Agent revocation is correctly constrained to its own Agent/run scope.",
            "Ordinary approve/decline routes reject viewers but grant every other active board membership decision authority. They have no dedicated approval permission, risk/data/action scope, accountable-human assignment, separation of duties or ResponsibilityContract check.",
            "The direct tools/call route validates only a nonempty Tool string. parameters, timeoutMs, approvedActionRequestId and idempotencyKey lack route schemas and explicit size/format bounds; type filtering silently discards malformed optional fields rather than returning stable validation codes.",
            "Runtime listing/control and audit reading use dedicated tools:manage_runtime and tools:view_audit permissions, showing the codebase already has a stronger permission pattern that session mint/revoke and approvals do not apply.",
            "Audit pagination is deterministic and tenant-scoped, with bounded page size and server-side enrichment. Search first loads every matching Agent/application/connection id and expands ids into OR clauses, so a broad term in a large tenant can create unbounded memory, SQL size and parameter work.",
            "Unknown outcome query values silently disable outcome filtering instead of returning validation failure. Search length is unbounded, while app/Agent UUID, window and cursor receive validation.",
            "The API returns raw activity rows and details alongside enriched names to users with audit permission; field-level sensitivity classification/redaction depends entirely on upstream writers and is not re-applied at this read boundary.",
            "Across the route, several errors remain English-only and response shapes vary between MCP, gateway, HttpError and ad hoc validation paths, so independent Web/mobile clients cannot rely on one stable structured error contract."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-830 exactly once against the pinned SHA-256.",
        "Verify protocol and management authentication, tenant hiding, approval authority, input bounds, actor attribution and stable error contracts against the service findings.",
        "Map the already-read Tool Gateway acceptance tests and route history before a canonical decision."
      ]
    },
    {
      "id": "application:server/services/tool-runtime-supervisor",
      "path": "server/src/services/tool-runtime-supervisor.ts",
      "lineCount": 889,
      "sha256": "2a52046b6e61310a3a1d885715ae4f4a11cfff26f6fa435c17fbac3ba433f38d",
      "linkedEvidence": {
        "history": [
          "cfa5e070 feat(mcp) [split 3/8]: add tool access policy core (#9558)"
        ],
        "tests": "Runtime-supervisor scenarios are embedded in the already-read server/src/__tests__/tool-gateway.test.ts:3201-3941."
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [
        {
          "start": 1,
          "end": 460,
          "readAt": "2026-08-18",
          "notes": [
            "Despite its name, this supervisor models a simulated Paperclip local-stdio runtime slot: providerRef is synthetic, process.spawnedPid is null and memory limits are metadata only. Actual subprocess execution/lifecycle is owned elsewhere, so this is an operational projection/state coordinator rather than a process supervisor.",
            "Authenticated public deployments fail closed without an explicitly trusted local runtime host, a sound managed-cloud versus trusted execution-node boundary.",
            "Slot logs are entry/byte bounded and apply basic token redaction, but regex redaction is not a complete Secret/DLP boundary and its recursive/structured log behavior must be reviewed in the remaining range.",
            "Idle cleanup loads every slot globally when no company is provided and sequentially scans/updates it. Capacity checks call this global cleanup on the request path, then load every active row and count in memory, which is unbounded and unsuitable for large/multi-region deployments.",
            "Idle expiry updates by id only after a stale read, without status/deadline/version predicate. A concurrent call can reactivate or extend a slot and still be overwritten as stopped by the cleanup worker.",
            "Capacity is a count-then-start check with no transaction, advisory lock or database constraint. Concurrent starts across service instances can both pass and exceed company/host limits.",
            "Restart counters, backoff and storm windows live in mutable JSON metadata and are computed by read-modify-write without CAS, allowing concurrent restarts to lose increments or bypass suppression.",
            "Runtime state changes, dedicated audit and general activity are sequential writes without a transaction/outbox. A slot can start or stop while its evidence write fails, and retries may duplicate transitions.",
            "Audit actors are either the Agent or generic system; no accountable human, execution-node identity/attestation or ResponsibilityContract is bound to runtime authority and lifecycle events."
          ]
        },
        {
          "start": 461,
          "end": 889,
          "readAt": "2026-08-18",
          "notes": [
            "getOrCreateSlot performs select-then-insert without conflict recovery. Even if the database uniquely constrains company/slotKey, concurrent creation can return an avoidable error rather than re-read the winner; capacity is still not reserved atomically.",
            "Caller-provided metadata is spread after fixture, hostId, counters and logs during slot creation, so callers can override supervisor-owned host/accounting fields. A forged hostId can make activeRows undercount host capacity until a later lifecycle write repairs metadata.",
            "ensureRunningSlot does not acquire an exclusive lease. Multiple calls can concurrently mark and use the same connection slot, execute callbacks against the same conceptual stdio process and overwrite metadata/idle state; no queue, fencing token or lease owner protects protocol serialization.",
            "Stuck recovery and explicit stop/restart update by id only after a stale read. They can mark an actively used slot failed/stopped or restart it while another call is executing. startSlot itself does not reserve capacity, and explicit restart bypasses a fresh capacity check.",
            "Critical confidentiality defect: execution errors are persisted in lastError and dedicated audit details after only character truncation, not redactLogLine or structured Secret filtering. Provider/process errors containing bearer tokens, API keys or customer data can enter durable database/audit records.",
            "useFixtureSlot has a different failure path from useConnectionSlot: it marks failure but emits no runtime_failed audit, producing inconsistent evidence for the bundled fixture path.",
            "Log append is locally entry/byte bounded and token-redacted, but metadata is persisted only in later idle/failure writes and concurrent handles mutate independent snapshots; log records can be lost by last-writer-wins updates.",
            "listSlots remains unpaginated and cleanup-coupled. It returns only active states, hiding failed/stopped operational history from this projection even though rows remain stored.",
            "The acceptance suite covers sequential lazy reuse, idle expiry, capacity, public-host gating, restart suppression and stuck recovery, but not any of these multi-instance races, metadata ownership, real process enforcement or error-Secret leakage."
          ]
        }
      ],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-889 exactly once against the pinned SHA-256.",
        "Trace slot claims, tenant/host capacity, process identity, timeout/idle/restart/stuck recovery, audit evidence and deployment exposure controls.",
        "Map already-read acceptance coverage and history before a canonical decision."
      ]
    },
    {
      "id": "application:server/services/plugin-tool-dispatcher",
      "path": "server/src/services/plugin-tool-dispatcher.ts",
      "lineCount": 455,
      "sha256": "75de7da75c6bc4337c69c7c3d424d1bc009ee4ea7b8711febea9e9ca6b0ff53f",
      "linkedEvidence": { "history": ["62863126 fix(plugin-tool-dispatcher): propagate pluginDbId so worker.isRunning resolves (#5671)", "70b1a910 Improve CLI API parity coverage (#6626)", "73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 455,
        "readAt": "2026-08-18",
        "notes": [
          "The dispatcher is a thin singleton facade over a global in-memory registry and worker manager. It contains no company, organization, permission, data-contract or accountable-human filtering; Company OS cannot treat its Agent-facing list as an authorized Connector capability projection without the separate Gateway policy layer.",
          "Plugin identity has two forms: lifecycle payload/database UUID and manifest pluginKey. A prior defect required propagating pluginDbId because workers are keyed by UUID, while registration/unregistration is keyed by pluginKey; the public unregisterPluginTools parameter is documented as pluginId but passed directly as a registry key, preserving semantic ambiguity.",
          "Enabled lifecycle registration is fire-and-forget. If a plugin is disabled/unloaded before the asynchronous database lookup completes, the late completion can register its Tools after unregistration, exposing disabled capabilities until another reconciliation.",
          "initialize lists every ready plugin and registers sequentially without pagination, snapshot/version or failure isolation; one invalid manifest/registration error can abort startup after a partially populated in-memory registry.",
          "teardown intentionally retains registrations for in-flight calls but has no draining generation or rejection of new calls. After listener removal, callers retaining the dispatcher can continue discovering/executing stale Tools, and reinitialization depends on registry overwrite behavior.",
          "Lifecycle failures are only logged and not represented as durable health/reconciliation state, so operators cannot distinguish a ready plugin whose Tools failed registration from a healthy one.",
          "The Agent descriptor exposes only schema and plugin DB id; it omits declared risk, version, capability proof, health, data categories, approval/cancellation/idempotency support and Connector attestation required by Company OS.",
          "Execution delegates entirely to the registry and logs identifiers/result flags only. Timeout, cancellation, isolation, idempotency and evidence are absent here and must not be assumed from this facade."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-455 exactly once and trace plugin discovery, identity, run binding, worker lifecycle, input/output trust and failure semantics."]
    },
    {
      "id": "application:server/services/plugin-tool-registry",
      "path": "server/src/services/plugin-tool-registry.ts",
      "lineCount": 461,
      "sha256": "b8e807fe37b002acd786d2b15d2fa9fb88cd7e2f9e4cd023bfa5a5c35ed334b6",
      "linkedEvidence": { "history": ["62863126 fix(plugin-tool-dispatcher): propagate pluginDbId so worker.isRunning resolves (#5671)", "73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 461,
        "readAt": "2026-08-18",
        "notes": [
          "The registry is a simple global in-memory two-map index with useful deterministic namespacing and O(1) lookup, but it has no tenant, plugin-version, health generation, permission or deployment scope.",
          "The module documentation claims parameter validation, yet executeTool never validates parameters against parametersSchema; it forwards arbitrary input and caller-supplied ToolRunContext directly to the worker. Correctness and authorization depend entirely on upstream callers and plugin code.",
          "Manifest declarations are assumed prevalidated. Registration removes the old plugin set before adding the new one, with no staged validation/swap; malformed declarations, duplicate names or unexpected runtime errors can leave a partial/reduced registry.",
          "Duplicate Tool names within one manifest overwrite byNamespace while the Set remains one entry, but logs/counts report the manifest array length. There is no collision/error signal or version hash for the effective capability set.",
          "listTools/getTool return live RegisteredTool objects and nested schema references. Callers can mutate parametersSchema or entry fields and corrupt subsequent discovery/execution because no clone/freeze boundary exists.",
          "Worker availability is a time-of-check before workerManager.call with no lease/generation binding. A restart between the two can route against a different worker generation or fail without an idempotency/cancellation contract.",
          "Execution has no timeout, cancellation, retry/idempotency key, capability attestation, structured stable error, result validation, evidence receipt or data-egress contract at this boundary.",
          "The run context is forwarded as trusted routing data without proving company/Agent/run/project ownership. Gateway callers currently validate it, but the registry is public through an escape hatch and cannot safely serve as a canonical Company OS Connector SDK host.",
          "Tool descriptions/schemas originate from plugin manifests and flow toward Agent prompts without content trust classification or injection review here."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-461 exactly once and trace manifest validation, namespacing, ownership, collisions, registration lifecycle and concurrency."]
    },
    {
      "id": "application:server/__tests__/plugin-tool-dispatcher-pluginDbId",
      "path": "server/src/__tests__/plugin-tool-dispatcher-pluginDbId.test.ts",
      "lineCount": 313,
      "sha256": "b67d5650f9ad726916d04d4f6446b6d453cf818bc72e5f4014fd9f6224f2723f",
      "linkedEvidence": { "history": ["62863126 fix(plugin-tool-dispatcher): propagate pluginDbId so worker.isRunning resolves (#5671)"], "focusedTest": "2026-08-18: direct Vitest run passed all 6 tests in 4ms; coverage is limited to UUID routing and does not prove lifecycle-event behavior." },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 313,
        "readAt": "2026-08-18",
        "notes": [
          "All six tests narrowly verify the plugin database UUID is retained for worker isRunning/call routing, including empty-id rejection, idempotent registration and a simulated worker bounce.",
          "The file's claimed lifecycle-event coverage is misleading: the plugin.enabled test never initializes the dispatcher, emits an event or uses registerFromDb. It directly invokes dispatcher.getRegistry().registerPlugin and calls that equivalent, so it cannot catch the fire-and-forget enable/disable race or database lookup failures.",
          "createDbStub is defined but unused, further confirming initialize/registerFromDb is not exercised.",
          "The worker manager is a pure mock; worker restart generation, in-flight calls, timeout, cancellation, RPC errors and process/container isolation are not tested.",
          "No test validates parameters against JSON Schema, manifest duplicate/collision handling, live-object mutation, registration rollback, tenant/run authorization, disabled-plugin late registration, Tool/result content trust or structured error contracts.",
          "The test fixture casts both manifest and worker manager through unknown, reducing compile-time contract evidence and allowing the mocked call result to ignore the real ToolResult shape.",
          "This is a useful regression for one historical identity-plumbing bug but insufficient evidence for the dispatcher's broader lifecycle or a production-grade Company OS Connector host."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-313 exactly once and map regression coverage to dispatcher/registry identity and lifecycle invariants."]
    },
    {
      "id": "application:server/services/plugin-worker-manager",
      "path": "server/src/services/plugin-worker-manager.ts",
      "lineCount": 1929,
      "sha256": "43cce60c233eb5bf1ccbc35cab7c2c0f161780fef130f53aa52239655d209929",
      "linkedEvidence": {
        "history": [
          "6b7e0814 feat(acp): stream Daytona sandbox agent output and remove the host output poll (#11049)",
          "af6b32d8 feat(observability): add provider OTel spans (#10764)",
          "5c5366d0 fix(server): honor explicit plugin RPC timeouts (#10460)",
          "f2f168f6 fix(plugins): seed proactive company scopes before worker setup and events resolver parity (#10113)",
          "3093c5e6 fix(plugin-worker): resolve a company scope for proactive worker-to-host calls (#10103)",
          "a1835cfa Harden plugin runtime invocation scope (#6547)",
          "1bd44c8a Harden Cloudflare sandbox execution (#5967)"
        ]
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 650,
        "readAt": "2026-08-18",
        "notes": [
          "This is the real out-of-process plugin boundary: one long-lived Node child per installed plugin, shared across its configured companies, with bidirectional JSON-RPC, host callbacks, crash recovery and optional streamed execution logs.",
          "Explicit RPC timeouts bypass the ordinary 15-minute cap and may reach Node's roughly 24.8-day timer maximum. The code assumes callers provide independent inactivity/safety guards; this boundary itself cannot guarantee a bounded call or cancellation for multi-hour executions.",
          "Incoming line and per-execution log defaults are each 128 MiB, far above a conservative control-plane message budget. More importantly, readline must accumulate a line before handleLine can reject its length, so the documented pre-parse bound does not prevent memory growth from a worker that never emits a newline.",
          "A single plugin worker multiplexes companies. The presence of crossCompanyBlocked log routing acknowledges that overlapping cross-company executions share one pipe and cannot safely attribute streaming output; dropping later logs avoids misdelivery but does not isolate plugin memory/state or other host calls.",
          "The host mints invocation ids and stores exact scope/trace parent/log routes rather than trusting worker-supplied company ids, a strong capability-routing pattern that should be traced through message handling before final judgment.",
          "Proactive calls receive a pre-seeded set of company scopes outside any host-issued invocation. This fixes setup-time workflows but creates durable ambient multi-company authority at worker level, without a task, accountable human, purpose/data contract or per-call capability shown here.",
          "WorkerStartOptions accepts resolved config, arbitrary environment variables and execArgv; the remaining spawn path must prove allowlisting, Secret isolation and prohibition of dangerous Node flags.",
          "Crash/backoff state is process-local and no durable worker generation/attestation is present in these definitions. A host restart resets counters and cannot by itself prove which worker generation produced an effect.",
          "The comments promise process isolation, but a Node child alone is not a sandbox: filesystem, network, host credentials and OS resources depend on the later spawn configuration and deployment wrapper."
        ]
      }, {
        "start": 651,
        "end": 1300,
        "readAt": "2026-08-18",
        "notes": [
          "Host-issued invocation scope is checked for worker-to-host requests, and proactive resolution only admits an explicitly referenced company in a configured set. This is a useful narrow capability pattern, though the proactive set remains ambient multi-company authority.",
          "Critical stream authorization gap: a stream notification without an invocation id, with no matching proactive scope, returns an empty context when no other invocation is active. The stream handler rejects only invalidInvocationScope or an explicit scope mismatch, so an idle worker can send streams.open/emit/close for an arbitrary companyId and reach onStreamNotification.",
          "Open stream channels are keyed only by channel, not company/generation. Same-name channels across companies overwrite each other, and crash cleanup can synthesize a close using worker-supplied company attribution.",
          "Cross-company execute-log routing deliberately fails closed once overlapping companies exist, but it uses a global latch that permanently drops live logs for every affected route. It protects delivery integrity at the cost of incomplete evidence and confirms the shared-worker isolation limitation.",
          "Plugin log notifications spread arbitrary worker-controlled metadata and message into the host logger with no field allowlist, size bound below the very large line cap, Secret redaction or prompt/data classification. A plugin can inject sensitive values and high-cardinality fields into production logs.",
          "Parse errors log the first 200 raw characters of an untrusted worker line, and stderr is logged verbatim plus retained in failure messages. Both paths can persist Secrets or customer data emitted by a plugin.",
          "Host-handler exception messages are logged and returned verbatim to the worker, potentially disclosing internal identifiers, database/provider details or Secret-bearing error text across the trust boundary.",
          "The child environment avoids wholesale process.env inheritance and overwrites key host-controlled fields after options.env, which is good. It still exposes host PATH/NODE_PATH and accepts arbitrary execArgv, with no local enforcement of an allowlist, working directory, uid/gid, filesystem/network isolation or OS resource limits.",
          "Node fork also opens an IPC channel in addition to the documented stdio JSON-RPC path; no message handler is shown here, but the extra channel expands the process interface without an expressed protocol need.",
          "Crash tracking and restart state are memory-only. Pending calls receive worker-unavailable responses, but no durable execution receipt distinguishes a request that was processed before the crash from one never processed, so automatic caller retry can duplicate plugin effects."
        ]
      }, {
        "start": 1301,
        "end": 1929,
        "readAt": "2026-08-18",
        "notes": [
          "Critical crash-loop defect: consecutiveCrashes is reset to zero immediately after every successful initialize. A worker that initializes successfully then crashes seconds later always restarts with count one, so exponential backoff and the ten-consecutive-crash cutoff never escalate for the common post-start crash loop.",
          "RPC timeout only rejects the host promise and removes invocation/log routes; it sends no cancellation to the worker. A timed-out execute can continue mutating external systems, and a caller retry can duplicate the side effect.",
          "The shutdown sequence races shutdown response against exit, but if the worker responds before exiting it waits only an additional 500ms before SIGTERM, not the documented ten-second drain. A cooperative worker acknowledging shutdown before finishing cleanup can be killed prematurely.",
          "Initialization failure kills the child, marks the handle crashed and leaves it in the manager map. Auto-restart is suppressed by intentionalStop, while a later startWorker rejects any existing non-stopped handle; recovery requires an explicit stop/remove path rather than a normal retry.",
          "startInternal sends full manifest, resolved config and database namespace to the child. The manager provides no field-level Secret minimization, per-company config partition or proof that a shared worker cannot retain one tenant's config/data for another tenant's invocation.",
          "Manager startup locking prevents duplicate in-process spawns for one plugin, but it is memory-local and does not coordinate multiple server processes/hosts. There is no durable worker lease, generation or owner fencing.",
          "stopWorker/stopAll can race an in-flight startup and do not coordinate through startupLocks; initialization and stop may interleave, producing rejected startup promises or stale lifecycle notifications.",
          "supportedMethods reported by initialize are stored for diagnostics but not enforced before calls, so capability/version mismatch is discovered only through worker response.",
          "Worker error/stderr messages are embedded in host-thrown errors and manager logs without Secret redaction. Together with earlier raw plugin log paths, the worker boundary is not safe for production credentials or customer content.",
          "The strongest reusable pattern is the host-minted invocation capability with company scope and timeout cleanup. Company OS still needs per-installation/per-company isolation, short-lived runtime attestation, cancellation/idempotency/effect receipts, durable leases and a Secret-safe structured protocol."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": [
        "Read lines 1-1929 exactly once against the pinned SHA-256.",
        "Trace worker identity, tenant/run invocation scope, local/remote isolation, Secrets/environment, RPC timeouts/cancellation, host callbacks, lifecycle/concurrency, telemetry and cleanup.",
        "Read the complete 1056-line first-party suite and its fixtures, run focused tests, then issue a canonical decision."
      ]
    },
    {
      "id": "application:server/__tests__/plugin-worker-manager",
      "path": "server/src/__tests__/plugin-worker-manager.test.ts",
      "lineCount": 1056,
      "sha256": "27bbd53a548a523f18a7cfe03c447268b3f8fcea2d78ce5d76e5e30174568863",
      "linkedEvidence": {
        "history": [
          "6b7e0814 feat(acp): stream Daytona sandbox agent output and remove the host output poll (#11049)",
          "5c5366d0 fix(server): honor explicit plugin RPC timeouts (#10460)",
          "f2f168f6 fix(plugins): seed proactive company scopes before worker setup (#10113)",
          "a1835cfa Harden plugin runtime invocation scope (#6547)",
          "b24c6909 Harden remote sandbox runtime probes, timeouts, and installs (#5685)"
        ],
        "fixtures": [
          "server/src/__tests__/fixtures/plugin-worker-delayed.cjs",
          "server/src/__tests__/fixtures/plugin-worker-execute-log.cjs",
          "server/src/__tests__/fixtures/plugin-worker-invocation-scope.cjs",
          "server/src/__tests__/fixtures/plugin-worker-terminated.cjs"
        ],
        "focusedTest": "2026-08-18: direct Vitest run passed all 37 tests in 890ms using real forked fixture workers."
      },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 550,
        "readAt": "2026-08-18",
        "notes": [
          "The suite uses real forked Node worker fixtures for RPC timing and invocation-scope flows, stronger evidence than pure mocks for message routing.",
          "Timeout tests prove default and explicit host promise timing only; they do not assert a timed-out worker operation is cancelled or that no delayed side effect occurs, leaving the source-level duplicate-effect risk intact.",
          "Seven pure timeout tests explicitly bless caller overrides up to Node's timer maximum but do not verify the assumed independent inactivity/safety guard at any call site.",
          "Stderr tests intentionally append raw worker stderr to thrown failure messages and only enforce truncation. They contain no Secret-redaction assertion and therefore codify the confidentiality defect rather than preventing it.",
          "Invocation tests strongly cover host-minted company scope, omitted/unknown ids during an active invocation and cross-company config/Secret denial through SDK host handlers.",
          "The active-invocation tests do not cover the idle/no-invocation Stream notification path where the source returns an empty context and forwards arbitrary company ids.",
          "Fixtures and handlers frequently cast through unknown/never, appropriate for adversarial protocol cases but weaker as type-compatibility proof."
        ]
      }, {
        "start": 551,
        "end": 1056,
        "readAt": "2026-08-18",
        "notes": [
          "Proactive host-call tests comprehensively cover configured company admission, outside-set denial, revocation, company-scoped state and setup-time events.subscribe filter parity using real workers.",
          "There are no tests for streams.open/emit/close notifications. The critical idle/no-invocation stream authorization gap and channel collision found in source therefore remain completely uncovered.",
          "Execute-log tests cover honest ordered delivery, missing/unknown ids, invalid payloads, same-company concurrency, cross-company forged ids, per-call output caps and no-sink behavior.",
          "The cross-company defense cannot prove exact invocation origin within one company. A malicious shared worker can forge another active same-company invocation id and inject output into its sink; the suite tests honest same-company ids but not this forgery case.",
          "The oversized-line fixture emits a complete newline-terminated JSON document. It proves rejection before JSON parsing after readline has produced the line, but not memory safety against a worker streaming an arbitrarily long line without a newline.",
          "No test exercises crash recovery/backoff escalation, the reset-on-initialize defect, initialization-failure recovery, startup/stop races, shutdown drain timing, auto-restart generation, durable effect ambiguity, environment/execArgv isolation or raw log/stderr Secret leakage.",
          "All four fixtures are small direct NDJSON workers and faithfully exercise the intended paths, but none simulates a hostile filesystem/network process, resource exhaustion, log metadata injection or delayed side effect after host timeout.",
          "The suite is strong for company-scope regression and streaming-route mechanics, but materially incomplete for lifecycle safety and production isolation."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-1056 plus all four listed fixtures exactly once and map claimed versus actual worker security/lifecycle coverage."]
    },
    {
      "id": "application:server/services/plugin-secrets-handler",
      "path": "server/src/services/plugin-secrets-handler.ts",
      "lineCount": 285,
      "sha256": "aaa839fd100515198cd9365fc027ca12e327f4c183bf8787ac6d43f7be4d156e",
      "linkedEvidence": { "history": ["1de0a3bb feat(mcp) [split 2/8]: add governed access contracts (#9557)", "778e775c Add secrets provider vaults and remote import (#5429)", "5bd0f578 Generalize sandbox provider core for plugin-only providers (#4449)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 285,
        "readAt": "2026-08-18",
        "notes": [
          "The handler correctly requires a structured secret_ref, explicit company id, exact company/plugin/Secret/version binding and an unambiguous config path before delegating plaintext resolution to the central Secret service.",
          "It does not verify plugin status, installation/company assignment, manifest capability or that the current plugin config still contains this exact reference. A plugin that knows any uniquely bound Secret id can resolve it without configPath even if the calling operation did not originate from that field.",
          "Actor type/id, issueId and heartbeatRunId are accepted from resolve params and forwarded into access evidence without independent validation here. If the Host API does not overwrite them, a plugin can forge human/Agent/run attribution for Secret access.",
          "The in-memory rate limiter is per handler/process, not distributed or durable, and retains an unbounded map of company/plugin keys. Failed attempts consume the same quota, enabling one plugin loop to block its own legitimate resolutions while leaving no denial/rate audit.",
          "Binding lookup fetches every matching row then filters version in memory and checks ambiguity; a database-side exact predicate/limit would be safer and cheaper.",
          "Secret-ref extraction recursively walks arbitrary config without cycle, depth, node-count or path-length bounds. Cyclic/programmatic objects can overflow/fail, and large configs can consume unbounded CPU/memory.",
          "Structured secret_ref objects are accepted anywhere in config even when schema does not declare a Secret field. Schema limits only legacy UUID detection, so the schema is not an allowlist for Secret-bearing paths.",
          "Dot-path construction is ambiguous for object keys containing dots and array-like segments, risking mismatch between extracted binding paths and later config reads unless upstream schemas forbid such keys.",
          "invalidSecretRef serializes the complete rejected object into an error message. Malformed inputs containing plaintext credentials can be propagated into worker/host logs through the already-audited raw error paths.",
          "Successful resolution returns plaintext directly to a long-lived shared plugin worker with no purpose, destination, TTL, one-time capability, data-egress rule or zeroization. Company OS needs a narrower credential projection/execution-node contract."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-285 exactly once and trace company scope, plugin binding, provider resolution, plaintext lifetime, audit, cache, errors and authorization assumptions."]
    },
    {
      "id": "application:server/__tests__/plugin-secrets-handler",
      "path": "server/src/__tests__/plugin-secrets-handler.test.ts",
      "lineCount": 213,
      "sha256": "c086b85135701f7db326af1864ec7ba9d5bb2a0739f3cf2bd701fa33ae66b849",
      "linkedEvidence": { "history": ["1de0a3bb feat(mcp) [split 2/8]: add governed access contracts (#9557)", "778e775c Add secrets provider vaults and remote import (#5429)"], "focusedTest": "2026-08-18: direct Vitest run passed 4 pure tests and skipped 2 embedded-PostgreSQL integration tests; no shared-vault runtime pass is claimed." },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{
        "start": 1,
        "end": 213,
        "readAt": "2026-08-18",
        "notes": [
          "Two pure guards verify companyId and structured secret_ref are required before database access, and schema extraction ignores ordinary UUID fields while rejecting legacy UUID strings in secret-ref fields.",
          "Two embedded-PostgreSQL cases verify a company/plugin binding resolves through the shared vault with a success access event, and a cross-company Secret is rejected before provider access.",
          "The successful plugin manifest declares no capabilities, yet direct handler resolution succeeds. The suite therefore proves binding enforcement but not that secrets.read-ref or another capability is required at this boundary; it assumes an upstream SDK gate.",
          "Cross-company denial intentionally leaves zero Secret access events, making unauthorized attempts invisible to this audit stream.",
          "Missing cases include disabled/uninstalled plugin, stale/deleted binding, configPath mismatch, fixed versus latest version, rotation/revocation races, provider failure, audit failure after plaintext release, concurrent resolution, remote vaults, response/log leakage and plaintext lifetime.",
          "No test binds Secret use to a run, task, accountable human, approval, purpose/data contract or external destination."
        ]
      }],
      "sourceReadComplete": true,
      "nextLine": null,
      "completionRequirements": ["Read lines 1-213 exactly once, run focused tests and map positive/negative Secret scope and leakage coverage."]
    },
    {
      "id": "application:server/services/plugin-lifecycle",
      "path": "server/src/services/plugin-lifecycle.ts",
      "lineCount": 849,
      "sha256": "950782c7c35e7eb33b9929194990bb353a0852e876968208ce5920923525c80e",
      "linkedEvidence": { "history": ["c91a0623 Runtime control-plane fixes (#6380)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 849 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The explicit state graph and loader/worker abstraction are useful reference patterns, but lifecycle coordination is not atomic: registry state, worker/file side effects and in-process events are updated in separate steps with no transaction, compensation log or durable outbox.",
        "load/enable transition the database to ready before activation; activation failure leaves persisted ready state and an already-emitted status_changed event without rollback or automatic error transition.",
        "disable/unload/error paths can record an offline lifecycle state after fallback worker shutdown failed because stopWorkerIfRunning deliberately swallows the failure; the worker may remain active despite disabled, error or uninstalled state.",
        "Transitions use read-then-update without compare-and-swap, version fencing or per-plugin serialization, so concurrent enable/disable/unload/upgrade operations can validate against stale state and interleave destructive side effects.",
        "EventEmitter notifications are process-local, non-durable and synchronous after persistence; a throwing listener can surface failure after state was committed, while crashed processes lose integration events entirely.",
        "Upgrade approval compares only newly added flat capability strings and is not bound here to a package or manifest digest, exact permission parameters, approver, or evidence. Re-upgrading from upgrade_pending with additional capabilities attempts the invalid upgrade_pending-to-upgrade_pending transition.",
        "Unload cleans runtime and install artifacts before registry deletion, without staged rollback. Hard-delete and data-retention ownership are represented by a boolean removeData rather than an explicit retention/export/tombstone contract.",
        "Lifecycle operations carry no actor, tenant/company, request id, reason code, approval reference or durable audit evidence; logs and free-form errors are operational telemetry rather than Company OS responsibility records.",
        "restartWorker correctly reloads manifests and idempotent migrations when runtime services exist, but a failed unload/load can leave the ready record with no worker and has only a narrow regression test."
      ],
      "completionRequirements": ["Read lines 1-849 exactly once and trace install/enable/disable/unload/restart, state transitions, worker/tool cleanup, data retention/removal, concurrency, rollback, audit and upgrade ownership."]
    },
    {
      "id": "application:server/__tests__/plugin-lifecycle-restart",
      "path": "server/src/__tests__/plugin-lifecycle-restart.test.ts",
      "lineCount": 129,
      "sha256": "556bd6bdd3c5bfcb422dcf0c58231af04906fc9b3f58807b779a2cbac72f959d",
      "linkedEvidence": { "history": ["c91a0623 Runtime control-plane fixes (#6380)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 129 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Regression scope is intentionally narrow: runtime-capable loaders must unload and reload so new plugin migrations run; loader-less setups only bounce the worker handle.",
        "The test verifies call selection and lifecycle event behavior, but not migration application, registry status transitions, reload failure recovery, concurrency, or cleanup after a partial restart."
      ],
      "completionRequirements": ["Read lines 1-129 exactly once, run focused tests and map restart regression coverage against the complete lifecycle implementation."]
    },
    {
      "id": "application:server/services/plugin-manifest-validator",
      "path": "server/src/services/plugin-manifest-validator.ts",
      "lineCount": 163,
      "sha256": "e8367009fd33e566d97b57c68acc9cb6aaa078c75dced85cfc1aef966e363726",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 163 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "This is a thin Zod wrapper; the shared pluginManifestV1Schema, not this service, is the true authority for fields, capability vocabulary and most compatibility semantics.",
        "SUPPORTED_VERSIONS is exposed but not independently checked here, so compatibility correctness depends entirely on the imported schema and the host keeping the two definitions aligned.",
        "Failure details preserve paths but provide only human-readable messages, with no stable machine error code, schema keyword, rejected version metadata or localization-neutral parameters.",
        "Validation establishes shape only: no package digest/signature/provenance, entrypoint/path containment, migration immutability, capability-policy approval or cross-field runtime compatibility is owned by this boundary.",
        "There is no dedicated focused validator test in the repository path set; indirect coverage cannot demonstrate unknown-field policy, all invalid capability/version cases, hostile input limits or backward-compatibility fixtures."
      ],
      "completionRequirements": ["Read lines 1-163 exactly once and assess schema authority, semantic validation, compatibility/versioning, path/package safety, capability governance, diagnostics and test ownership."]
    },
    {
      "id": "application:server/services/plugin-loader",
      "path": "server/src/services/plugin-loader.ts",
      "lineCount": 2540,
      "sha256": "c260774c8f32520f52ce2eeb25e867db70c39c27adb5209de1d367e7c6e4c977",
      "linkedEvidence": { "history": ["78f8c6c3 Recover managed bundled plugin workers on demand (#10429)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 2540 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-650 establish that PluginLoader combines discovery, package installation/build, manifest/version/capability checks, migration, runtime activation and shutdown in one large I/O service.",
        "buildPluginWorkerEnv forwards raw model-provider API keys and Kubernetes discovery variables to every plugin declaring environment.drivers.register; a single coarse capability becomes authority to receive multiple vendors' durable host credentials.",
        "The service separates optional runtime services from discovery/install mode and exposes explicit load/unload results, but uses Paperclip records/manifests throughout and leaves lifecycle persistence coordination to callers.",
        "Installation comments promise compatibility and capability validation, while provenance, signatures, reproducible package resolution and accountable approval are not represented in the public types seen so far.",
        "Lines 651-1950 show declared manifest/worker/UI paths are path.resolve results without package-root containment; an entrypoint may escape the package directory if later consumers do not recheck.",
        "npm installs disable lifecycle scripts, but the loader dynamically imports the unvalidated manifest module, which executes publisher-controlled JavaScript in the host process before Zod validation; discovery alone can trigger that code.",
        "npm resolution mutates a shared install directory and package.json/lock state with no digest pin, staging directory or per-package lock. Failed validation leaves installed artifacts and concurrent installs/upgrades can interfere.",
        "Install plus database migration is transactionally grouped, a strong pattern, but package download/import/build occurs outside it and filesystem artifacts have no compensation.",
        "Upgrade fetches or overwrites package files before capability escalation is rejected, then throws instead of persisting a digest-bound approval request; code on disk can therefore diverge from the old database manifest/runtime.",
        "Discovery walks configured directories and imports matching manifests; names and package.json metadata are only conventions, not trust or provenance controls.",
        "Lines 1951-2540 show activation refreshes a changed on-disk manifest and persists it after schema parsing only, bypassing fetchAndValidate's API-version, capability-consistency, route-conflict, minimum-host-version and capability-escalation checks.",
        "Worker entrypoint containment uses string startsWith rather than path.relative/realpath and later permits any absolute manifest path that exists, so a schema-valid manifest can select code outside its package; symlink containment is not addressed.",
        "Proactive company authority is inferred from the presence of stored plugin configs. A stale or broadly provisioned config row grants background host-call scope without an explicit active authorization contract or expiry.",
        "Activation is a sequential saga with best-effort teardown rather than durable desired/observed reconciliation. Scheduler and worker cleanup failures are swallowed, process-local registrations are not transactional, and no fence prevents concurrent activation.",
        "loadAll activates all ready plugins concurrently; loadSingle's installed path recursively delegates through lifecycle and returns synthetic registration counts rather than the actual activation result.",
        "Raw operational errors and config-delivery failures are logged without stable external codes or Responsibility evidence; lifecycle error marking can itself fail after partial runtime side effects."
      ],
      "completionRequirements": ["Read lines 1-2540 exactly once and trace discovery, installation, source trust, manifest/build/migration/config/runtime activation, upgrade, cleanup, failure recovery, concurrency and deployment ownership against direct tests."]
    },
    {
      "id": "application:server/__tests__/plugin-install-autobuild",
      "path": "server/src/__tests__/plugin-install-autobuild.test.ts",
      "lineCount": 402,
      "sha256": "a210896d8e5268eeef9d85030912389cde7f91e9cf4bfe429a9cae6006f09507",
      "linkedEvidence": { "history": ["3ae2c30f feat(skills): import skills from projects (#9620)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 402 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The pure cases establish that auto-build is restricted to repository-bundled paths, can be disabled, and invokes pnpm install/build with fixed arguments and 120-second timeouts for standalone bundled plugins.",
        "The route cases cover successful artifact creation and manual-command errors, but depend on embedded PostgreSQL and execute fixture-controlled package scripts on the audit host.",
        "The suite does not test hostile package.json scripts, symlink/path races, package-manager lockfile/reproducibility, integrity/signature/provenance, network isolation, output limits, timeout cleanup or partial build/install rollback.",
        "Admin actor setup is route scaffolding only; no accountable approval or digest-bound authorization is asserted before executing build/postinstall code."
      ],
      "completionRequirements": ["Read lines 1-402 exactly once, run focused tests and map build/install source trust, package-manager execution, artifact validation and failure cleanup coverage to plugin-loader."]
    },
    {
      "id": "application:server/__tests__/plugin-tenant-isolation",
      "path": "server/src/__tests__/plugin-tenant-isolation.test.ts",
      "lineCount": 528,
      "sha256": "239c741b5d5ec825a56bf59a493344e20a83f74f9fb24b97dc7bfc30fa0b2295",
      "linkedEvidence": { "history": ["05bcd3ce feat(security): plugin tables get company_id FK for tenant isolation (#5865)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 528 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The suite strongly checks company foreign keys, cascade deletion, company-scoped external-ID uniqueness including NULLS NOT DISTINCT, and registry methods carrying companyId into entities, job runs and webhook deliveries.",
        "All cases require embedded PostgreSQL; when unavailable the entire suite skips, so no tenant-isolation assertion executes locally.",
        "The tests establish storage partition keys, not authorization: service calls take caller-supplied companyId directly and no authenticated actor, company membership, accountable human, data authorization or database RLS policy is exercised.",
        "NULL companyId deliberately creates instance-wide rows for logs, entities, runs and deliveries; the suite verifies persistence and deletion behavior but not who may create, read or export that broader scope.",
        "Deletion assertions favor hard cascade and do not cover legal hold, evidence retention, export, tombstones or cross-company shared-responsibility records."
      ],
      "completionRequirements": ["Read lines 1-528 exactly once, run focused tests and map tenant ownership, cross-company denial, uniqueness, deletion and actor-context coverage to plugin-registry."]
    },
    {
      "id": "application:server/services/plugin-registry",
      "path": "server/src/services/plugin-registry.ts",
      "lineCount": 808,
      "sha256": "967069cd1da4041fcde205a5e2d24d1799727dd837ae7e96b8a421fa50ffbae0",
      "linkedEvidence": { "history": ["a7186dce fix(plugin-sdk): thread companyId through configChanged + fail-closed cross-tenant guard (LOOA-687) (#10096)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 808 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The registry is a deliberately low-level persistence facade; it consistently carries companyId for config/entity lookup/create paths highlighted by the tenant regression suite, but it has no authenticated actor or authorization policy itself.",
        "listEntities filters only by pluginId/type/externalId and cannot scope by companyId; deleteEntity, updateJobRun and updateWebhookDelivery accept only opaque row IDs, creating unsafe cross-tenant primitives if any caller exposes attacker-controlled IDs.",
        "Soft-uninstalled rows are reactivated by manifest.id while preserving plugin-scoped data and allowing packageName/packagePath replacement, with no publisher/digest continuity proof; a different package claiming the key can inherit retained state.",
        "Install order, config/settings/entity/job upserts use read-then-write without transaction, conflict retry, CAS or row lock. Concurrent install order duplicates, insert conflicts and shallow patch lost updates are possible.",
        "upsertEntity searches externalId null as an empty string but inserts the original null; repeated nullable-ID upserts can miss the existing row and collide with NULLS NOT DISTINCT uniqueness.",
        "Job runs do not verify jobId belongs to pluginId/companyId, and webhook payloads/headers, config JSON, job logs and errors have no size, secret or retention controls at this layer.",
        "List operations are partly unbounded and offset pagination is unstable under writes; lifecycle, config and operational record mutations emit no durable audit/outbox evidence."
      ],
      "completionRequirements": ["Read lines 1-808 exactly once and trace installation identity, status/config persistence, tenant scoping, uniqueness, soft/hard deletion, ordering, concurrency, audit and callers against tenant-isolation tests."]
    },
    {
      "id": "application:server/__tests__/plugin-database",
      "path": "server/src/__tests__/plugin-database.test.ts",
      "lineCount": 662,
      "sha256": "b022c28c5d438a2ebb60ab9a01033422541e1fe0070f42511677aa5980aa5aad",
      "linkedEvidence": { "history": ["4ad94d0b feat(server): kubernetes execution integration for sandbox-provider plugins (stage 2/3) (#7938)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 662 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The pure validator cases cover representative allowed namespace DDL/backfills/core reads and rejected public writes/anonymous DO blocks; environment tests explicitly confirm coarse capability-based raw provider-key passthrough.",
        "Embedded cases cover multi-file order, idempotent reapply, plugin-install transaction rollback, namespace escape rejection, applied-checksum tamper detection and a real bundled migration set.",
        "The refresh test positively requires a mutable disk manifest to add agent.tools.register, tools and different coreReadTables during activation without upgrade approval, confirming rather than mitigating Loader policy bypass.",
        "The SQL validator suite is example-based rather than grammar/adversarial: it lacks comments, dollar quoting variants, Unicode/encoding, nested CTE, search_path, function/operator, COPY, extension, lock, resource exhaustion and transaction-control bypass corpora.",
        "Database tests exercise a shared privileged connection and namespace naming, not distinct database roles, row-level tenant policies, per-company data boundaries, statement timeout/row limits or accountable audit evidence.",
        "All end-to-end migration cases depend on embedded PostgreSQL and may skip as a block on unsupported hosts."
      ],
      "completionRequirements": ["Read lines 1-662 exactly once, run focused tests and map namespace, migration validation, transactional behavior, idempotency, tamper, failure and tenant isolation coverage to plugin-database."]
    },
    {
      "id": "application:server/services/plugin-database",
      "path": "server/src/services/plugin-database.ts",
      "lineCount": 572,
      "sha256": "6ab593f7b75e8844d1113504a7db039567a7626bc7a2718314441b3ded7e313b",
      "linkedEvidence": { "history": ["eb38b226 Fix LLM Wiki package and migration validation (#6010)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 572 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Namespace derivation, identifier quoting, checksum ledger, advisory transaction lock and install-transaction integration are valuable patterns, but isolation ultimately relies on application regexes while queries execute through the shared database client.",
        "Runtime SELECT validation only extracts qualified schema.table references; unqualified FROM/JOIN names are invisible and can resolve through search_path to public tables outside the coreReadTables allowlist.",
        "SELECT permits arbitrary PostgreSQL functions and has no AST-level volatility/side-effect policy, database role restriction, statement timeout, row/byte cap or cancellation; functions such as pg_sleep and any privileges held by the server role remain reachable.",
        "Whitelisted public core-table reads do not inject or require companyId predicates, so a plugin with table permission can read every company's rows from that table. Namespace tables are likewise shared across companies with no RLS requirement.",
        "Migration regex extraction covers a limited grammar; unqualified source relations in backfills can evade reference checks once a qualified target is present, and dollar-quoted strings/search_path/functions/operators are not robustly modeled.",
        "Migration directory containment is lexical and does not realpath a symlinked directory. Files are locale-sorted without a monotonic/version rule, so a newly added earlier filename can be applied after later migrations.",
        "Queries materialize all rows and mutations have no resource budget. There is no down/rollback migration, export, schema retention/deletion or per-company lifecycle contract.",
        "Applied checksum mismatch and transaction locking are sound defensive choices, but mutable manifest coreReadTables can broaden read access during activation without approval, as the focused test explicitly demonstrates."
      ],
      "completionRequirements": ["Read lines 1-572 exactly once and trace namespace allocation, SQL validation/execution, migration ledger/checksums, transactions, concurrency, rollback, privilege isolation, upgrade and cleanup against focused tests."]
    },
    {
      "id": "application:server/__tests__/plugin-install-guard",
      "path": "server/src/__tests__/plugin-install-guard.test.ts",
      "lineCount": 161,
      "sha256": "d3c7241e0ea049d7ac805d44eacb9075e89240a38eb5e55cc24668dcd4a13242",
      "linkedEvidence": { "history": ["0ef3b320 Harden POST /plugins/install: canonicalize localPath for all instances; bundled-only floor for managed instances (#10067)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 161 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Pure tests thoroughly cover managed-config presence fail-closed behavior, realpath canonicalization, null/nonexistent/file rejection, segment-based root containment, sibling-prefix rejection and symlink escape.",
        "The contract correctly requires callers to canonicalize before root checking, but does not itself prove the route always preserves that sequencing or that package contents remain immutable after the check.",
        "No race test swaps the canonical directory or nested manifest/worker/migration symlinks between authorization and Loader reads, and no platform-specific case covers case-folding or mount replacement."
      ],
      "completionRequirements": ["Read lines 1-161 exactly once, run focused tests and map canonicalization, symlink, deployment profile, allowlist and source-mode cases to plugin-install-guard."]
    },
    {
      "id": "application:server/__tests__/plugin-install-route-security",
      "path": "server/src/__tests__/plugin-install-route-security.test.ts",
      "lineCount": 386,
      "sha256": "28918e41fe95b3e2a22cde2c5c7c7605fdc820762aa817396e8d866151982a55",
      "linkedEvidence": { "history": ["0ef3b320 Harden POST /plugins/install: canonicalize localPath for all instances; bundled-only floor for managed instances (#10067)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 386 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Route cases cover cloud rejection of npm/outside/traversal/symlink sources, allow one bundled path, validate every local path and preserve self-hosted npm behavior.",
        "Every route case requires embedded PostgreSQL and the suite mocks lifecycle/activity/live events; no case executes locally when database support is unavailable.",
        "Fixtures contain executable JavaScript manifests and validate only source-directory admission, not content digest/signature, nested entrypoint containment, post-check mutation, manifest policy, package identity continuity or worker sandboxing.",
        "The actor is always an instance-admin board principal; negative anonymous, non-admin, stale-session and confused-deputy cases belong to separate authz coverage and are not proven here.",
        "One self-hosted test intentionally attempts a real npm resolution for a random package name, creating network/time nondeterminism and testing failure wording rather than a stubbed source boundary."
      ],
      "completionRequirements": ["Read lines 1-386 exactly once, run focused tests and trace actor authorization plus install source normalization through POST /plugins/install without assuming mocked Loader safety."]
    },
    {
      "id": "application:server/services/plugin-install-guard",
      "path": "server/src/services/plugin-install-guard.ts",
      "lineCount": 108,
      "sha256": "c91cea3b6ffade1b7c167dddeda4ded84b48a19c18e981c08e6528d3f5d0e36c",
      "linkedEvidence": { "history": ["5858ccb9 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 108 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The guard correctly uses realpath and path.relative rather than lexical normalization or string prefix, and treats any managed-config variable presence as the restrictive cloud profile.",
        "It authorizes a directory path, not immutable package content. There is no open-file descriptor/content digest binding, TOCTOU defense, nested entrypoint/migration realpath check or provenance/signature validation.",
        "Cloud admission trusts every directory beneath the bundled catalog root equally; because Loader dynamically imports manifest JavaScript before validation and permits problematic entrypoints, root containment alone is not a complete RCE boundary.",
        "Self-hosted mode canonicalizes but deliberately allows arbitrary local directories and npm sources; deployment ownership must therefore require explicit operator authority and isolation rather than treating canonicalization as trust."
      ],
      "completionRequirements": ["Read lines 1-108 exactly once and assess canonical/real paths, TOCTOU, allowed roots, deployment profiles, npm sources, error contracts and ownership against both focused suites."]
    },
    {
      "id": "application:server/__tests__/plugin-environment-driver-seam",
      "path": "server/src/__tests__/plugin-environment-driver-seam.test.ts",
      "lineCount": 410,
      "sha256": "d4068355aec20278d362cfaa6d0662ec679e5cf4f9ba190fe85dc9e23bd0a79b",
      "linkedEvidence": { "history": ["8d9f9fd2 Add reusable sandbox custom images (#8794)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 410 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The suite proves schema feature-to-capability refinements and duplicate provider/driver keys for environment drivers and external object references, then cross-checks selected operation requirements.",
        "Worker SDK tests prove supported-method discovery, successful hook dispatch and METHOD_NOT_IMPLEMENTED behavior, but they dispatch caller-supplied companyId directly without authenticated host scope, approval, lease attestation or evidence assertions.",
        "Capability cases are representative rather than exhaustive; they do not enumerate every operation/feature mapping, unknown capability, wildcard/implication behavior, capability removal/upgrade or divergence between shared schema and server map.",
        "Environment execution inputs include a free command string and provider lease ID, but the test does not implement or verify execution, cancellation, timeout, idempotency, proof or secret/data egress behavior.",
        "External-object fixtures use sanitized URLs and snapshots but do not test SSRF, URL ownership, tenant mismatch, staleness, malicious output size/content or provenance."
      ],
      "completionRequirements": ["Read lines 1-410 exactly once, run focused tests and map environment-driver declaration/capability, manifest, config, runtime registration and rejection coverage to the capability validator."]
    },
    {
      "id": "application:server/services/plugin-capability-validator",
      "path": "server/src/services/plugin-capability-validator.ts",
      "lineCount": 525,
      "sha256": "9c630f8628dea259ede50fdad20250f5f93e733ed9a2e1cd6ecd4e93795c6d88",
      "linkedEvidence": { "history": ["5a00040c feat(plugin-sdk): interactions/approvals respond + attachment read capabilities for the chat gateway (#10066)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 525 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The validator has a clear fail-closed unknown-operation path and central feature/UI/launcher requirement tables, but treats manifest declaration as effective grant with no separate approved-grant record.",
        "Capabilities are unscoped strings: no company/resource set, action parameters, purpose, data class/destination, risk, expiry, accountable approver, contract/digest binding or runtime attenuation is represented.",
        "environment.drivers.register unlocks validation, probe, lease, workspace, arbitrary execution, interactive setup and template lifecycle as one unit; Loader also uses the same bit to forward all configured model-provider keys and Kubernetes discovery variables.",
        "approvals.respond, issues updates, external objects, HTTP, Secrets and database operations are similarly coarse; underlying handlers must independently enforce actor/company/object scope, so this map cannot be the canonical authorization decision.",
        "The static operation map can drift from host handlers and shared schema. Unknown operations reject in checkOperation, while getRequiredCapabilities returns an empty array for unknown, an ambiguous API that tests or callers can misread as no capability required.",
        "Install validation only checks that declared features have required strings; it does not reject unused excessive capabilities, prove operator approval or compare an immutable package digest. Mutable local manifest refresh bypasses it entirely.",
        "Forbidden responses and logs use English strings without stable denial code/parameters or durable evidence; method implementations rely on this binding and can fail if detached from the returned object."
      ],
      "completionRequirements": ["Read lines 1-525 exactly once and trace the capability vocabulary, manifest feature requirements, runtime RPC gates, wildcard/implication semantics, unknown capabilities, upgrade comparison and governance/evidence ownership against direct tests and the already-complete shared-package audit."]
    },
    {
      "id": "application:server/__tests__/plugin-access-authorization-host-services",
      "path": "server/src/__tests__/plugin-access-authorization-host-services.test.ts",
      "lineCount": 324,
      "sha256": "5dac15e7cae45ff1a4e4843c33993a0eed5c10815dc8931615b33eed877e3d3a",
      "linkedEvidence": { "history": ["6ffe9df8 fix(auth): clarify protected-agent assignment blocks (#10893)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 324 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The embedded-database suite covers cross-company grant target rejection, invite/token/default redaction, allow/deny audit filtering, persisted protected-agent policy, injected board actor non-bypass, policy sanitization and plugin-attributed activity.",
        "Every test constructs buildHostServices directly with plugin ID/key but no manifest capability set, authenticated invocation scope, approved grant or accountable human; it therefore tests service behavior beneath the principal authorization boundary, not end-to-end authorization.",
        "createInvite returns a bearer token to the plugin and updatePolicy/setGrants perform security-sensitive administration; tests do not require high-risk approval, exact action digest, expiry/purpose, plugin installation grant or destination/evidence binding.",
        "Actor objects in preview inputs are caller-supplied data. One injected-board case proves policy still blocks assignment, but does not prove actor identity cannot alter other authorization decisions.",
        "Redaction is key/value heuristic evidence, not proof that nested arrays, alternate key names, encoded secrets, logs or downstream errors cannot leak. All five cases depend on embedded PostgreSQL and may skip together."
      ],
      "completionRequirements": ["Read lines 1-324 exactly once, run focused tests and map principal/company scope, allow/deny, access contract, audit evidence and failure coverage to plugin-host-services."]
    },
    {
      "id": "application:server/services/plugin-host-services",
      "path": "server/src/services/plugin-host-services.ts",
      "lineCount": 3398,
      "sha256": "31ae28fa4b7a48e265a09d16aa449559e5e258486fc4589ad5df6382cd4d0025",
      "linkedEvidence": { "history": ["9ace548f feat(observability): rename sandbox provider spans and add run-time wrapper spans (#10999)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 3398 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-850 establish a very broad host bridge that directly composes Company, Agent, Project, Issue, Goal, Approval, Budget, Access/Authorization, Secret, HTTP, filesystem, database, managed-resource, telemetry and runtime services.",
        "ensurePluginAvailableForCompany is explicitly a no-op because plugins are instance-wide; companyId is required syntactically but no per-company installation/grant is enforced at this layer, so later invocation-context checks are critical.",
        "SSRF protection pins a DNS result and blocks common private ranges, but the denylist is incomplete for reserved/CGNAT/benchmark/multicast IPv4 and IPv6 ranges, permits URL userinfo, buffers up to 200 MB per response and has no global concurrency/egress destination contract.",
        "The DNS timeout timer is not cleared after a successful lookup. HTTP bodies and responses are materialized in memory and raw headers/body can carry credentials or sensitive enterprise data without DataAuthorization/egress evidence in the boundary seen so far.",
        "Plugin logs use a process-global buffer and swallow persistence failures, so they are lossy operational telemetry rather than durable audit evidence. Meta/message size clamps and reserved-key stripping are useful, while a 100-entry/5-second buffer has no global backpressure bound.",
        "applyWindow slices already-loaded arrays and treats absent limit as unbounded, which can preserve expensive full-table reads. Provider-span name/attribute/time/traceparent clamping is a strong trust-boundary pattern.",
        "Lines 851-1700 verify actorUserId only denotes an active non-viewer member; the plugin still chooses that user ID and can attribute comments, interactions or approval decisions to a human without proving their authenticated session, intent or exact approval.",
        "pluginAssignmentActor converts caller-supplied board userId/companyIds or agentId/companyId into an AuthorizationActor. A policy may still deny, but identity authenticity is not established by this conversion.",
        "Local-folder configuration/read/write, state scope, database SQL, entity upsert/list, events, HTTP, Secret resolution, activity, metrics/logs and telemetry are exposed on one HostServices object; capability checks occur in a different handler layer and are not structurally encoded in these methods.",
        "Entity listing directly calls the previously identified registry method with no company predicate. State/database calls also contain no tenant scope. Company IDs on events, metrics and logs are caller-supplied, and availability checking is a no-op.",
        "Activity action names and telemetry dimensions are plugin-controlled; log/meta clamps help operational safety but do not provide durable, authenticated responsibility evidence.",
        "Lines 1701-2550 mostly enforce record.companyId equality for projects/issues, but plugins remain globally available and choose the companyId, so tenant checks prevent accidental ID mismatch rather than proving the plugin was granted access to that company.",
        "issues.create passes plugin-supplied actorUserId as createdByUserId and actorResponsibleUserId with trustExplicitResponsibleUserId=true without calling requireActiveHumanMember; issues.update likewise extracts actorUserId/actorAgentId from the patch without identity validation.",
        "Even paths that call requireActiveHumanMember only prove the named user could act, not that the user initiated or approved this exact comment, interaction or wake. This permits false human attribution by an authorized plugin.",
        "Bulk wakeups are sequential and non-transactional: earlier agents can be queued before a later issue fails. Wake requests are executed as system/plugin while plugin-supplied human/agent IDs appear only in activity metadata.",
        "Subtree/orchestration projections perform breadth traversal plus multiple per-issue relation/document/approval and per-agent budget queries, with a 100-run cap but no subtree/page bound; this creates N+1 and resource-exhaustion risk.",
        "Human interaction replay checks pending state before mutation but no local transaction/fence is visible; exact idempotency and approval binding depend on lower services.",
        "Lines 2551-3398 repeat the human-attribution flaw for approval decisions: any active non-viewer member ID supplied by the plugin can approve/reject and wake the requester without session proof or exact action consent.",
        "Attachment content has an optional caller-chosen maxBytes; absent a cap it streams the entire object, buffers it, then creates a larger base64 copy. No data-purpose/destination or content-class policy is checked.",
        "Agent pause/resume/invoke, Goal mutation, membership update, invite creation/revocation, permission grant replacement and authorization-policy writes are available as high-impact primitives with company record checks but no accountable approval at this layer.",
        "setGrants accepts plugin-supplied grantedByUserId without validating that human; policy sanitization redacts suspected secret values into stored policy instead of rejecting an invalid security document.",
        "Agent session ownership uses SQL LIKE on a taskKey containing pluginKey. Underscore and percent metacharacters are not escaped, so valid plugin keys containing wildcard characters can overlap another plugin's session namespace; no pluginId column predicate is used.",
        "Session event forwarding passes full live-event payloads to the plugin, close deletes a session but does not cancel its run/subscription, and dispose is not awaitable: buffered audit-like logs can still be lost. Only sendMessage checks disposed.",
        "The 3398-line facade centralizes dozens of unrelated authorities and depends on separate string capability dispatch for safety; it is too broad to make privilege, tenancy, data egress and responsibility invariants locally reviewable."
      ],
      "completionRequirements": ["Read lines 1-3398 exactly once and trace every worker-to-host operation from capability check through authenticated invocation/company scope, database query, Secret/HTTP/data access, mutations, approval, audit, telemetry, errors, cleanup and resource limits against focused tests."]
    },
    {
      "id": "application:server/__tests__/plugin-host-services-span",
      "path": "server/src/__tests__/plugin-host-services-span.test.ts",
      "lineCount": 314,
      "sha256": "f5c65ce5a0f4484fe1f6dd82983903e1dd2f456132c2a1fb5edbff904bee7248",
      "linkedEvidence": { "history": ["9ace548f feat(observability): rename sandbox provider spans and add run-time wrapper spans (#10999)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 314 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The suite strongly covers host-boundary span name, attribute, status, time and traceparent clamping, including forbidden command/stdout/stderr/path data and capability denial/admission through real SDK handlers.",
        "Tests use mocked instrumentation and direct HostServices with an empty database stub, appropriate for this pure boundary; all supported name values and several invalid clock cases are enumerated.",
        "It does not test telemetry cardinality/rate budgets, tracer failure behavior, trace ownership across tenants/runs, or that every environment provider uses this path, but the narrow trust-boundary assertions are high quality.",
        "servicesFor instances are not disposed in tests, so the suite does not cover cleanup interactions."
      ],
      "completionRequirements": ["Read lines 1-314 exactly once, run focused tests and map capability denial, traceparent, name/attribute/status/time clamping and sensitive-data rejection to the already-read HostServices span boundary."]
    },
    {
      "id": "application:server/services/plugin-host-service-cleanup",
      "path": "server/src/services/plugin-host-service-cleanup.ts",
      "lineCount": 59,
      "sha256": "bd8ccab5c0044b1b1bfb6619346b09532b557b1c77ac009151ea836aa32965d5",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 59 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The controller centralizes disposal on lifecycle stopped/unloaded and worker crash events, retains a disposer across stop/crash for possible restart and removes it only on unload.",
        "Disposers are synchronous and unguarded: one throwing disposer aborts disposeAll before remaining plugins and before map clear; there is no async completion, timeout, retry or failure evidence.",
        "teardown only unregisters listeners and does not dispose resources. plugin.worker.restarted is accepted by the event type but intentionally ignored, with no focused tests documenting replacement semantics.",
        "The lifecycle full-loader deactivate path may not emit plugin.worker_stopped, so correctness depends on other wiring invoking cleanup or replacing the map entry; this unit alone cannot prove leak-free lifecycle integration."
      ],
      "completionRequirements": ["Read lines 1-59 exactly once and assess registration identity, duplicate replacement, disposal ordering, async completion, leak/failure behavior and lifecycle integration."]
    },
    {
      "id": "application:server/__tests__/plugin-scoped-api-routes",
      "path": "server/src/__tests__/plugin-scoped-api-routes.test.ts",
      "lineCount": 477,
      "sha256": "410b4b236771fc34865f67ff902c6c6d7378ace4c75e587e71ba92c177312297",
      "linkedEvidence": { "history": ["3c73ed26 Expand plugin host surface (#5205)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 477 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The pure route suite covers board and Agent dispatch envelopes, authorization-header stripping, response-header allowlisting, checkout owner calls, disabled/no-worker rejection and manifest core-path exclusion.",
        "One test explicitly permits a non-assignee Agent to invoke an in-progress route marked required-for-agent-in-progress without asserting checkout ownership; this weakens the route label and can expose mutation handlers to peer Agents.",
        "The board fixture is always an instance admin and resolves arbitrary companyId from query; no ordinary member, missing membership, forged company, anonymous, stale token or cross-company Agent negative is present.",
        "Worker output status/body is trusted in the fixtures and only selected headers are tested; response size/type, invalid status, timeout/cancel, duplicate/idempotent POST and error-code stability are absent.",
        "Registry, lifecycle, Issue and activity/live-event services are mocked, so tests do not prove actual actor authentication, company membership, capability approval, audit evidence or worker invocation-scope binding."
      ],
      "completionRequirements": ["Read lines 1-477 exactly once, run focused tests and trace public plugin API authentication, plugin/company scope, lifecycle status, capability, validation, response redaction and cross-tenant denial to its production routes/services."]
    },
    {
      "id": "application:server/routes/plugins",
      "path": "server/src/routes/plugins.ts",
      "lineCount": 3140,
      "sha256": "9fa9a7018892499fbd387a6b8c35e955e7e59727e70c020814c04b01c5fa19fd",
      "linkedEvidence": { "history": ["5858ccb9 feat: make in-app features cloud-aware (#10850)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 3140 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-800 show one 3140-line Express router owns instance plugin lifecycle, config/Secrets, UI, bridge, tools, jobs, webhooks, diagnostics and custom APIs, making authorization and side-effect review highly coupled.",
        "Scoped API request/response header allowlists and 1 MB request-body constant are good boundary primitives; route matching directly decodeURIComponent calls without local malformed-encoding handling.",
        "required-for-agent-in-progress checkout enforcement deliberately returns early for a peer Agent, so a route labeled checkout-protected can dispatch a mutating request from a non-assignee without any run ownership check.",
        "Company resolution can take body/query values; later call sites must bind them with assertCompanyAccess. Auth mode checks principal type only at this helper.",
        "Instance-wide plugin mutation audit resolves every company in the database and writes the same event to each, conflating global administration with company activity, scaling with tenant count and omitting future companies.",
        "Secret-ref validation checks current secret company/status but not yet exact plugin/config-path/version binding, approver, purpose or destination. Bridge scope without company requires instance admin and scoped paths use assertCompanyAccess.",
        "Bundled catalog metadata is regex-extracted from TypeScript source for display and cached; it is not a trust/provenance decision and can drift from the built manifest.",
        "Lines 801-1100 validate run, Agent, company and project foreign-key scope before tool dispatch, but the board path bypasses the Agent ToolGateway policy layer and invokes the dispatcher directly after company-access checks.",
        "Tool and route errors remain mostly free-form English strings with message-substring classification; only ToolGateway errors expose a reasonCode, so clients lack a uniform stable failure contract.",
        "Tool discovery is company/policy-filtered only for Agents when ToolGateway is configured; board callers receive dispatcher-global tools, relying on board access rather than explicit per-company plugin availability.",
        "Lines 1101-1400 restrict installation to instance admins, canonicalize local paths and impose a bundled-root allowlist in managed cloud; these are useful deployment controls, while npm/local installation failures still return raw loader messages and no package signature or immutable digest is established in this route.",
        "Bridge RPC errors have stable categorical codes, a stronger client contract than most routes, but attachPluginBridgeErrorContext copies request body, params and query into observability context without local redaction, potentially capturing plugin-defined sensitive values.",
        "Lines 1401-1700 duplicate bridge and URL-keyed endpoints. Read calls require board access while action calls begin with generic authentication and defer company authorization to an optional body companyId; absent companyId permits an instance-admin global scope, creating an implicit dual-scope contract rather than manifest-declared scope.",
        "Lines 1701-2000 show the SSE bridge correctly requires board and company access and keys subscriptions by plugin/channel/company, but does not verify ready status or a stream capability and has no replay cursor, heartbeat or bounded backpressure contract.",
        "Scoped APIs require ready/running status, manifest capability and declared route, resolve and authorize company scope, restrict JSON bodies to 1 MB, sanitize forwarded request headers and allowlist response headers. These are strong reusable boundary primitives.",
        "Scoped API response bodies are not size-limited, worker status/body schemas are trusted, errors expose raw messages, and checkout enforcement still permits peer Agents for the protected route class; mutation idempotency, timeout/cancel and proof of run ownership are absent.",
        "Lines 2001-2300 keep enable, disable, uninstall and upgrade behind instance-admin checks and emit audit/live events, but lifecycle errors and plugin lastError/log rows are returned verbatim to board users, so Secret and external-system redaction depends entirely on upstream writers.",
        "Health is a shallow registry/manifest/status/lastError projection rather than an active worker, dependency or company-configuration probe. Log queries are bounded to 500 but accept arbitrary level strings and have no cursor pagination.",
        "Configuration reads are company-scoped, while writes are restricted to instance admins even though records are company-owned; this creates an operational bottleneck and does not model delegated company administrators.",
        "Lines 2301-2600 validate configuration against manifest schema, strip the SSRF-capable devUiUrl for non-admins, bind Secret references to the company and synchronize reference metadata without returning Secret values—useful patterns for Company OS.",
        "Secret-reference synchronization occurs before config persistence without an explicit transaction, so a later upsert failure can leave reference metadata inconsistent. Worker notification failures are intentionally swallowed after persistence, yielding eventual configuration activation without a durable delivery/outbox status.",
        "Configuration testing is available to board users and forwards the candidate config to the plugin worker; worker warnings/errors become display strings, and neither this route nor the schema establishes purpose-limited Secret access or accountable-human approval.",
        "Lines 2601-2900 restrict manual job triggering to instance admins and bind job IDs to plugin IDs, but expose raw scheduler errors and provide no request idempotency or accountable approval for side-effecting jobs.",
        "The public webhook route verifies only plugin readiness, declared capability and endpoint key; signature verification is delegated to plugin code after the host has persisted the full parsed payload and every request header, including potentially authorization, cookie and signature material.",
        "Webhook delivery has no host-level tenant resolution, replay/idempotency key, rate limit, retention/redaction, retry/dead-letter or pre-persistence authenticity gate in this route. Invalid unauthenticated traffic can therefore create stored delivery rows and reach the worker.",
        "Company-scoped local-folder inspection is exposed to any board member with company access; validation accepts an arbitrary absolute path and can act as a host-filesystem existence/shape oracle unless lower services impose additional roots not visible here.",
        "Lines 2901-3140 also let any company board member persist an arbitrary validated host path as plugin settings; there is no instance-admin gate, approved root policy, human approval or explicit read-versus-write grant in this route.",
        "The dashboard aggregates process IDs, crash state, raw job errors and global webhook delivery errors for any board user. Because webhook deliveries have no companyId, this projection cannot enforce tenant isolation and can expose cross-company operational metadata.",
        "Overall route verdict: REJECT as a reusable module because of its multi-responsibility surface and inconsistent authority, tenant, evidence and error contracts; ADAPT only narrow primitives such as path canonicalization, scoped company binding, request/response header allowlists, body limits and categorical RPC errors."
      ],
      "completionRequirements": ["Read lines 1-3140 exactly once and trace every plugin management/config/UI/webhook/scoped-API route through request validation, actor/company authorization, capability/lifecycle/worker checks, package trust, Secret handling, side effects, redaction, audit/events, errors, pagination and concurrency against focused tests."]
    },
    {
      "id": "application:cli/__tests__/access-parity",
      "path": "cli/src/__tests__/access-parity.test.ts",
      "lineCount": 140,
      "sha256": "547f94db7594fe5a6c9c47a09f7a017fd7dd75df51e0bc92e49e1feb2b8c4311",
      "linkedEvidence": { "history": ["70b1a910 Improve CLI API parity coverage (#6626)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 140 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The two tests are endpoint-shape parity checks: they assert 42 CLI commands map to expected HTTP methods and URLs using a globally mocked fetch and a fixed board token.",
        "The suite does not inspect Authorization headers, payload bodies, response/error mapping, exit codes, token redaction, role enforcement, tenant denial or real server behavior, so its access-parity name overstates what it proves.",
        "High-risk operations such as instance-admin promotion, company-access mutation, database backup, invite acceptance and join claim are treated identically to reads; no confirmation, accountable-human evidence, idempotency or stable error-code behavior is tested.",
        "Useful only as a lightweight CLI/API route-contract smoke test; Company OS should generate or validate operator commands from its canonical API contract and add security/effect assertions instead of copying this suite.",
        "Focused Vitest passed 2/2 when invoked through the existing local binary; pnpm exec first attempted dependency reconciliation and aborted non-interactively before running tests, without modifying the read-only upstream worktree."
      ],
      "completionRequirements": ["Read lines 1-140 exactly once and assess CLI/API access-control parity, actor and tenant binding, negative coverage, stable error contracts and relevance to Company OS operator tooling."]
    },
    {
      "id": "application:cli/__tests__/activity-parity",
      "path": "cli/src/__tests__/activity-parity.test.ts",
      "lineCount": 53,
      "sha256": "7d98917052db9c12ec768e1e2dc3fd86ac00f23665caed461a53afc9f848c900",
      "linkedEvidence": { "history": ["70b1a910 Improve CLI API parity coverage (#6626)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 53 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The single test checks only that list, create and issue-activity CLI commands produce three expected HTTP method/URL pairs with a fixed board token and mocked fetch.",
        "It does not assert Authorization, request payload, actor attribution, company/issue ownership, pagination or time cursor semantics, server denials, stable errors, output/redaction or immutable audit properties.",
        "The terminology conflates operational activity with responsibility-grade audit evidence; nothing here proves append-only storage, causality, integrity, retention or accountable-human identity.",
        "Focused Vitest passed 1/1 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-53 exactly once and assess activity-query CLI/API parity, tenant binding, pagination/filter semantics, audit-versus-activity meaning and negative/error coverage."]
    },
    {
      "id": "application:cli/__tests__/admin-asset-skill-parity",
      "path": "cli/src/__tests__/admin-asset-skill-parity.test.ts",
      "lineCount": 177,
      "sha256": "63fabf6ec49650fd1b64d973799542d1c87c8cc2ddddc554bf629183cc6f9b9c",
      "linkedEvidence": { "history": ["70b1a910 Improve CLI API parity coverage (#6626)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 177 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four tests map 37 company, adapter, asset and skill commands to HTTP methods/URLs; one additional assertion rejects a portable export filename escaping the designated output directory.",
        "The export path containment check and encoded adapter ID case are useful boundary tests, and asset upload verifies inferred PNG MIME type, but downloaded content integrity, overwrite policy, symlink handling and size/type limits are absent.",
        "Install/reinstall/delete adapters, import/update/delete skills, company archive/import apply and model environment tests are high-effect operations tested only as route spelling with a fixed board token—no role, tenant, confirmation, provenance, signature, rollback, Secret or evidence behavior is exercised.",
        "The suite mocks every server response and does not assert most payloads, authorization headers, structured errors, output redaction or lifecycle results; it is not proof of administrative safety or portability correctness.",
        "Focused Vitest passed 5/5 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-177 exactly once and assess administrative asset/skill CLI parity, authority and tenant scope, destructive effects, payload/Secret handling, output/error coverage and Company OS relevance."]
    },
    {
      "id": "application:cli/__tests__/agent-jwt-env",
      "path": "cli/src/__tests__/agent-jwt-env.test.ts",
      "lineCount": 137,
      "sha256": "7fc67ed1c8d5f05df2e722a62919d5ceca2253de342c7c78811393c29f00c80c",
      "linkedEvidence": { "history": ["9485ffea fix(config): preserve env files during managed updates (#10980)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 137 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Despite the name, this suite covers one shared Agent JWT signing Secret stored beside CLI config and generic managed .env merging; it does not test JWT claims, issuance, audience, subject, scope, expiry, rotation or revocation.",
        "It proves explicit-config adjacency, dotenv loading, a doctor presence check, hash-value quoting, CRLF/operator-line preservation, non-overwrite of DATABASE_URL and inode preservation for no-op updates.",
        "The tests write fixture Secrets with mode 0600 but never assert permissions of newly created or rewritten files, atomicity, symlink resistance, entropy, backup/log exclusion, concurrent writers or failure recovery.",
        "Temporary directories created by tempConfigPath are not removed, so the focused suite leaves Secret-bearing fixtures under the OS temporary directory until external cleanup.",
        "Focused Vitest passed 6/6 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-137 exactly once and assess Agent JWT issuance/storage/environment lifecycle, audience/scope/expiry, rotation and revocation, file permissions, update preservation and Secret-leak coverage."]
    },
    {
      "id": "application:cli/__tests__/agent-lifecycle",
      "path": "cli/src/__tests__/agent-lifecycle.test.ts",
      "lineCount": 125,
      "sha256": "18de28457409edf3999fca01657c3dbc6b75086b013ee2b13f23dadb232c61f8",
      "linkedEvidence": { "history": ["5da382fd feat(skills): require explicit merge modes (#10978)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 125 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Two tests map 26 Agent lifecycle, permission, revision, runtime-session, skill and instruction-file commands to expected HTTP method/URL pairs using a static board token and mocked fetch.",
        "The surface recognizes useful lifecycle concepts—hire, approve, pause/resume, terminate, config revisions/rollback, runtime reset and explicit skill merge mode—but the tests assert none of their transition preconditions, concurrency or resulting state.",
        "Permission escalation, approval, termination, deletion, rollback, session reset, instruction path/file mutation and skill replacement lack actor role, tenant, accountable-human intent, confirmation, idempotency, digest, provenance, path confinement and audit-evidence assertions.",
        "Delete is invoked with --yes, but there is no negative test proving omission prompts or blocks; an arbitrary /tmp/AGENTS.md instruction path is accepted by the CLI test without validating the server-side filesystem boundary.",
        "Focused Vitest passed 2/2 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-125 exactly once and assess Agent create/update/archive/restore lifecycle CLI contracts, role/tenant/responsibility semantics, destructive confirmation, adapter/skill configuration, payload validation and error coverage."]
    },
    {
      "id": "application:cli/__tests__/allowed-hostname",
      "path": "cli/src/__tests__/allowed-hostname.test.ts",
      "lineCount": 80,
      "sha256": "9e1a17f9490f827a110fe9393b174640e808974d0de3d96224576d90358db314",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 80 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The only test adds one mixed-case URL with scheme/port and the equivalent bare hostname, proving lowercase normalization, scheme/port removal and duplicate suppression in the persisted config.",
        "It does not test runtime Host-header enforcement or exact/wildcard/subdomain/IP/IPv6/IDN/trailing-dot/userinfo/path/invalid-input semantics, so it cannot establish DNS-rebinding or host-header protection.",
        "The config file is created with default fs.writeFileSync permissions and the temporary directory is never removed; atomic concurrent config updates and symlink handling are also absent.",
        "Focused Vitest passed 1/1 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-80 exactly once and assess hostname allowlist parsing, exact/wildcard/IP/IDN/port/case/trailing-dot semantics, fail-closed behavior and deployment relevance."]
    },
    {
      "id": "application:cli/__tests__/auth-command-registration",
      "path": "cli/src/__tests__/auth-command-registration.test.ts",
      "lineCount": 80,
      "sha256": "0800050ac890d2c0185a7c1242104eab59b6376a660ccddbf8f88bd0eb5f3968",
      "linkedEvidence": { "history": ["9cd62cf3 fix(cli): handle headless browser-open failure in board auth (#8328)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 80 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The suite proves auth login registers one company-id option and a --no-browser flag, then maps six challenge/revocation commands to expected endpoints.",
        "Challenge GET places the secret token in the URL query and the test accepts this, exposing it to HTTP access logs, proxy histories and observability; direct --token also risks shell history while --token-env is the safer tested path.",
        "No browser/headless fallback behavior is executed despite the file history, and no challenge expiry, single-use/replay, state/PKCE, approver identity/authority, cross-company denial, stable error or token redaction assertion exists.",
        "The fixed board API key and mocked fetch mean the suite proves neither actual authentication nor that approve/cancel/revoke effects are bound to an accountable human.",
        "Focused Vitest passed all 3 tests in this file as part of a 6/6 two-file run."
      ],
      "completionRequirements": ["Read lines 1-80 exactly once and assess board-auth command registration, headless fallback, bind/redirect/origin/token safety, error handling and operator experience."]
    },
    {
      "id": "application:cli/__tests__/board-auth",
      "path": "cli/src/__tests__/board-auth.test.ts",
      "lineCount": 53,
      "sha256": "3cbbf3c94d69f16eb97af52a60e7403965998882d28698ad66d546b465549e85",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 53 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "This file tests plaintext board credential storage rather than callback parsing: missing-file default, trailing-slash API-base normalization, token/user retrieval and removal.",
        "It does not assert file/directory permissions, encryption/keychain integration, atomic writes, symlink resistance, corruption recovery, concurrent updates, token expiry/rotation, selective revocation or redaction.",
        "Only one HTTP localhost origin is tested; scheme/host case, default ports, path/query fragments, HTTPS requirements and credential separation across subtly different origins are unverified.",
        "Temporary credential directories are never removed, so token-123 fixtures persist under the OS temporary directory until external cleanup.",
        "Focused Vitest passed all 3 tests in this file as part of a 6/6 two-file run."
      ],
      "completionRequirements": ["Read lines 1-53 exactly once and assess board-auth callback token parsing, URL/hash/query handling, secrecy, validation, replay/expiry/state/PKCE and test gaps."]
    },
    {
      "id": "application:cli/__tests__/common",
      "path": "cli/src/__tests__/common.test.ts",
      "lineCount": 201,
      "sha256": "ce724a4b261d0b14a8e27d4edcdb40fe54e53ccd7bede319f7007ac21dc6486b",
      "linkedEvidence": { "history": ["876ac759 test(cli): cover API path construction (#9254)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 201 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The suite covers command-context precedence, required company scope, API-base precedence and trailing-slash normalization, authentication-source precedence, attachment MIME inference and safe tagged-template encoding/rejection of empty path segments.",
        "Explicit option > environment > profile is clear, and profiles reference an environment variable name instead of persisting an Agent key. However, the test titled stored board auth never reaches or asserts the stored-credential fallback, and explicit --api-key still normalizes shell-visible bearer input.",
        "API bases accept arbitrary HTTP origins without HTTPS, URL validity, credentials/path/query rejection or origin canonicalization tests; company IDs are only presence-checked here, not bound to credential audience or membership.",
        "apiPath dynamic-segment encoding and blank rejection are strong reusable primitives. MIME inference duplicates the server allowlist by comment rather than generated contract and relies on extension, not content validation.",
        "Temporary context/auth/config directories are never removed; file permissions, symlink/atomic write behavior, malformed config and missing environment-key errors are absent.",
        "Focused Vitest passed 10/10 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-201 exactly once and assess shared CLI HTTP construction, authentication precedence, origin/path/query safety, payload/error/redaction behavior, file output and Company OS client-contract relevance."]
    },
    {
      "id": "application:cli/__tests__/company",
      "path": "cli/src/__tests__/company.test.ts",
      "lineCount": 899,
      "sha256": "021e3b627cecdb01c17b9707a21bd58614cf2af5c1f5d6fe91f45e59e9b9ba60",
      "linkedEvidence": { "history": ["5da382fd feat(skills): require explicit merge modes (#10978)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 899 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-300 test company current/list behavior under explicit or Agent-derived scope, including graceful fallback from denied board-wide listing and a specific instance-admin error for create.",
        "Import preview/apply route selection distinguishes existing-company from new-company targets and rejects blank company IDs; non-interactive and JSON apply require --yes while interactive text mode prompts, a useful destructive-operation UX gate.",
        "The tests use direct --api-key values and mocked fetch, so membership/audience authorization and actual creation/import effects remain server assumptions. Existing-company path and dashboard prefix examples contain only safe characters, leaving dynamic-segment encoding unproven.",
        "Lines 301-600 exercise rich preview/result rendering and reveal the portability schema covers company, Agent hierarchy, adapter/runtime config, permission grants, skills with provenance-like fields, projects/workspaces, tasks/routines, comments, environment-input declarations and collision plans.",
        "Secret env inputs are represented as declarations with null defaults in fixtures rather than values, a sound portability direction, but human principals, accountable owners, responsibility contracts, approvals/evidence, data authorization/export rules and identity rebinding are absent from the shown manifest.",
        "Preview/result rendering tests use only trusted-looking strings; terminal escape/control-character injection and Secret/error redaction are not tested. Warnings can advise API-key review but no mandatory unresolved-Secret or permission-risk gate is proven.",
        "Lines 601-899 prove entity selection defaults to all, keeps project versus task selection separate, and selects nested project/task/Agent/skill files consistently; archive traversal, duplicate/case-conflicting paths, symlinks, decompression limits, hashes/signatures and content schema enforcement are not covered.",
        "The extension file .paperclip.yaml is always included even when every entity is deselected, so extension-controlled behavior can cross the import boundary outside the user's entity selection unless separately reviewed and consented.",
        "Legacy process Agents are silently default-mapped to claude_local while explicit adapters remain unchanged. This vendor-specific fallback conflicts with Company OS runtime neutrality and should instead require an explicit compatible Connector selection or leave the import unresolved.",
        "Selection tests do not require re-binding human identities, accountable owners, Agent credentials, permissions, data contracts or responsibility contracts before apply; preview/apply plan drift and transactional rollback are also untested.",
        "Focused Vitest passed 18/18 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-899 exactly once and assess company create/import/export lifecycle, archive/delete safety, portable data coverage, identity/Secret handling, path/archive/network trust, preview/apply parity, rollback/idempotency and test quality."]
    },
    {
      "id": "application:cli/__tests__/company-delete",
      "path": "cli/src/__tests__/company-delete.test.ts",
      "lineCount": 98,
      "sha256": "ff094b9fb05aab63ffe2883cb54ea7425cd8440c03d1613fe71ea19cacb1c96a",
      "linkedEvidence": { "history": ["ded813ad feat(interactions): add governed agent addressees (#10252)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 98 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Five selector tests distinguish UUID versus case-insensitive issue prefix and explicit selection modes; four confirmation tests require both --yes and a matching target UUID or prefix.",
        "The two-factor CLI confirmation is a useful last-mile guard, but the suite does not execute deletion or prove instance-admin/company authority, latest target re-fetch, concurrent rename/prefix change, idempotency, dependency cascade, retention/recovery or immutable audit evidence.",
        "Fixtures expose defaultResponsibleUserId and interactionResolverGovernance fields in the Company model, relevant signals for later implementation audit, but deletion confirmation does not require or notify that responsible human.",
        "Focused Vitest passed 9/9 in this file as part of a 14/14 two-file run."
      ],
      "completionRequirements": ["Read lines 1-98 exactly once and assess destructive company-delete confirmation, identity/tenant authority, dependency cascade, retention/recovery, idempotency and audit evidence coverage."]
    },
    {
      "id": "application:cli/__tests__/company-export-force",
      "path": "cli/src/__tests__/company-export-force.test.ts",
      "lineCount": 51,
      "sha256": "2993845ee94d9ce94dd36356230522314d7e91b8b829d8b5f15cc8d01645c29a",
      "linkedEvidence": { "history": ["1b8738da feat(cli): add --force to company export for non-interactive runs (#10054)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 51 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The suite distinguishes missing, empty, non-empty and file output paths; non-interactive overwrite of a non-empty directory requires --force and temporary fixtures are cleaned up.",
        "With --force, a directory containing both a user file and .git is accepted, so this check alone authorizes writing into a repository or mixed-content directory without manifest-scoped overwrite, backup or stale-file policy.",
        "Symlinks, permission errors, concurrent writers, atomic staging/rename, disk exhaustion, artifact confidentiality, checksums/signatures and partial-export cleanup are not tested.",
        "Focused Vitest passed 5/5 in this file as part of a 14/14 two-file run."
      ],
      "completionRequirements": ["Read lines 1-51 exactly once and assess export overwrite/force semantics, non-interactive behavior, path/symlink safety, confidentiality and artifact integrity coverage."]
    },
    {
      "id": "application:cli/__tests__/company-import-export-e2e",
      "path": "cli/src/__tests__/company-import-export-e2e.test.ts",
      "lineCount": 623,
      "sha256": "55cb4ba4c7900341a1f45face08c1b2641270f99a85253edb181b498c5e7e4ad",
      "linkedEvidence": { "history": ["9c1f8e78 feat(decisions): add first-class propose mode (#10010)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 623 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 build a real-process test harness with embedded Postgres, actual server/CLI subprocesses, migration auto-apply, isolated PAPERCLIP paths and cleanup helpers—substantially stronger than mocked route parity.",
        "The server runs local_trusted with signup enabled and no UI, so this E2E deliberately bypasses human authentication/tenant authorization; it can prove data-path fidelity but not identity, responsibility or production access control.",
        "A fixed decision-signing Secret is test-only and isolated. The harness scrubs PAPERCLIP_* variables but retains unrelated environment variables, uses an allocate-then-release port with a race window, and CLI JSON parsing searches for the first bracket in mixed output.",
        "The text-file collector recursively trusts the exported directory and decodes every regular file as UTF-8; symlinks are skipped via Dirent type, while artifact size/binary/hash/signature behavior remains to be assessed in test cases.",
        "Lines 251-500 start the actual application, verify PAPERCLIP_HOME context isolation, create a company/Agent/project/large task through real APIs, export selected entities to disk, and import them into a new company through the real CLI.",
        "This is genuine end-to-end data-path coverage rather than a mocked fixture, and the large task description exercises non-trivial payload fidelity. However, it uses claude_local and local_trusted mode, does not create a human principal or responsibility/data-governance records, and exports only company/agents/projects/issues.",
        "The export asserts presence/schema marker but not a manifest digest, signature, deterministic bytes, file permissions or absence of Secrets. New-company import uses --yes without a separate reviewed-plan digest, so preview/apply time-of-check binding remains unproven.",
        "Lines 501-623 dry-run an existing-company rename import, then independently apply it and prove repeated import creates renamed duplicate Agents, projects and tasks with unique identifiers; this validates collision semantics but explicitly shows the operation is not idempotent.",
        "The preview result is not passed back as an immutable plan/digest or concurrency token, so source archive and target state can change before apply. No failure injection proves transaction rollback after a partially created graph.",
        "A generated stored ZIP imports successfully, but malicious traversal/absolute/case-conflicting/duplicate/symlink entries, compression bombs, oversized files, invalid encodings, signature failure and remote-source trust are not exercised.",
        "Focused E2E could not execute on this host: sandbox run skipped on loopback listen EPERM; escalated rerun reached embedded Postgres setup but skipped after five init failures with exit code null. This is missing runtime evidence, not a passing or failing product assertion."
      ],
      "completionRequirements": ["Read lines 1-623 exactly once and assess end-to-end export/import fidelity, archive/network trust, preview/apply binding, identities/Secrets/permissions/responsibility, failures/rollback/idempotency, artifact integrity and actual process coverage."]
    },
    {
      "id": "application:cli/__tests__/company-import-url",
      "path": "cli/src/__tests__/company-import-url.test.ts",
      "lineCount": 74,
      "sha256": "6d8726e195553eb7183ef4e02f3a959eaa0eed068551493b433c16bd0021f36e",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 74 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The pure tests classify HTTP/HTTPS versus paths, restrict repo URLs to GitHub-shaped owner/repo forms, distinguish Windows/relative/absolute local paths from shorthand, and normalize GitHub shorthand/tree URLs with encoded ref/path.",
        "Plain HTTP is explicitly accepted. Default shorthand pins mutable main, while --ref accepts branch-like names rather than requiring a full commit SHA; no fetched content digest/signature or provenance record is asserted.",
        "No actual network request is exercised, so redirects, DNS rebinding, private/link-local/metadata targets, credentials/userinfo, proxy behavior, timeouts, response size/content type, GitHub rate/auth failures and TOCTOU ref changes are untested.",
        "Focused Vitest passed 11/11 in this file as part of a 12/12 two-file run."
      ],
      "completionRequirements": ["Read lines 1-74 exactly once and assess remote import URL/provider normalization, SSRF/redirect/DNS/private-network controls, pinning/integrity, credentials, size/timeouts and source provenance."]
    },
    {
      "id": "application:cli/__tests__/company-import-zip",
      "path": "cli/src/__tests__/company-import-zip.test.ts",
      "lineCount": 51,
      "sha256": "f590bdc4882722c4123089a315ddb8af39a20a392364b7889233e4b83514c60b",
      "linkedEvidence": { "history": ["916c1350 Replace host-to-host Cloud Sync with full-fidelity company Import/Export (#10507)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 51 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The one happy-path test writes a stored ZIP under a wrapper directory, resolves only recognized portable text files and a binary blob, base64-encodes the blob, ignores an unrelated notes file and cleans temporary directories.",
        "Root normalization and binary preservation are useful, but traversal/absolute/backslash paths, duplicate and case-colliding entries, symlinks, special files, nested archives, compression ratios, per-file/total/count limits, CRC corruption and invalid UTF-8 are untested.",
        "No manifest hash/signature, source provenance, permission metadata or extraction atomicity is verified; this suite cannot establish that untrusted ZIP import is safe.",
        "Focused Vitest passed 1/1 in this file as part of a 12/12 two-file run."
      ],
      "completionRequirements": ["Read lines 1-51 exactly once and assess ZIP extraction traversal/absolute/symlink/duplicate/case/bomb/size controls, root layout normalization, cleanup, integrity and provenance coverage."]
    },
    {
      "id": "application:cli/__tests__/config-store",
      "path": "cli/src/__tests__/config-store.test.ts",
      "lineCount": 139,
      "sha256": "2739ed1cee449ef5635ecda5ce1f3923d8adea3b52d216fb76f7b078359b4fd0",
      "linkedEvidence": { "history": ["35132af1 fix(config): preserve extensions and guard invalid repairs (#11005)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 139 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Three focused tests prove recursive unknown extension-key preservation across typed updates, semantic no-op detection with stable mtime/no backup, and collision-safe invalid-byte backup followed by an explicitly authorized atomic repair.",
        "The invalid repair test verifies backup bytes, file and directory fsync ordering, refusal to overwrite without the exact backup path, atomic temp rename, valid reread and temp-file cleanup—unusually strong local durability coverage.",
        "Unknown keys are preserved rather than validated or quarantined, which supports forward/plugin compatibility but can retain stale or malicious extension configuration; ownership/schema/version provenance for extensions is not tested.",
        "File/directory modes and ownership, symlink/hardlink protection, concurrent writers/version compare, disk-full/rename failure recovery, backup retention/encryption and Secret redaction are absent. Semantic no-op also intentionally discards metadata-only source/timestamp changes, so audit metadata is not a write trigger.",
        "Focused Vitest passed 3/3 as part of a 7/7 three-file run."
      ],
      "completionRequirements": ["Read lines 1-139 exactly once and assess config load/write/migration, unknown extension preservation, schema validation, atomicity, permissions, symlink/concurrency/corruption handling, rollback and Secret hygiene."]
    },
    {
      "id": "application:cli/__tests__/configure",
      "path": "cli/src/__tests__/configure.test.ts",
      "lineCount": 124,
      "sha256": "0fc62966bce8dedc0de2b273ad1c7f84dc04b9acb26e8cc26fe5ff3840f9ae1e",
      "linkedEvidence": { "history": ["35132af1 fix(config): preserve extensions and guard invalid repairs (#11005)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 124 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "All three tests are fail-closed paths: unknown section, missing config, and invalid config in non-interactive mode set exitCode 1; invalid bytes are backed up exactly and the original is not replaced.",
        "The suite cleans temporary roots and restores stdin TTY state. It does not exercise any successful interactive or non-interactive configuration change, schema field validation, unknown-extension preservation after configure, or before/after diff/confirmation.",
        "Deployment/auth/signup/telemetry/storage/backup/Secret defaults appear only as a fixture, so the tests provide no evidence that risky combinations are prevented—for example exposing local_trusted beyond loopback, weak Secret mode, public signup, or telemetry/data policy changes.",
        "No permissions/ownership, symlink/concurrency, atomic-write failure, rollback, audit record or structured exit/error-code contract is asserted.",
        "Focused Vitest passed 3/3, but its real console output included the JSON parser excerpt containing the invalid original bytes. A malformed config containing credentials can therefore leak Secret-adjacent content to terminal or CI logs unless the command layer redacts parser details."
      ],
      "completionRequirements": ["Read lines 1-124 exactly once and assess interactive/noninteractive configuration defaults, deployment/auth/storage/Secret decisions, validation, extension preservation, operator intent and test gaps."]
    },
    {
      "id": "application:cli/__tests__/configure-repair",
      "path": "cli/src/__tests__/configure-repair.test.ts",
      "lineCount": 83,
      "sha256": "859b19048cbc05176e674b66f563ba46fda2165bcfbd172dacf971eb43847e79",
      "linkedEvidence": { "history": ["35132af1 fix(config): preserve extensions and guard invalid repairs (#11005)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 83 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The single interactive happy-path test forces a TTY, confirms a default-false repair prompt, verifies exact invalid-byte backup, successful valid reread and atomic temp-file rename, then cleans the temporary root and restores TTY globals.",
        "The actual server prompt is mocked to return current defaults unchanged, so field-level repair choices, invalid partial-value salvage, extension preservation, deployment/auth/Secret policy and post-repair behavior are not exercised.",
        "Decline/cancel, prompt exception, backup failure, validation failure after staging, rename/fsync failure, concurrent modification, rollback and structured audit evidence are absent.",
        "Repair from defaults is explicitly lossy by design; the prompt names the backup but does not show a diff, risk classification or require re-entry/rebinding of Secrets and identity settings.",
        "Focused Vitest passed 1/1 as part of a 7/7 three-file run."
      ],
      "completionRequirements": ["Read lines 1-83 exactly once and assess invalid-config repair safety, backup/rollback, lossy transforms, extension preservation, validation-before-write, operator confirmation and failure recovery."]
    },
    {
      "id": "application:cli/__tests__/connect",
      "path": "cli/src/__tests__/connect.test.ts",
      "lineCount": 197,
      "sha256": "b9ce4be1c8440994871f271c20f77d317dab9dad5402cca5b9985cfd3dbc8cca",
      "linkedEvidence": { "history": ["70b1a910 Improve CLI API parity coverage (#6626)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 197 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Two interactive tests cover board and Agent personas: health check, company/Agent selection, board login request metadata, new API-key creation and versioned profile metadata writes.",
        "Context stores token ID/name and persona/company/Agent metadata rather than raw returned token, a good separation signal, but the test does not assert where/how the new bearer token is persisted, its file/keychain permissions, redaction or retrieval.",
        "The fixture explicitly uses plain HTTP and creates a board key with expiresAt null. TLS/origin trust, audience/scope, expiry/rotation/revocation, challenge/PKCE behavior and cross-company authorization are not covered because loginBoardCli is mocked.",
        "If key creation succeeds but context or credential persistence fails, no compensating revoke/rollback is tested; retries/idempotency can create orphan or duplicate keys. Temporary context directories are not cleaned.",
        "Agent-key issuance is performed through a board persona selecting an Agent, but no accountable-human responsibility, exact grant digest, Agent owner approval or evidence chain is asserted.",
        "Focused Vitest passed 2/2 in this file as part of a 7/7 two-file run."
      ],
      "completionRequirements": ["Read lines 1-197 exactly once and assess deployment connection/profile setup, authentication source, TLS/origin/redirect trust, token persistence/redaction, company selection and failure/rollback behavior."]
    },
    {
      "id": "application:cli/__tests__/context",
      "path": "cli/src/__tests__/context.test.ts",
      "lineCount": 133,
      "sha256": "6b5cabd93da7f419a1c7be75669c6de068724e5d79cd99f2fd90fe4f072b49d7",
      "linkedEvidence": { "history": ["70b1a910 Improve CLI API parity coverage (#6626)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 133 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Five tests cover missing-file defaults, profile upsert/current selection, undefined-as-preserve patches, version 1 to 2 read migration and whitespace normalization to an empty safe profile.",
        "Profiles store API origin, company/persona/Agent metadata and only an environment-variable name for a key—not the raw Secret—supporting multi-deployment context separation in principle.",
        "Plain HTTP origins are accepted and only simple local examples are used. Origin canonicalization/TLS, audience-company consistency, persona transition cleanup, explicit field clearing, duplicate profile names and stale Agent/company bindings are untested.",
        "No file mode/ownership, atomicity/fsync, symlink, concurrent writer, corruption backup, schema-forward extension preservation or migration persistence/rollback is asserted. Temporary context directories are never removed.",
        "Focused Vitest passed 5/5 in this file as part of a 7/7 two-file run."
      ],
      "completionRequirements": ["Read lines 1-133 exactly once and assess multi-profile context storage, origin/company/credential separation, precedence, permissions, atomicity, corruption/concurrency and managed/self-hosted relevance."]
    },
    {
      "id": "application:cli/__tests__/data-dir",
      "path": "cli/src/__tests__/data-dir.test.ts",
      "lineCount": 79,
      "sha256": "d0185c8e2ca7344c3fad7fdf43a91a49f1c4b734ea472ba0e66a979ca118cdc1",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 79 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four pure tests cover tilde/absolute data-root resolution, per-instance config defaults, shared context path, preservation of pre-existing config/context environment values and commands without those options.",
        "Per-instance config directories plus a shared profile context are useful managed/self-hosted isolation primitives, but instance IDs use only a safe dev_1 fixture; traversal, separators, dot names, Unicode/case collisions and length validation are not tested.",
        "No directory creation, canonicalization, ownership/mode, symlink, writable/free-space, cross-device or multiple-process collision behavior is exercised. Environment-versus-flag precedence is ambiguous because pre-existing env paths remain even when flag paths are supplied to this helper.",
        "Focused Vitest passed 4/4 in the initial two-file run."
      ],
      "completionRequirements": ["Read lines 1-79 exactly once and assess data-directory resolution/isolation, environment/tilde/symlink/path trust, permissions, multi-instance collision and self-hosted lifecycle relevance."]
    },
    {
      "id": "application:cli/__tests__/doctor",
      "path": "cli/src/__tests__/doctor.test.ts",
      "lineCount": 102,
      "sha256": "9adffd22c8dbff4824dffff1971669ba5a1da8a1cf1c6914370a2ea0ad04be80",
      "linkedEvidence": { "history": ["73eb2373 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 102 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The single integration-style test runs doctor with repair:true and yes:true against a temporary local-trusted config, then asserts zero failures/warnings and presence of PAPERCLIP_AGENT_JWT_SECRET after repair.",
        "It validates the important behavior that repairable checks are rerun rather than leaving stale failure status, but does not identify which checks ran or assert filesystem/database/storage/server/backup/Secret permissions individually.",
        "No dry-run/decline, partial repair, repair exception/rollback, structured check codes, Secret-safe output, strict production/authenticated profile, external database/S3, network or Connector diagnostics are covered.",
        "The temporary config/runtime root is not removed. The test asserts a signing Secret exists in process.env but not its entropy, persistence permissions, rotation/revocation or absence from logs.",
        "Initial sandbox run failed only because the port-availability check produced a listen EPERM warning; escalated loopback rerun passed 1/1 with 12 checks after JWT and Secret-adapter repairs."
      ],
      "completionRequirements": ["Read lines 1-102 exactly once and assess diagnostics coverage, Secret-safe output, deployment/auth/storage/database/runtime checks, remediation safety, structured codes and production relevance."]
    },
    {
      "id": "application:cli/__tests__/env-lab",
      "path": "cli/src/__tests__/env-lab.test.ts",
      "lineCount": 24,
      "sha256": "3f0323e997907787afd2308033ace7ad916c5bebd0e1c8d88e56d547274eed14",
      "linkedEvidence": { "history": ["e4995bbb Add SSH environment support (#4358)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 24 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Two very shallow tests prove the SSH fixture state path is nested under instances/<id>/env-lab/ssh-fixture and that a missing fixture reports running:false, environment:null and a boolean support flag.",
        "No fixture start/stop, SSH host-key verification, user/key/password handling, port/process isolation, command/file transfer, timeout, cleanup, crash recovery or concurrent-instance behavior is exercised.",
        "Instance IDs use safe literals only; traversal/separator/collision validation is not proven. Environment-lab is diagnostic test infrastructure, not evidence of production Connector runtime attestation or isolation.",
        "Focused Vitest passed 2/2 in this file as part of an 11/11 three-file run."
      ],
      "completionRequirements": ["Read lines 1-24 exactly once and assess environment-lab isolation, SSH trust, credential handling, lifecycle/cleanup, capability/evidence semantics and test strength."]
    },
    {
      "id": "application:cli/__tests__/feedback",
      "path": "cli/src/__tests__/feedback.test.ts",
      "lineCount": 177,
      "sha256": "ea4c0bc293e98e8d9bce1541498e9c3658efe8aae4f1b7e15ef4e7eb1516cc83",
      "linkedEvidence": { "history": ["70b1a910 Improve CLI API parity coverage (#6626)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 177 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four tests cover command registration, query encoding, text report summary and a local directory+ZIP export containing votes/traces/manifest.",
        "The model carries company/user/Agent/issue/project identifiers, free-text reason/excerpt, destination, consent/schema/bundle/payload versions, digest/redaction summaries, retry/failure timestamps and payload snapshot—valuable explicit data-governance surface.",
        "The query builder forces includePayload=true even with sharedOnly=true. renderFeedbackReport is called with includePayloads:false yet the assertion requires the free-text reason from payloadSnapshot, showing the flag does not fully suppress payload-derived content in the report contract.",
        "The bundle includes both pending and local_only traces with payloads; local export may be legitimate, but no explicit export consent, field-level redaction, classification, encryption, permissions, overwrite, digest/signature, retention or accountable-human approval is asserted.",
        "The temporary export directory is never removed. Network upload/destination authentication, tenant denial, rate/size limits and actual redaction are not exercised; payloadDigest and redactionSummary are null in fixtures.",
        "Focused Vitest passed 4/4 in this file as part of an 11/11 three-file run."
      ],
      "completionRequirements": ["Read lines 1-177 exactly once and assess feedback/telemetry data scope, consent, redaction, identity/tenant binding, outbound destination, retention, payload/attachment limits and errors."]
    },
    {
      "id": "application:cli/__tests__/http",
      "path": "cli/src/__tests__/http.test.ts",
      "lineCount": 107,
      "sha256": "6e41422aeedee95d1c5db6c14c3e2463b8e32817b391645e14708d93e1b0b201",
      "linkedEvidence": { "history": ["230126d8 test(cli): clean API client fetch stubs (#9253)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 107 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Five tests cover bearer/run-ID headers, ignored 404, structured status/message/details errors, connection guidance and one automatic POST retry after interactive authentication recovery.",
        "The client accepts plain HTTP and a caller-supplied x-paperclip-run-id; this header is correlation metadata, not trustworthy runtime attestation or evidence.",
        "ApiRequestError exposes free-form message/details without a stable error code. Connection errors embed the full request URL and curl guidance, which can leak sensitive query values if callers place tokens or data in URLs.",
        "A generic 403 triggers recoverAuth and automatically replays the same POST with a new board token. This conflates authentication expiry with authorization denial, can elevate a limited caller to board authority, and retries non-idempotent effects without an idempotency key or fresh human confirmation.",
        "Timeout/AbortSignal, redirect/origin containment, TLS, response/body size/content-type validation, retry-after/backoff, 401 versus 403 policy, Secret redaction and observability behavior are untested.",
        "Focused Vitest passed 5/5 in this file as part of an 11/11 three-file run."
      ],
      "completionRequirements": ["Read lines 1-107 exactly once and assess HTTP client URL/origin trust, auth header/redaction, body/content handling, errors/codes, timeout/retry/cancel, redirects, size limits and observability safety."]
    },
    {
      "id": "application:cli/__tests__/home-paths",
      "path": "cli/src/__tests__/home-paths.test.ts",
      "lineCount": 46,
      "sha256": "17f58341ee4c3a919d18ced1cf4ac0af1787401d3ff2c6d006caa7d8747847df",
      "linkedEvidence": { "history": ["0096b56a [codex] Add LLM Wiki plugin host support (#5597)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 46 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four tests cover default/explicit instance layout, PAPERCLIP_HOME tilde expansion, explicit instance ID and rejection of one slash-containing ID.",
        "The safe nested instances/<id>/config.json layout and rejection signal improve on the shallower data-dir test, but only bad/id is adversarial; backslash, dot/dotdot, absolute, Unicode/case collisions, reserved names and length are not covered.",
        "PAPERCLIP_HOME canonicalization, symlink/mount/ownership/mode and config/context/auth/Secret path co-location or migration are not exercised. The first test creates a temporary directory without removing it.",
        "Focused Vitest passed 4/4 through the existing local binary."
      ],
      "completionRequirements": ["Read lines 1-46 exactly once and assess product-home/config/context/auth path precedence, environment isolation, multi-instance safety, canonicalization and migration relevance."]
    },
    {
      "id": "application:cli/__tests__/install-command",
      "path": "cli/src/__tests__/install-command.test.ts",
      "lineCount": 376,
      "sha256": "c8c86418c297c5ced8fb8b3f27db44fe8e5b4fbcebf55839d7cef969294e18ca",
      "linkedEvidence": { "history": ["fc5a3080 feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 376 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-200 cover npm stable/canary/exact selection, GitHub ref-to-full-SHA resolution, fork override rules, noninteractive consent, SHA-keyed payload reuse, codeload/build/package workflow and explicit NODE_ENV clearing.",
        "Resolving mutable refs to a full commit SHA before storage and requiring --yes for noninteractive Git installs are good provenance/intent primitives; clearing ambient production mode avoids silently omitting build dependencies.",
        "Arbitrary forks and branch refs are accepted, curl/tar/corepack/bash/npm execute a large supply-chain surface, and tests mock all content. No HTTPS certificate/publisher allowlist, archive/package digest/signature, lockfile integrity, malware/script sandbox, dependency audit or license policy is proven.",
        "Cached payload reuse is keyed by only the first 12 SHA characters and validated by executing its CLI/version path, not by rechecking full content digest; a tampered local cache could execute before trust is re-established.",
        "Lines 201-376 verify dependency-closure ordering, stderr diagnostics, npm registry isolation via temporary userconfig, atomic current shim/manifest provenance, uninstall-before-service ordering, preservation of user data, cross-instance service refusal, fail-closed unsupported service manager, symlink defenses, unverified-store refusal and mutation locking.",
        "These are strong operational safety tests, especially keeping instance data during uninstall and refusing ambiguous/symlinked roots. However, child-process stderr is propagated verbatim and can leak registry tokens, paths or build Secrets into CLI errors.",
        "No failed-update atomic rollback from current to previous payload, database migration compatibility/rollback, service restart health gate, payload garbage collection/retention, signature/SBOM/license/vulnerability validation or privileged-user boundary is exercised.",
        "Focused install-command Vitest passed 16/16."
      ],
      "completionRequirements": ["Read lines 1-376 exactly once and assess managed install/update/service lifecycle, package/source verification, privilege boundaries, configuration preservation, atomic switch/rollback, process ownership and failure/security tests."]
    },
    {
      "id": "application:cli/__tests__/install-store",
      "path": "cli/src/__tests__/install-store.test.ts",
      "lineCount": 206,
      "sha256": "e60ea21273b0593afe7fd9f4b2162eafcdd10347897fdaabae1d631699d9e8b8",
      "linkedEvidence": { "history": ["fc5a3080 feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 206 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Eleven focused tests cover deterministic payload layout, 0600 atomic manifest, crash-safe current symlink flip, two-version rollback retention/pruning, stable shim and shell RC mode preservation, exact marker validation, locking/stale recovery, provenance/current consistency and symlink/hardlink defenses.",
        "This is high-quality local install-state engineering: fail-closed ownership checks, old-current survival before rename, strict shim recognition and user RC protection are directly reusable patterns.",
        "Manifest records source/version/channel/path/time/previous but no package/content digest, signature, SBOM, license or migration compatibility. Current/manifest atomicity as a multi-file transaction and post-switch health/automatic rollback are not proven.",
        "Lock recovery trusts process existence and can face PID reuse/host or network-filesystem semantics; concurrent independent processes, corrupted/truncated manifest, fsync ordering, prune failure/partial deletion and disk-full cases are absent.",
        "Focused install-store Vitest passed 9/11 and failed 2/11 on Node 24/macOS: atomic-flip cleanup masked the simulated crash with Path is a directory, and the symlink-defense test's own fs.rmSync cleanup also failed on a directory symlink."
      ],
      "completionRequirements": ["Read lines 1-206 exactly once and assess install metadata/version/channel persistence, atomicity/concurrency/corruption, permissions/symlinks, provenance/digest, migrations and rollback state."]
    },
    {
      "id": "application:cli/install-store.ts",
      "path": "cli/src/install-store.ts",
      "lineCount": 483,
      "sha256": "a0808e177caabb216b5d5303fc3402605bd0344519dbb1818188924dc2ed77e8",
      "linkedEvidence": { "history": ["fc5a3080 feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 483 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 define a compact managed-install state machine with private directories/marker, exact store verification, hard-link lock token, payload identifier regex, versioned manifest, atomic writers and install-root containment.",
        "Store and marker checks reject symlinks/hardlinks and wrong ownership/content; uninstall additionally validates manifest payload containment. The lock uses create+hard-link acquisition and token-checked release, a thoughtful cross-process design for local filesystems.",
        "Lock stale recovery relies only on PID liveness, so PID reuse can strand a lock and a reused PID from an unrelated process blocks progress. Lock file lstat/owner/mode are not verified before reading/removing, and network filesystem hard-link/process semantics are unspecified.",
        "Manifest validation is shallow and accepts arbitrary repo/ref/version/channel/time/previous records without full path/digest schema. writeInstallManifestAtomic has no file/directory fsync, uses a predictable temp name without wx and does not coordinate current+manifest as one transaction.",
        "Lines 251-483 confirm payload realpath containment, relative current symlink, two-version manifest retention, pruning, exact shim grammar/quoting, current-manifest provenance and careful shell RC ownership/mode/symlink handling.",
        "Observed Node 24/macOS failure is implementation-owned: flipCurrentAtomic always calls fs.rmSync(temporaryLink,{force:true}) in finally. When beforeRename throws and the temporary link targets a directory, rmSync throws Path is a directory, masks the original error and leaves cleanup behavior non-portable.",
        "pruneInstallPayloads deletes every non-dot unretained entry recursively after checking only the source root, with no per-candidate lstat/owner/manifest digest; remove/rollback safety depends on exclusive store ownership. Post-switch health and atomic coordination with manifest/service/database migrations remain absent."
      ],
      "completionRequirements": ["Read lines 1-483 exactly once and trace install-store path trust, manifest/current/shim atomicity, locking, symlink/hardlink defenses, pruning, permissions, provenance, failure cleanup and observed Node 24 test failures."]
    },
    {
      "id": "application:cli/__tests__/managed-install-check",
      "path": "cli/src/__tests__/managed-install-check.test.ts",
      "lineCount": 88,
      "sha256": "77df088f984b6a9cfc42993c48b7acc797801c01d9f6a156f06de5c00d298be0",
      "linkedEvidence": { "history": ["fc5a3080 feat(cli): add managed install, update, and service lifecycle (#10045)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 88 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Four tests cover a consistent manifest/current/shim/PATH, fail on managed marker without manifest, and correctly ignore update-notice-only or empty-installs residue.",
        "The health check distinguishes harmless shared CLI state from an actual managed installation, reducing false alarms, but only missing-manifest corruption is negative-tested.",
        "Payload digest/signature, manifest/current/version/entrypoint mismatch, shim tamper variants, ownership/modes/symlinks, lock state, service health, previous rollback and repair guidance are not asserted.",
        "Each test creates a temporary install root without removing it.",
        "Focused Vitest passed 4/4 in this file as part of a 9/9 two-file run."
      ],
      "completionRequirements": ["Read lines 1-88 exactly once and assess managed-install diagnostics/provenance, tamper and current/manifest mismatch detection, repair guidance and production readiness."]
    },
    {
      "id": "application:cli/__tests__/network-bind",
      "path": "cli/src/__tests__/network-bind.test.ts",
      "lineCount": 69,
      "sha256": "8873e26fb15ff40b4920c5c6b491d82de867f3e4adb1f82be874af14ad686351",
      "linkedEvidence": { "history": ["4811d8dd Fix wrapped company issue prefix conflicts (#6423)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 69 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Five pure tests prove local_trusted rejects LAN/wildcard exposure, tailnet resolves a detected address, custom requires a host, tailnet presets persist detected address and missing Tailscale falls back to loopback.",
        "The fail-closed local_trusted invariant and loopback fallback are strong defaults. However, authenticated/managed modes, LAN/custom exposure, allowed Host/origin, proxy headers and TLS are not tested.",
        "PAPERCLIP_TAILNET_BIND_HOST is trusted in the preset fixture without validation; non-Tailscale/private/public/IPv6/malformed values, runtime interface disappearance and DNS/address changes are absent.",
        "Focused Vitest passed 5/5 in this file as part of a 9/9 two-file run."
      ],
      "completionRequirements": ["Read lines 1-69 exactly once and assess bind/host/exposure/deployment-auth invariants, IPv4/IPv6/wildcard/proxy semantics, fail-closed defaults and test gaps."]
    },
    {
      "id": "application:server/services/company-search-rate-limit",
      "path": "server/src/services/company-search-rate-limit.ts",
      "lineCount": 63,
      "sha256": "b72ce7ff11f6f5d5000002b784986d5f566dc6b71190992e9155ba9dae65b235",
      "linkedEvidence": { "history": ["320fd5d23b77c7f14700437bdc937acece41d411 Add full company search page (#5293)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 63 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The service is a dependency-free in-memory sliding-window limiter keyed by companyId, actorType and actorId. Structured allowed/limit/remaining/retryAfterSeconds output and injectable time make behavior simple and testable.",
        "State is process-local, so horizontal replicas, restarts and multiple deployment profiles have independent quotas. It cannot provide a canonical production control for managed-cloud, and it has no persistence/degradation policy for self-hosted clustering.",
        "The colon-concatenated key is ambiguous when IDs contain colons, allowing distinct principals to share a bucket. Each unique key remains in the Map forever because expired keys are cleaned only when that same actor consumes again; high-cardinality inputs cause unbounded memory growth.",
        "windowMs/maxRequests are not validated, wall-clock regression is not handled, and every request filters and reallocates the full hit array. There is no capability/query-cost/data-sensitivity dimension, policy provenance, standardized denial code or evidence hook.",
        "Focused route Vitest passed 3/3 with isolated PAPERCLIP_HOME and approved local TCP listening. It checks same-actor rejection plus two route concerns, but not cross-company/actor isolation, key collisions, expiration cleanup, invalid config, clock movement, high cardinality, concurrency or multi-instance behavior."
      ],
      "completionRequirements": ["Read lines 1-63 exactly once and assess rate-limit identity and tenancy scope, distributed/deployment behavior, memory lifecycle, time semantics, error contract and test coverage."]
    },
    {
      "id": "application:server/__tests__/company-search-service",
      "path": "server/src/__tests__/company-search-service.test.ts",
      "lineCount": 726,
      "sha256": "1dd93ec92a839e553d85a4d46aa9dc7a3c6b9b821dcc503c11d7682717b0bd7a",
      "linkedEvidence": { "history": ["606aa4f266f6c092ea2916f385cc3f9329dc1ae7 feat(search): filters, sorting, operators & command-palette parity (#9327)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 726 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 combine pure query-schema tests with embedded-PostgreSQL integration setup and real pg_trgm. They cover query truncation, filter/sort/pagination validation, offset-aware branch caps, identifier ranking, phrase/all-token ranking, cross-field issue/comment matching, snippets/highlights and comment counts.",
        "The fixture deletes all searched tables after each case and uses company-scoped factories, but this segment has not yet shown adversarial tenant isolation or authorization; service calls accept companyId directly with no Principal.",
        "Lines 251-500 cover issue-document and artifact projections, artifact high-offset handling, combined filters before sort/pagination, filter-only searches, zero-result loosen suggestions, hidden issue/artifact exclusion, harness issue exclusion and explicit cross-company isolation.",
        "The test intentionally counts facet options before active filtering in one case and treats hidden/harness records as globally absent. It still tests companyId isolation as a data query property rather than caller authority to choose that company.",
        "Lines 501-726 strongly exercise SQL LIKE wildcard escaping for %, _ and backslash across every result branch, cross-type merge pagination, pg_trgm fuzzy matching and common transpositions. These are useful correctness/security regression cases.",
        "The suite does not cover empty/whitespace-only unconstrained searches, Unicode/CJK tokenization and normalization, malicious highlight/snippet content, huge comments/documents, stable pagination under ties/concurrent writes, query plans/index use, permission/data-contract filtering or per-result evidence visibility.",
        "Focused run: 2 pure validation tests passed; all 17 embedded-PostgreSQL service tests were explicitly skipped because the embedded database failed to start after five attempts (init exit code null). No database behavior is claimed as passing on this host."
      ],
      "completionRequirements": ["Read lines 1-726 exactly once and assess company/identity isolation, query/filter/sort semantics, result authorization, pagination/bounds, SQL safety, evidence exposure and regression quality."]
    },
    {
      "id": "application:server/__tests__/company-search-extract-service",
      "path": "server/src/__tests__/company-search-extract-service.test.ts",
      "lineCount": 269,
      "sha256": "c36a755b856e7ded6eb12aa5c14baf9292a85d33c9bb340decb4e22451172a85",
      "linkedEvidence": { "history": ["ea0e8999059b57d57858bec42309bdb0a135b661 fix(search): honor extract match limits + harden pr-gardening candidate discovery (#9652)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 269 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-200 validate literal/URL extraction inputs, reject regex and ambiguous missing kind, cap limit/offset/matches-per-issue, reject conflicting time filters, and use real embedded PostgreSQL fixtures.",
        "Tests cover URL extraction/deduplication and source-field attribution across issue title/description, comments and linked document title/body, including scheme-less containment queries. They do not yet show tenant/hidden-record or result authorization behavior.",
        "Lines 201-269 cover status/time filters, default and explicit distinct-match caps with truncation flags, and cross-company data exclusion. Missing cases include hidden/harness issues, foreign-company documents/comments attached by malformed relations, duplicate values with different provenance, wildcard escaping, extremely long source content, Unicode/CJK, stable paging and caller authority.",
        "Focused run: 1 pure validation test passed; all 6 embedded-PostgreSQL extraction tests were explicitly skipped because the embedded database failed to start after five attempts (init exit code null). No database behavior is claimed as passing on this host."
      ],
      "completionRequirements": ["Read lines 1-269 exactly once and assess extract authorization, selectors, result caps, content/provenance fidelity, adversarial input and regression quality."]
    },
    {
      "id": "application:server/__tests__/company-search-extract-routes",
      "path": "server/src/__tests__/company-search-extract-routes.test.ts",
      "lineCount": 104,
      "sha256": "eae777e4fa079c38d1621f023b1bc5313997c372e6de88db64ad3ef7607b0739",
      "linkedEvidence": { "history": ["ea0e8999059b57d57858bec42309bdb0a135b661 fix(search): honor extract match limits + harden pr-gardening candidate discovery (#9652)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 104 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "All 104 lines use a real Express route with stubbed search/extract services. Tests cover parsed/capped query delivery, deny-before-service for a board actor lacking company membership, and reuse of the same search limiter with Retry-After.",
        "The test actor is an instance admin and only board/session identity is exercised. Missing cases include Agent authentication, unauthenticated requests, inactive membership, granular search/data permission, stable structured error codes, malformed extract queries and proof that rate-limit identity cannot be forged or collide.",
        "Focused route Vitest passed 3/3 with isolated PAPERCLIP_HOME and approved local TCP listening."
      ],
      "completionRequirements": ["Read lines 1-104 exactly once and assess transport authz, validation/error codes, tenant binding, rate limiting and route-to-service contract coverage."]
    },
    {
      "id": "application:server/services/company-search-extract",
      "path": "server/src/services/company-search-extract.ts",
      "lineCount": 349,
      "sha256": "98afb44df08d716e4ed494dbf1bdefafa9f8600c99e576e9c40babbdac7989a0",
      "linkedEvidence": { "history": ["ea0e8999059b57d57858bec42309bdb0a135b661 fix(search): honor extract match limits + harden pr-gardening candidate discovery (#9652)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 349 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-200 implement SQL prefiltering plus in-process exact extraction. Drizzle parameterization and explicit LIKE/regex escaping reduce injection/wildcard risk; visibleIssueCondition and company predicates are present in issue/comment/document branches.",
        "URL extraction is a custom ASCII/domain regex and literal matching lowercases JavaScript strings. It deduplicates solely by lowercased value, so the first source wins and repeated identical evidence from different comments/documents is discarded, weakening provenance and completeness.",
        "Full source text is later needed to create excerpts; current code has no content-length or aggregate-memory constraint in this segment. Data authorization is only companyId and visibility, with no Principal, DataContract or field-level permission.",
        "Lines 201-349 use stable updatedAt/id ordering and limit+1 pagination, then issue two batched child queries rather than N+1. Company predicates, nondeleted comments and same-company document joins are explicit, which are strong tenancy/integrity details.",
        "The service loads entire matching comment/document bodies for up to 200 issues and can emit up to 200 distinct matches per issue. It reruns sourceOccurrences on each full text and has no byte/time/cost budget; a high-volume extraction request can be CPU/memory heavy even behind a request-count limiter.",
        "Labels and excerpts are English presentation strings in the service. The response exposes issue, assignee, comment and document identifiers plus original excerpts without data-classification redaction, access-decision evidence or an immutable extraction/query digest."
      ],
      "completionRequirements": ["Read lines 1-349 exactly once and trace extraction query planning, tenant/data authorization, boundedness, SQL/content safety, provenance and dependency direction."]
    },
    {
      "id": "application:server/services/company-search",
      "path": "server/src/services/company-search.ts",
      "lineCount": 1340,
      "sha256": "4fe5a5460e92cb5363ceab041f3dd5feda2acc19314072a022437a32ac1230fc",
      "linkedEvidence": { "history": ["ae7790861811323de7ffcedffa9f56cd88b98bc0 feat(search): add bulk extract endpoint (#9507)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 1340 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 show a search service coupled directly to many database tables, shared presentation DTOs, artifacts, extract search and route URL construction. Search query parsing/ranking, Markdown simplification, snippets/highlights, navigation and artifact image discovery are mixed in one service.",
        "Parameterized SQL-array helpers and LIKE escaping are sound primitives. Tokenization supports Unicode letter/number boundaries and caps token count, but code-unit length, whitespace tokenization and English fuzzy thresholds are not designed for Chinese segmentation or locale-aware normalization.",
        "plainText is a regex-based Markdown approximation and extractFirstImageUrl returns an unvalidated Markdown target. Presentation consumers must not treat that URL as trusted; Company OS needs a sanitized asset/provenance boundary rather than discovering media from arbitrary text.",
        "Lines 251-500 build issue filters and facet-omission queries with parameterized values and explicit company-scoped label EXISTS checks. Stable sort tie-breaking by ID and filtering before cross-type pagination are thoughtful correctness choices.",
        "Facet and result functions embed English labels, route paths and issue presentation. The service exposes assignee human/Agent IDs, project IDs and raw-derived preview URLs based only on company scope; no per-principal field/evidence/data-contract visibility is represented.",
        "Agent/project relevance scoring later occurs in JavaScript over fetched rows rather than one canonical indexed ranking. Chinese tokenization, pinyin/word segmentation, locale collation and enterprise ACL-aware search remain outside the model.",
        "Lines 501-750 compose one sophisticated issue-side SQL pipeline with capped per-branch fetching, escaped phrase/token patterns, company-scoped nondeleted comment CTEs, same-company document joins and precomputed flags/facets to avoid correlated N+1 queries.",
        "The query deliberately optimizes PostgreSQL pg_trgm/fuzzy matching and assumes database extensions/planner behavior. Title fuzzy word splitting is hardcoded to [a-z0-9], explicitly English-centric; it cannot serve Company OS Chinese search without a separate locale-aware search abstraction/index strategy.",
        "For multi-pattern comments the service joins every company comment against unnest(patterns), while documents UNION one scan per pattern. Caps limit result rows, not scan work. Common/short queries over large enterprise content can produce high CPU/IO before the request limiter reacts.",
        "Lines 751-1000 use one MATERIALIZED matched CTE and dynamically UNION ranked rows, per-type counts, every facet, time buckets and zero-result omission totals. This avoids application N+1 and gives coherent results within one SQL statement, but branch count and work grow with active filters/facets.",
        "The matched CTE materializes every visible company issue matching the text before filter branches and result caps. Offset/limit bound returned rows but not match-set, comment/document scans, facet groups or fuzzy work; a large tenant/common query remains an expensive database operation.",
        "Counts labeled comment/document are counts of matched issues, not occurrences or source records. This projection semantic should be explicit; otherwise Agent Boss users may misinterpret data volume and evidence coverage.",
        "Lines 1001-1250 enrich only the fetched issue window with one lateral comment/document query and keep company/same-company joins, avoiding classic per-result N+1. However it selects complete comment/document bodies to create snippets and still lacks field-level evidence visibility/redaction.",
        "Agent/project branches are company-scoped and projects exclude archived rows, but Agents have no lifecycle/status visibility filter. Agent capabilities and identity metadata can therefore appear based on text match even if a principal should not see terminated/restricted workers.",
        "Artifact list and three artifact count branches duplicate query work, then run alongside issue aggregates, snippets, Agent/project rows and counts in Promise.all. One search can fan out into many heavy concurrent SQL statements; request-count limiting is not a query-cost budget.",
        "Artifact media is defined by Paperclip-specific work-product provider and Agent-created heuristics, so this taxonomy is not a neutral Company OS evidence/artifact model and should not be copied.",
        "Lines 1251-1340 merge issue/artifact/Agent/project results in memory with deterministic sorting and then paginate. Routes, labels and result presentation remain embedded in the service rather than returned as neutral projections.",
        "Zero-result recovery can launch three nonissue count queries for the unfiltered query plus three more for every active filter, all concurrently. With many filters this creates a large database fan-out on the exact no-result path likely to contain broad/common searches.",
        "hasMore derives from the truncated in-memory branch rows rather than canonical counts. Artifact fetching is capped independently of offset+limit, so high-offset artifact or cross-type pages can report no rows/hasMore=false while countArtifacts reports additional matches; the existing high-offset test has no artifact rows and does not catch this.",
        "The service accepts only companyId/query and returns human/Agent/evidence-derived data without a verified Principal or access decision. Company OS must move authorization/data-contract filtering into the canonical search projection boundary, not rely on a route membership check."
      ],
      "completionRequirements": ["Read lines 1-1340 exactly once and trace query grammar, SQL generation, company/principal/data scope, result projections, pagination/sorting, performance and Company OS governance fit."]
    },
    {
      "id": "application:server/__tests__/company-search-rate-limit-routes",
      "path": "server/src/__tests__/company-search-rate-limit-routes.test.ts",
      "lineCount": 121,
      "sha256": "e854d33dc55af43c99adf9dc2bc477986fc328c8b98524157a0805fc91508aa8",
      "linkedEvidence": { "history": ["606aa4f266f6c092ea2916f385cc3f9329dc1ae7 feat(search): filters, sorting, operators & command-palette parity (#9327)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 121 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "All 121 lines use a real Express router with a synthetic instance-admin local_implicit board actor and stubbed search. Tests prove same-actor denial before search execution, Retry-After, board assigneeUserId=me resolution, and invalid sort/Agent-ID rejection before search.",
        "Focused route Vitest passed 3/3 in the prior verified run. Missing cases include cross-company/unauthenticated/Agent/inactive identities, limiter separation and key collisions, whether invalid queries consume quota, stable machine error codes, distributed replicas and authority beyond company membership."
      ],
      "completionRequirements": ["Read lines 1-121 exactly once and assess deny-before-work, actor/company scope, query validation, stable error/headers and distributed limiter test gaps."]
    },
    {
      "id": "application:server/services/company-export-readme",
      "path": "server/src/services/company-export-readme.ts",
      "lineCount": 172,
      "sha256": "e86c42799dd93c5953600c0df4268308dbfd83e44d6c8dfaf17a083edac214f1",
      "linkedEvidence": { "history": ["73eb23734f25c570d88665e672f80d740797c632 docs: use structured agent mentions in paperclip skill (#4103)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 172 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The renderer is a small presentation helper over the full portability manifest, but hardcodes Paperclip/Agent Company branding, English roles/headings, pnpm CLI syntax, public URLs and current wall-clock date. Output is non-reproducible and not a vendor-neutral migration contract.",
        "Mermaid label escaping handles quotes and angle brackets but not ampersands, line breaks or other grammar controls. Sanitized node IDs can collide (for example a-b and a_b), causing merged nodes or incorrect reporting edges; no collision map or test exists.",
        "Company/project/Agent/skill text and sourceLocator are interpolated into Markdown headings, blockquotes, lists, tables and links without context-specific escaping. Pipes/newlines/formatting can rewrite the document, and arbitrary source URLs including credential-bearing locators can be exposed as clickable README content.",
        "The generated org chart contains Agents only and calls them the company: accountable humans, departments, responsibility contracts, permissions, approvals, data contracts and evidence are absent. Counts also omit routines and other manifest domains, so it is not an authoritative inventory.",
        "There is no dedicated test file; the already-audited portability suite only exercises broader package flows and does not prove adversarial escaping, ID collisions, deterministic output, localization or responsibility completeness."
      ],
      "completionRequirements": ["Read lines 1-172 exactly once and assess presentation/domain coupling, escaping/injection, portability accuracy, i18n, responsibility semantics, drift and test coverage."]
    },
    {
      "id": "application:server/__tests__/company-artifacts-service",
      "path": "server/src/__tests__/company-artifacts-service.test.ts",
      "lineCount": 892,
      "sha256": "ee9164432dbd98f84d42317a4af7c3aebba07185c5ab7ec53492c52c70bb1445",
      "linkedEvidence": { "history": ["c111ee4cb3312077ce4df7b128002079345c083e feat(server): add per-user document stars (#9952)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 892 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 set up real embedded-PostgreSQL service/route fixtures plus a storage stub. Seed data deliberately spans two companies, Agent- and user-authored documents, a system continuation summary, human plan, runs, comments and assets.",
        "The taxonomy already equates Agent-created documents and selected work products/attachments with artifacts while user-authored context and system documents are intended exclusions. This is provenance heuristic by creator/key/provider, not a responsibility/evidence classification contract.",
        "Lines 251-500 cover per-user starred document views through a real Express route, Agent callers receiving an empty starred view, company isolation, exclusion of user/comment/system sources, deduplication of attachment-backed work products, storage-backed text previews, filters and cursor ordering.",
        "Starred mode intentionally expands beyond Agent artifacts to human plan/upload documents for the current user, so one endpoint has two different domain definitions. A viewer membership is sufficient for the route test; no document-level ACL/data contract is exercised.",
        "Lines 501-750 add unusually strong malformed-reference regressions: attachment dedupe beyond the visible work-product window, foreign-run Agent attribution suppressed, foreign issue/project links excluded in flat/grouped/selected-group modes.",
        "A work product with a foreign run remains visible but creator attribution becomes null. For Company OS this should be an explicit provenance-integrity failure/quarantine state, not silently converted into an unattributed valid artifact.",
        "Grouping tests cover filters and task summaries but assert presentation URLs in the service contract, reinforcing route/UI coupling. Full dedupe beyond a page implies broader prefetch/query work whose scalability must be checked in implementation.",
        "Lines 751-892 cover grouped cursor pagination, parent-task ancestor aggregation, selected-group paging/filter metadata, foreign selected-group exclusion and deny-before-read for cross-company Agent route access.",
        "Missing cases include cyclic/cross-company parent graphs, malformed/forged cursors, concurrent insert pagination, very large groups, storage timeout/oversized text/invalid encoding, unsafe content/open paths, private document membership ACL, Agent lifecycle/authority and evidence immutability.",
        "Focused verbose Vitest: the cross-company Agent route authorization case passed 1/1; all 11 embedded-PostgreSQL service cases were explicitly skipped after five database-start attempts (init exit code null). No database behavior is claimed as passing on this host."
      ],
      "completionRequirements": ["Read lines 1-892 exactly once and assess artifact ownership/provenance, company and issue visibility, human/Agent access, star identity, pagination/filter/count semantics, unsafe content paths, lifecycle and test quality."]
    },
    {
      "id": "application:server/services/company-artifacts",
      "path": "server/src/services/company-artifacts.ts",
      "lineCount": 773,
      "sha256": "c4552f005703dd2bb898b4901676b3f1d3a709200fcf34e07f8c18c75e0f1061",
      "linkedEvidence": { "history": ["c111ee4cb3312077ce4df7b128002079345c083e feat(server): add per-user document stars (#9952)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 773 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 mix database/service projection with HTTP route construction, Markdown preview cleanup, storage range reads, media classification, cursor encoding and task hierarchy grouping.",
        "Text storage previews are bounded to 4096 bytes and storage failures degrade to no preview, useful resilience controls. Content type and UTF-8 suitability are trusted from database metadata, errors are silent, and there is no timeout/cancellation or classification redaction.",
        "Cursors are unsigned/unscoped timestamp+ID payloads, not bound to company/query/snapshot. pageByCursor always compares item.updatedAt even when sorting can use an external sortDates map, creating a potential order/cursor mismatch examined in later code.",
        "Ancestor loading is company-scoped and cycles terminate, but different nodes in a cycle can resolve to different roots; hierarchy corruption is silently projected rather than surfaced as an integrity failure.",
        "Lines 251-500 reveal two modes in one service: normal artifacts are Agent-authorship/provider heuristics, while starred mode is any current user's starred document including human plans. A caller-supplied userId selects membership rows; the service itself receives no verified Principal or document ACL.",
        "Document joins are carefully company-bound and exclude hidden/harness issues, system keys and malformed foreign projects/Agents. However createdByAgent coalesces creator and updater, mislabeling an updater as creator, and the model cannot represent human authorship/responsibility.",
        "Document queries select full latestBody for up to 500 grouped items then reduce it to 280 display characters; q also performs ILIKE over full bodies. Grouping caps source rows before aggregation, so large tenants can receive incomplete groups/counts without a truncation contract.",
        "Starred ordering uses membership.starredAt and stores it only in artifactSortDates while returned artifact.updatedAt remains document.updatedAt. Flat paging correctly encodes the map's sort date later, but any generic cursor helper using artifact.updatedAt remains unsafe for starred/grouped combinations.",
        "Lines 501-750 have strong company-bound joins from work products/attachments through issues, projects, runs and Agents. A malformed foreign run yields null Agent attribution rather than cross-tenant identity leakage.",
        "Work-product identity is hardcoded to type=artifact/provider=paperclip and metadata paths are returned as content/open/download URLs. The fallback accepts any string metadata.openPath; there is no allowlisted internal-path/asset capability or provenance verification in this service.",
        "Attachment dedupe queries attachment IDs separately but limits them to sourceFetchLimit without ordering. A backing work product outside that nondeterministic window can reappear as a duplicate direct attachment; the test's narrow fixture does not prove arbitrary-depth correctness.",
        "Text attachment previews launch one storage getObject per fetched attachment in unbounded Promise.all (up to grouped cap 500), each silently swallowing errors. Range size is bounded, but concurrency, latency and aggregate backend pressure are not.",
        "Grouping fetches at most max-limit×10 rows per source before computing counts/groups, with no returned truncation flag. Parent-task selected groups cannot be pushed down by descendant membership and therefore depend on this incomplete global window.",
        "Lines 751-773 page selected artifacts and group summaries entirely in memory. The helper compares returned updatedAt, not artifactSortDates; if the schema permits starred grouping, ordering/cursors are inconsistent. Group updatedAt likewise uses issue/artifact updates rather than starredAt.",
        "The final API returns presentation-rich groups/artifacts but no canonical evidence ID, content digest, producing run/action, approval decision, responsible human, data authorization, integrity state or provenance warning. It is a gallery projection, not Company OS's evidence chain."
      ],
      "completionRequirements": ["Read lines 1-773 exactly once and trace all artifact source branches, joins/tenancy, provenance, content access, filtering/sorting/pagination/counts, authorization boundary, performance and Company OS evidence fit."]
    },
    {
      "id": "application:ui/api/artifacts",
      "path": "ui/src/api/artifacts unit",
      "lineCount": 195,
      "files": [
        { "path": "ui/src/api/artifacts.test.ts", "lineCount": 110, "sha256": "6dd4d96932c8af93aa7c82c456fdba2978f16f125fc1ce0336beacdd9eaab7bd", "completedRanges": [{ "start": 1, "end": 110 }] },
        { "path": "ui/src/api/artifacts.ts", "lineCount": 85, "sha256": "a9342477e37b1d34d9790532d8f2c7f791be31314f800997c3b07e25e8cbd513", "completedRanges": [{ "start": 1, "end": 85 }] }
      ],
      "linkedEvidence": { "history": ["d8e1004551f159d185570ae789710db998acac80 PAP-10440: group artifacts by task stacks (#7654)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 195 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The 110-line API test covers company-scoped endpoint construction, omission/default rules, URLSearchParams encoding for filters/group/cursor, grouped envelopes and a legacy bare-array compatibility normalization.",
        "It does not test companyId path encoding, abort signals, HTTP/stable-code errors, malformed response validation, credential/auth headers, starred mode, unsafe backend paths or removal strategy for the legacy shape.",
        "The 85-line client is thin and uses URLSearchParams for query values, but interpolates companyId into the path without encoding, offers no AbortSignal, stable error mapping or runtime response validation, and imports server-facing shared DTOs directly.",
        "ListArtifactsParams omits the backend's starred mode even though service tests define it. The docstring says Agent-produced outputs while the service's starred contract includes human documents, showing API/domain drift.",
        "Bare-array normalization silently preserves an unspecified legacy contract forever and can mask partial/malformed responses rather than fail with a versioned compatibility signal.",
        "Focused UI Vitest passed 8/8 API client tests. Passing assertions prove serialization/compatibility behavior but not response trust, authorization or error semantics."
      ],
      "completionRequirements": ["Read both files completely exactly once and assess API query serialization, identity/auth assumptions, error/cancellation contract, cursor/group/star semantics and tests."]
    },
    {
      "id": "application:ui/components/artifacts",
      "path": "ui/src/components/artifacts unit",
      "lineCount": 694,
      "files": [
        { "path": "ui/src/components/artifacts/ArtifactCard.test.tsx", "lineCount": 245, "sha256": "c8d713d6df36992c5d8c6eaade41b612cc048e74dbf8b8da2887699a3142bd5d", "completedRanges": [{ "start": 1, "end": 245 }] },
        { "path": "ui/src/components/artifacts/ArtifactGroupCard.test.tsx", "lineCount": 121, "sha256": "c840538d4890c59ea1dbabaf31eea4ad84df7432cbfe419a0df6ccdceb30c634", "completedRanges": [{ "start": 1, "end": 121 }] },
        { "path": "ui/src/components/artifacts/ArtifactCard.tsx", "lineCount": 239, "sha256": "1b77cc2bc23645cb1517ee3f18427c4509bd5c4d13e39346fc1d4a52be7a22e3", "completedRanges": [{ "start": 1, "end": 239 }] },
        { "path": "ui/src/components/artifacts/ArtifactGroupCard.tsx", "lineCount": 89, "sha256": "c98c2b55aebb3a0765a489a3b3dd02689665f30cb2ebb5b0067e947ac41b7e32", "completedRanges": [{ "start": 1, "end": 89 }] }
      ],
      "linkedEvidence": { "history": ["c90e66bdd6a8cdd8de44c1190dd816fed00318e8 feat(ui): design-system component convergence — Card/Badge adoption, multiplicative radius ladder, unified list surfaces (#9240)", "d8e1004551f159d185570ae789710db998acac80 PAP-10440: group artifacts by task stacks (#7654)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 694 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "ArtifactCard test lines 1-200 cover image/video/document/empty previews, issue-anchor links, absolute edited dates, video metadata seek and a 3-second fallback. Tests use SSR markup plus jsdom event behavior.",
        "Many assertions bind Tailwind class strings and English date copy rather than semantic accessibility/responsiveness. This segment does not test unsafe URLs, image/video errors, alt/focus/keyboard, reduced motion, mobile sizing or evidence provenance.",
        "Lines 201-245 verify action omission when paths are absent and fixed title-row layout. They do not test rel/referrer/download behavior when paths exist, creator/responsible-human labels, integrity warnings or permission-denied action states.",
        "ArtifactGroupCard's 121-line test covers decorative stack layers, singular/plural English count, destination, task subject, first image and empty placeholder. It omits semantic heading/link names, keyboard/focus, alt text, unsafe/missing media, truncation/integrity badges, responsive layout and large counts.",
        "ArtifactCard lines 1-200 implement fixed aspect previews, lazy images, video metadata seeking with timer cleanup, plain React text rendering and accessible labels on secondary actions. Media errors fail to placeholders without crashing.",
        "All artifact href/content/open/download strings are trusted as browser URLs. No same-origin/allowed-scheme/capability check prevents a forged work-product metadata path from becoming navigation, download or network media input.",
        "Every video uses preload=metadata and seeks a frame without viewport admission; a grid can issue many concurrent authenticated range requests. There is no reduced-data preference, IntersectionObserver, poster pipeline or resource budget.",
        "Placeholder labels, action titles and media taxonomy are English display literals. The card exposes no provenance-integrity, approval, data classification, responsible-human or permission status.",
        "Lines 201-239 place Open and Download <a> elements inside the outer card Link <a>. Nested interactive anchors are invalid HTML and create ambiguous focus/click/accessibility behavior; stopPropagation does not repair semantics.",
        "Secondary actions are visually hidden until hover/focus-within, which is fragile on touch. The outer link's accessible name also absorbs inner action labels and metadata. Creator display supports Agent only and silently omits human/unverified provenance.",
        "ArtifactGroupCard's 89 lines provide a clear single-link stack with decorative layers, preview, count badge, task identifier/title and date. Unlike ArtifactCard it has no nested interactive elements.",
        "It renders backend count as authoritative with no truncation/integrity indication even though the audited service caps grouping before aggregation. English plural/date strings are hardcoded, and preview media inherits untrusted URL/resource-loading issues from ArtifactPreview.",
        "Focused UI Vitest passed all 15 component tests, but React emitted an explicit runtime warning that <a> cannot be nested inside <a> and this will cause a hydration error. The green suite therefore demonstrates a missing console/error quality gate rather than disproving the defect."
      ],
      "completionRequirements": ["Read all four files completely exactly once and assess content URL safety, XSS/rendering, accessibility/keyboard, responsive behavior, identity/provenance presentation, loading/error handling and test quality."]
    },
    {
      "id": "application:server/__tests__/company-skill-policy-routes",
      "path": "server/src/__tests__/company-skill-policy-routes.test.ts",
      "lineCount": 201,
      "sha256": "7f3496354ec7b42f42986506f0a8578a01bd6934734242c6ec974ea57fafa514",
      "linkedEvidence": { "history": ["89ce36d7afcb3b2920c45c47b55f210d635b9976 feat(skills): open-by-default company skill policy and core UX (#9564)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 201 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "All 201 lines use a real Express route and embedded PostgreSQL. Tests cover Agent read/self-evaluation, unauthenticated and cross-company stable errors, board-only replace/reset/simulation, expectedRevision conflict, denial evaluation, mutation activity and validation of unknown actions/secret-bearing locators.",
        "Board is a synthetic local_implicit instance admin with no company membership, so tests do not cover ordinary accountable-human membership roles, enterprise identity, separation of duties or approval for policy changes.",
        "Reset is expected to return revision 0 and DELETE carries no expectedRevision, permitting ABA/stale-client races after a prior materialized policy is removed. Activity assertions check action/entity only, not actor authority, old/new digest, reason, approval or full decision evidence.",
        "No route tests cover oversized/deep rule sets, duplicate rule IDs/priorities, locator userinfo/fragments/IPv6, concurrent resets, replay/idempotency, human self-evaluation, Agent lifecycle or output-data/tool-capability decisions.",
        "Focused run: all 3 route tests were explicitly skipped by the embedded-PostgreSQL support gate; none passed and no route behavior is claimed on this host."
      ],
      "completionRequirements": ["Read lines 1-201 exactly once and assess route principal/authz, read/write policy, denial codes, validation, audit evidence and test gaps."]
    },
    {
      "id": "application:server/__tests__/company-skill-policy-service",
      "path": "server/src/__tests__/company-skill-policy-service.test.ts",
      "lineCount": 282,
      "sha256": "31638515157224b72e04502be25f959ea0b6559104ca54385ea5945a42dd0233",
      "linkedEvidence": { "history": ["89ce36d7afcb3b2920c45c47b55f210d635b9976 feat(skills): open-by-default company skill policy and core UX (#9564)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 282 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Service test lines 1-200 use real embedded PostgreSQL and assert open-by-default permission for create/import/install/edit/update/test/reset/remove when no policy exists. For Company OS these include supply-chain, execution and deletion actions that cannot share one implicit allow default.",
        "Rules are priority ordered so a lower-number Agent or role allow overrides later broad denies. The test also accepts direct Agent-authored policy replacement at the service boundary and a legacy skills:create grant that can allow skills.edit when no rule matches, requiring careful implementation review.",
        "Good cases include explicit decision reasons/revision, protected resources, role/Agent/source constraints and normalized GitHub locator matching. Principal role is constructed by the caller in this fixture rather than proven from identity/organization state.",
        "Lines 201-282 cover normalized and legacy GitHub locator equivalence, cross-company Agent-principal denial with a stable code, and transactional rollback when required activity persistence fails—strong governance regression patterns.",
        "Missing cases include concurrent expectedRevision races, duplicate priorities/IDs, deny-overrides versus first-match ambiguity, human principals, Agent lifecycle/role changes, source digest/signature, capability/data classifications, policy expiry/delegation, separation of duties and high-risk approval binding.",
        "Focused run: all 7 service tests were explicitly skipped by the embedded-PostgreSQL support gate; none passed and no database behavior is claimed on this host."
      ],
      "completionRequirements": ["Read lines 1-282 exactly once and assess policy defaults, hierarchy/precedence, persistence/concurrency, principal and scope semantics, decisions/reasons and regression quality."]
    },
    {
      "id": "application:server/routes/company-skill-policy",
      "path": "server/src/routes/company-skill-policy.ts",
      "lineCount": 137,
      "sha256": "cf25bcc64777dfb92c0519db5534d73a6eef0d09c6e83d6dba637451c7f7fc2e",
      "linkedEvidence": { "history": ["89ce36d7afcb3b2920c45c47b55f210d635b9976 feat(skills): open-by-default company skill policy and core UX (#9564)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 137 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "The route provides stable auth/company/admin/validation codes and resolves Agent principals from database state before simulation/evaluation. Board membership authority flows through accessService, but local_implicit/instance admins bypass company-scoped permission checks.",
        "Any Agent holding users:manage_permissions can replace/reset the executable Skill policy, potentially including policy governing its own create/import/install/edit/remove actions. Company OS requires accountable-human ownership, separation of duties and high-risk approval rather than Agent self-governance through a broad user-permission key.",
        "PUT and POST run Zod body validation before authentication/company/admin checks. Unauthenticated or cross-tenant callers can force rule/resource parsing and receive schema validation behavior before deny; the established deny-before-parse boundary is absent.",
        "Every company actor can GET the full policy, exposing protected skill IDs, Agent IDs, roles and internal source locators beyond a minimal self-decision interface. Evaluation decisions are not activity/evidence logged.",
        "DELETE has no expectedRevision/idempotency input. Board principal falls back to synthetic id=board, and mutation activity lacks request/policy digest, reason, approval and destination responsibility contract."
      ],
      "completionRequirements": ["Read lines 1-137 exactly once and trace transport authentication, company binding, write authority, actor/audit mapping, validation/errors and dependency direction."]
    },
    {
      "id": "application:server/services/company-skill-policy",
      "path": "server/src/services/company-skill-policy.ts",
      "lineCount": 292,
      "sha256": "f69d74b2ab2787aa37db3e23675ff1f44dde75fdf65eb58aa16b17ff1df6098f",
      "linkedEvidence": { "history": ["89ce36d7afcb3b2920c45c47b55f210d635b9976 feat(skills): open-by-default company skill policy and core UX (#9564)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 292 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-200 define a compact first-match rule engine with stable decision reasons, revisions, normalized roles/source types/locators and same-company Agent resolution. Explicit sorting makes outcomes deterministic.",
        "The absence of a policy returns implicit allow for every action. A materialized default-deny policy can still grant every action through legacy skills:create or skills:suggest-changes, including install/update/reset/remove; this compatibility escalation is broader than the permission names imply.",
        "Rules select only skill ID/key, coarse source type and locator. They cannot express publisher/signature/digest, code capability, data access/egress, tool scope, environment, responsible human, approval/risk level, expiry or test/certification result.",
        "Decision output omits principal/resource/policy digest and is not an immutable evidence record. The service trusts caller-supplied board principal/role and get() returns open default even for a nonexistent company.",
        "First matching priority wins regardless of effect, so a high-priority allow can override a broad deny. This is deterministic but lacks deny-overrides or conflict diagnostics and makes policy ordering safety-critical.",
        "Lines 201-292 implement sound compare-and-swap replacement inside one transaction and require activity persistence before commit. Concurrent update/insert conflicts are handled without lost update.",
        "Reset deletes the canonical row with no expected revision and returns revision 0. This creates ABA semantics: a client holding expectedRevision=0 from before a create/reset cycle can write as if no intervening policy existed.",
        "Reset erases the actual rules and activity stores only revision/default/count summaries, so historical authorization decisions cannot be reconstructed or cryptographically bound. Caller-supplied activity identity is not verified at the service boundary.",
        "Evaluation has no decision logging/cache/version lease; a skill operation can be authorized at revision N and execute after policy N+1 without binding the action digest to the evaluated revision."
      ],
      "completionRequirements": ["Read lines 1-292 exactly once and trace policy model/defaults, database lifecycle, authorization assumptions, evaluation scope, auditability, concurrency and Company OS data/tool governance fit."]
    },
    {
      "id": "application:server/__tests__/company-skills-detail",
      "path": "server/src/__tests__/company-skills-detail.test.ts",
      "lineCount": 232,
      "sha256": "ead8f0fc444a89e585c05ef01ff50c6528502592e04a48de75c98de6ac292895",
      "linkedEvidence": { "history": ["35a9dc37b00574c46e31fbd624c49ba1bc5de858 [codex] Speed up company skill detail loading (#4380)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 232 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Both tests target detail-load performance and truthfulness: Agent usage is derived from desired configuration without invoking potentially hanging adapter listSkills, and actualState remains null instead of being fabricated.",
        "A proxy asserts usage resolution never performs implicit SELECT * from companySkills, preventing unrelated 32KB markdown payloads from being loaded. Desired Skill matching covers canonical key and slug compatibility.",
        "The 1-second Promise.race guards hangs but does not cancel underlying work. Tests create local directories/files even though their relevance to detail usage is unclear, and validate implementation query shape through a Drizzle proxy rather than a database query/byte budget.",
        "Missing coverage includes cross-company ID lookup, not-found, large Agent counts/N+1, duplicate slug/key ambiguity, terminated Agents, secret/source locator redaction, test/policy/version/provenance projection, filesystem disappearance and responsible-human/data authority.",
        "Focused verbose Vitest: both embedded-PostgreSQL tests were explicitly skipped after the database failed to start following five attempts; 0 passed and no behavior is claimed on this host."
      ],
      "completionRequirements": ["Read lines 1-232 exactly once and assess detail query batching/performance, company/provenance isolation, source/version/test/policy visibility, missing/notfound semantics and regression quality."]
    },
    {
      "id": "application:server/__tests__/company-skills",
      "path": "server/src/__tests__/company-skills.test.ts",
      "lineCount": 433,
      "sha256": "5673ab57ce4c16bac18f0183ebbc3e853cb5af1e5d34d056a1cd9ec208d70074",
      "linkedEvidence": { "history": ["717684ad8f5f33fd3b6e8cf7a96aa7062d8ff29b Add project folder browsing to skill imports (#9930)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 433 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-250 are pure/local-filesystem tests for source parsing, GitHub/skills.sh normalization, secret-bearing URL rejection without echo, supported workspace discovery, bounded root inventory, symlink rejection and frontmatter metadata parsing.",
        "Strong security patterns include no shell execution, no query/fragment credentials, canonical remote locators, explicit support folders and rejecting reachable symlinks. Missing here are shell-metacharacter/quoted command adversaries, non-GitHub hosts/schemes, Unicode/path normalization, hard links, TOCTOU swaps, device/FIFO files and size/depth/count bombs.",
        "Project-root import deliberately includes scripts and assets, turning local discovery into executable supply-chain admission. Tests prove file selection but not human approval, publisher/digest, code capability, data/egress scope or sandbox requirements.",
        "Lines 251-433 cover custom YAML folded/literal/chomping behavior, explicitly selected non-standard directories and reconciliation when a local directory/SKILL.md disappears.",
        "Explicit selected paths have no adversarial tests for ../ escape, absolute paths, Unicode separators, symlinked parents or selection outside workspace. YAML cases omit duplicate/reserved keys, anchors/tags, deep nesting, huge scalars and prototype pollution.",
        "Missing-source reconciliation is useful honest-state behavior but only returns IDs; tests do not require observedAt, reason/evidence, affected Agents, automatic execution pause or accountable-human remediation.",
        "Focused isolated run: 17/19 passed and 2 discovery tests failed because implementation returns macOS realpath /private/var/... while expectations use path.resolve /var/.... This proves path identity/expectation is not canonicalized consistently across filesystem aliases. The initial non-isolated run could not import because logger attempted ~/.paperclip; final result uses isolated PAPERCLIP_HOME."
      ],
      "completionRequirements": ["Read lines 1-433 exactly once and assess Skill import/source/path parsing, filesystem/network trust, collision/version/provenance, tenant context, safety limits and test quality."]
    },
    {
      "id": "application:server/services/company-skills",
      "path": "server/src/services/company-skills.ts",
      "lineCount": 6956,
      "sha256": "98b1a08f7ac661c47bd0b24d409be8f21f0be0557286158cb19097bb06216a89",
      "linkedEvidence": { "history": ["5da382fd59222819cff110f85ca738a74fb335d1 feat(skills): require explicit merge modes (#10978)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 6956 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 are import surface only. The service directly couples filesystem and hashing, Drizzle/database tables, adapter-utils preference persistence and a very broad shared Skill/API type vocabulary; this is already a high-responsibility orchestration module rather than a narrow domain service.",
        "The earlier attempt to display lines 1-250 was truncated by the tool and is not counted as reviewed evidence; lines are credited only as the smaller ranges become fully visible.",
        "Lines 101-200 add direct dependencies on instance-root resolution, HTTP error construction, GitHub transport, Agents, Projects, Folders, Documents, Work Products, catalog/provenance and portable-path services. Persistence rows are reshaped through large hand-maintained Pick projections, increasing schema/API drift risk and confirming this unit owns many unrelated lifecycles.",
        "Lines 201-300 define import/persist DTOs and an explicit replace/rename/skip conflict policy. External GitHub/skills.sh sources must pin a 40-hex commit and any external source classified scripts_executables is rejected, which is a strong supply-chain default; URL sources are exempt from commit pinning and the check relies on upstream trust-level classification.",
        "Lines 301-400 reserve bundled Paperclip keys, define extensive provenance/update/audit metadata, explicit missing-local-source markers, workspace scan targets, runtime materialization/version options and a coarse Agent/User/System actor. Provenance is stored in an open metadata record rather than a typed immutable lifecycle; the actor shape has identity IDs but no accountable-human or authority/evidence binding.",
        "Lines 401-500 introduce a process-local promise map for inventory refresh deduplication, a full-column Skill selector, a broad allowlist of agent-tool project directories, and project-root inclusion of references/scripts/assets. Catalog file size is capped at 1 MiB, but no aggregate archive/directory limits appear in this segment. Process-local coordination cannot by itself serialize multiple server replicas.",
        "Lines 501-600 normalize package paths and identity keys, compute deterministic inventory hashes, derive collision suffixes and runtime names, and begin a byte-preserving frontmatter-name rewrite. Hash-based runtime suffixes use only 10 hex characters (naming, not integrity), while unique key/slug generation consults caller-maintained sets and therefore still needs database uniqueness/concurrency handling later.",
        "Lines 601-700 derive canonical keys from flexible frontmatter/metadata and source identity, then classify inventory files by directory/extension. Local and URL identities embed a 10-hex locator hash, tying identity to machine paths or URLs and allowing theoretical collisions; script detection is extension/list based and omits executables without recognized suffixes, shebang/content analysis and several executable ecosystems.",
        "Lines 701-800 derive trust entirely from inventory classification, wrap GitHub fetches, resolve default branches/commits and parse/normalize remote URLs. HTTPS and credential/query/fragment rejection are good; remote error messages include full URLs, branch fallback silently assumes main, and parsing accepts arbitrary hostnames as GitHub-shaped pending later checks. Trust remains file-type risk classification, not publisher/content/integrity trust.",
        "Lines 801-900 pin symbolic refs, tokenize a narrow npx skills add syntax without executing it, and canonicalize registry/key imports to GitHub. The parser intentionally ignores most command tokens, which is safer than shelling out, but quote handling is simplistic and warnings remain unused in this segment. Skills.sh is treated as discovery provenance while GitHub becomes the fetched source.",
        "Lines 901-1000 enforce HTTPS for explicit schemes, detect generic Git-like sources, locate bundled roots through several runtime-relative candidates and start deriving source provenance from frontmatter metadata. Generic repository detection accepts any HTTPS host/path as a Git repo; provenance fields are accepted from untrusted skill metadata and require later verification before they can be authoritative.",
        "Lines 1001-1100 finish URL/catalog provenance derivation, parse inline package Skills, assign every co-located file under each Skill directory to its inventory and recursively walk local directories while skipping only .git and node_modules. Inline inventory contains path/kind but no size/digest here; recursive traversal has no visible depth/file-count/byte limit and follows directory entries based on Dirent classification, requiring later symlink and containment checks.",
        "Lines 1101-1200 implement lexical plus realpath containment, reject symlinks along the selected path and recursively in admitted trees, require a regular SKILL.md, and produce sanitized user-facing scan failures. This is careful workspace escape prevention. Remaining concerns include TOCTOU between validation/read, hard links/mount aliases, unbounded traversal, and error strings embedding absolute local paths before the sanitized scan wrapper.",
        "Lines 1201-1300 collect path/kind-only local inventories, compare them without content hashes, provide deterministic JSON comparison, and begin bundled-skill folder/metadata normalization. Inventory equality cannot detect content-only changes, so it is unsuitable as integrity or update evidence unless a later stage refreshes digests.",
        "Lines 1301-1400 compare nearly every persisted field to avoid unnecessary updates and begin local-directory import. When workspaceRoot is absent, the import skips containment/symlink validation entirely; it reads SKILL.md before collecting inventory and overlays caller metadata after parsed metadata, enabling trusted caller context to override sourceKind but not providing a content snapshot or atomic read.",
        "Lines 1401-1500 discover root, explicit and known-tool Skill directories. Explicit paths are realpathed and containment checked; automatic directory roots, however, accept symlinked entries whose targets are directories and add their resolved lexical path before later validation. Selection normalization rejects absolute and dot segments, and the NUL-delimited workspace/path key avoids simple concatenation ambiguity.",
        "Lines 1501-1600 import arbitrary local files/directories and begin remote repository ingestion. Direct local imports again lack workspace containment and symlink validation, recurse without bounds, and derive each nested Skill inventory by prefix, so a parent Skill can absorb nested Skills/files. GitHub ingestion requests the entire recursive tree before filtering, with no truncation flag, entry-count or repository-size handling visible yet.",
        "Lines 1601-1700 fetch only each SKILL.md body while inventories claim every repository blob under its directory. Thus trust classification can see script paths, but file content/digests are not fetched or verified at import. Generic HTTPS markdown imports become markdown-only single-file Skills with no immutable sourceRef, publisher proof or response size/content-type controls visible here.",
        "Lines 1701-1800 map permissive database JSON/enums to API models and normalize sharing/categories. Inventory entry kinds and several enums are type-cast without runtime allowlisting; unknown sharing values default to company, which is a fail-open visibility choice. Public-link writes are disabled even though rows/tokens remain modeled.",
        "Lines 1801-1900 normalize storefront metadata with length caps and serialize full version file contents into database JSON. URL-like icon/homepage fields are length-limited but not scheme/host validated here. Version inventories preserve content but still type-cast kind/path values; this creates a potentially large database payload and needs total-size limits and secret scanning elsewhere.",
        "Lines 1901-2000 define a built-in natural-language test harness that asks an Agent not to cause durable/external effects, plus a constrained placeholder renderer and coarse run-status normalization. Prompt instructions are not a security boundary: no sandbox, capability denial, data egress rule or accountable-human approval is established here, so the claim of a read-only/default-safe harness must depend on later runtime enforcement.",
        "Lines 2001-2100 project test-run state/cost, compare version snapshots by sorted JSON, model missing local sources in mutable metadata and begin reference resolution. Unknown stored run states silently become queued, which can misrepresent corruption/new states; missing-source detectedAt is sensibly preserved across repeat reconciliation but is not an immutable event.",
        "Lines 2101-2200 resolve requested Skills by stable ID, normalized key or unique slug, report ambiguity explicitly, normalize desired version selection and verify a version belongs to both company and Skill. This is a sound tenant/version binding check, though returned selection order is deduplicated by Set and error details expose supplied identifiers.",
        "Lines 2201-2300 optionally preserve stale unknown desired keys while always rejecting ambiguous slugs, then map Agent config preferences to canonical keys/version entries. This honest stale-reference behavior is useful for operator remediation. Resolution performs one database query per selected version, creating an avoidable N+1 path for larger Agent skill sets.",
        "Lines 2301-2400 resolve runtime/local paths, detect missing sources sequentially, scope managed rename targets to a direct company-owned directory and begin byte-level audit collection. Error detail exposes absolute source paths, missing checks are sequential filesystem I/O, and resolveLocalSkillFilePath does not itself prove the normalized child remains within the Skill root—safety depends on normalizePortablePath semantics and callers.",
        "Lines 2401-2500 recursively read every regular file into memory, reject symlinks/path escapes, perform a basic binary heuristic and extract markdown links with regex before starting installed-byte audit. There are still no traversal depth/file-count/per-file/aggregate-byte caps; lstat size is recorded before read, so TOCTOU and memory exhaustion remain possible. Markdown-link regex is intentionally shallow and not a full parser.",
        "Lines 2501-2600 compute full installed content hashes, compare recorded paths, validate basic frontmatter and scan text using regex heuristics for remote execution, secret exfiltration, network and secret references. This is useful defense-in-depth but not a sandbox or robust static analysis: patterns have easy false negatives/positives, oversized files are read before rejection, assets bypass content scanning, and warnings do not establish approval/data-egress policy.",
        "Lines 2601-2700 infer display languages and source editability. Remote/bundled sources are read-only while local/project files are editable. Managed-root detection uses string startsWith rather than path containment, so a sibling such as <managedRoot>-evil can be misclassified editable/Paperclip-owned; source labels expose local paths and editability is a UI/service projection, not authorization.",
        "Lines 2701-2800 enrich list/detail/fork projections and infer divergence from version count or mutable userModifiedAt metadata. Fork authorship is actor-relative but stored in metadata, while summaries expose source locators/tokens upstream. Divergence is a heuristic, not a verified three-way comparison to origin/current upstream.",
        "Lines 2801-2900 finish list projections, batch last-editor lookup with company-scoped Agent join, construct the large service and begin local-source authorization. Last editor can be a User or Agent but has no accountable human/role context; the query is sensibly batched and deterministically ordered.",
        "Lines 2901-3000 complete approved-root authorization using canonical paths, silently swallow bundled import errors, validate release-manifest containment and start materializing complete version files. The approved-root check is strong for the explicit import service path. Bundled failures returning an empty set hide corruption/packaging faults; version collection reads every file as UTF-8 without limits or binary handling.",
        "Lines 3001-3100 verify immutable bundled release snapshots by content hash, create missing versions, reconcile category folders and begin local-source reconciliation. Release integrity checking is strong after seeding but has no signature/publisher proof. Folder and source reconciliation execute many per-item updates/filesystem scans without a transaction or batching.",
        "Lines 3101-3200 mark missing-but-used local Skills for remediation, but automatically delete missing-and-unused library rows plus runtime materializations during ordinary inventory refresh. This destructive lifecycle is triggered by list/read paths, has no grace period, approval, tombstone or audit event, and can race multi-replica refreshes because deduplication is process-local. Every list also runs company existence, bundled release/folder and filesystem reconciliation before querying.",
        "Lines 3201-3300 implement all search/filter/category/attachment counting/sorting in memory after loading every Skill, folder and Agent. Attached counts recompute desired Skill resolution for every Skill×Agent pair, making the list O(S×A×S-reference-resolution) and unpaginated. Search intentionally overrides folder filtering, a subtle UI semantic encoded in service logic.",
        "Lines 3301-3400 provide company-scoped ID/key/unique-slug/version reads, folder enrichment, actor-relative stars and generic metadata patching. Most tenant predicates are sound, but updateSkillMetadata updates only by Skill ID and trusts the already-loaded object; it has no optimistic revision and can lose concurrent metadata fields despite merge-on-stale-object behavior.",
        "Lines 3401-3500 persist audit/update-hold metadata, compute desired-only Agent usage, count versions and assemble detail/fork prechecks. Audit is limited to local/catalog sources and mutates metadata after scan without CAS. Existing fork summaries issue one count query per fork (N+1); usage deliberately avoids runtime truth, so desired and actual deployment remain distinct.",
        "Lines 3501-3600 snapshot version contents via one read per inventory entry and create monotonically numbered versions inside a row-lock transaction. Row locking plus company predicates is a solid concurrency pattern for revision allocation/current pointer updates. Snapshot collection happens before the transaction and is not atomic with mutable files; actor IDs are recorded but authority/responsibility/evidence are not.",
        "Lines 3601-3700 implement star counters and threaded comments. Stars use check-then-insert and separately recompute denormalized counts, so correctness relies on a database uniqueness constraint and concurrent refreshes can write stale counts. Any board User is considered able to mutate any comment at this service layer, while Agents may edit only their own; role/permission enforcement must exist outside.",
        "Lines 3701-3800 soft-delete comments and plan fork reassignments. Comment mutation is company/Skill scoped but has no optimistic version/history preservation for edits. Reassignment loads terminated Agents too, verifies company and current desired use, and reports precise failures; it does not yet establish accountable approval for changing Agent capabilities.",
        "Lines 3801-3900 apply Agent reassignments under row locks in one database transaction and define compensating cleanup for failed forks. Transactional config changes are strong, but selected pinned versions are reset to null on fork. Cleanup commits database deletion before filesystem deletion; a filesystem failure leaves orphaned bytes, and neither reassignment nor cleanup emits an immutable activity/evidence record here.",
        "Lines 3901-4000 implement a non-transactional filesystem→database→version→Agent fork saga. It force-deletes the chosen fork directory before creation, copies text-only file detail, mutates counts separately and only compensates reassignment failures—not earlier partial failures. Slug/key uniqueness is based on a stale in-memory snapshot, so concurrent forks can target the same directory; robust DB uniqueness alone cannot protect bytes already overwritten.",
        "Lines 4001-4100 begin managed rename with no-op detection, in-memory conflict checks, directory existence guard, preserved version pins and a reversible filesystem-first stage followed by a DB transaction. The design explicitly plans compensation and is stronger than fork, but conflict checks remain race-prone and absolute directory paths leak in errors.",
        "Lines 4101-4200 complete rename compensation, purge stale runtime materialization and begin update-status computation. Rollback failures are swallowed, so a failed DB transaction can still leave filesystem identity inconsistent; post-commit runtime deletion failure surfaces after durable rename. Update status reruns the full byte audit and uses catalog content hashes, correctly separating supported/unsupported tracking states.",
        "Lines 4201-4300 resolve GitHub update refs and implement inventory-gated file reads across local/catalog/GitHub/URL sources. Remote files are fetched lazily from the pinned ref, which is good, but local path containment still depends on normalized inventory entries and files are decoded as UTF-8 regardless of asset/binary kind. Missing remote SKILL.md falls back to the stored body, making availability resilient but potentially hiding source drift/outage.",
        "Lines 4301-4400 update storefront fields without optimistic concurrency and begin managed Skill creation/fork copying. Creation checks database key then force-deletes the target directory, repeating the fork race/data-loss hazard: concurrent or orphaned directory state can be destroyed before authoritative uniqueness is established. URL fields still lack scheme validation, and copied assets are treated as UTF-8 text.",
        "Lines 4401-4500 finish creation through several non-atomic filesystem/DB/count/version steps and begin direct file editing. A failure can leave a directory, Skill row, counters or missing initial version out of sync; there is no compensation. Editable is derived metadata, then arbitrary normalized paths are written with no content/size/type/risk approval or expected-content revision at this point.",
        "Lines 4501-4600 update metadata after a file write, create a version only when content changed, and implement file/folder deletion followed by version creation. Both are filesystem-first non-atomic sagas: DB/version failure leaves unversioned changes, and concurrent edits have no expected hash/revision. Internal updates filter only by Skill ID. Deletion protects SKILL.md but does not capture deleted bytes as explicit evidence beyond the post-delete snapshot.",
        "Lines 4601-4700 gate updates on audit findings/local modifications, allow force-discard, audit a catalog origin snapshot, copy it into the live managed directory and then update DB metadata. Preflight audit is valuable, but copy→DB→post-audit is non-atomic; a failed post-audit leaves the new bytes and DB state installed, merely marks failure, and force has no responsibility/approval record here.",
        "Lines 4701-4800 re-import remote updates and implement catalog reset from a pinned, hash-verified origin snapshot. Origin verification and explicit force requirement are strong recovery safeguards. Reset still copies over the live directory before DB update and records no pre-reset immutable version/evidence in this segment, so database failure can leave untracked destructive restoration.",
        "Lines 4801-4900 post-audit reset, browse a company-scoped local project workspace and begin scan orchestration. Browse uses realpath containment, excludes symlinks/.git/node_modules and caps output at 250. It slices filesystem enumeration before sorting, so which entries are omitted is platform/order dependent; per-directory isSkill checks also cause up to 250 sequential stats.",
        "Lines 4901-5000 normalize selective scan requests, gather company project/workspace targets and start sequential discovery. Invalid selections are retained for later reporting, and company scoping comes through Projects. Warnings expose configured absolute workspace paths, all projects/workspaces are traversed serially, and remote_managed exclusion used by browse is not visible in scan target construction.",
        "Lines 5001-5100 validate each discovered directory with workspaceRoot, sanitize import errors, recognize same-path/bundled duplicates and allow explicit renaming. Same-source refresh preserves the existing key/slug, which is stable. Candidate/skipped outputs still expose directory paths; selected renamed keys use only a short path hash and are machine-location-derived.",
        "Lines 5101-5200 detect key/slug conflicts against the evolving accepted set and update same-source entries. Conflict reporting is explicit and prevents silent overwrite, but returns both existing/new absolute source locators. These are application-level checks; concurrent scans in different processes still require database constraints/transactional conflict handling.",
        "Lines 5201-5300 finish scan results, materialize catalog package files and begin an atomic-directory-replacement helper. Scan reporting is comprehensive. Catalog materialization still force-deletes its live directory and writes string-only files before DB coordination; target path safety depends on portable-path normalization. The staging helper signals a better replacement strategy used later.",
        "Lines 5301-5400 implement same-parent staging/rename/rollback and use it for manifest installs, immutable origin snapshots and byte-preserving directory copies with explicit containment. This is a strong reusable filesystem primitive. Cleanup of the previous directory occurs after the new target is live and can throw, surfacing failure despite successful replacement; rollback restoration errors are swallowed, and there is still no cross-database transaction.",
        "Lines 5401-5500 build catalog provenance, enforce compatibility, construct a synthetic company-scoped audit object and begin catalog install with key/slug conflict checks. Provenance retains mutable prior userModified/updateHold metadata by default, which is useful continuity but not immutable history. The synthetic audit identity is random/transient and carries no publisher signature or responsible actor.",
        "Lines 5501-5600 handle unchanged/local-modified installs, pre-audit a pinned origin, atomically materialize bytes and prepare persistence values. Re-audit before replacement is good. Force can discard locally modified catalog state without an approval/evidence contract. Materialization occurs before DB persistence; failures after this try block can leave new live bytes, and cleanup may remove a deterministic origin snapshot that pre-existed.",
        "Lines 5601-5700 persist catalog installation, post-audit it and begin runtime/version materialization. Post-audit failure leaves the installed row/bytes in place. Runtime materialization force-deletes the live directory, silently skips unreadable inventory files, writes all content as UTF-8 and only requires SKILL.md; partial Skills can therefore run. It does not use the safer atomic replacement helper.",
        "Lines 5701-5800 compare cached version snapshots byte-for-string, rematerialize non-matching snapshots, resolve pinned/live runtime sources and begin runtime entries. Version containment and existence checks are good, but replacement is force-delete/non-atomic; errors are caught and collapsed to null, so a selected version can disappear from runtime output without structured failure evidence. All files remain string snapshots.",
        "Lines 5801-5900 project deterministic runtime entries and begin package import with mandatory reserved-key/source checks plus explicit skip/replace/rename policy. Safety validation before conflict handling is good. Catalog files are materialized before deciding skip/replace, so even a nominal skip may mutate/delete a deterministic materialized directory; conflict decisions use an in-memory snapshot and are not yet atomic.",
        "Lines 5901-6000 finish deterministic conflict results and begin sequential upsert. Provenance of renamed imports is retained. Upsert revalidates safety, protects bundled Paperclip keys from matching upstream GitHub imports and assigns bundled/project folders. It performs per-Skill reads/folder writes sequentially and trusts incoming frontmatter/store metadata after only normalization.",
        "Lines 6001-6100 complete non-transactional per-Skill upsert, enforce approved roots for local source import, normalize skills.sh provenance and begin test-input CRUD. Imports can partially persist before a later Skill fails; updates use ID-only predicates after a company-scoped lookup and have no CAS. The explicit local import entry does correctly invoke the approved workspace-root gate.",
        "Lines 6101-6200 implement company/Skill-scoped soft-deletable test inputs and company-scoped test templates with placeholder validation and actor attribution. Test input createdBy collapses User and Agent IDs into one untyped field, making principal interpretation ambiguous unless the schema adds type elsewhere. CRUD has no ownership/role checks or revisions at service level.",
        "Lines 6201-6300 finish template CRUD and snapshot resolution, then begin run-version capture. Built-in templates are protected and named template lookup is company scoped. A caller-supplied templateSnapshot bypasses database existence/ownership and is accepted as authoritative after placeholder syntax validation, so provenance must explicitly distinguish client snapshot from persisted template.",
        "Lines 6301-6400 auto-version current files, snapshot Agent configuration, aggregate run costs and begin test-run creation. Critical for Company OS evaluation: snapshotAgentConfig copies the full adapterConfig and runtimeConfig into a persisted run snapshot with no redaction/allowlist. If these records can contain credentials, private session data or sensitive endpoints, this violates the required secret-free control-plane/runtime-proof boundary.",
        "Lines 6401-6500 bind company/Skill/input/version/Agent, snapshot rendered harness inputs, create an Issue before a DB transaction, supersede prior runs for the same input identity and insert the queued run. IDs/fingerprints are explicit and cleanup is prepared. Superseding groups all ad-hoc (null inputId) runs for a Skill regardless of Agent/version, and persisted harness/config text can contain sensitive original input/output context.",
        "Lines 6501-6600 wake the harness after commit, list runs and assemble detail from Issue/Documents/Interactions/Attachments/Work Products. If wake fails, the new run remains queued while prior runs are already cancelled and the Issue remains; no compensation/retry state is visible here. Run listing is unpaginated and returns full snapshots/configs, increasing sensitive-data exposure and response cost.",
        "Lines 6601-6700 construct a rich, company-scoped evidence projection with immutable version, documents, attachments (including SHA), work products, interactions and explicit harness unavailable reasons. This is a strong reference for responsibility/evidence UX, but it remains task-output aggregation: it does not bind permissions/data used, approvals or accountable human to a signed responsibility record.",
        "Lines 6701-6800 complete runs from Issue outcome/output documents, atomically claim queued→running and cancel through the external harness. The queued→running predicate is sound. Completion uses select-then-update without a current-status/superseded predicate, so concurrent terminal outcomes can overwrite one another; cancel performs the external action first, and DB completion failure leaves Issue/run state inconsistent.",
        "Lines 6801-6900 soft-delete terminal runs, best-effort hide harness Issues, prune expired harnesses and hard-delete unused Skills. Retention preserves run snapshots while hiding task content, a useful pattern. Pruning is sequential and reports selected rather than successfully changed count. Skill deletion checks desired Agent use but hard-deletes DB first, then runtime bytes; it does not remove managed source/version snapshot directories explicitly or emit tombstone/evidence.",
        "Lines 6901-6956 expose the service's full API surface: resolution, catalog/list/detail, versioning, social features, fork/rename/edit/delete, test harness lifecycle, imports/scans, audit/update/reset and runtime materialization. The breadth confirms a 6,956-line application god-service spanning at least storage, filesystem, supply chain, policy-adjacent mutation, runtime projection and test orchestration."
      ],
      "completionRequirements": ["Read lines 1-6956 exactly once and trace all local/remote/catalog/generated Skill inputs, path and archive safety, parsing/inventory/digests, database lifecycle/version/collision/merge, policy/identity/approval, Agent runtime sync/test/install/update/remove, secrets/network/process boundaries, audit/evidence, concurrency/rollback, performance and Company OS fit against completed tests."]
    },
    {
      "id": "application:server/__tests__/company-skill-test-runs-service",
      "path": "server/src/__tests__/company-skill-test-runs-service.test.ts",
      "lineCount": 753,
      "sha256": "b754a3044919475440a766e2dfadbf53e9b19650dbea9baf60dc5ceb3f1b922e",
      "linkedEvidence": { "history": ["b13eb5b2b5cc7ad721f96b914c55dad9e66c5f81 Skill Studio: three-pane skill IDE with sandboxed test runs (#9241)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 753 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 set up an embedded-Postgres integration suite with real temporary Skill files and direct service calls. It conditionally skips the entire suite when the embedded database is unsupported, thoroughly deletes database fixtures between tests and seeds a company, idle Codex Agent and markdown-only local Skill.",
        "Lines 101-200 verify cleanup when DB run persistence fails, built-in template rendering and explicit no-template behavior. The failure is induced with NaN retention and proves pre-commit Issue cleanup plus no wake call. It does not cover a wake failure after the run transaction commits. Template assertions validate prompt composition, not sandbox or capability enforcement.",
        "Lines 201-300 cover custom template CRUD, allowed placeholders, read-only built-in behavior and reject unknown/cross-company persisted template IDs. Tenant binding is well tested. Service-level authorization roles, template content/size abuse and the caller-supplied templateSnapshot bypass are not tested in this segment.",
        "Lines 301-400 deliberately prove re-runs can supply the prior templateSnapshot after the persisted template changes, plus terminal-only soft deletion and explicit Skill-version pinning. Reproducibility is strong, but the test normalizes caller-provided snapshot authority without proving it came from the viewed run; arbitrary template IDs/names/bodies can therefore masquerade as historical provenance unless the route binds them.",
        "Lines 401-500 prove superseded runs reject later start/complete/cancel transitions, remain viewable until soft deletion and preserve an explicit cancellation reason. This is a useful stale-event guard. It also confirms every later ad-hoc run with null inputId supersedes the prior one even when the content differs, rather than grouping by an input/content fingerprint.",
        "Lines 501-600 verify input/version/config/output snapshots survive zero-day harness retention while ephemeral Issue content becomes unavailable with an explicit expired reason. This is a strong retention projection. The config assertion positively expects adapter/instructions metadata but never seeds a secret or asserts redaction; because it uses objectContaining, full adapter/runtime configs can leak while the test remains green.",
        "Lines 601-700 seed documents, SHA-bearing attachments and work products for two harness Issues, then assert rich detail contains only the requested run's Issue content. The fixture meaningfully tests Issue scoping and metadata shape. It does not yet challenge cross-company foreign IDs, authorization/redaction, malicious metadata/content or signed evidence provenance.",
        "Lines 701-753 finish compatibility artifact assertions, prove cross-company run detail isolation and distinguish externally deleted harness content from retained run snapshots. The suite has no test for real execution isolation, data/tool/egress denial, high-risk approval, accountable human, secret-free attestation, wake/cancel failure compensation, concurrent completion, pagination or cleanup idempotency under races.",
        "Focused Vitest was attempted in the sandbox, then twice outside it with fresh isolated PAPERCLIP_HOME directories. The sandbox run skipped because listen on 127.0.0.1 was EPERM; both escalated runs skipped all 12 tests after embedded PostgreSQL failed to start following five attempts. 0 tests executed and no runtime pass is claimed."
      ],
      "completionRequirements": ["Read lines 1-753 exactly once, run the focused suite in an isolated Paperclip home, and assess version/input/template/config snapshotting, Issue creation/wake/cancel/cleanup, status concurrency, retention, evidence projection, secret handling and whether sandbox claims are enforced or prompt-only."]
    },
    {
      "id": "application:server/__tests__/company-skills-service",
      "path": "server/src/__tests__/company-skills-service.test.ts",
      "lineCount": 3017,
      "sha256": "01110092d63f084cdf01c0e8bab410e5188adb86308e0c7dd0a896582bbaa5ee",
      "linkedEvidence": { "history": ["5da382fd59222819cff110f85ca738a74fb335d1 feat(skills): require explicit merge modes (#10978)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 3017 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 establish a 3,017-line embedded-Postgres/real-filesystem integration suite, isolate PAPERCLIP_HOME and instance ID, clean Company/Agent/Skill/Project/Folder/User fixtures, and begin a list-projection test. The suite is entirely skipped when embedded PostgreSQL support fails.",
        "Lines 101-200 prove a 250KB markdown body is omitted from list results while identifiers, source locator, attachment count and editability remain. This guards response size but still intentionally exposes absolute local source paths. The next test seeds User/Agent/unattributed/versionless editor identities.",
        "Lines 201-300 verify lastEditor is opt-in, selects the latest version author, distinguishes User/Agent and represents unattributed/versionless Skills as null. This is a useful bounded projection, but no accountable-human role or edit authorization/evidence is asserted. The suite then begins a missing-company refresh test.",
        "Lines 301-400 assert missing-company 404, unchanged bundled refresh does not retouch timestamps, bundled releases seed idempotently and a pinned champion snapshot has exact file hashes and materializes byte-for-byte. This is strong reproducibility coverage for shipped assets, though it is tied to Paperclip-specific release IDs/content and does not test signatures or tampering/crash recovery.",
        "Lines 401-500 test repair of a user-squatted reserved bundled folder, preservation of stale missing-source metadata on bundled Skills and begin unchanged local-import idempotence. Reserved namespace repair is useful. Preserving an impossible bundled missing-source marker avoids timestamp churn but also leaves misleading state; the test asserts storage behavior rather than operator truth/remediation.",
        "Lines 501-600 finish unchanged-import timestamp stability, clean legacy null provenance fields on refresh, reject auditing GitHub Skills and ensure the failed audit does not mutate metadata. Migration cleanup/idempotence are valuable. Remote sources have no equivalent byte/content audit coverage, reinforcing that pinned path inventory is not verified installed content.",
        "Lines 601-700 verify category filtering, content-bearing version creation with User attribution, idempotent stars and comment ownership basics. Version content reproducibility is useful. The comment test rejects a different Agent but permits an arbitrary board User to delete a User-authored comment, so its 'actor ownership' title overstates ownership/role enforcement; star concurrency/unique conflicts are not tested.",
        "Lines 701-800 confirm deleted comments cannot be resurrected/replied to, star removal, private/company sharing and public-link rejection, plus case-insensitive category dedup/filter/count semantics. These are clear product-state tests but contain no caller authorization/role assertions, optimistic concurrency or sensitive visibility checks.",
        "Lines 801-900 prove folder-subtree filtering, intentionally global search overriding folder scope, canonical folder paths and rejection of cross-company folder IDs during Skill creation. Tenant folder binding is strong. Search overriding an explicit folder is a surprising product rule now locked into tests rather than an orthogonal query composition.",
        "Lines 901-1000 verify unique-slug deep links, ambiguous-slug refusal and begin a creation-flow fork fixture with copied reference files and lineage/storefront overrides. Ambiguity fail-closed behavior is good; duplicate slugs are inserted directly, so the test documents tolerant reads rather than database uniqueness guarantees.",
        "Lines 1001-1100 assert copied files, lineage/actor metadata, source counters and initial version for both createLocalSkill fork and dedicated forkSkill. Happy-path provenance is thorough. No failure injection, concurrent same-slug fork, binary file preservation, target-directory collision or compensation assertion protects the non-atomic filesystem/database sagas found in implementation.",
        "Lines 1101-1200 verify precheck usage, selective Agent reassignment, unaffected Agents and actor-relative fork summary, then begin external-source fork coverage. Reassignment correctness is tested only for legacy desiredSkills strings; pinned version preservation/reset, terminated Agents, concurrent config changes, transaction rollback and human approval for capability mutation remain untested.",
        "Lines 1201-1300 fork GitHub/skills.sh/URL/catalog sources twice with slug deduplication and validate version-aware desired selections. Fixtures seed GitHub refs as floating main and expect fork reads from main, bypassing import pin enforcement; this documents that legacy/direct rows can still fetch mutable source content during fork. Version-to-Skill binding is correctly tested.",
        "Lines 1301-1400 test strict unknown desired-key rejection, opt-in stale-key preservation and ambiguity remaining fatal, then begin missing-but-used local Skill reconciliation. Honest stale state and fail-closed ambiguity are strong patterns for Company OS remediation UX. No expiry/revocation reason or accountable resolution workflow is attached.",
        "Lines 1401-1500 assert missing-but-desired Skills are retained with a stable detectedAt remediation marker, but explicitly require list() to hard-prune missing local Skills when no active Agent desires them. Thus destructive read-time reconciliation is intentional tested behavior, not an accidental gap; there is no grace period, tombstone, approval, version preservation or audit event assertion.",
        "Lines 1501-1600 verify list refresh expands stale path/kind inventory from disk, direct SKILL.md import includes sibling references and begin root-import bounding. This guards discoverability but not integrity: no content-only change hash, aggregate size/count limit, TOCTOU swap or unexpected executable classification assertion appears here.",
        "Lines 1601-1700 finish root bounding and test default skip plus explicit rename/replace package-conflict outcomes, then begin executable-package rejection. Conflict semantics are well specified for DB projections. The skip assertion checks stored markdown only and would not detect the implementation's pre-decision catalog-directory materialization side effect; concurrent conflicts/rollback are absent.",
        "Lines 1701-1800 prove external packages classified with scripts are rejected before DB persistence and unbundled imports cannot claim reserved Paperclip keys, then seed restored-source reconciliation. These are strong supply-chain namespace/default checks. They do not test disguised/shebang executables, hard links, aggregate resource bombs or filesystem artifacts left before rejection.",
        "Lines 1801-1900 verify a returned local source clears its missing marker, read-only runtime listing exposes a structured missing entry, and begin DB-markdown fallback materialization. Honest unavailable state is valuable, but the test explicitly expects missingDetail to contain the absolute server path, which is unsuitable for broadly exposed multi-tenant APIs.",
        "Lines 1901-2000 require runtime recovery from stored SKILL.md when local bytes vanish while non-SKILL inventory files return 404, then begin GitHub root-file reading. This makes partial fallback intentional: an Agent may receive instructions without referenced files. Tests do not require runtime sourceStatus/degraded evidence to disclose the missing remainder or fail a required capability closed.",
        "Lines 2001-2100 assert GitHub root/slug path derivation and stored-SKILL.md fallback during network failure, again using floating main refs seeded directly, then begin edit-version history. Availability fallback is well specified but source outage/integrity drift is hidden from returned content and no warning/attestation is required.",
        "Lines 2101-2200 prove initial/change-only version snapshots and selected non-standard project Skill browsing/import with symlink exclusion. Version idempotence is good. The browse fixture creates 251 symlinks which are filtered before the cap, so it does not exercise deterministic truncation among more than 250 visible entries; edit DB/filesystem failure and concurrent saves remain untested.",
        "Lines 2201-2300 verify preview discovers Codex/Cursor candidates without persisting project_scan rows and same-source import becomes already_imported rather than conflict. Preview purity and path identity are useful. No test proves previews avoid all filesystem/database reconciliation side effects, remain stable under workspace mutation or handle concurrent scans.",
        "Lines 2301-2400 assert a project candidate duplicating a bundled slug is already available, not conflicting, and begin selected replacement-slug import for a genuine conflict. Built-in precedence is explicit. The match is slug-based and tests do not address a malicious unrelated Skill choosing a bundled display slug with different canonical identity/content.",
        "Lines 2401-2500 complete explicit conflict rename and verify selective import only accepts a rediscovered in-workspace path, reports traversal selection as unmatched and ignores an unselected symlink escape without leaking a symlink warning. Selective least-work behavior is good, though the replacement key remains machine-path-hash derived and no path-swap race is tested.",
        "Lines 2501-2600 prove cross-company Project/Workspace selections are not scanned and return null foreign metadata rather than leaking names/paths, then begin a selected symlink-escape test. This is strong negative tenant-boundary coverage at the service result layer. Caller authorization to enumerate the in-scope workspace is still outside this suite.",
        "Lines 2601-2700 reject selected file/directory symlink escapes, sanitize human-readable reasons/warnings and ensure no persistence, then begin project-folder ownership. The test still explicitly expects skipped.path to equal the absolute linkedSkillDir, so path disclosure survives in the structured result even while message text is sanitized.",
        "Lines 2701-2800 prove re-import refreshes content without moving a User-organized Skill, and validate managed rename directory/key/frontmatter synchronization plus YAML-special quoting. Rename preserves only the original version count—no new immutable version or activity evidence is expected—so a material identity/content mutation is intentionally absent from version history.",
        "Lines 2801-2900 cover name-only/no-op rename, distinct slug/key conflict branches and begin rejecting non-managed source types. Conflict behavior is clear but only sequential; there is no concurrent target creation/directory collision or rollback failure test for the race-prone filesystem-first implementation.",
        "Lines 2901-3000 complete non-managed rename rejection and verify rename rewrites selected Agent keys while preserving pinned/loose selection shape. The pinnedVersionId is a random nonexistent ID, so the test explicitly preserves stale/invalid pins without validation; this favors transparent state but requires later readiness failure/remediation evidence. No approval is required for bulk Agent capability mutation.",
        "Lines 3001-3017 assert rename deletes the stale runtime materialization. The suite ends without testing deletion failure after the database rename commits, rollback failure, concurrent materialization or a structured revocation/cleanup receipt.",
        "Focused Vitest was run outside the sandbox with a fresh isolated PAPERCLIP_HOME and instance ID. Embedded PostgreSQL failed to start after five attempts, so the single file and all 56 tests were skipped; 0 tests executed and no runtime pass is claimed."
      ],
      "completionRequirements": ["Read lines 1-3017 exactly once, run the focused suite in an isolated Paperclip home, and map coverage for import/upsert/conflicts, source/provenance/inventory, local and remote safety, versions/fork/rename/edit/delete, audit/update/reset/materialization, Agent reassignment, transactions/crash recovery, concurrency, secrets, approvals and destructive lifecycle behavior found in the completed service audit."]
    },
    {
      "id": "application:server/__tests__/company-skills-catalog-service",
      "path": "server/src/__tests__/company-skills-catalog-service.test.ts",
      "lineCount": 529,
      "sha256": "0888ea21aa482f8600476a8f993237d84ca0aea93326126388d7be0d0c725996",
      "linkedEvidence": { "history": ["52aea902632fcdb9b3e689c60e3a148a2009358b feat: organize skills with nested folders and My Skills (#9633)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 529 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 define catalog integrity helpers and a bundled skill fixture with per-file hashes plus a deterministic aggregate contentHash. The test mocks the catalog package metadata and file-copy boundary, then gates all behavior on embedded PostgreSQL. The fixture records package name/version, trust level and compatibility, but carries no publisher identity, signature, license/SPDX expression or authenticated catalog manifest evidence.",
        "Lines 101-200 isolate PAPERCLIP_HOME per test and verify install materializes files, creates a taxonomy folder, persists catalog/package/hash provenance and records a pass verdict/scan version. This is a useful supply-chain projection pattern, but the mock makes copied bytes trustworthy by construction: the test does not prove post-copy digest verification, authenticated package provenance, publisher/license policy or cleanup when filesystem and database steps diverge.",
        "Lines 201-300 verify inventory refresh repairs folder placement, binary assets survive without UTF-8 rewriting, update/reset leave no hold for unchanged bytes, and script-bearing skills are installable after a static audit with only warning severity. Treating executable content as warning-plus-install conflicts with Company OS default-deny execution governance: installation, execution capability grant and accountable-human approval must be distinct. Assertions still do not compare materialized bytes to catalog hashes.",
        "Lines 301-400 show package import can restore catalog provenance and audit/local-modification state verbatim from YAML metadata, then verify idempotent reinstall and begin drift detection. Portable provenance is valuable, but imported audit verdicts/timestamps and origin hashes are trusted as data rather than demonstrably revalidated against bytes and an authenticated source; Company OS must distinguish asserted historical evidence from locally verified admission evidence.",
        "Lines 401-500 verify missing catalog entries become unsupported, restored bytes clear local-modification holds, missing SKILL.md is a 422 hard stop, reset requires force when locally modified, and even force cannot override a remote-execution hard stop. These are strong fail-closed lifecycle semantics to adapt. Weaknesses are English message contracts, no explicit accountable-human approval/evidence for destructive reset, and no tested recovery if replacement fails between directory and row updates.",
        "Lines 501-529 test same-company duplicate slug rejection with 409. The suite does not exercise cross-company isolation, concurrent installs, folder or directory races, partial copy/rename/database failures, malicious paths/symlinks, mismatched file hashes, package signatures, publisher/license policy, approval publication or an immutable installation receipt.",
        "Focused Vitest was run outside the sandbox with a fresh isolated PAPERCLIP_HOME and instance ID. Embedded PostgreSQL failed to start after five attempts, so the single file and all 13 tests were skipped; 0 tests executed and no runtime pass is claimed."
      ],
      "completionRequirements": ["Read lines 1-529 exactly once, run the focused suite in an isolated Paperclip home, and assess catalog install/update/reset, hash/provenance/origin snapshots, local-modification holds, audit hard stops, force semantics, directory/database failure recovery, tenant boundaries, publisher/license evidence and Company OS package supply-chain fit."]
    },
    {
      "id": "application:server/__tests__/company-skills-routes",
      "path": "server/src/__tests__/company-skills-routes.test.ts",
      "lineCount": 2319,
      "sha256": "c6dec8f2d6c7fa11da9c8a45d89c63083a631faad932667dd1540de6ebea8a63",
      "linkedEvidence": { "history": ["fcf66f3a91e6d1e5b81200f5ebf945b0d6ecad9d feat(skills): add managed skill rename API (#9688)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 2319 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 establish a heavily mocked Express route suite covering broad Skill CRUD/version/social/import/catalog/project/test-run surfaces plus Agent lookup, access decisions, Skill policy, Issue/heartbeat orchestration, activity/telemetry and a separate reflection-consent gate. The allow fixture models a permission grant and reason, but returns English explanation text and no accountable-human identity, approval/action digest, data scope, evidence or responsibility-contract binding.",
        "Lines 101-200 define denial contracts with reason/policy revision/rule/remediation and wire actual route/authz/error modules around mocked services. Stable reason and policy identifiers are useful, but remediation/explanation remain presentation strings and mocked services prevent this route suite from proving database tenant isolation, transactionality or real activity-log durability. The separate change-consent gate suggests mutation policy is split across access, Skill policy and consent services, a composition risk to trace in endpoint behavior.",
        "Lines 201-300 build an Express app with an arbitrary injected actor and reset extensive mocked defaults before every test. Version fixtures preserve separate Agent/User author IDs—a useful attribution primitive—but sourceLocator exposes an absolute local path and fork metadata begins to carry cross-company lineage/public-sharing fields. Because the actor and service results are synthetic, endpoint assertions must be checked for explicit company binding rather than assuming downstream services enforce it.",
        "Lines 301-400 continue response fixtures for fork/rename/precheck/comments/catalog install. Rename returns explicit previous identifiers plus Agent reassignments, a useful impact receipt, while comments retain User-versus-Agent authorship and soft-delete time. Fixtures also expose upstream GitHub URL/ref and catalog local sourceLocator directly through the service result; route assertions must show whether these operational locations are filtered before client delivery.",
        "Lines 401-500 define catalog/local Skill and file mutation/audit fixtures. Local absolute source paths, public sharing tokens/lineage fields and raw file contents are all present in service DTOs, while deleteFile returns a useful explicit deleted-path receipt and audit returns structured verdict/codes/findings/version. No fixture binds file mutation or destructive deletion to an approval, expected prior digest, idempotency key or responsibility evidence.",
        "Lines 501-600 define update/reset and Skill test-input/template fixtures. Test inputs and run templates have tenant IDs, creator attribution and soft-deletion timestamps, and runs begin with immutable-looking input and Skill-version snapshots—strong reproducibility primitives. However arbitrary content/template bodies can be execution instructions, and the fixture has no data classification, egress/tool scope, approver, responsible human or sanitization/retention contract.",
        "Lines 601-700 complete run/cancel fixtures with full Agent config, rendered prompt, harness issue description, outputs/errors and token/cost snapshots, then mock Issue/heartbeat orchestration and permissive default access. Reproducibility and cost capture are useful, but persisting raw Agent config can leak credentials/private runtime settings; cancellation attribution is only an English string, and no run attestation, capability/data scopes, approval pause/resume proof or responsible-human binding appears.",
        "Lines 701-800 establish default-allow when no Skill policy exists, permit a local implicit board actor to import an arbitrary GitHub URL, and verify preview/import workspace scans pass one generic skill_config:update gate with company scope plus activity logging for import. Preview is treated as mutation authority even though it can disclose local workspace paths; input selection is forwarded largely verbatim, with validation and workspace/company ownership delegated to the service.",
        "Critical lines 801-900 intentionally grant one board-user path create/import/catalog install/update/delete/audit/reset under the same skill_config:update decision, then explicitly allow import even when that access decision denies skills:create if no explicit Skill policy exists. This fail-open policy composition lets absence of a second policy override an RBAC denial and collapses read-like audit, destructive reset/delete and executable supply-chain admission into one authority—directly incompatible with Company OS least privilege and risk-matched accountable approval.",
        "Lines 901-1000 return a useful structured skill_policy_denied code/reason while hiding matched rule/revision, reject credential/query/fragment-bearing and malformed remote URLs before policy/service calls without echoing secrets, and keep platform actor restrictions ahead of optional policy. Canonicalized-source policy evaluation is a strong pattern, but validation examples do not yet restrict schemes/hosts/ports/private IPs or provide stable codes for URL errors, and hiding policy revision weakens reproducible decision evidence for administrators.",
        "Lines 1001-1100 verify shorthand and stored GitHub locators are canonicalized before policy matching and map version creation to a distinct skills.create action. Normalization-before-authorization avoids textual bypasses and is worth adapting. The edit/version checks fetch source context first and rely on getById/service tenancy; the suite should prove a cross-company Skill ID cannot supply policy context or be mutated, and policy evidence should preserve the exact normalized resource plus revision/digest.",
        "Lines 1101-1200 extend canonicalization coverage to npx shorthand and case-insensitive Markdown suffixes while preserving case-sensitive repository subpaths, then begin catalog-read behavior. This is solid parser/authorization regression coverage, though accepting command-shaped npx text as an import locator broadens the parser attack surface and requires a grammar with no shell execution, ambiguity rejection and canonical provenance—not merely string normalization.",
        "Lines 1201-1300 require authentication for all global catalog reads, short-circuit unauthenticated import before secret-bearing source parsing, and short-circuit edits before stored resource lookup—good anti-enumeration/secret hygiene. Authenticated catalog file reads accept a caller-provided path delegated directly to the catalog service, so containment/file allowlisting and content-disposition concerns must be proven elsewhere; global catalog authorization is authentication-only rather than tenant/package-policy scoped.",
        "Lines 1301-1400 assert catalog installation activity includes catalog ID/key/origin hash and telemetry exposes a stable public GitHub Skill reference while preparing a private-host suppression case. Provenance-rich activity is useful, but it omits actor/responsible human, exact requested options/action digest, policy revision, package license/signature, result digest and approval. The GitHub fixture is unpinned (sourceRef null), so telemetry identity does not prove installed content.",
        "Critical lines 1401-1500 correctly suppress private/missing GitHub metadata from telemetry and begin cross-company Agent denial, but explicitly allow a same-company Agent to import a Skill even when access denies skill changes and the Agent fixture says canCreateSkills:false, solely because no explicit policy exists. This is a tested privilege bypass, not an incidental gap, and conflicts with Company OS's requirement that Agent authority be explicitly granted, scoped, revocable and accountable to a human.",
        "Lines 1501-1600 verify company-scoped list filters, category/share updates, version creation and file/folder deletion with User attribution and detailed activity records. Activity receipts for deleted paths and version IDs are good operational evidence. Gaps include unbounded query/filter inputs, no stable event schema/version or before-state digest, and no approval/idempotency/concurrency control for destructive file-tree deletion or sharing-scope changes.",
        "Lines 1601-1700 test star/fork/precheck/comment and managed rename endpoints plus activity projection. Fork and rename return impact/reassignment information and record changed Agent IDs, which is useful for change planning. Rename does not pass actor context into the service even though its activity later attributes the request; bulk Agent capability rewrites therefore lack an atomic authorization/responsibility input at the domain mutation boundary.",
        "Lines 1701-1800 suppress no-op rename activity, attribute Agent API key/run identity, reject missing or multiline names, preserve null rather than inventing a shared board User, and allow a direct skills:create Agent grant. These are mostly sound attribution/input patterns, but permitting a board mutation with userId:null leaves no accountable human, and authorization remains generic company-wide skill_config:update rather than package/action/data/target-scoped authority.",
        "Critical lines 1801-1900 repeatedly assert the legacy consent gate is deliberately bypassed—even when access returns deny_missing_consent or the gate would fail—and allow same-company Agents with neither legacy grant to create/import Skills. A mocked second allow decision is never consumed. This codifies open-default Agent self-extension and makes policy absence/failure permissive, fundamentally conflicting with Company OS's requirement that Agents cannot self-authorize new executable capabilities or bypass human responsibility.",
        "Lines 1901-2000 confirm unrelated agents:create is ignored, then route test-input and run-template CRUD through the generic Skill mutation family. Creation passes actor attribution, but test-input update/delete omit actor context at the service boundary while template update includes it. The inconsistent attribution contract risks unaudited destructive changes and shows why Company OS commands must carry a uniform principal/responsibility/idempotency envelope.",
        "Lines 2001-2100 show test-run creation as a hidden Issue plus heartbeat wake and cancellation as hidden Issue cancellation. Origin fingerprinting is a useful idempotent-correlation primitive, and immutable input/version/template snapshots improve replayability. Yet the route owns orchestration callbacks across Skill, Issue and heartbeat services without an evident transaction/outbox; failures after Issue creation or before wake/cancel can leave divergent run state, and raw Agent config remains snapshotted.",
        "Lines 2101-2200 verify wake payload correlation and cancellation writes the hidden Issue with actorUserId, then require Agents to pass both skills.test policy and tasks:assign for create/cancel/delete. Layered checks are stronger here than general Skill mutation, but all operations share tasks:assign despite different risk, error response is an English string, Agent cancellation attribution is not asserted, and the test does not cover partial wake/cancel failure, retries or idempotency.",
        "Lines 2201-2300 keep reads side-effect free instead of pruning expired harness issues, soft-delete terminal runs while hiding their Issue, and return 404 for missing runs. Avoiding read-time cleanup is a good correction to destructive-read patterns. Delete still spans run and Issue services without failure compensation, records no deletion actor/approval/evidence in the service call, and tests only the successful terminal path—not active-run races or hidden-Issue update failure.",
        "Lines 2301-2319 assert a 422 blocks deletion while Agents still use a Skill and suppresses success activity logging. Referential blocking is sound, but the API contract exposes Agent display names in an English error string with no stable code, structured blocker IDs or remediation projection; it also passes no actor or expected dependency version to deleteSkill.",
        "The first focused Vitest run inside the sandbox failed all 53 tests because Supertest could not bind 0.0.0.0 (EPERM); this was an environment restriction, not a product assertion failure. The suite was rerun outside the sandbox with the same isolated PAPERCLIP_HOME and instance ID: 1 file and all 53 tests passed."
      ],
      "completionRequirements": ["Read lines 1-2319 exactly once, run the focused route suite in an isolated Paperclip home, and assess authentication/authorization and tenant scoping for every endpoint; stable API/error contracts; input/file/path/size validation; import, version, fork, update, reset, rename and deletion lifecycle; approval/responsibility evidence; concurrency/idempotency; secret and local-path exposure; and suitability as a reference for Company OS API boundaries."]
    },
    {
      "id": "application:server/__tests__/company-skills-import-authz-routes",
      "path": "server/src/__tests__/company-skills-import-authz-routes.test.ts",
      "lineCount": 212,
      "sha256": "ee06d80af7b2862619e393a41a4c05abf41e7080899559c9683b72ffbd0334e4",
      "linkedEvidence": { "history": ["89ce36d7afcb3b2920c45c47b55f210d635b9976 feat(skills): open-by-default company skill policy and core UX (#9564)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 212 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 set up a sequential real Express/auth middleware/route plus embedded-Postgres integration suite over Users, memberships, Agents, permission grants, runs, Skills and activity logs. It uses a fixed test JWT secret and isolated managed Skill root, with explicit cleanup and an approved-root fixture comment. This is stronger than the mocked route suite for real tenant/auth persistence, though runtime proof remains conditional on embedded PostgreSQL and the test owns process-global environment mutation.",
        "Lines 101-200 seed a company, Agent, responsible User, memberships, direct skills:create grant and running heartbeat, then authenticate a local Agent JWT bound to company/run/responsibleUser and import only from the managed company root. This is close to Company OS responsibility needs, but the permission grant itself has grantedByUserId:null, the Agent is role=ceo, scope is null/company-wide, and no responsibility contract or approval binds the exact package/action digest. The test begins verifying durable activity after successful import.",
        "Lines 201-212 assert the durable activity identifies company, Agent actor/ID, run, import action and company entity. The activity does not assert responsibleUserId, permission grant, source/package hash, imported Skill IDs, policy decision, approval, evidence/result or outcome digest, so the responsibility chain is incomplete despite the JWT/run carrying a responsible User.",
        "Focused Vitest was run outside the sandbox with an isolated PAPERCLIP_HOME and instance ID. Embedded PostgreSQL failed to start after five attempts, so the single test was skipped; 0 tests executed and no runtime pass is claimed."
      ],
      "completionRequirements": ["Read lines 1-212 exactly once, run the focused route suite in an isolated Paperclip home, and assess import authentication, company membership/tenant isolation, User-versus-Agent authorization semantics, fail-open/fail-closed policy composition, stable denials, secret-safe parsing, activity evidence and Company OS responsibility fit."]
    },
    {
      "id": "application:server/routes/company-skills",
      "path": "server/src/routes/company-skills.ts",
      "lineCount": 1410,
      "sha256": "51f6542ff22f88087308f7f39f34f1be20ae051be4fd2bcc7f8f3d00444e6d6a",
      "linkedEvidence": { "history": ["717684ad8f5f33fd3b6e8cf7a96aa7062d8ff29b Add project folder browsing to skill imports (#9930)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 1410 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 show one Express module importing all Skill/catalog/test schemas plus access, policy, Skill, Issue, heartbeat, activity and telemetry services. It owns both policy/authorization composition and cross-domain orchestration, so dependency flow is route→many application services rather than a narrow command handler. Denial types have a stable skill_policy_denied code/reason, but broader errors and activity/event contracts remain to inspect; test-run assignment authorization already needs a multi-entity scope structure.",
        "Lines 101-200 suppress private GitHub telemetry, parse booleans/arrays with English bad-request text, map board actors without User IDs to a synthetic policy id 'board' while mutation attribution may remain null, and build policy resources by loading stored Skills then canonicalizing source type/locator. Import classification treats any HTTP(S) non-git input as external_package and everything else as workspace; destination/network/data sensitivity is absent. Stored-resource lookup before policy must rely on tenant-safe svc.getById.",
        "Critical lines 201-300 confirm the tested privilege semantics: authentication/company boundary and selected platform restrictions fail closed, but deny_no_grant, deny_missing_consent and deny_missing_grant are explicitly ignored before the optional Company Skill policy. Thus a second layer can authorize what the primary access layer denied. Test-harness orchestration instead requires tasks:assign and reconstructs scope from the stored run/Issue, falling back to run IDs if the Issue is missing or cross-company. Catalog file reads are authentication-only and forward arbitrary ref/path to the catalog service.",
        "Lines 301-400 expose company-scoped list/category/detail/fork/version/test-input reads after assertCompanyAccess, with no per-Skill/data-sensitivity permission or pagination visible at the route. Detail/fork calls receive actor attribution, while version/test-input reads do not. Not-found responses are English-only. Test-input creation uses skills.edit policy and passes actor to the service, then begins a separate activity write, indicating mutation and audit are not inherently atomic.",
        "Lines 401-500 confirm test-input create activity is separate from persistence; update/delete do not pass actor into the service at all and only append route-level activity afterward. changedKeys is useful minimization, but no before/after digest, idempotency or responsibility binding exists. Template reads require only company access, while create uses generic skills.edit without a specific Skill resource and again splits mutation from activity publication.",
        "Lines 501-600 continue template update/delete, where delete again omits actor at the service boundary. Test-run list/detail require only company membership and may return raw snapshots/content without a more specific permission or pagination. Creation is better layered: skills.test plus tasks:assign scoped to target Agent, then service orchestration callbacks create a hidden Issue with actor/run attribution. Cross-service atomicity and exactly-once behavior remain unresolved.",
        "Lines 601-700 log Issue creation, wake the Agent with actor/context snapshot, offer cleanup that verifies Issue company before cancelling/hiding it, and then log test-run creation. These compensating callbacks are thoughtful, but Issue creation, wake, cleanup, run persistence and two activity writes remain a route-managed saga without durable outbox/step ledger. Cancellation uses a hardcoded English reason and may cancel a runtime before Issue/run state persistence succeeds.",
        "Lines 701-800 complete cancel/delete and version creation. Delete hides the Issue through another non-atomic callback, and cancellation/deletion activities omit result/content hashes and responsible User. Versioning correctly uses skills.create and actor attribution. Star is permitted by company membership alone and logs separately; this may be acceptable social metadata but still permits unauthenticated-to-entity semantics if membership is overly broad and has no idempotency/activity atomicity.",
        "Lines 801-900 implement unstar/fork/rename. Fork passes actor and records affected Agent IDs; rename authorizes at the route but calls svc.renameSkill without actor, then logs only if normalized identity changed. Bulk reassignment is therefore not intrinsically bound to the authorizing principal or an expected pre-state. Neither operation requires explicit approval for modifying Agent capability assignments, nor supplies idempotency or action/result digests.",
        "Lines 901-1000 permit comment CRUD, update status and Skill file reads with company access only; comment service receives actor and route logs separately. File path is coerced with String rather than a route schema and passed to svc.readFile, so containment and size/encoding enforcement are wholly delegated. Read authorization does not distinguish executable scripts, sensitive test artifacts or private provenance, and responses use English not-found strings.",
        "Lines 1001-1100 create generated Skills, update metadata/sharing and update files after policy checks. Create/file update pass actor to the service, but metadata update does not; every activity is a later independent write. File activity records path and markdown flag but no previous/new digest, content size, trust transition or audit verdict. Sharing-scope changes are treated as ordinary skills.edit and are not separated as data/publication risk.",
        "Lines 1101-1200 delete files with actor and detailed deleted paths, then import from a caller source after policy classification. Import does not pass actor/responsibility into the service and logs the raw source plus slugs after persistence; remote credential/query inputs are schema-rejected, but local absolute paths or other sensitive locators may enter durable activity. Telemetry is selectively minimized. Catalog installation uses a distinct skills.install action and begins provenance activity, which is a stronger boundary.",
        "Lines 1201-1300 finish catalog provenance activity, expose project browse/scan under skills.import and delete under distinct skills.remove. Browse returns local workspace information without activity; preview and import scan share the same mutation permission, and the route logs scan summary even for preview. Delete does not pass actor to the service and its later activity lacks blocker/dependency snapshot, pre-state digest, approval or deletion receipt. Audit is classified as skills.test rather than a read/inspection permission.",
        "Lines 1301-1400 produce useful audit/update/reset activities with scan codes and before/after origin hashes/versions, drift, force and audit verdict. Distinct skills.update and skills.reset actions are good risk vocabulary. However before is fetched separately from mutation and activity, creating TOCTOU/inaccurate evidence under concurrency; actor/approval/idempotency are not passed into installUpdate/resetSkill, and a boolean force is not an accountable approved action digest.",
        "Lines 1401-1410 only complete reset activity and return the Router. The module contains no route-local transaction/outbox wrapper, API version negotiation, optimistic concurrency token or durable approval/evidence publication boundary.",
        "Verification links the already completed route-contract suite (53/53 passed outside the network sandbox) and import-authz integration suite (1 skipped after embedded PostgreSQL failed five times; 0 passed). Those sources were not reread."
      ],
      "completionRequirements": ["Read lines 1-1410 exactly once and trace every route's authentication, company/tenant binding, access and Skill-policy composition, validation and stable error contract, service/activity/event orchestration, transaction/idempotency behavior, source/path/secret handling, Agent test-run lifecycle, responsible-human/approval/evidence semantics and Company OS API reuse decision. Link the already completed focused route and import-authz test evidence rather than rereading those files."]
    },
    {
      "id": "application:server/__tests__/skills-catalog-service",
      "path": "server/src/__tests__/skills-catalog-service.test.ts",
      "lineCount": 235,
      "sha256": "90d8158247b982748857f6d5a113bf322950a01d0cc1bd2eed3cbab2059139d9",
      "linkedEvidence": { "history": ["5e086cb828dac27a3fd16bf22e71597382458c85 fix(server): resolve published skills catalog package root and fallback (#8327)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 235 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 define a fully mocked filesystem/module-resolution suite with catalog fixtures containing schema/package version, generated time, per-file and aggregate hashes, trust/compatibility and roles. No publisher identity, signature or license fields are in the manifest fixture. The file hashes use inconsistent-looking fixture values (sha256:slug rather than real digests), so tests may validate plumbing rather than cryptographic correctness. Cache invalidation appears based on manifest mtime/size, to be traced.",
        "Lines 101-200 verify synchronous cache reload on mtime change, package metadata refresh, binary preview rejection, commit-pinned raw GitHub URL construction and published package resolution. Pinning remote reads to a full commit is strong. The remote test does not assert response/content length, redirect/host policy, content type or hash verification after fetch; the expected per-file hash happens to match content but the assertion only checks returned content. Cache validity based on filesystem timestamp may miss same-mtime replacement unless size/hash is also compared in implementation.",
        "Lines 201-235 complete published-layout assertions and test a fail-soft unavailable catalog that caches package-resolution failure and logs once. This avoids log storms and permits optional catalog absence, but a permanent process-lifetime negative cache can prevent recovery after installation/mount repair; returning an empty list is indistinguishable to callers from a legitimately empty catalog unless health/diagnostics expose the unavailable state.",
        "Focused Vitest completed in the sandbox: 1 file and all 5 tests passed."
      ],
      "completionRequirements": ["Read lines 1-235 exactly once, run the focused suite, and assess catalog root/package resolution, manifest and per-file integrity, traversal/symlink handling, package/publisher/license provenance, fallback behavior, ambiguity, caching/version drift, failure modes and Company OS capability-package supply-chain fit."]
    },
    {
      "id": "application:server/services/skills-catalog",
      "path": "server/src/services/skills-catalog.ts",
      "lineCount": 355,
      "sha256": "fff360dc948f5b7212d98ece9a7d4ac6a89d40f827e993f5333786b3b31df411",
      "linkedEvidence": { "history": ["5e086cb828dac27a3fd16bf22e71597382458c85 fix(server): resolve published skills catalog package root and fallback (#8327)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 355 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 define a process-global synchronous catalog cache, published/dev package resolution and a custom unavailable error. The manifest interface only declares package name/version/skills and is a TypeScript compile-time shape, not yet runtime schema evidence. Paths are derived from module resolution or a repository-relative dev root. A false sentinel plus cached error creates permanent negative caching, while operational error messages expose local paths and build commands in English.",
        "Lines 101-200 parse manifest JSON with an unchecked type assertion and cache it by mtime+size. There is no runtime schema/version validation, duplicate ID/path detection, signature/hash authentication or atomic-write protection; same-stat replacement can remain stale and malformed structures fail later unpredictably. All Skills are cloned with package metadata on every access. Language inference is extension-based, and sourceRootPath normalizes source.path before string concatenation but containment against an authorized repository/package root is not yet proven.",
        "Lines 201-300 enforce local path containment with resolved-root prefix checks, restrict remote source type to GitHub, build a commit-pinned raw URL, require HTTP success and verify SHA-256 bytes against the manifest entry—strong integrity patterns absent from the earlier test assertion. However file size is not checked, full responses buffer without local cap/timeout/content-type control, error strings reveal repository/path/commit, hostname/network safety is delegated to ghFetch, and manifest hashes remain unauthenticated. Catalog search is full in-memory and unpaginated; unavailable manifests fail soft to an empty list.",
        "Lines 301-355 reject ambiguous slugs, normalize requested paths, require manifest membership, block asset previews, hash-check bytes, and expose read/copy/package metadata. Non-asset bytes are blindly decoded as UTF-8 for preview based on declared kind; declared kind/content mismatch is not detected. copyCatalogSkillFile accepts an unrestricted targetPath and relies entirely on callers for destination containment/symlink/atomic replacement. Errors remain English-only, and no publisher/license/signature/admission evidence is returned. Linked focused tests passed 5/5 and were not reread."
      ],
      "completionRequirements": ["Read lines 1-355 exactly once and trace manifest/package resolution, schema validation, cache and failure recovery, reference ambiguity, local and GitHub path construction, traversal/symlink containment, network and resource limits, per-file/content hash verification, binary handling, provenance/license/signature gaps and Company OS reuse decision. Link the completed 5/5 focused test evidence without rereading its source."]
    },
    {
      "id": "application:server/__tests__/plugin-managed-skills",
      "path": "server/src/__tests__/plugin-managed-skills.test.ts",
      "lineCount": 281,
      "sha256": "49c5ef91320395be1f02172b7fa4adddb9dfb85e257b6c101e23a99f2ac16c97",
      "linkedEvidence": { "history": ["0096b56a1c4e69c077320c0194c804cdaea7615d [codex] Add LLM Wiki plugin host support (#5597)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 281 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 set up an embedded-Postgres host-service integration suite with a minimal event bus stub and a Plugin manifest declaring apiVersion/version/author/capability/worker plus an inline managed Skill reference file. The manifest has author text but no publisher identity, signature, package digest or license. The Skill has no explicit entrypoint/SKILL.md, trust level, content hashes, data/tool/egress scopes or approval requirements. Event emission is a no-op, so durable event semantics are outside this suite.",
        "Lines 101-200 verify stable Plugin resource keys/bindings, creation of generated SKILL.md plus references, idempotent resolution, preservation of operator edits during reconcile, explicit reset to manifest defaults and no-op binding timestamps. Preserving local edits and explicit reset are strong reconciliation semantics, but reset has no actor/approval/action digest, generated content lacks asserted hashes/provenance version, and sourceType='catalog' conflates Plugin-managed ownership with catalog origin.",
        "Lines 201-281 verify relinking a canonical orphaned Skill without overwriting edits, reporting file-level default drift, clearing drift after reset and injecting a canonical managed key into manifest Markdown. This is a useful ownership/reconciliation model. Missing coverage includes cross-company/resource-key collisions, Plugin upgrade/downgrade/uninstall, deleted/renamed Skills, concurrent reconcile/reset, transaction/activity/event failures, malicious paths/content, executable scripts, license/signature and Agent assignment impact.",
        "Focused Vitest was run outside the sandbox with an isolated PAPERCLIP_HOME and instance ID. Embedded PostgreSQL failed to start after five attempts, so the file and all 6 tests were skipped; 0 tests executed and no runtime pass is claimed."
      ],
      "completionRequirements": ["Read lines 1-281 exactly once, run the focused suite, and assess Plugin-owned Skill discovery/install/update/removal, ownership and tenant boundaries, idempotency/reconciliation, version/hash/provenance/license, executable capability admission, failure recovery, uninstall cleanup, Agent assignment impact and Company OS Plugin/Connector fit."]
    },
    {
      "id": "application:server/services/plugin-managed-skills",
      "path": "server/src/services/plugin-managed-skills.ts",
      "lineCount": 359,
      "sha256": "8ce9b497c51c6dbc1d2b4b0effbe0f0e0ea5cb39c08c085d59556ec8a3eadce4",
      "linkedEvidence": { "history": ["0096b56a1c4e69c077320c0194c804cdaea7615d [codex] Add LLM Wiki plugin host support (#5597)"] },
      "auditStatus": "COMPLETE",
      "completedRanges": [{ "start": 1, "end": 359 }],
      "sourceReadComplete": true,
      "nextLine": null,
      "notes": [
        "Lines 1-100 couple Plugin resource bindings directly to Company Skill service/DTOs and compile manifest declarations into a portable-package-like string map. Canonical keys normalize only pluginKey; declaration.skillKey and file.path are concatenated as paths/ordinary object keys here, so traversal, reserved keys and collision safety depend on upstream manifest validation or downstream import normalization. YAML scalar values use JSON quoting, but existing frontmatter key replacement is regex-based and may accept malformed/duplicate YAML structures.",
        "Lines 101-200 flatten declared files, record defaults as names/paths rather than content hashes, and compare defaults through a hand-rolled stable JSON serializer. Undefined serializes to undefined inside template interpolation and exotic numbers are not relevant but reveal non-canonical generality. Declaration lookup uses first matching skillKey, so duplicate manifest keys are not rejected here. Binding reads are correctly scoped by company, plugin ID, kind and key; upsert begins as a read-then-write sequence, leaving concurrency behavior to inspect.",
        "Lines 201-300 confirm binding upsert is non-transactional read-then-insert/update. Drift loads every unioned file sequentially and converts every read error—including permission/storage/integrity failures—into null, misclassifying outages as content drift. Reconcile adopts any same-company Skill with the canonical key and binds it without verifying prior provenance/ownership. Otherwise it imports with onConflict:replace, selects a result through multiple fallbacks and only then writes the Plugin binding; import/binding failure can leave orphaned or overwritten Skills.",
        "Lines 301-359 expose only get/reconcile/reset. Reconcile with an already bound Skill silently refreshes defaults metadata without activity; missing reconcile/reset logs Plugin actor and stable keys only after import/binding. Reset always uses replace and is callable without human identity, approval or expected prior digest. There is no detach/uninstall/upgrade/rollback API, deletion/tombstone handling, responsibility evidence, outbox or Connector-node activation boundary. Linked tests were not reread and their 6 cases were skipped after embedded PostgreSQL failed five times."
      ],
      "completionRequirements": ["Read lines 1-359 exactly once and trace Plugin manifest-to-Skill compilation, stable ownership/binding, tenant isolation, reconcile/adopt/reset, drift hashing, transactions/concurrency, upgrade/downgrade/uninstall, path/content safety, executable admission, activity/events, Agent assignment and Company OS Plugin/Connector reuse decision. Link the completed test evidence without rereading it."]
    },
    {
      "id": "application:server/__tests__/company-portability",
      "path": "server/src/__tests__/company-portability.test.ts",
      "lineCount": 5421,
      "sha256": "621734dbcb5c74e00c4c17a6a9f71ef40baf643a3afa84981e919a209907b8be",
      "linkedEvidence": { "history": ["5da382fd59222819cff110f85ca738a74fb335d1 feat(skills): require explicit merge modes (#10978)"] },
      "auditStatus": "IN_PROGRESS",
      "completedRanges": [{ "start": 1, "end": 4500 }],
      "sourceReadComplete": false,
      "nextLine": 4501,
      "notes": [
        "Lines 1-100 establish a very broad mocked portability integration surface spanning Company, Agents, membership/grants, projects/workspaces, Issues/comments/labels/attachments/import helpers, documents, work products, routines, Skills, assets, secrets and Agent instructions. This confirms the test exercises a monolithic migration orchestrator rather than narrow ports. Secret mocks return persistence/runtime configs largely unchanged and a fixed secret ID, so secret isolation/redaction and per-binding lifecycle require explicit adversarial cases later.",
        "Lines 101-200 replace every domain/infrastructure dependency plus org-chart rendering, then import the real 6,179-line portability service and begin large default fixtures. This is a wide orchestration-unit suite with mocks rather than a database/filesystem end-to-end test; transactionality and cross-service invariants cannot be proven unless failure/rollback behavior is explicitly modeled. Company defaults include board-approval and branding fields, while secret resolution is deliberately secret-free only by mock convention.",
        "Lines 201-300 seed two Agents, including secret_ref env bindings, absolute instruction/cwd/command paths, desired Skills, runtime heartbeat, budgets and permissions. These fixtures are well chosen to test portability redaction/machine dependence later. Agent responsibility is only reporting hierarchy and role; no accountable human or responsibility contract is present. Most remaining services default empty/success, which biases broad export/import tests toward happy paths.",
        "Lines 301-400 seed routine/trigger creation with null creator attribution and begin GitHub versus local Skill fixtures. The GitHub Skill is pinned by full commit plus mutable trackingRef; the local Skill carries an absolute sourceLocator. Inventories omit per-file size/hash and licenses/publisher signatures, so exported package provenance/integrity must be constructed elsewhere. Webhook trigger defaults include secret/signing fields but no test secret material yet.",
        "Lines 401-500 complete Skill/file, membership and managed-instruction mocks, then assert YAML/frontmatter rendering handles arrays of 130,000 items without stack overflow. Iterative rendering is robust, but accepting/rendering that volume without an admission quota still enables CPU/memory/output amplification. Agent instruction materialization injects absolute temp paths, creating a useful portability-scrubbing target. GitHub URL parsing tests explicit encoded ref/path/companyPath, with security validation to follow.",
        "Lines 501-600 verify default export includes referenced Skill stubs, full local Skill files, Agent instructions and Paperclip extension while omitting prompts, absolute paths/commands, secret IDs/types, PATH, sync config and zero/default fields. This is strong sanitization coverage, though secret inputs become optional empty values and company new-Agent approval is omitted rather than migration-reviewed. Permission grants are exported with arbitrary scope records; no grantor, expiry, responsible human, approval or schema-versioned scope evidence appears.",
        "Lines 601-700 preserve hire-approval policy only when true and explicitly prove a legacy inline API key value never appears in serialized output. Sensitive-key conversion to secret input declaration is strong, while ordinary NODE_ENV retains its default. Both become optional, potentially weakening required configuration semantics; descriptions are generated English strings. Detection coverage is name-based and this segment has no canaries for less obvious DSNs/certs/tokens or secret values hidden in nested non-env config.",
        "Lines 701-800 verify deterministic sidebar ordering independent of source order, referenced-Skill expansion on demand, and begin catalog provenance export. Sorting makes diffs reproducible and expansion keeps the default bundle lean. Catalog metadata records package/version/origin hash and path classification, but the visible inventory still lacks per-file hashes, sizes, licensing and signatures; sourceLocator is an absolute local path that must remain scrubbed from the portable representation.",
        "Lines 801-900 prove the portable Skill frontmatter and manifest retain catalog identity, origin/installed hashes, package version and audit verdict/codes while excluding the local origin snapshot path. This is a useful provenance pattern, but no license, signer, per-file digest or immutable package-distribution locator is asserted. Skill filtering supports data minimization; however, a filter that matches nothing falls back to exporting every Skill with a warning, a fail-open behavior that can unintentionally broaden disclosure.",
        "Lines 901-1000 confirm that fail-open Skill selection exports both Skills, then cover company-logo binary export through tenant-scoped storage and begin duplicate-slug namespacing fixtures. Embedding the logo makes bundles self-contained, but the test asserts no byte/format/dimension limits, content digest, malware/polyglot screening, declared-versus-detected MIME agreement or missing-object behavior. Namespaced Skill paths are the right collision strategy; local-path provenance remains machine-specific and must not escape.",
        "Lines 1001-1100 complete collision-safe local versus upstream Skill paths, assert export previews exclude tasks unless explicitly requested, and begin project workspace-policy round-trip coverage. The explicit task opt-in is a sound minimization default. Upstream Skill provenance combines a pinned commit with mutable trackingRef, useful for update discovery but requiring the pinned digest to remain authoritative. Workspace policy exports operational topology; later assertions must prove machine paths, credentials and stale IDs are normalized rather than blindly reproduced.",
        "Lines 1101-1200 use concrete Git and local workspaces plus issue bindings to prove export keeps portable repository/ref/setup/cleanup metadata, remaps workspace IDs to stable keys, strips absolute cwd and warns while omitting a local-only workspace. This is a strong portability boundary, yet setupCommand and cleanupCommand are executable supply-chain inputs (including a destructive example) with no trust, approval, digest or sandbox policy in the fixture; repoUrl/ref also lack allowlist/signature assertions.",
        "Lines 1201-1300 prove import reconstructs a new workspace ID, patches the project policy, and remaps the issue workspace binding while retaining repository and visibility metadata. Referential remapping is well covered, but mocks cannot prove atomicity across workspace creation, project update and issue import, nor cleanup after partial failure. Setup/cleanup command persistence is present but not asserted as deliberately admitted executable behavior. The next case begins validation of imported icon names.",
        "Lines 1301-1400 normalize an unknown project icon to null and begin a filesystem/Git test that infers portable repository metadata from a local checkout. Icon allowlisting prevents arbitrary presentation identifiers from becoming a contract. Git discovery improves usability and suppresses per-task warning fan-out, but it executes the local git binary against an attacker-selectable checkout during export; later coverage must establish timeout, environment isolation, config safety, repository ownership/symlink handling and deterministic failure behavior.",
        "Lines 1401-1500 complete successful Git remote inference and begin testing warning aggregation when many tasks reference one omitted local-only workspace. Inferring repoUrl and stable workspace key avoids leaking cwd and preserves task linkage; aggregating diagnostics is good operator ergonomics. The success-only Git case still leaves subprocess failure/hang and malicious repository configuration untested. Warning deduplication is presentation quality, not a replacement for a structured omission manifest that callers can enforce.",
        "Lines 1501-1600 assert one summarized warning for three tasks pointing to an omitted workspace, then preview explicit secret env inputs and begin import-time secret materialization. Preview exposes structured input descriptors without values, a reusable onboarding pattern. Yet exported secret requirements are optional even for keys the next test calls required, and input identity is only key plus Agent/project slugs—no declaration ID, purpose, owner, allowed runtime, rotation/expiry policy or accountable approver.",
        "Lines 1601-1700 prove a supplied required secret becomes a company-scoped encrypted secret, is normalized to a secret_ref, persisted into the Agent adapter config and synchronized as an env binding. Values are kept out of portable files and actor context carries the importing user, which is strong. However secret creation and Agent creation are only mock-observed, with strictMode false and version latest; no rollback, duplicate/idempotent retry, minimum strength, rotation pinning, value disposal or approval evidence is tested. Permission-grant import begins next.",
        "Lines 1701-1800 show imported package metadata can directly grant an Agent two permissions, including an arbitrary target-Agent scope, attributed to the importing user. This is a major trust-boundary decision: no allowlist, privilege ceiling, human confirmation, signed package authority, preview diff or responsibility contract appears before elevation. The next test recognizes compensating deletion of a secret when later Agent creation fails, which is necessary but only one side effect in a much larger non-transactional import graph.",
        "Lines 1801-1900 complete secret compensation on Agent-create failure and introduce an expectedFileCount guard that rejects truncated inline payloads with stable 422 code/details before company or Agent writes. The structured error is excellent API-contract material. Count equality only detects missing/extra entries, not content truncation/substitution, duplicate normalized paths or tampering; a manifest digest and per-file hashes are still required. Compensation deletion also needs failure handling and an auditable orphan-reconciliation path.",
        "Lines 1901-2000 prove the matching file-count path imports one Agent, then begin hierarchy reconciliation into an existing company: an imported root CTO is intended to attach beneath an existing CEO before imported QA-to-CTO links resolve. This addresses an important merge lifecycle problem. Identity matching currently begins from role/name-like fixtures rather than an explicit stable external identity or human-approved mapping; later assertions must show collision ambiguity, cycles, missing managers and accountable-human ownership are handled.",
        "Lines 2001-2100 complete two-stage hierarchy remapping via explicit existing-Agent ID/slug plus imported slug, then verify project env export removes concrete secrets and secret IDs while retaining benign defaults and typed declarations. Explicit mapping is safer than name inference, but it still lacks proof the referenced manager belongs to the target company and was human-approved. Secret classification again relies on key heuristics: a plain OPENAI_API_KEY is scrubbed, while generic plain values remain exportable.",
        "Lines 2101-2200 show project secret inputs round-trip into a structured preview with empty default, then begin recurring-routine export fixtures covering project/Agent assignment, priority, paused state, concurrency and catch-up policies, cron/timezone, webhook identifiers and actor fields. Routine portability is strategically useful, but source fixtures permit null creator/updater responsibility. Webhook secretId is present in storage and must be proven absent from export; cron/timezone also need validation against abuse and semantic drift.",
        "Lines 2201-2300 prove routine export preserves schedule/webhook behavior, concurrency/catch-up policies and replay window while stripping webhook publicId and secretId. This is good secret minimization, but importing signingMode without key material needs an explicit disabled/unconfigured lifecycle, not silent apparent readiness. Responsibility provenance is entirely dropped. The next case starts excluding built-in managed Agents/routines, a sensible separation between portable tenant intent and product-owned automation.",
        "Lines 2301-2400 identify built-ins through Paperclip-specific Agent metadata and routine originKind/originId, then assert they are omitted with aggregate warnings while tenant Agent content remains. Separating rehydratable product-managed bundles from tenant-owned exports is excellent. The discriminator is vendor-specific metadata with no portable ownership taxonomy or extension version; collisions/tampering could cause tenant data omission. Import of recurring task packages as routines begins next.",
        "Lines 2401-2500 prove preview explicitly labels a recurring task's conversion to a routine and import resolves project/Agent slugs before creating the routine plus two triggers. A visible plan reason is good operator UX and avoids accidental one-shot work creation. The case accepts executable schedules/webhooks from an unsigned bundle and appears to recreate triggers even with no signing secret; approval, activation state and responsible owner must be made explicit. Human-readable English reason is not a stable machine contract.",
        "Lines 2501-2600 finish routine-versus-issue assertions and prove pauseAutomations forces imported Agents and routines paused, tagging Agents with system pause reason/time. A global safe-import switch is a strong deployment control. The default is explicitly being tested as active when the option is absent, which is unsafe for untrusted portability packages; Company OS should default imported execution surfaces to quarantined and require a scoped human activation approval bound to the reviewed bundle digest.",
        "Lines 2601-2700 explicitly confirm the unsafe-by-default path creates idle Agents and active routines when pauseAutomations is omitted, then begin backward-compatible migration from legacy schedule.recurrence metadata. Compatibility handling is valuable, but automatic activation compounds legacy semantic uncertainty. Weekly recurrence uses timezone plus a dated start and must define DST, missed-run and invalid-calendar behavior; migration should emit a structured transformation record rather than only a human preview reason.",
        "Lines 2701-2800 verify legacy weekly recurrence becomes cron and never a one-time issue, and recurring tasks lacking assignee/project import as paused routines with preview/result warnings. Pausing an ownerless automation is the correct invariant, although no accountable human is required to claim it. The next case tests a vendor-neutral package without the Paperclip extension, demonstrating graceful degradation and a useful separation between open portable core metadata and vendor extensions.",
        "Lines 2801-2900 prove a vendor-neutral COMPANY/AGENTS Markdown package previews and imports without .paperclip.yaml, assigning the generic process adapter and preserving company description/Agent title content. This low-coupling fallback is highly reusable, but defaulting an underspecified Agent to an executable process adapter can create ambiguous readiness; it should import as unbound/quarantined until a Connector and responsible human are explicitly attached. Role precedence testing begins next.",
        "Lines 2901-3000 preserve Agent role from neutral frontmatter and extend secret-name heuristics to separator-free APIKEY, GITHUBAUTH and PRIVATEKEY forms, proving their values are scrubbed. This improves legacy leakage resistance but remains denylist heuristic security; entropy/content-based detection, explicit classification and nested configuration scanning are still absent. Packaged-Skill import and desired-Skill restoration begin next, introducing executable-content trust concerns.",
        "Lines 3001-3100 prove text Skill package files import under rename collision strategy and returned canonical keys are rewired into the Agent's desired-Skill sync config. This is sound reference remapping, but binary files are silently excluded, result reasons are English text, and the mocked import hides package path/content/license/trust validation. A renamed Skill can become desired executable behavior without a separate review/approval gate. Company-logo attachment import begins next.",
        "Lines 3101-3200 prove an embedded base64 logo is written to tenant storage, registered as an asset with importing-user attribution, then attached to the new company. The lifecycle is coherent, but no byte/MIME/image validation or rollback across storage, asset row and company update is exercised, and the test mutates YAML by string replacement rather than schema construction. The next case begins copying source memberships during an internal safe new-company import.",
        "Lines 3201-3300 show agent-safe internal cloning copies every active user membership from the source company instead of making one importer owner, then verify imported timer heartbeats are forcibly disabled while retaining max concurrency. Both are thoughtful safety/continuity measures. Bulk membership copying may over-share the new company absent role/data-purpose review, and heartbeat disablement does not cover other activation surfaces (routines, webhooks, manual runs). Selective-file import begins next.",
        "Lines 3301-3400 prove selectedFiles can import only one Agent while leaving company metadata unchanged and pruning other Agent content passed to the Skill importer; COMPANY.md is retained as context. This is useful least-data selection with explicit unchanged result. The semantics couple object selection to file paths and still send COMPANY.md to a broad package importer, so schema-level field authorization and a structured selection manifest are preferable. Adapter override behavior begins next.",
        "Lines 3401-3500 reveal import caller adapterOverrides may explicitly set dangerouslyBypassApprovalsAndSandbox=true and it is passed into Agent creation, while path/prompt fields are stripped and AGENTS content is materialized through a managed bundle. Managed instruction ownership is a strong pattern, but accepting an approval/sandbox bypass as an ordinary override is a critical governance conflict unless the route enforces exceptional human authorization and immutable evidence. The following safe-default test only checks implicit defaults, not authority for explicit bypass.",
        "Lines 3501-3600 show Codex-local adapter defaults add --skip-git-repo-check while caller legacy args survive, and confirm no implicit approval/sandbox bypass when not supplied. Vendor-specific adapter normalization belongs outside Company OS core and should be capability/version negotiated. Label portability then maps tenant-local IDs to names/colors under schemaVersion 6, a sound stable-key pattern; collision, case-folding, reserved-name and color validation remain to be checked.",
        "Lines 3601-3700 complete label ID remapping, reuse existing labels by name, preserve target color on disagreement and emit a warning. This deterministic target-wins merge is reasonable and avoids silent visual overwrite. Exact-name identity may still duplicate labels across case/Unicode normalization, and warnings are unstructured English. Legacy raw-label-ID degradation begins next; it prioritizes import continuity over strict referential integrity.",
        "Lines 3701-3800 prove legacy raw label IDs are dropped with a count warning rather than linked across tenants, which is the correct isolation choice. It loses original identifiers from the imported record; a migration evidence ledger should preserve them as unresolved source references. The task-fidelity fixture then begins documents, work products, monitoring and dependency relations, including an out-of-selection dependency—important for testing partial-graph semantics.",
        "Lines 3801-3900 define document and GitHub pull-request work-product fixtures, then use a fake DB that captures relation inserts and monitor updates rather than a real transaction. Export retains document bodies, portable work-product evidence and monitor intent while stripping workspace/run IDs and dropping outside-export blockers with warnings. This is strong data-minimization behavior, but work-product URL/metadata remain external untrusted data and provenance loses the creating run, weakening responsibility reconstruction.",
        "Lines 3901-4000 prove import pre-generates issue IDs, attaches documents with importer attribution, recreates portable work products without runtime/run provenance, reconstructs an internal blocker edge and imports monitor notes unarmed. Keeping monitors unarmed is an excellent safety default. Re-attributing documents/relations solely to the importer obscures original authorship; Company OS should keep source actor/evidence separately from the migration actor and preserve an immutable source bundle digest. Selection pruning begins next.",
        "Lines 4001-4100 verify partial task selection excludes the unselected task's documents, work products and dependency edge, with an explicit dangling-blocker warning. This is coherent subgraph import behavior. Attachment tests then establish object-key fixtures, digest helpers and fake storage; source comments are system-authored and lack immutable author identity, foreshadowing provenance loss. Resource limits, path normalization and content verification are the critical next checks.",
        "Lines 4101-4200 prove attachment export reorders chronologically, recomputes stale hashes, content-addresses and deduplicates identical bytes while retaining per-reference MIME/name/comment linkage. This is an excellent portable blob model. Canonical blob MIME becomes application/octet-stream from the first reference even when another reference is image/png, so manifest-level MIME is not authoritative. Import begins with no company byte limit; global/bundle limits and streaming behavior remain essential.",
        "Lines 4201-4300 prove attachment rows remap issue/comment IDs, carry importer attribution, and degrade with one aggregate warning when source or target storage is unavailable. They then tamper with blob bytes and expect digest verification before attachment writes—and explicitly before any company write. Content-addressed fail-closed preflight is high-quality and directly reusable. Graceful storage omission should still produce structured loss records, not only warnings.",
        "Lines 4301-4400 complete the pre-write tamper invariant and show missing blobs plus per-company oversized attachments are skipped with precise warnings, while the rest of the import proceeds. Per-object limits are useful but do not bound total decoded bundle bytes, blob count or base64 expansion. Missing blobs are treated as recoverable whereas digest mismatch aborts globally—a sensible integrity distinction. Embedded asset rewriting across task/comment/document Markdown begins next.",
        "Lines 4401-4500 prove one embedded image referenced by task description, comment and document is exported once as a content-addressed blob, recreated as one target asset and every Markdown reference is rewritten to the minted tenant-local ID. Deduplication plus full reference remapping is excellent. The source asset ID is retained in manifest identity without a namespace/issuer, and imported content is trusted as image/png without magic-byte scanning. Foreign/dangling reference handling begins next."
      ],
      "completionRequirements": ["Read lines 1-5421 exactly once, run the focused suite in an isolated Paperclip home, and map coverage for export/import schemas, compatibility versions, selection/data minimization, path/parser/archive safety, secrets and consent, every entity lifecycle, collision/merge/idempotency, transactions/crash recovery/concurrency, external fetch/resource limits, responsibility evidence, performance and the completed service findings. Before reading, confirm no formal audit batch or prior completed ledger exists for this exact unit."]
    }
  ]
}
