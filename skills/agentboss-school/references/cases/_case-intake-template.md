# AgentBoss School 案例输入表 v1

> 用途：由案例提供者填写，供内部审核、匿名化和事实核验。**这份原始输入表不得直接进入 RAG，也不得放进 `references/cases/` 作为可检索案例。**审核人只能把获准、最小化后的内容转写到 `_case-template.md`。

填写规则：不知道就写“未知”，没有证据就写“未验证”，不填真实姓名、公司全称、客户名单、账号、密钥、生产数据或原始内部文档。每项材料标注使用范围：`仅内部审核 | 开源课程可用 | 公开宣传可用`。

## A. 基本信息

- 输入表版本：`1`
- 建议案例 ID：`case-英文小写短名`（可空，由审核人生成）
- 案例工作标题：
- 语言：`zh-CN | en`
- 案例类型：`DEMO_FIXTURE | SYNTHETIC_SCENARIO | PROJECT_OWNED | AUTHORIZED_CLIENT | PUBLIC_SOURCE`
- 行业标签（英文小写）：
- 部门或职能：
- 企业规模区间（不要写企业名）：
- 案例提供者角色（不要写姓名）：
- 信息截至日期：`YYYY-MM-DD`
- 本表允许使用范围：`仅内部审核 | 开源课程可用 | 公开宣传可用`

## B. 授权与隐私门

- 内容权利人类型：`项目自身 | 已授权客户 | 公开来源 | 演示 fixture`
- 使用授权依据：
- 授权或同意记录引用（只写编号/路径，不粘贴合同或聊天原文）：
- 是否允许匿名后进入开源 RAG：`是 | 否 | 待确认`
- 是否允许用于公开 FDE/陪跑宣传：`是 | 否 | 待确认`
- 是否允许公开引用结果数字：`是 | 否 | 仅允许以下数字`
- 必须删除或泛化的标识：
- 是否已去除姓名、公司名、域名、账号、地理精确位置：`是 | 否`
- 是否包含凭据、token、cookie、客户名单、生产数据或原始证据：`否 | 是（停止提交并先清理）`
- 重新识别风险及处理方式：

> `AUTHORIZED_CLIENT` 案例只有在“允许匿名后进入开源 RAG=是”、存在授权引用且完成匿名化后，才能进入 catalog。

## C. 业务背景

- 业务目标：这件事最终要改善哪个业务结果？
- 客户/团队最初怎么描述需求？
- 现场调查后确认的真正问题是什么？
- 为什么原始说法和真正问题不同？
- 主要约束：时间、合规、数据、系统、预算、人员或流程。
- 哪些内容只适用于这个企业，不能向其他场景泛化？

## D. 改造前的工作方式

- 触发条件：什么事件开始这项工作？
- 输入：使用哪些信息类别？不要粘贴原数据。
- 真人角色：谁发起、谁执行、谁负责、谁审批？只写角色。
- 原流程步骤：
  1.
  2.
  3.
- 使用的系统类别：CRM、ERP、表格、邮件、内部数据库等。
- 平均处理时间及单位：
- 人工步骤数量：
- 常见错误、返工或等待：
- 最难处理的异常：
- 原流程的证据引用：
- 上述数字验证状态：`未验证 | 来源支持 | 客户验证`

## E. 委派与 Agent 工作设计

- 最终选择：`自动化 | Agent 辅助 | 有边界委派 | 保留真人`
- 为什么选择这种模式？
- 哪些部分交给 Agent？
- 哪些部分必须保留真人？
- 执行 Agent 的角色：
- 对结果负责的真人角色：
- 备用或升级真人角色：

### 结果合同

- 业务目的：
- 交付结果：格式、结构、数量、语言、截止时间。
- 验收证据：每个重要结论如何核对？
- 数据范围：允许和禁止使用什么类别？
- 允许动作：读取、起草、编辑、发送、发布、支出、运行命令等。
- 必须审批的动作：
- 暂停/升级条件：
- 失败、超时或结果未知时如何报告？
- 明确禁止的静默重试或外部副作用：

## F. FDE 或实施过程

- 是否经过现场调查：`是 | 否`
- 最关键的现场发现：
- 最终方案类型：`改流程 | 买现成软件 | 做 Skill | 定制 SaaS | 建数据库 | Agent 工作流 | 暂不使用 AI`
- 为什么没有选择其他方案？
- 需要连接的系统类别：
- 数据准备或治理工作：
- 最小试点范围：
- 试点退出条件：
- 实施中的关键取舍：
- 哪些部分尚未上线或仍是假设？

## G. 责任、权限与审批

- 目标发起真人角色：
- 结果负责真人角色：
- 执行 Agent：
- 允许动作：
- 数据授权引用：
- 权限引用：
- 高风险动作：
- 审批绑定了哪些内容：动作、输入摘要、工作、责任合同、Agent、真人、证据、有效期。
- 发生过哪些审批、拒绝、暂停或升级？
- 最终责任链如何被复查？

## H. 结果声明与证据

每一个结果单独复制填写一组。没有证据也可以记录，但必须标为 `UNVERIFIED`。

### 结果声明 1

- 声明：例如“平均处理时间从 X 降到 Y”。
- 指标名称：
- 改造前数值、单位、时间区间：
- 改造后数值、单位、时间区间：
- 样本量或观察次数：
- 证据引用（只写受控路径、公开 URL 或记录编号）：
- 验证角色（不要写姓名）：
- 验证状态：`UNVERIFIED | SOURCE_BACKED | CLIENT_VERIFIED`
- 是否允许进入开源课程：`是 | 否`
- 是否允许公开宣传：`是 | 否`
- 限制和可能的其他解释：

## I. 异常、失败与限制

- 实施中发生过什么失败或误判？
- Agent 在什么情况下必须停？
- 哪次介入选择最关键：等待、澄清、重试、改派、暂停、取消？
- 是否出现外部结果未知？如何避免重复副作用？
- 哪些目标没有实现？
- 当前仍存在什么风险？
- 如果重做一次，会改变什么？

## J. 可教学知识

- 本案例最适合解释的三个知识点：
  1.
  2.
  3.
- 它纠正的常见误解：
- 最关键的 Agent Boss 决策：
- 可让学生作决定的情境题：
- 推荐课程节点（可多选）：
  - `role/not-a-prompt-engineer`
  - `role/delegation-fit`
  - `role/outcome-contract`
  - `governance/exact-action-approval`
  - `shared/demo-responsibility-loop`
- 可迁移到其他团队的部分：
- 不能迁移或不能公开的部分：

## K. School、陪跑与 FDE 的关系

- 哪些缺口可以继续通过免费 School 解决？
- 哪些缺口需要 Agent Boss 团队陪跑？
- 哪些缺口必须进入真实现场，由 FDE 诊断或实施？
- 本案例能否用于说明陪跑/FDE？授权范围是什么？
- 禁止使用的销售表述：

## L. 附件引用

只列引用，不粘贴内容；不得引用无权访问或包含凭据的材料。

| 引用编号 | 材料类型 | 受控路径或公开 URL | 权利/授权 | 可公开范围 |
|---|---|---|---|---|
| E-001 | 例如脱敏流程图 |  |  |  |

## M. 提交前自检

- [ ] 没有姓名、公司全称、账号、凭据、客户名单或生产数据。
- [ ] 客户案例已有授权引用，并完成匿名化。
- [ ] 每个数字都有证据和验证状态；无证据的明确写 `UNVERIFIED`。
- [ ] Demo/演示没有被写成真实客户效果。
- [ ] 事实、推断、观点和营销表述已经分开。
- [ ] 已标明开源课程、公开宣传和内部审核的不同使用范围。
- [ ] 原始输入表不会直接进入 RAG。

## N. 内部审核结果（案例提供者不填）

- 审核结论：`退回清理 | 仅内部保留 | 可转写为 DRAFT | 可发布为 VERIFIED`
- 核准案例类型：
- 核准证据等级：`ILLUSTRATIVE | SOURCE_BACKED | CLIENT_VERIFIED`
- 核准课程节点：
- 允许进入 catalog 的结果声明：
- 必须删除或改写的内容：
- 审核角色与日期：
- 转写后的正文路径：
- catalog 记录 ID：
